# WP4 — Phone and desktop: Sada feed, Karta as the timeline map, three tabs, folded Događanja

Part of the companion brief `docs/companion-2026-09-22.md` (§15). Line numbers refer to the working tree at build `b300af3`; re-anchor with `grep` before editing. Read-only rules of the brief §1 apply: no `wrangler deploy`, no production screen outside the documented local flow, `docs/prijava` and `app/prijava` untouched.

## 0. Reconciliation with the other packages (binding; read first)

The planner text below was written before the seven packages were compared and before the adversarial verifier ran. Where this section, the verifier verdict at the end, or the brief §15 disagree with the planner text, **this section wins, then the verdict, then the planner text**.

**Owner rulings of 22 September, evening ([O-62]–[O-74], binding over everything below).** (1) **Karta keeps its map/schema toggle** [O-72] as one small control in the sheet head ("Shema" / "Karta"), stored in the existing map-mode preference (`app/src/core/map-mode-store.ts`, key `kajima:map-mode:v1` — the browser-side saved preference the plan called the "device store"); the `t-map-menu` disclosure still goes. (2) **Default place without a screen place is Trg bana Jelačića** [O-65] (already the `defaultLocation` reference; `resolvePlace` returns kind `'city'` named "Trg bana J. Jelačića" with departures from 106_1). (3) **End of session** [O-62]: cleared to the scan invitation as planned; write into the PRODUCT.md approval paragraph the deferred vision, verbatim from the owner: content a person needs should later be keepable "without too much fuss" (static export; real-time tracking that can stay pinned until the tram comes) — a layer of its own, not this round. (4) T5 (reload drops the screen label) is WP4 chunk D [O-67]. (5) Standard, natural Croatian; no "zid" [O-66].

These decisions override the planner text below wherever they differ. Apply **every correction in the verifier verdict** (the last section of this file), with the following settlements. Canonical ids: the plan credits `ScreenMetadata.place` to "WP1" — it is **WP3's** (seam S1); the trust package is **WP0**.

**Lane and gate.** Lane P. **Chunk A** (steps 1–2 place resolution over seam S3 and the departures block; step 4 chrome/tabs/share; step 5 Još; step 7 kultura "do d. m."; desktop CSS; the `columnsFor`/`zagrebInstant` move) runs **beside lane W** from the start. **Chunks B and C** (step 3 Sada feed, step 6 Karta, the phone sentence call, the new step 11) start **after deploy D2** so `curatedCityPoints`, `frameView`, `selectNearby`, `sentenceFacts` and `ScreenMetadata.place` exist on the wire — no local fallbacks, no stubs. Ships as deploy **D3**.

**Names of what WP4 consumes (binding).**
- `shared/city/place.ts` (S3): `ScreenPlace`, `derivePlace`, `TRAM_NEAR_M = 400`, `BUS_NEAR_M = 300`. `app/src/city/place.ts` is a thin `resolvePlace({ screen, saved, stops, location }): PlaceContext` in the order screen → saved → nearest, calling `derivePlace`; `rankStops(stops, { near, limit: stops.length })`; `DEPARTURES_STOP_M = 800` stays WP4's rule for `departuresStop`.
- `shared/city/frame.ts` (S2): `FrameStops`, `frameRadiusM`, `DEFAULT_FRAME_STOPS`; `app/src/map/frame.ts`: `frameView(center, radiusM, w, h)` (the plan's `frameAround`).
- `app/src/city/curated.ts` (WP2): `curatedCityPoints(city, dogadanja, now, CURATED_WALL)` (the plan's `curatedPoints`; no `night` option, no pharmacy marker); `CityLabels` `'all' | 'venues' | 'none'`; `MAP_PRESENTATIONS.placeClusters`.
- `app/src/city/nearby.ts` (S5, WP1): `selectNearby(input: NearbyInput): NearbyRow[]`, kinds `last`/`first` (not `lastDepartures`/`firstTram`), `nearbyHead(i18n, frame)`; `app/src/city/sentence.ts` (S6): `sentenceFacts`, `templateSentences`, `createSentenceSequence`; `fetchSentences` in `app/src/api.ts`. `app/src/city/feed.ts` is the one adapter `nearbyInput(ctx)`; it ships **no** fallback implementation.
- `ScreenMetadata.place` reaches the phone through the room `joined` frame (`worker/do/room-do.ts:374,389,593` already forward `screenMetadata()`); `defaultLocation(screen.stop)` is replaced by `resolvePlace`.

**New step 11 — end of the ten minutes [O-59] (brief Q1 default).** `app/src/dashboard.ts freeze()` (:858-884) clears `#ki-main` and renders `[data-testid=session-ended]` with the scan-again invitation (`a[href^="/s/"]`) and `a[href="/hitno"]`; `snapshotLine`/`.ki-snapshot` (:441) and every export action go; no further `/api/data` requests after expiry. Tests: the 17 frozen assertions in `test/app/dashboard.test.ts`, 6 in `test/app/transport-detail.test.ts`, `e2e/experience.spec.ts:89,146` (`frozen-line`), `e2e/mobile.spec.ts:522-548`, `test/app/copy-guards.test.ts:231`. `session.expired` → "Deset minuta je prošlo. Zaslon u blizini otključava novih 10 minuta.", `session.expiredHint` → "Sigurnost ostaje otvorena na /hitno." (owner to confirm; pinned by `i18n.test.ts:78-84`). WP5 later removes `export.*`, `cast.frozen`, `shell.frozen*` and rewrites DESIGN.md:220; the submitted proposal is not edited.

**New step 12 — the phone's sentence.** `dashboard.ts refresh()` calls `fetchSentences({ locale, budget: 80, facts: sentenceFacts(nearbyInput(ctx)) })` at most once per 60 s (the route is rate-limited per IP), falls back to `templateSentences`, and writes `ctx.sentence` → `[data-testid=sada-sentence][data-kicker]`.

**Sada / Karta details settled.** `modesArg()` = `kiosk() ? new Set(modes) : mapMode === 'schema' ? new Set([ROUTE_TYPE_TRAM]) : null` (the paired wall keeps trams-first). `departureRow(i18n, row, kindOf, frozenAt?)`; the stop detail keeps `data-testid="arrival-rows"` on its `<ul>`; the hidden "uživo" word is emitted only when not frozen. The Sada block keeps `provenanceBlock` crediting ZET (fixed constraint "labelled ZET estimate") and `arrivals.note` once in the stop detail. `.day-times`/`.day-time` rules move to `layers.css` (the Događanja chips keep them) before the rest of `.day-*` is deleted. Row titles reuse **`kiosk.nearby.*`** keys (seam S9); new phone keys only under `sada.*`, `directory.*`, `arrivals.timetable` ("Vozni red" — the one timetable word; WP5 sweeps synonyms to it). `layers.u-pokretu` → **"Karta"** here, with `test/app/kiosk-local.test.ts:95` and `test/app/kiosk.test.ts:1416`. The `quiet` key deletion in `app/src/city/strings.ts` is WP4's (WP1 does not touch the file). `columnsFor` + `zagrebInstant` move to `app/src/kiosk/columns.ts` when `grad-sada.ts:15` drops `buildTimeband`, so WP5 can delete `timeband.ts` without touching `paired.ts`. WP4 removes every `workspace.ts` cluster call site (:35, :248, :439, :1165) and owns `transport-workspace.test.ts`. Schema on the phone stays reachable through the device store `kajima:map-mode:v1` (brief Q14; WP5 decides the toggle).

**Probes (brief §15 table).** `sada-place`, `sada-sentence[data-kicker]`, `sada-map-band`, `day-departures` (existing id) > `li.sada-departure[data-live]`, then the shared `nearby` list markup (same class and attribute names as the wall: `nearby`, `nearby-head`, `nearby-rows`, `li.nearby-row[data-id][data-kind][data-when|data-always][data-live]`); header `[data-testid=share-city][data-action=share-city]` ("Podijeli grad") and `share-code`; `session-ended`; `tab-more`, `dir-kultura`. WP4 does **not** edit the research classifier `analyse.mjs` (WP6 ports it to `e2e/inventory.ts`); WP4 edits only `run-slot.mjs:58-59` (post-deploy timing).

**Docs.** Only the phone/desktop sentences this package invalidates (PRODUCT.md approvals of 17/18/20 Sep, DESIGN.md "Phone and desktop"); the full §11 rewrite is WP5 step 10.

## Goal

Make the ten-minute visit answer before it explains (brief §5, §11 phone/desktop contracts, §12, [O-12] [O-31] [O-50] [O-51] [O-56] [O-61]). Sada opens on the place as the title, one sentence card, a 112 px map band, three departures at the automatically chosen nearest stop (screen stop → saved stop → nearest to the reference; tram if near, else bus, else the address) and the same "U blizini · 2 km · ~15 min" rows the wall shows; no "Sada u gradu.", no date line, no instruction sentence, no counts, no day-facts bands, no time-band lanes. Karta is the timeline's map: vehicles drawn at once with full-number pills, curated markers (WP2 profile), tap a stop → its departures above the fold, one search field over routes/stops/places/streets, no group taxonomy, no "Što tražiš?" and no "Alati karte" disclosures. Tabs Sada · Karta · Još; Događanja folded (week agenda as a Još row with its count line, timed events in the feed, venues on the map). Desktop = the phone, wider: feed and Karta side by side, no six-domain bar. Present/share protocol unchanged, but "Podijeli grad" becomes a labelled header button beside the session pill on every screen of a direct session. Producers/timeband code left dead is named for WP5.

## Depends on

- WP1 — selection layer and sentence writer: `selectNearby(input: NearbyInput): NearbyRow[]` (rows {key, kind: 'departure'|'closure'|'event'|'solar'|'lastDepartures'|'firstTram'|'opening'|'always'|'pharmacy', atMs|null, title, sub, badges?, selection?: PublicSelection, layer}) and the sentence `{ kicker, text ≤ 80 }` exposed to the phone (`ctx.sentence`); WP4 consumes both through `app/src/city/feed.ts nearbyInput(ctx)` — if WP1's names differ, adapt that one file. WP1 must also carry the wall's derived place into `ScreenMetadata` (`place?: {name, lon, lat}` beside `stop`) so a phone unlocked from a whole-city temporary screen opens on the wall's place rather than the Jelačić default
- WP2 — map profile: `curatedPoints(city, dogadanja, now, {night})` (extracted from `kiosk/mapview.ts:502-515`), `frameAround(place, stopsN, box)` (frame arithmetic from WP2 notes: 4/6/8 stops ≈ 1.3/2/2.7 km; handheld clamps to 12.7), full-number pills with no '+N', bus visibility by zoom, and a `data-bajs` census probe; WP4 falls back to `fieldZoom(width, lat, 2000)` (`kiosk/mapview.ts:99`) and `cityWindowPoints` until WP2 lands
- WP0/WP3 (trust) — independent; the share-QR aria fix `dashboard.ts:846` `kiosk.qrLabel` → `kiosk.invite.qrLabel` may be done by either package (idempotent one-liner)

## Data, protocol and artefact changes

No wire schema change in WP4 itself. Dependencies on other packages: WP1 adds `ScreenMetadata.place?: { name; lon; lat }` (worker/protocol.ts:36-41) so the phone inherits the wall's derived place. The phone stops sending `PresentationTarget.time` (still accepted by worker/presentation.ts:9-16). Client-only artefacts: `layers.u-pokretu` catalogue value 'Promet' → 'Karta' (en 'Map'); new keys `sada.*`, `nearby.*`, `directory.{eventsWeek,saved,savedEmpty,savedRecord,route,stop,remove,destinations,preferences}`, `arrivals.timetable` in both hr.json and en.json; sessionStorage `vidikovac.view-filters` keys `tb-col`, `city-group`, `city-category`, `city-window`, `city-scroll` are no longer written (stale values ignored); a second map slot id `grad-sada-map`. Dead after WP4, to be removed in WP5: `app/src/experience/timeband.ts` (keep `columnsFor`/`zagrebInstant` for `kiosk/paired.ts:27` by moving them), `experience/timeband-sync.ts`, `experience/producers/*` (move `SAFETY_ICON`/`safetyVerdict` to `experience/safety-state.ts` for `kiosk/frame.ts:13`), `experience/tiles.ts`, `city/day.ts dayOpportunities`, `transport/view.ts` overview/peek/delays/results/castButton, `layers/u-pokretu.ts renderLightweight`, `chrome.ts fabMarkup/clockMarkup/searchLaunchMarkup/bellMarkup`, `dashboard.ts` FAB slot/cast timers/openSearch/mapFull, CSS `.tb-*` (layers.css:201-246,518-550), `.ki-fab*`, `.ki-domains*`, `.ki-clock`, `.ki-search`, `.ki-bell`, city.css:2-7, i18n `cityOverview.*`, `timeband.*`, `tiles.*`, `transit.mostDeviating`, `transit.vehiclesMoving_*`, `overview.allClearConfirmed`, `transport.{trams,buses,showClosures,fitCity,mapModeSchema,mapModeMap,schemaTramsOnly,modesLabel,toolsLabel,peekLoading,runningRoutes,noRunning,screenStop,notices,openNotice,closuresNow_*}`, `panels.{delays,moreRoutes_*,mapExpand,mapCollapse}`, `cast.*`, `shell.{clockLabel,clockOnly}`, `city/strings.ts` keys `layers,all,city,movement,useful,heritage,list,legend,here,locate,network,cluster,allVenues,activeVenues,rentMode,returnMode,streetBrowse,quiet,preview,leave,conditions,start,next,cityLife`, and tests timeband/timeband-sync/producers/producers-mobility/tiles/lastrun:165 plus the `.tb-*` assertions in shell-css.test.ts and workspace-css.test.ts:25. Full detail: review.local/companion/plan/WP4/notes.md.

## Contracts this package supersedes

- PRODUCT.md, Approval (18 Sep 2026): 'Phone: Sada, Karta, Događanja, Još. Time-led Sada and place-led Karta complement the chronological agenda.'
- PRODUCT.md, Approval (17 Sep 2026): 'all six domains directly accessible on desktop; Sada, Promet, Događanja and Još on the phone.'
- PRODUCT.md, Approval (20 Sep 2026, personal devices): 'Sada leads with local departures rather than line-wide fleet statistics. Još retrieves saved routes, stops and places. Location context is named explicitly.' — location context becomes the title (the place), not a labelled line; Još also carries the week agenda
- DESIGN.md, Phone and desktop: 'Phone destinations are Sada, Karta, Događanja and Još. The current destination retains a readable label at text zoom. Desktop exposes all six domains directly.'
- DESIGN.md, Phone and desktop: 'Sada has two reading regions: local facts now, and what comes next. Names and destinations are readable; weather is useful in the first view. Time filters change the agenda without removing the local context.'
- DESIGN.md, Phone and desktop: 'Karta has one map, one search entry, a peek/detail/open sheet on a phone, and a contextual board on desktop. Secondary map actions live in one disclosure. The peek state keeps search visible…' — the disclosure goes; peek keeps search
- DESIGN.md, Phone and desktop: 'Filters use one labelled disclosure rather than an unmarked horizontal strip.' and 'Sada reads the same departure cache and arrival logic as the stop detail. Its agenda flows vertically without empty time quadrants or duplicated preview events.' (the shared cache stays; the agenda leaves Sada)
- DESIGN.md, Phone and desktop: 'Living city, culture, transport, useful places and heritage are progressive map groups. Active venue pins have bounded size and explicit event counts; geographic clusters use a distinct plus-count mark.' — no groups, no cluster marks
- DESIGN.md, Phone and desktop: 'Događanja uses a dated agenda and compact category selection.' — becomes 'the week agenda is a Još page with its count line'
- docs/kiosk.md: nothing superseded (phone mentions at lines 8-9 and 231 remain true)

## Steps

### 1. Place resolution: the automatically chosen stop and place for the phone  `[M]`

Files: `app/src/city/place.ts (new)`, `app/src/city/location.ts`, `app/src/core/contracts.ts`, `app/src/dashboard.ts`, `app/src/city/markup.ts`, `test/app/place.test.ts (new)`

New `app/src/city/place.ts`: `export interface PlaceContext { name: string; lon: number; lat: number; kind: 'screen'|'saved'|'nearest'|'address'|'city'; stop: ScreenStop|null; departuresStop: ScreenStop|null }` and `export function resolvePlace(input: { screen?: ScreenContext; saved?: Pick<SavedStore,'list'>; stops?: readonly ScreenStop[]; location?: LocationContext }): PlaceContext`. Order [O-26]/[O-12]: (1) `screen.stop` → kind 'screen'; (2) `saved.list().find(r=>r.kind==='stop')` resolved in `stops` → 'saved'; (3) reference = `location ?? defaultLocation(screen)` (`city/location.ts:7-10`); rank `rankStops(stops,{near: ref})` (`app/src/kiosk/stops.ts:61`, reuse, do not reimplement) → first stop with a tram route (`routeType(id)===0`, `kiosk/stops.ts:37`) within `export const TRAM_NEAR_M = 400` → kind 'nearest'; else nearest bus stop within `export const BUS_NEAR_M = 300`; else the reference alone: kind 'address' when `location.kind` is 'device'|'area', else 'city' (name 'Trg bana Jelačića' from defaultLocation). `departuresStop` = `stop ?? rankStops(...)[0]` within 800 m ?? null (the phone always shows departures at the nearest platform group even when the place is an address). Export the two constants so WP1's wall setup uses the same rule. `city/location.ts`: delete `locationLabel` (:11-16) and its call sites — `layers/grad-sada.ts:57` (whole line goes in step 3), `transport/workspace.ts:698` (the city-browse block goes in step 6), `city/markup.ts:54` → print `${distance} m · ${reference.name}` (reference is now `PlaceContext`). Keep `defaultLocation` and the `LocationContext` type (imported by `core/contracts.ts:11`). `core/contracts.ts ExperienceActions` (:52-86): add `place?: PlaceContext` (import type from `../city/place`). `dashboard.ts`: `render()` :424 `if (screen().stop || saved.list().some(...)) ensureStops();` → always `ensureStops();` (the 245 kB catalogue is now needed for the nearest stop; it was already fetched whenever a stop was saved); `layerContext()` :402-418 add `place: resolvePlace({ screen: screen(), saved, stops: stops ?? undefined, location: locationContext })`; `ensureLastRun()` :501-515: key on `resolvePlace(...).departuresStop?.id` instead of `session.snapshot().screen?.stop` so the last-departures row works for the chosen stop (keep `FLAGS.FEED_LASTRUN` gate; `core/lastrun.ts` stays). `test/app/place.test.ts`: screen stop wins; saved stop beats nearest; a tram stop at 350 m beats a bus stop at 120 m; bus fallback at 250 m when the nearest tram is 900 m; address fallback when nothing within thresholds; `departuresStop` set in the address case.

### 2. Departures block: three rows at the chosen stop, one empty rule, no instruction and no per-row 'Procjena'  `[S]`

Files: `app/src/city/next-departures.ts`, `app/src/transport/view.ts`, `app/src/ui/overview.css`, `test/app/copy-guards.test.ts`

Rewrite `app/src/city/next-departures.ts` as `export function departuresBlock(ctx: LayerContext, place: PlaceContext, opts: { rows?: number; heading?: boolean } = {}): string` and `export function departureRow(i18n, row: ArrivalRow, frozenAt?: number, kindOf: (routeId)=>'tram'|'bus'|'other'): string` (shared by Sada and the Karta stop detail). Stop = `place.departuresStop`; ids = `platformIds(stop, ctx.stops)` (`app/src/kiosk/arrivals.ts:57`); `ctx.boards.ensure('zet', ids, ctx.onLocalData)` unless frozen; rows = `arrivalsAt(held, vehicleFixes(ctx.snapshots['zet-rt'], ctx.now), ctx.now, { stopIds: ids, rows: opts.rows ?? 3 })` (`shared/city/arrivals.ts:87`). Row markup: `<li class="sada-departure" data-key="{tripId}|{atMs}" data-live="{row.live}">{lineBadge(row.routeName, kind, 'm')}<span class="sada-dest">{headsign||routeName}</span>{arrivalTime(i18n,row,frozenAt)}</li>` — reuse `arrivalTime` (`transport/view.ts:529-536`: live rows carry the `t-live` dot with aria 'uživo' and `data-live`; scheduled rows a plain `<time>`). Delete the prompt `Odaberi i spremi stajalište…` (:14), the titles 'Sa stajališta zaslona'/'Tvoje spremljeno stajalište' (:19), the `<small>Procjena|po redu vožnje</small>` (:22) and the `ZET · arrivals.note` foot (:24): colour + dot carry tracked vs timetable (brief §4.2). Heading: `<h3 class="sada-departures-title">{stop.name}</h3>` only when `opts.heading` (Sada passes `heading: place.kind==='address'||place.kind==='city'` so the stop is named when it differs from the title); the block root `<section class="sada-departures" data-testid="day-departures" aria-label="{i18n.t('sada.departuresAt',{stop})}">` keeps the testid the walkthrough runner waits on (`run-slot.mjs:58`). Empty rule (slop 24, one rule): status 'none' & live → one aria-hidden skeleton row + `aria-busy=true`; 'down' → one line `arrivals.down`; live board with no row → `arrivals.none` (rare: the board spans 24 h, `worker/city/schedules.ts:168-193`); frozen & 'none' → `arrivals.frozen`. CSS in `overview.css`: `.sada-departure { display:grid; grid-template-columns:auto minmax(0,1fr) auto; align-items:center; min-block-size:3.5rem; gap:var(--sp-3); border-block-end:1px solid var(--tone-stroke) }`, `.sada-departure .t-eta[data-live] { color: var(--tone-action-brand); font-weight: var(--weight-bold) }`, `.sada-departure .t-eta:not([data-live]) time { color: var(--tone-text-muted); font-variant-numeric: tabular-nums }`. `transport/view.ts arrivalsSection` (:543-563) → rows via `departureRow` (no `sub` word), see step 6. `test/app/copy-guards.test.ts:203-233`: replace 'every row carries t-live or the scheduled word' with 'a live row carries `class="t-live"` and `data-live`, a scheduled row carries `<time` and neither', keep 'arrivals.note appears once' (foot of the stop detail) and 'never the word arrivals.live as text'.

### 3. Sada layer rewrite: place · sentence card · map band · departures · U blizini · provenance  `[L]`

Files: `app/src/layers/grad-sada.ts`, `app/src/city/feed.ts (new)`, `app/src/city/nearby-markup.ts (new)`, `app/src/experience/blocks.ts`, `app/src/experience/tiles.ts`, `app/src/ui/overview.css`, `app/src/i18n/hr.json`, `app/src/i18n/en.json`, `test/app/sada.test.ts (new)`

`renderGradSada(ctx)` returns, in DOM order: `<section class="layer ws ws-sada" id="layer-grad-sada" data-layer="grad-sada" data-reconcile aria-labelledby="layer-title-grad-sada">` → (1) `<h2 class="layer-title sada-place" id="layer-title-grad-sada" tabindex="-1" data-testid="sada-place">{place.name}</h2>` (the place is the title [O-31]); (2) `<article class="sada-sentence" data-testid="sada-sentence" data-kicker="{kicker}"><span class="kicker">{kickerWord}</span><p>{text}</p></article>` from `ctx.sentence` (WP1 writer, ≤80 chars, kicker ∈ Promet|Kultura|Vrijeme|Bicikli|Noćas|Radovi); fallback when WP1 gives none: `sentenceFallback(i18n, snapshots, now)` in `city/feed.ts` = weather line built from `weatherStatus` (`experience/weather-status.ts:32`): kicker 'Vrijeme', text `${condition}, ${temp}` capitalised (never a count, never empty; omit the card when weatherStatus is null); (3) phone only (`ctx.screen?.surface==='phone'` and `ctx.maps`): `<a class="sada-map" href="#layer=u-pokretu" data-action="nav" data-layer="u-pokretu" data-testid="sada-map" aria-label="{sada.mapBand}">` wrapping `ctx.maps.slot({ id: SADA_MAP_SLOT_ID='grad-sada-map', renderer:'map', className:'map-canvas sada-map-canvas', testid:'sada-map-canvas', interactive:false, attributionCompact:true, presentationProfile:'handheld', points:[...vehiclePoints(zet,now), ...curatedPoints(...)] (from `layers/u-pokretu.ts:66` and WP2's curated set), lines: closureLines(prometnice), center:[place.lon,place.lat], zoom: WP2 frame (fallback `fieldZoom(390, place.lat, 2000)` from `kiosk/mapview.ts:99`, clamp 12.7–15.5), stop: place.stop, reducedMotion, theme, locale })` — the band is 112 px on the phone (mock v2, accepted [O-31]); `maps.sweep()` in dashboard already releases unused slots; lagano/no-maps → omit the band; (4) `departuresBlock(ctx, place)` (step 2); (5) `<section class="nearby" data-testid="nearby" aria-labelledby="nearby-title"><header class="nearby-head"><h3 id="nearby-title">{nearby.title}</h3><span class="nearby-pill" data-testid="nearby-pill">{nearby.pill}</span></header><ol class="nearby-rows">{rows}</ol></section>` with rows from WP1's `selectNearby(nearbyInput(ctx))` capped at `NEARBY_PHONE_ROWS = 8` (`NEARBY_DESK_ROWS = 12`), rendered by new `app/src/city/nearby-markup.ts` `nearbyRowsMarkup(i18n, rows, cap)`: `<li class="nearby-row" data-kind="{kind}" data-key="{key}">` + `<time datetime>{HH:MM}</time>` or `<span class="nearby-always">{nearby.always}</span>` + `<span class="nearby-main"><strong>{title}</strong><span class="nearby-sub">{sub}</span></span>`; a row with `selection` wraps in `<a data-action="nav" data-layer="{layer}" data-selection="{json}" href="{selectionHref(layer, selection)}">` — move `tileHref` (`experience/tiles.ts:70-73`) to `experience/blocks.ts` as `export function selectionHref(layer, selection?)` and re-export from tiles.ts until WP5; (6) `provenanceBlock(i18n, snapshots)` unchanged (credit lines belong on the phone below the fold, principle 5). New `app/src/city/feed.ts`: `export function nearbyInput(ctx: LayerContext): NearbyInput` — one adapter for Sada and Karta: `{ now: ctx.frozenAt ?? ctx.now, place, radiusM: 2000, stops: ctx.stops, boards: ctx.boards, vehicles: vehicleFixes(...), snapshots: ctx.snapshots, city: ctx.city, lastRun: ctx.lastRun, sun: sunTimes(new Date(now)) (`ui/solar.ts:27`), pharmacies: LJEKARNE + PHARMACY_POINTS (`worker/hitno/ljekarne.ts:32`, `kiosk/local.ts:362`), locale }` — if WP1's names differ, this is the only file to adapt. Remove everything else from grad-sada.ts: `buildTimeband`, `FILTER_KEY`, `tileMarkup`/`skeletonTileMarkup`, `weatherStatus` header, `zagrebWeekdayDate` date line, `cityOverview.*` strings, `day-now`/`Promet ↗`, `dayOpportunities`, `day-facts`, feet, `day-ahead`/`Događanja ↗`, segments, lanes, `day-city`. `overview.css`: rewrite for `.ws-sada`, `.sada-place` (font-size var(--type-display), phone 1.75rem), `.sada-sentence` (card: surface-1, stroke, radius var(--r-lg), padding var(--sp-4); `.kicker` coloured by `data-kicker` via `--tone-transit|--tone-events|--tone-weather|--tone-bike|--tone-komunalno`), `.sada-map` (block-size 112px phone, radius var(--r-lg), overflow hidden, display block), `.nearby-head` (flex, pill = surface-2 chip, tabular), `.nearby-row` (grid `4.5rem minmax(0,1fr)`, min-block-size 4rem, border-block-end stroke; `.nearby-always` muted small caps), `.sada-departure` (step 2); delete every `.day-*` rule. i18n (hr / en, both files — `test/app/i18n.test.ts:49` pins key parity): `sada.mapBand`: 'Karta oko mjesta {place}. Otvori Kartu.' / 'Map around {place}. Open the map.'; `sada.departuresAt`: 'Sljedeći polasci, {stop}' / 'Next departures, {stop}'; `nearby.title`: 'U blizini' / 'Nearby'; `nearby.pill`: '{km} km · ~{min} min' (same in en); `nearby.always`: 'uvijek' / 'always'; kicker words: `nearby.kicker.transit` 'Promet', `.culture` 'Kultura', `.weather` 'Vrijeme', `.bikes` 'Bicikli', `.night` 'Noćas', `.works` 'Radovi' (en: Transport, Culture, Weather, Bikes, Tonight, Works) — unless WP1 already adds them under its own group, in which case reuse WP1's keys. `test/app/sada.test.ts`: (a) no `.day-stop-prompt`, text has no 'Sada u gradu.', no /\b\d+ zatvaranja\b/, no 'Radovi u gradu', no 'Odaberi'; (b) without screen stop and nothing saved, `[data-testid=sada-place]` = the nearest tram stop's name for the fixture reference; (c) `.sada-departure` count between 1 and 3 with a fixture board; (d) every `.nearby-row` has `<time>` or `.nearby-always`; (e) the h2 is followed by `.sada-sentence` then `[data-testid=day-departures]` (phone ctx without maps: no `.sada-map`); (f) desktop ctx renders no `.sada-map`.

### 4. Chrome: tabs Sada · Karta · Još, 'Karta' as the one word, header 'Podijeli grad' button, desktop header without the six-domain bar  `[M]`

Files: `app/src/experience/chrome.ts`, `app/src/dashboard.ts`, `app/src/ui/dashboard.css`, `app/src/i18n/hr.json`, `app/src/i18n/en.json`

`chrome.ts:23` `PHONE_TABS` = [grad-sada, u-pokretu]; `:24` `MORE_LAYERS` = ['kultura','zrak-i-nebo','sigurnost','uprava-i-pravo'] (Događanja first [O-60]); `layerLabel` (:86-89) → `i18n.t(`layers.${layer}`)` and drop the `ct(i18n,'map')` override; hr.json `layers.u-pokretu`: 'Promet' → 'Karta', en.json 'Transit' → 'Map' (one word, slop 11; the kicker 'Promet' stays a topic word; consumers that change with it: `dashboard.ts:379` document title 'Kaj ima? · Karta', `presentation.ts:466` target label, `kiosk.ts:395` session pill — intended). New `export function shareButtonMarkup(i18n, s: ShellState): string` → `s.canShare ? `<button type="button" class="ki-share" data-key="share" data-action="share-city" data-testid="share-city-header" aria-haspopup="dialog" title="${session.share}">${iconMarkup('share-2')}<span>${i18n.t('session.share')}</span></button>` : ''` — the label is the existing 'Podijeli grad' (`session.share`), the action already routed at `dashboard.ts:1043` (`case 'share-city': session.share()`), refusals already handled at `dashboard.ts:908-915`. `statusLineMarkup` (:108-119): phone = `${wordmark}${display}${share}${session}${safety}`; desktop = `${wordmark}<div class="ki-status-space" data-key="space"></div>${display}${share}${session}${moreButtonMarkup(i18n,s)}${safety}` — remove the `domains` list and `clockMarkup` call (keep `clockMarkup`, `searchLaunchMarkup`, `bellMarkup`, `fabMarkup` exported but unused → WP5). `dashboard.ts:359-360`: drop the desktop `weatherStatus` computation for the status line (pass null). `dashboard.css`: phone `.ki-head` (:110) `grid-template-columns: auto minmax(0,1fr) auto auto auto auto` and the `@media (max-width: 59.99rem)` placements (:195-199) → screen col 3, share col 4, session col 5, safety col 6; `.ki-share` = `.ki-screen` box (:119) with the label visible (`.ki-share > span { position: static }`), min-inline-size var(--target), font-size var(--type-control), weight bold, colour var(--tone-action-brand); under `@container header (max-width: 18rem)` (:188-194) the label hides visually (the `title` and the icon stay; add `aria-label` = the label in markup so the name survives); `.ki-tabs` (:203) `repeat(3, 1fr)`; desktop block (:256-292): delete `.ki-domains*` rules (:263-268), head columns `auto minmax(0,1fr) auto auto auto auto auto`, `--ki-top: 3.5rem` (one row now; `html:has(.ki) scroll-padding-block` :258 follows), keep `.ki-more`, add `.ki-safety` visible on desktop (it already renders; only the phone placement rule excluded it). Kiosk untouched.

### 5. Još directory: Događanja ovaj tjedan row with the count line, destinations on both surfaces, hard-coded headings into the catalogue  `[M]`

Files: `app/src/experience/directory.ts`, `app/src/layers/kultura.ts`, `app/src/i18n/hr.json`, `app/src/i18n/en.json`

`directory.ts:26-28` `directoryLayers()` → return `MORE_LAYERS` on both surfaces (drop the desktop `[]`); `directoryModules()` follows. Row list (`LINES` :73-79 + `items` :98-102): 'kultura' row title = new key `directory.eventsWeek` 'Događanja ovaj tjedan' / 'Events this week' (the other three keep `layers.*`), sub-line = `eventsCountLine(i18n, ctx)`: export from `kultura.ts` `export function eventsCount(snapshot, city, now): { count: number; ongoing: number }` extracted from `renderKultura` :553-576 (deduplicate → inZagreb → upcoming 7-day window → ongoing) and format with `events.countLine`/`events.count`/`events.ongoingCount` exactly as `kultura.ts:574-576` does ([O-53]: the count line is the one count that stays). Delete `transitLine` (:35-39; u-pokretu is no longer a directory row) and the `LINES['u-pokretu']` entry. Move the inline HR/EN ternaries to the catalogue (both json files): `directory.saved` 'Spremljeno'/'Saved'; `directory.savedEmpty` 'Spremi liniju, stajalište ili mjesto na karti. Ovdje ih možeš ponovno otvoriti na ovom uređaju.' / 'Save a route, stop or place on the map. Find it here next time, on this device.'; `directory.savedRecord` 'Spremljeni zapis {id}' / 'Saved record {id}'; `directory.route` 'Linija'/'Route'; `directory.stop` 'Stajalište'/'Stop'; `directory.remove` 'Ukloni'/'Remove'; `directory.destinations` 'Odredišta'/'Destinations'; `directory.preferences` 'Osobne postavke'/'Preferences' (lines :93,94,95,97,125,126). Section order [O-60]: Spremljeno · Odredišta (Događanja ovaj tjedan, Vrijeme, Grad, Sigurnost) · Osobne postavke (bell, session) · Stranice. The `#layer=kultura` page itself (agenda) stays reachable only from here; `tabbarMarkup` already names the open extra layer on the Još tab (`chrome.ts:184-187`).

### 6. Karta as the timeline's map: vehicles on, curated markers, no menus, nearby rows as the default sheet, stop detail with departures first  `[L]`

Files: `app/src/transport/workspace.ts`, `app/src/transport/view.ts`, `app/src/ui/map.css`, `app/src/city/nearby-markup.ts`, `app/src/city/feed.ts`, `test/city/workspace.test.ts`

`workspace.ts` markup (:170-214): delete the `<details class="t-map-menu">` block (:173-187) and the `<details class="city-filter-disclosure">` block (:203-207); keep `.t-map-status`, the sheet head/peek/toggle, the search combobox, body and note. Delete the element refs `modeButtons`, `modesGroup`, `closuresButton`, `schemaLegend`, `tools`, `mapModeButton`, `fitCityButton`, `fullButton`, `filtersDisclosure` (+ its toggle listener :239-243); state `cityGroup`, `cityCategory`, `activityWindow`, `bikeMode`, `cityFilterKey` (:286-288,298-299); functions `paintCityFilters` (:320-333), `saveCityFilters` (:334-340); handlers `bike-mode`, `city-group`, `city-category`, `city-window`, `city-area`, `city-locate` (:897-922; MapLibre's own GeolocateControl at `map/city-map.ts:1428` remains), `fit-city`, `toggle-map-mode`, `toggle-mode`, `toggle-closures`, `toggle-full` (:937-991); the filter restore block (:1177-1184) and `if (!c.city) cityGroup='transport'` (:1171); `renderChrome` lines that paint the removed controls (:590,603-630) and `element.dataset.cityGroup` (:593); fix the double search label (:595,597) → hidden label `tr(i18n,'searchLabel')`, placeholder `ct(i18n,'search')` ('Mjesto, ulica, linija ili stajalište'). `modesArg()` (:387-390) → `mapMode==='schema' ? new Set([ROUTE_TYPE_TRAM]) : null` (all modes drawn; bus thinning by zoom is WP2's profile rule via `busesVisible`, `kiosk/mapview.ts:130`). `syncMap` renderer (:1062) → `c.lightweight ? 'map' : c.mapMode?.snapshot() ?? 'map'`. `discovery()` (:309-314) is called only while `query` is non-empty: `discover(cityState(), items, { group:'living', category:'', query, window:'today', center: place, radius: 5000, now })` (category terms like 'wc', 'voda', 'tržnica' still resolve through `CATEGORY_TERMS`, `city/discovery.ts:8-14` — categories appear only as results); `askCity()` keeps the query branch (:318) and drops the group/category branch (:317). `cityMapPoints()` (:341-349) → `[...curatedPoints(cityState(), dogadanja, now, { night: !isDaylight(now) })` (WP2's extraction of `kiosk/mapview.ts:502-515 cityWindowPoints`: BAJS discs with counts, venues with a programme today, pharmacy at night — no `clusterPlaces`), `...(query ? discovery().points : [])`, plus the selected place kept outside filters as today (:344-347). `renderSheet()` (:660-734): default branch (no query, no selection) → `html = nearbyRowsMarkup(i18n, selectNearby(nearbyInput(ctx())), NEARBY_PHONE_ROWS)` and `peekHtml = `<strong>${esc(place.name)}</strong><span class="t-peek-count">${esc(nearby.pill)}</span>`` (no vehicle/closure counts); delete `overview()` (:748-766) and `overviewPeek()` (:768-780), `screenStopRoutes()` (:742-746) and the `delaysOpen`/`folds.has('routes'|'closures')` state; `detailMarkup` stop case (:805-822): `arrivalsAt(..., { stopIds: group.ids, rows: 12 })`. `view.ts`: `stopDetailMarkup` (:566-578) → title (detailHead with save toggle) · `<section class="t-block" data-testid="stop-arrivals">` with the first three rows as `departureRow` (large, step 2) · `<h4 class="t-head">${i18n.t('arrivals.timetable')}</h4>` ('Vozni red' — new key hr/en 'Vozni red'/'Timetable') with rows 4–12 as `departureRow` (grey clock rows) · one `<p class="t-note">${arrivals.note}</p>` · `stop-meta` · `Prikaži na karti` · `stopRoutes` list; delete `arrivalsSection`'s per-row `sub` word (:548). Delete `overviewMarkup`, `peekMarkup`, `delaysMarkup`, `OverviewData`, `PeekData`, `DelayRow`, `DELAY_ROWS`, `PEEK_BADGES` from view.ts only if `layers/u-pokretu.ts renderLightweight` (:200-238) still compiles (it imports `badge`, `closuresMarkup`, `delayTrail`, `kindWord`, `NOTICE_ROWS`, `RUNNING_ROWS`, `vehicleCountGlyph` — keep those; the lightweight face is unreachable and dies in WP5). Camera on first render (:1172-1175): `camera = frameAround(place, 6, { width: stage.clientWidth || 390, height: stage.clientHeight || 600 })` from WP2 (fallback `{ center:[place.lon,place.lat], zoom: Math.min(15.5, Math.max(12.7, fieldZoom(width, place.lat, 2000))) }`, `kiosk/mapview.ts:99`); when the place changes (a saved stop added) re-frame once. `map.css`: delete the `.t-map-menu*`, `.t-map-chips`, `.t-map-tools`, `.t-schema-legend`, `.t-toggle` rules (:101-129 block, :124, :128-129, :265-269, :276-278, :283-286); keep `.t-map-status`, sheet and stage rules. `test/city/workspace.test.ts:58` → delete the `dataset.cityGroup` assertion. Presentation: a route/stop/place/street selection relays as today (`relay()` :422-428); nothing else changes on the wire.

### 7. Događanja page (reached from Još): multi-day items say 'do d. m.' instead of 'cijeli dan'  `[S]`

Files: `app/src/layers/kultura.ts`, `test/app/layers.test.ts`

`kultura.ts timeCell` (:436-441): when `item.data?.precision==='range'` or the until-day differs from the at-day, print `<span class="ev-allday">${i18n.t('events.untilDate',{date: dayMonth(item.until, now)})}</span>` ('do 25. 9.') instead of `time.allDay` [O-53 slop 22]; a single-day `day`-precision item keeps 'cijeli dan' (`test/app/layers.test.ts:668` still holds). `eventRow`'s sub-line `untilText` (:459-464) then drops the duplicate 'do …' for range items. Keep the Danas/Sutra/Idućih 7 dana chips, search, category select and the count line: the page is a Još destination, not the first viewport. Export `eventsCount` (step 5). `LAYER_MODULES.kultura` unchanged (`layers/index.ts:39`); the wire `LayerId 'kultura'` stays (kiosk paired `renderKultura`, `kiosk/paired.ts:786`).

### 8. Desktop = the phone, wider: Sada feed and Karta side by side  `[M]`

Files: `app/src/dashboard.ts`, `app/src/ui/dashboard.css`, `app/src/ui/overview.css`, `app/src/ui/map.css`

`dashboard.ts render()` (:423-449): when `surface()==='desktop' && !directory && (layer==='grad-sada' || layer==='u-pokretu')`, build `next = doc.createElement('div'); next.className='ki-desk'; next.dataset.key='desk'; next.append(renderGradSada(ctx), renderUPokretu(ctx))` (the workspace section carries `data-reconcile` and its `kaj-persist` slot, `layers/u-pokretu.ts:97-133`, so `reconcile(main, wrapper)` keeps the live map) and set `element.dataset.stage='desk'`; phone keeps `data-stage='map'` for u-pokretu only. `updateTitle()` (:378-383): on the desk both layers title 'Kaj ima? · Sada'. `navigate()` between the two on the desk is a no-op for the workspace (same pair). `currentPresentationTarget()` (:309-317): drop the `tb-col` `time` (no time filter exists any more; the wire still accepts it — WP1 rewrites the wall's Sada). `dashboard.css`: `.ki[data-stage='desk'] .ki-desk { display:grid; grid-template-columns: minmax(22rem, 32rem) minmax(0,1fr); gap: var(--sp-6); align-items:start }`; `.ki[data-stage='desk'] .ki-desk > .layer[data-layer='u-pokretu'] { position: sticky; inset-block-start: var(--ki-top); block-size: calc(100dvh - var(--ki-top)); min-block-size: 0 }`; reuse the stage rules from `map.css:231-240,293-296` under `.ki[data-stage='desk']` (map fills the column, sheet as a column of `minmax(20rem,23rem)`); `.ki[data-stage='desk'] .ki-main { padding-block-end: var(--sp-6) }`. `overview.css`: delete the desktop two-column `.day-overview` rules (:34,58-66); `.ws-sada` max-inline-size 32rem on the desk. The map's stage mode already reports 'desk' (`workspace.ts:498-502`), so the sheet is a column with no detents and `hitTolerancePx: 8`/`presentationProfile:'desktop'` (:1102-1103).

### 9. Tests, e2e and the walkthrough classifier  `[M]`

Files: `test/app/dashboard.test.ts`, `test/app/layers.test.ts`, `test/integration/feed-to-layers.test.ts`, `test/app/event-location.test.ts`, `test/app/transport-workspace.test.ts`, `test/app/copy-guards.test.ts`, `test/app/shell-css.test.ts`, `test/app/i18n.test.ts`, `e2e/companion-phone.spec.ts (new)`, `e2e/experience-fixtures.ts`, `e2e/a11y.spec.ts`, `e2e/cast.spec.ts`, `e2e/city.spec.ts`, `e2e/experience.spec.ts`, `e2e/mobile.spec.ts`, `e2e/redesign.spec.ts`, `e2e/round-f.spec.ts`, `e2e/schema.spec.ts`, `review.local/companion/walkthroughs/1720-mon/analyse.mjs`

Unit: see testsToUpdate for line-exact changes. `e2e/experience-fixtures.ts` (`installExperienceFixture` :63): add a route for `**/api/city/departures**` answering a `DepartureBoard` for every platform of `FIXTURE_STOP` (12 rows from `FIXTURE_NOW`, operator 'zet', status 'live') and `**/data/stops.json` with the real file, so the fixture Sada shows three rows. New `e2e/companion-phone.spec.ts` (both playwright projects): (1) phone 390×844 → INSTRUCTION/COUNT census = 0 (port the `INSTR` and `COUNTRE` regexes from `analyse.mjs:27-29` and count elements whose bounding top < 844 and whose own text matches), `.sada-departure` with top<844 ≥ 3, `.ki-tabs .ki-tab` texts ['Sada','Karta','Još'], `[data-testid=share-city-header]` visible with text 'Podijeli grad'; (2) Karta cold open with tiles routed to 404 (`round-f.spec.ts:90`): `expect.poll(() => probe('pills'))` non-empty within 2 000 ms, `[data-testid=transport-workspace] details` count 0, no text 'Alati karte'/'Što tražiš?'; (3) search 'Jelač' → first `[data-action=select-stop]` → `[data-testid=stop-arrivals] .sada-departure` first row `bottom <= 844`; (4) desktop 1440×900 → `.ki-domains` count 0, `#layer-grad-sada` and `[data-testid=transport-workspace]` both visible, `[data-testid=share-city-header]` visible. `analyse.mjs` classifier (:63-74): add `sada-departure|nearby-row|stop-arrivals` to the ANSWER branch, `sada-sentence` → ANSWER, `sada-place|nearby-pill` → CONTEXT, `share-city-header` → INVITE (so the post-deploy slot re-run measures the new first viewport).

### 10. Docs: supersede the phone/desktop paragraphs in PRODUCT.md and DESIGN.md  `[S]`

Files: `PRODUCT.md`, `DESIGN.md`

PRODUCT.md 'Approval': add a dated paragraph (22 September 2026, companion round) stating: phone destinations Sada · Karta · Još; Sada opens on the place, one sentence, a map band, three departures at the automatically chosen stop and 'U blizini · 2 km · ~15 min'; Karta is the timeline's map with vehicles on by default and one search; Događanja is a Još row; desktop is the phone, wider, without the six-domain bar; 'Podijeli grad' is a labelled header button; the frozen-snapshot contract is unchanged by this package (WP-end-of-session supersedes it separately). Mark the superseded sentences (see `supersedes`). DESIGN.md 'Phone and desktop': replace the four paragraphs with the contract above (keep the Zaslon paragraph, the map-symbol profile sentence and the 'Događanja uses a dated agenda' sentence rewritten as 'the week agenda is a Još page'). docs/kiosk.md needs no change (its phone mentions at lines 8-9 and 231 stay true).

## Parallel lanes

Chunk A (steps 1–2: place + departures block) first — it defines `PlaceContext`, `departuresBlock`, `departureRow` that B and C import. After A, in parallel: Chunk B (step 3 Sada + `city/feed.ts` + `nearby-markup.ts`), Chunk C (step 6 Karta workspace + view + map.css), Chunk D (steps 4–5: chrome/tabs/share/Još — independent of A, can start at once), Chunk F (step 7 kultura — independent, can start at once). Chunk E (step 8 desktop pair) after B and C. Chunk G (step 9 tests/e2e/classifier) runs alongside each chunk for its own tests, with the e2e spec finished after E. Step 10 docs at the end. Sequential constraints: A → {B, C}; {B, C} → E; everything → final `npm run typecheck && npx vitest run && npx playwright test`.

## Tests to update

- test/app/dashboard.test.ts:155-156 — tabs `['Sada','Karta','Još']`; `.ki-tabs [data-layer=kultura]` → toBeNull; :280 EN `['Now','Map','More']`
- test/app/dashboard.test.ts:183-197 — directory rows `['kultura','zrak-i-nebo','sigurnost','uprava-i-pravo']`; add: `dir-kultura .row-title` = 'Događanja ovaj tjedan' and `.row-sub` matches /događanj/
- test/app/dashboard.test.ts:913-924 — desk: open kultura via `[data-testid=status-more]` then `dir-kultura` (no `.ki-domains`)
- test/app/dashboard.test.ts:1108-1120 — status keys phone `['wordmark','session','safety']` (a scanner with a screen: `['wordmark','screen','share','session','safety']`), desk `['wordmark','space','session','more','safety']` (+screen/share); `.ki-domains [data-layer]` count 0; `status-search` still null
- test/app/dashboard.test.ts:1163-1170 — 'desktop transport navigation exposes the single search field in lightweight mode': assert `[data-testid=transport-search]` exists inside `#ki-main` after mount (desk pair) instead of clicking `.ki-domains`
- test/app/dashboard.test.ts:1182-1184 — loop over `['u-pokretu','grad-sada']` only
- test/app/dashboard.test.ts:1301-1320 — desk directory: `.dir-item[data-layer]` count 4, first `kultura`; remove `.ki-domains [data-layer=kultura]` and the `.ki-domains` click (navigate via `[data-action=nav][data-layer=u-pokretu]` or drop)
- test/app/dashboard.test.ts:561 — `[data-testid=tb-weather][data-status=stale]` → `[data-testid=sada-sentence]` present (stale weather fallback still renders a sentence)
- test/app/dashboard.test.ts:255-286,305-308 — the session-sheet 'Podijeli grad' row stays; add: `[data-testid=share-city-header]` present for the scanner, absent for a peer (:286) and after freeze
- test/app/layers.test.ts:119 — grad-sada title check → `[data-testid=sada-place]` text non-empty (not `cityOverview.title`)
- test/app/layers.test.ts:125-281 — delete the 'grad-sada (Sada, the time band)' describe; replaced by test/app/sada.test.ts (step 3)
- test/app/layers.test.ts:652-727 — kultura tests keep; :668 'cijeli dan' (day precision) still passes; add a `range` item asserting 'do d. m.' in the time cell
- test/integration/feed-to-layers.test.ts:51-67 — grad-sada: assert `[data-testid=day-departures]`, `[data-testid=nearby]` present and no UNAVAILABLE word; :86-107 — replace 'tiles the deviating lines on Sada and lists a delay per route in Promet' with: Sada shows ≤3 departure rows; the transport workspace stop detail lists arrivals (no `transport-delays`)
- test/app/event-location.test.ts:34 — `.day-overview` → `[data-testid=nearby]`
- test/app/transport-workspace.test.ts:137-230 — schema/map switch: drive `ctx.mapMode.set('schema')` instead of `[data-testid=map-mode-toggle]`; remove assertions on `t-map-menu`, mode chips, `fit-city`, `toggle-full`
- test/app/transport-workspace.test.ts:267-297 — overview → default sheet = `[data-testid=nearby]` rows; no 'Linije u pokretu', no notices block; honesty note once
- test/app/transport-workspace.test.ts:300-348 — peek = place name + nearby pill (no `.line[data-size=s]` badges, no 'vozila u pokretu · zatvaranja'); :350-359 delete (busiest-lines peek)
- test/app/transport-workspace.test.ts:361-388 — folds: keep the twelve-stop fold in the route detail; drop the routes and closures folds
- test/app/transport-workspace.test.ts:541-556 — kiosk still gets a still map and open sheet; drop 'no controls' assertions that referenced the menu
- test/app/transport-workspace.test.ts:557-577 — delete (delays list gone)
- test/app/transport-workspace.test.ts:818-891 — arrivals first, three large rows then 'Vozni red' rows; per-row word gone; `Procjena iz ZET-ovih podataka o vozilima` once (:873 still true)
- test/city/workspace.test.ts:58 — delete `dataset.cityGroup` assertion
- test/app/copy-guards.test.ts:203-233 — live row carries `class="t-live"`, scheduled row carries `<time` and no `arrivals.scheduled`; `arrivals.note` once; `arrivals.live` never as text
- test/app/shell-css.test.ts:96 — phone head columns `auto minmax(0, 1fr) auto auto auto auto`; :185 (`.ki-domains` grid-column) delete; :196 desk head columns `auto minmax(0, 1fr) auto auto auto auto auto`; add `.ki-tabs` `repeat(3, 1fr)` and `.ki-share` min-inline-size var(--target)
- test/app/i18n.test.ts:49 — parity holds only if every new key lands in both hr.json and en.json; :62 DEAD_KEYS list must not contain the new keys
- test/app/timeband.test.ts, timeband-sync.test.ts, producers.test.ts, producers-mobility.test.ts, tiles.test.ts, lastrun.test.ts:165 — untouched in WP4 (modules stay); deleted with the modules in WP5
- e2e/a11y.spec.ts:268-270 — delete the `.city-filter-disclosure > summary` clicks
- e2e/cast.spec.ts:36 — remove the `.ki-domains [data-layer=u-pokretu]` click (the desk shows Karta beside Sada)
- e2e/city.spec.ts:27-29,45 — remove the disclosure, `city-group`/`city-category` clicks and the `.ki-domains` click; search 'BAJS' instead to reach bikes as results
- e2e/experience.spec.ts:138 — delete the `map-full-toggle` click; :163-172 — `.sada-departure` rows with top<700 ≥ 3; :186 — delete `.day-time` count 5
- e2e/mobile.spec.ts:224-237 — `.sada-departure` (≥3 in the first 700 px); :354-370 delete the time-filtering test; :444-446 delete the `tb-seg` 200 % check; :548-568 `DESK_DIRECTORY = ['kultura','zrak-i-nebo','sigurnost','uprava-i-pravo']`, `.ki-domains [data-layer]` count 0
- e2e/redesign.spec.ts:167 — open kultura on phone b via `tab-more` + `dir-kultura`
- e2e/round-f.spec.ts:96-101 — delete the `city-group transport` click (vehicles drawn by default); :225 `map-mode-toggle` → set the map-mode device store via `page.addInitScript` before load
- e2e/schema.spec.ts:16,137 — delete the `city-group` clicks; :22,92 `map-mode-toggle` → device store as above
- review.local/companion/walkthroughs/1720-mon/analyse.mjs:63-74 — classifier branches for `sada-*`, `nearby-*`, `share-city-header` (post-deploy measurement only)

## Acceptance (executable)

| # | Criterion | How (command / threshold) |
|---|---|---|
| A1 | Type check and unit suite green | `npm run typecheck && npx vitest run` exit 0 (after the listed test updates; timeband/producer tests still pass because their modules stay until WP5) |
| A2 | Sada first viewport: 0 INSTRUCTION, 0 COUNT, ≥3 departure ANSWER units, the place is the title | `npx vitest run test/app/sada.test.ts` (new): no `.day-stop-prompt`, no 'Sada u gradu.', no /\b\d+ zatvaranja\b/, no 'Radovi u gradu'; `[data-testid=sada-place]` = nearest stop name; 1 ≤ `.sada-departure` ≤ 3; every `.nearby-row` has `<time>` or `.nearby-always`; DOM order h2 → sentence → departures → nearby. Then `npx playwright test e2e/companion-phone.spec.ts -g "phone first viewport"`: INSTR/COUNTRE census (regexes from analyse.mjs:27-29) over elements with top<844 = 0 and 0; `.sada-departure` with top<844 ≥ 3 |
| A3 | Karta cold open shows vehicles with zero taps and has no disclosures | `npx playwright test e2e/companion-phone.spec.ts -g "Karta cold open"`: `data-pills` on `[data-testid=map-canvas]` non-empty within 2 000 ms (tiles routed to 404 as in round-f.spec.ts:90); `[data-testid=transport-workspace] details` count 0; no text 'Alati karte' or 'Što tražiš?'; BAJS count census deferred to WP2's `data-bajs` probe |
| A4 | Tap a stop → departures above the fold | `npx playwright test e2e/companion-phone.spec.ts -g "stop detail"`: after search 'Jelač' and the first `[data-action=select-stop]`, the first `[data-testid=stop-arrivals] .sada-departure` has `bottom <= 844` at 390×844 |
| A5 | Three tabs, Događanja folded, header share button | `npx playwright test e2e/companion-phone.spec.ts -g "tabs"`: `.ki-tabs .ki-tab` texts `['Sada','Karta','Još']`; `tab-more` → `dir-kultura` row text starts with 'Događanja ovaj tjedan' and its sub-line matches /događanj/; `[data-testid=share-city-header]` visible with text 'Podijeli grad' in a direct session and absent in a peer session (`npx vitest run test/app/dashboard.test.ts -t share`) |
| A6 | Desktop = phone, wider | `npx playwright test e2e/companion-phone.spec.ts -g "desktop"` at 1440×900: `.ki-domains` count 0; `#layer-grad-sada` and `[data-testid=transport-workspace]` visible at once; `[data-testid=share-city-header]` visible; `document.documentElement.scrollWidth <= 1441` at 100/125/200 % text (reuse experience.spec.ts:176-190 loop without the `.day-time` assertion) |
| A7 | No slop strings render on Sada/Karta | `grep -rn "Odaberi i spremi\\|Gradska referentna\\|Sada u gradu\\|Što tražiš\\|Živi grad\\|Alati karte\\|Ovdje i sada\\|Što slijedi" app/src --include=*.ts` returns hits only inside `app/src/city/strings.ts` (dead keys, WP5) and none in `app/src/layers/`, `app/src/transport/`, `app/src/experience/` |
| A8 | Accessibility unchanged or better | `npx playwright test e2e/a11y-session.spec.ts e2e/a11y.spec.ts` exit 0 (axe serious/critical = 0 on Sada, Karta, Sigurnost at phone and desk) |
| A9 | Production first viewport after deploy (brief P1 #12, P2 #13-14) | After `git push` to main and Cloudflare build: `node review.local/companion/walkthroughs/1720-mon/run-slot.mjs --skip-portrait --skip-desktop --log-minutes=1 && node review.local/companion/walkthroughs/1720-mon/analyse.mjs` → inventory.json phone Sada: INSTRUCTION 0, COUNT 0, ANSWER ≥ 3 (classes updated in step 9); one temporary screen, ≤2 redemptions |

## Risks

- WP1/WP2 naming drift: WP4 consumes `selectNearby`, `ctx.sentence`, `curatedPoints`, `frameAround`; isolate every import in `app/src/city/feed.ts` and the map-band/camera lines so a rename touches one file each
- Whole-city temporary screens carry `stop: null` today, so without WP1's `ScreenMetadata.place` the phone's Sada from such a screen opens on the Jelačić default (still a real stop with departures, but not the venue's place)
- Desktop two-pane reconcile: the persistent workspace lives inside a new `.ki-desk` wrapper; verify `reconcile(main, wrapper)` keeps the `kaj-persist` node (dashboard.test.ts:477-516 'reconciliation across polls') and that switching desk ↔ phone width (`onMedia`) does not detach the map
- `ensureStops()` now runs for every session (245 kB JSON); acceptable (already fetched with any saved stop) but check first-paint timing in e2e; consider a Cache-Control check on `/data/stops.json`
- Renaming `layers.u-pokretu` to 'Karta' ripples into the kiosk session pill and document titles (intended); grep `layers.u-pokretu` consumers and update `test/app/kiosk*.test.ts` strings if any assert 'Promet' there
- The schema renderer loses its button; it remains reachable only through the device store (map-mode-store) and `?prikaz=shema` on the kiosk — decide in WP5 whether it stays a phone feature
- Removing the phone `time` from the presentation target changes what a kiosk paired Sada receives until WP1 rewrites the wall; the protocol still accepts `time`, so nothing breaks on the wire
- `copy-guards.test.ts` and `i18n.test.ts:52-60` pin catalogue strings (`transport.trams`, `transport.vehiclesNow_few`) — keep those keys in WP4 even though their renderers go; delete keys and pins together in WP5
- Karta sheet default = nearby rows means ZET notices ('Obavijesti ZET-a') and the per-route delay table leave the phone entirely; they remain on the kiosk paired Promet — confirm with the owner in the strike-through round (slop 25 says line-state words leave the first viewport)
- Countdown freshness on Sada relies on the transit poll (~10 s) re-rendering; no per-second tick — acceptable per brief §4.2, but confirm `arrivalTime` minutes never show a stale 'sada' for more than one poll

## Agent briefs (self-contained; one per lane)

### Brief 1

CHUNK A — Place and departures (steps 1–2). Repo /home/matija/projects/kajima/working-deployed. Create `app/src/city/place.ts` exporting `PlaceContext` and `resolvePlace(input)` exactly as step 1 describes (reuse `rankStops`/`routeType` from `app/src/kiosk/stops.ts:61,37`, `defaultLocation` from `app/src/city/location.ts:7`; constants `TRAM_NEAR_M=400`, `BUS_NEAR_M=300`, `DEPARTURES_STOP_M=800`). Delete `locationLabel` from `city/location.ts:11-16` and change `city/markup.ts:54` to `${m} m · ${reference.name}` (the `reference` parameter becomes `PlaceContext`). Add `place?: PlaceContext` to `ExperienceActions` in `app/src/core/contracts.ts:52-86`. In `app/src/dashboard.ts`: call `ensureStops()` unconditionally at :424; add `place: resolvePlace({screen: screen(), saved, stops: stops ?? undefined, location: locationContext})` to `layerContext()` :402-418; key `ensureLastRun()` :501-515 on `place.departuresStop?.id`. Rewrite `app/src/city/next-departures.ts` as `departuresBlock(ctx, place, opts)` + `departureRow(i18n,row,frozenAt,kindOf)` per step 2 (reuse `arrivalTime` from `app/src/transport/view.ts:529`, `lineBadge` from `experience/blocks.ts:134`, `platformIds` from `kiosk/arrivals.ts:57`, `arrivalsAt` from `shared/city/arrivals.ts:87`); no prompt, no 'Procjena', one empty rule; keep `data-testid="day-departures"` on the root. Add CSS for `.sada-departure` in `app/src/ui/overview.css` (grid auto/1fr/auto, live time blue `--tone-action-brand`, scheduled grey). Write `test/app/place.test.ts` (6 cases in step 1) and `test/app/next-departures.test.ts` (rows ≤3, live row has `data-live="true"` and `.t-live`, scheduled row a `<time>`, no 'Procjena', empty ladder). Update `test/app/copy-guards.test.ts:203-233` per testsToUpdate. Do not touch grad-sada.ts or workspace.ts beyond the `locationLabel` call sites (they are rewritten by chunks B and C); leave `next-departures`'s old export name aliased (`export const nextDepartures = (ctx)=>departuresBlock(ctx, ctx.place ?? resolvePlace({...}))`) so grad-sada.ts still compiles until chunk B lands. Acceptance: `npm run typecheck && npx vitest run test/app/place.test.ts test/app/next-departures.test.ts test/app/copy-guards.test.ts`.

### Brief 2

CHUNK B — Sada layer (step 3). Repo /home/matija/projects/kajima/working-deployed; requires chunk A merged. Rewrite `app/src/layers/grad-sada.ts` to the DOM order in step 3: `h2.sada-place[data-testid=sada-place]` = `ctx.place.name`; `article.sada-sentence[data-testid=sada-sentence][data-kicker]` from `ctx.sentence` (WP1) with the fallback `sentenceFallback` (weather line from `experience/weather-status.ts:32`); phone-only `a.sada-map[data-testid=sada-map]` wrapping `ctx.maps.slot({ id:'grad-sada-map', renderer:'map', interactive:false, presentationProfile:'handheld', attributionCompact:true, points, lines, center:[place.lon,place.lat], zoom, stop: place.stop, theme, locale, reducedMotion })` (points = `vehiclePoints` + `closureLines` from `layers/u-pokretu.ts:66-76` + WP2 `curatedPoints` or `kiosk/mapview.ts cityWindowPoints` fallback; zoom = WP2 `frameAround` or `fieldZoom(390, lat, 2000)` clamped 12.7–15.5 from `kiosk/mapview.ts:99`); `departuresBlock(ctx, ctx.place, { heading: place.kind==='address'||place.kind==='city' })`; `section.nearby[data-testid=nearby]` with header 'U blizini' + pill and `ol.nearby-rows` from new `app/src/city/nearby-markup.ts nearbyRowsMarkup(i18n, rows, cap)` (rows from WP1 `selectNearby(nearbyInput(ctx))`, adapter in new `app/src/city/feed.ts`; `NEARBY_PHONE_ROWS=8`, `NEARBY_DESK_ROWS=12`); `provenanceBlock` unchanged. Move `tileHref` (`experience/tiles.ts:70-73`) to `experience/blocks.ts` as `selectionHref` and re-export from tiles.ts. Delete all imports of `buildTimeband`, `FILTER_KEY`, `tileMarkup`, `skeletonTileMarkup`, `weatherStatus`, `zagrebWeekdayDate`, `dayOpportunities`, `locationLabel` from grad-sada.ts. Rewrite `app/src/ui/overview.css` (delete every `.day-*` rule; add `.ws-sada`, `.sada-place`, `.sada-sentence` (+ `.kicker` tones by `data-kicker`), `.sada-map` 112 px block, `.nearby-head`, `.nearby-pill`, `.nearby-row` grid `4.5rem minmax(0,1fr)` min 4rem, `.nearby-always`). Add hr/en keys from step 3 (`sada.mapBand`, `sada.departuresAt`, `nearby.title`, `nearby.pill`, `nearby.always`, `nearby.kicker.*`) unless WP1 already provides them. Write `test/app/sada.test.ts` (assertions a–f in step 3) and update `test/app/layers.test.ts:119` and delete :125-281, `test/integration/feed-to-layers.test.ts:51-67,86-107`, `test/app/event-location.test.ts:34`, `test/app/dashboard.test.ts:561`. Acceptance: `npm run typecheck && npx vitest run test/app/sada.test.ts test/app/layers.test.ts test/integration test/app/dashboard.test.ts`.

### Brief 3

CHUNK C — Karta workspace (step 6). Repo /home/matija/projects/kajima/working-deployed; requires chunk A. In `app/src/transport/workspace.ts` delete the `t-map-menu` details (:173-187) and the `city-filter-disclosure` details (:203-207) with every reference, state, painter and handler listed in step 6 (lines :222-231, :239-243, :286-288, :298-299, :320-340, :387-390, :590-630 (affected lines only), :897-922, :937-991, :1062, :1171, :1177-1184); fix the search label at :595-597; `modesArg()` → `mapMode==='schema' ? new Set([ROUTE_TYPE_TRAM]) : null`; `cityMapPoints()` → curated set (WP2 `curatedPoints`, fallback `kiosk/mapview.ts:502 cityWindowPoints`) + searched discovery points, no `clusterPlaces`; `discovery()` only while `query`; `renderSheet()` default branch → `nearbyRowsMarkup(i18n, selectNearby(nearbyInput(ctx())), NEARBY_PHONE_ROWS)` with peek `<strong>place.name</strong>` + nearby pill; delete `overview()`, `overviewPeek()`, `screenStopRoutes()`; stop detail `arrivalsAt(..., {stopIds: group.ids, rows: 12})`; first-render camera via WP2 `frameAround(place, 6, box)` (fallback `fieldZoom`). In `app/src/transport/view.ts` rewrite `stopDetailMarkup` (:566-578) and `arrivalsSection` (:543-563): three `departureRow`s, then `<h4 class="t-head">Vozni red</h4>` (new key `arrivals.timetable` hr 'Vozni red' / en 'Timetable') with rows 4–12, then one `arrivals.note`, then meta, 'Prikaži na karti', lines. Delete `overviewMarkup`, `peekMarkup`, `delaysMarkup`, `DelayRow`, `DELAY_ROWS`, `PEEK_BADGES`, `OverviewData`, `PeekData` only if `layers/u-pokretu.ts` still compiles (it imports `badge`, `closuresMarkup`, `delayTrail`, `kindWord`, `NOTICE_ROWS`, `RUNNING_ROWS`, `vehicleCountGlyph` — keep those). In `app/src/ui/map.css` delete the menu/chip/tool rules (:101-129 block, :124, :128-129, :265-269, :276-278, :283-286). Update `test/app/transport-workspace.test.ts` (:137-230, :267-297, :300-348, :350-359 delete, :361-388, :541-556, :557-577 delete, :818-891) and `test/city/workspace.test.ts:58` per testsToUpdate; update e2e `a11y.spec.ts:268-270`, `city.spec.ts:27-29,45`, `experience.spec.ts:138`, `round-f.spec.ts:96-101,225`, `schema.spec.ts:16,22,92,137` (schema via the map-mode device store set in `addInitScript`). Acceptance: `npm run typecheck && npx vitest run test/app/transport-workspace.test.ts test/city test/app/copy-guards.test.ts`.

### Brief 4

CHUNK D — Chrome, tabs, share button, Još (steps 4–5). Repo /home/matija/projects/kajima/working-deployed; independent of A–C. `app/src/experience/chrome.ts`: `PHONE_TABS` (:23) = grad-sada, u-pokretu; `MORE_LAYERS` (:24) = kultura, zrak-i-nebo, sigurnost, uprava-i-pravo; `layerLabel` (:86-89) plain `layers.*`; new `shareButtonMarkup(i18n, s)` (`.ki-share`, `data-key="share"`, `data-action="share-city"`, `data-testid="share-city-header"`, label `session.share` = 'Podijeli grad', rendered when `s.canShare`); `statusLineMarkup` (:108-119) phone `wordmark·display·share·session·safety`, desktop `wordmark·space·display·share·session·more·safety` (no clock, no `ki-domains`). `app/src/i18n/hr.json` `layers.u-pokretu` → 'Karta', en.json → 'Map'. `app/src/dashboard.ts:359-360` drop the desktop weather for the status line. `app/src/ui/dashboard.css`: phone head columns (:110) `auto minmax(0,1fr) auto auto auto auto`, placements (:195-199) screen 3 / share 4 / session 5 / safety 6, `.ki-share` styles (visible label, 44 px, brand colour; icon-only under `@container header (max-width:18rem)` with `aria-label`), `.ki-tabs` (:203) `repeat(3,1fr)`, desktop block (:256-292) delete `.ki-domains*`, `--ki-top: 3.5rem`, head columns `auto minmax(0,1fr) auto auto auto auto auto`. `app/src/experience/directory.ts`: `directoryLayers()` = `MORE_LAYERS` on both surfaces; kultura row title `directory.eventsWeek` 'Događanja ovaj tjedan' with sub `eventsCountLine` (export `eventsCount(snapshot, city, now)` from `layers/kultura.ts` extracted from :553-576 and format with `events.countLine|count|ongoingCount`); delete `transitLine`; move the six inline HR/EN ternaries (:93,94,95,97,125,126) to `directory.saved|savedEmpty|savedRecord|route|stop|remove|destinations|preferences` in both json files (Croatian strings verbatim from the current source). Update `test/app/dashboard.test.ts:155-156,183-197,280,913-924,1108-1120,1163-1170,1182-1184,1301-1320` and add share-button assertions (:255-286 area), `test/app/shell-css.test.ts:96,185,196`, e2e `cast.spec.ts:36`, `mobile.spec.ts:548-568`, `redesign.spec.ts:167`. Acceptance: `npm run typecheck && npx vitest run test/app/dashboard.test.ts test/app/shell-css.test.ts test/app/i18n.test.ts test/app/copy-guards.test.ts`.

### Brief 5

CHUNK E — Desktop pair (step 8). Repo /home/matija/projects/kajima/working-deployed; requires chunks B, C, D. In `app/src/dashboard.ts render()` (:423-449): on `surface()==='desktop' && !directory && layer ∈ {grad-sada, u-pokretu}` render `div.ki-desk[data-key=desk]` containing `renderGradSada(ctx)` then `renderUPokretu(ctx)`, set `element.dataset.stage='desk'`; `updateTitle()` (:378-383) → 'Sada' for both on the desk; `currentPresentationTarget()` (:309-317) drop the `tb-col` time. CSS in `app/src/ui/dashboard.css` and `map.css` per step 8 (`.ki-desk` grid `minmax(22rem,32rem) minmax(0,1fr)`, sticky map column `calc(100dvh - var(--ki-top))`, stage rules under `.ki[data-stage='desk']`). Delete the desktop `.day-overview` rules in `overview.css` if chunk B left any. Verify `test/app/dashboard.test.ts:477-516` (reconciliation) and :1163-1170 pass with the pair; add a unit test: desk mount has both `#layer-grad-sada` and `[data-testid=transport-workspace]`, a poll keeps the same workspace node. Acceptance: `npm run typecheck && npx vitest run test/app/dashboard.test.ts && npx playwright test e2e/companion-phone.spec.ts -g desktop e2e/mobile.spec.ts -g 1440`.

### Brief 6

CHUNK F — Događanja page (step 7). Repo /home/matija/projects/kajima/working-deployed; independent. `app/src/layers/kultura.ts timeCell` (:436-441): range-precision or multi-day items print `events.untilDate` ('do 25. 9.') in the time column; `eventRow` (:459-464) drops the duplicate until in the sub-line for those items; keep chips, search, category select, count line. Export `eventsCount` (for chunk D) if not already done. Update `test/app/layers.test.ts:652-727`: add a `range` item case asserting 'do d. m.' and keep :668. Acceptance: `npx vitest run test/app/layers.test.ts -t kultura`.

### Brief 7

CHUNK G — Verification (step 9–10). Repo /home/matija/projects/kajima/working-deployed; after E. Extend `e2e/experience-fixtures.ts installExperienceFixture` with routes for `**/api/city/departures**` (12-row live `DepartureBoard` per FIXTURE_STOP platform from FIXTURE_NOW) and `**/data/stops.json`. Write `e2e/companion-phone.spec.ts` with the four tests in step 9 (phone census with the INSTR/COUNTRE regexes ported from `review.local/companion/walkthroughs/1720-mon/analyse.mjs:27-29`; Karta cold open `data-pills` within 2 s using the tile-404 route from `e2e/round-f.spec.ts:90` and `probe` from :56; stop detail rows above the fold; desktop pair). Update `analyse.mjs:63-74` classifier for `sada-*`, `nearby-*`, `stop-arrivals`, `share-city-header`. Write the PRODUCT.md approval paragraph and the DESIGN.md 'Phone and desktop' replacement (step 10; superseded sentences listed in the plan). Run the full gate: `npm run typecheck && npx vitest run && npx playwright test`. Do not deploy: deployment is only `git push` to main by the owner's flow; production screens only through the documented local flow.

## Verifier verdict (adversarial pass, 22 Sep)

Refuted as written: **yes**. Problems found:

- (1) Cited file does not exist: step 4 names `presentation.ts:466` as the target-label consumer of `layers.u-pokretu`. There is no `app/src/presentation.ts`; the consumer is `app/src/experience/presentation.ts:40` (88-line file): `i18n.t(`layers.${target.layer}`)`.
- (1) kultura.ts line pointers are ~240 lines past the end of the file (366 lines): `timeCell` is at :198-203 (plan :436-441), `untilText` :216-218, `eventRow` :221-227 (plan :459-464), `renderKultura` :312-345 (plan :553-576), the count line :335-340 (plan :574-576). `isAllDay` (:124-127) already treats `precision==='range'` as all-day, and `eventRow` already appends `untilText` in the sub-line for multi-day items — the change is to route range/multi-day items to `untilText` inside `timeCell` and drop it from the sub-line, not to add a new key (`events.untilDate` 'do {date}' exists).
- (1) dashboard.css pointers are off by ~85-90 lines: phone `.ki-head` grid is at :21-31 (plan :110), phone placements `[data-key='screen'|'session'|'safety']` :109-113 (plan :195-199), `.ki-screen` :33-35 (plan :119), `@container header (max-width: 18rem)` :102-108 (plan :188-194), `.ki-tabs` :117 (plan :203), desktop block `@media (min-width: 60rem)` :170-206 (plan :256-292), `.ki-domains*` :177-182 (plan :263-268), `--ki-top: 6.5rem` :171 and `scroll-padding-block` :172 (plan :258). shell-css.test.ts:96 pins `grid-template-columns: auto minmax(0, 1fr) auto auto auto` (note the space after the comma) and :196 the desk value — new values must keep that exact spacing.
- (1) map.css: the `.t-map-menu*` rules are at :335-343 (plus `app/src/ui/city.css:73`), not in the ':101-129 block'; :101-129 holds `.t-map-status` (keep) and `.t-map-tools` (:113-129, delete). `.t-map-chips` :56-59, `.t-toggle*` :60-70, `.t-schema-legend` :71-…; :265-269, :276-278, :283-286 are correct. The `.city-filter-disclosure`/`.city-groups`/`.city-filter` rules in city.css:2-7 also become dead in WP4 (plan defers them to WP5 — acceptable but say so).
- (1) Minor pointer drift: `analyse.mjs` INSTR/COUNTRE regexes are at :23-25 (plan :27-29); classifier Sada branches :62-79 (plan :63-74). `worker/city/schedules.ts departuresFrom` is :168-188 (file has 188 lines; plan :168-193). `PHARMACY_POINTS` is at `kiosk/local.ts:361`. `LAYER_MODULES.kultura` is `layers/index.ts:37`. `ScreenMetadata` is `worker/protocol.ts:34-40`.
- (2)/(5) Step 3 'delete every `.day-*` rule' in overview.css breaks step 7: `layers/kultura.ts:342` renders the kept Danas/Sutra/Idućih 7 dana chips with `class="day-time"`, and `.day-times`/`.day-time`/`.day-time:hover` (overview.css :36-38, :55) are their only styles. Deleting them unstyles the Događanja page the plan says stays.
- (2) Step 6 `modesArg()` → `mapMode==='schema' ? Set([TRAM]) : null` drops the kiosk paired workspace's trams-first default (`workspace.ts:1186-1189 modes.delete(ROUTE_TYPE_BUS)` when `kiosk()`), so the paired Promet on the wall would start drawing all buses. Contradicts step 4 'Kiosk untouched' and the kiosk's own contract (R-P1, workspace.ts:254-256).
- (2)/(5) Step 1 `rankStops(stops,{near: ref})` uses the default `limit: 12` (`kiosk/stops.ts:63`): at a bus terminal the 12 nearest platforms can all be bus stops, so the 'first tram stop within 400 m' scan silently fails and falls to bus. Pass `limit: stops.length` (or pre-filter stops with a tram route) — the ranking is O(n) anyway.
- (5) Step 3 `ctx.maps.slot({...})` omits `ariaLabel`, which is required on `MapSlotOptions` (`app/src/map/map-slots.ts:25`); typecheck fails. Also the WP1/WP2 symbols the plan imports do not exist in the tree (`selectNearby`, `NearbyInput`, `NearbyRow`, `curatedPoints`, `frameAround`, `shared/city/frame.ts`, `ctx.sentence`): chunks B and C cannot typecheck 'in parallel' with WP1/WP2 unless `city/feed.ts` ships a local fallback (`selectNearby` over departures/closures/solar/pharmacy and a null `sentence`) that WP1 later replaces.
- (5) Step 2 signature `departureRow(i18n, row, frozenAt?: number, kindOf: (routeId)=>…)` is invalid TypeScript (optional parameter before a required one). Reorder to `(i18n, row, kindOf, frozenAt?)`.
- (4) testsToUpdate misses tests that break: `test/app/dashboard.test.ts:1321-1367` ('the last departure from the screen stop', asserts `[data-testid=tile-lastrun]` inside `.day-event[data-col=veceras]` and `[data-testid=tb]`; and :1351-1357 'asks nothing while the session has no screen stop' contradicts keying `ensureLastRun` on the resolved nearest stop when stops are present); `test/app/dashboard.test.ts:547-566` also asserts `.day-weather-now strong` (:556, :562) and the glasnik retry button in `.day-more` (:557, :564) — the plan lists only :561; the Sada rewrite must give the module retry (`[data-action=retry][data-module]`) a home or the test must drop it.
- (4) Renaming `layers.u-pokretu` to 'Karta' breaks `test/app/kiosk-local.test.ts:95` (`hr.layers` toEqual with 'u-pokretu': 'Promet') and `test/app/kiosk.test.ts:1416` ('Otključano do 14:42 · Promet'); kiosk `s.layers` is built from `layers.${layer}` (`kiosk/strings.ts:395`). The plan names this only as a risk; both lines must be in testsToUpdate.
- (4) e2e files/lines the plan does not list but that reference removed markup: `e2e/a11y-session.spec.ts:144` (`getByTestId('tb')` — this also makes acceptance 7 fail as written), `e2e/a11y.spec.ts:271-272` (`[data-testid=running-routes] button` — gone with the overview sheet; only :268-270 listed), `e2e/motion.spec.ts:289` (`running-routes`) and :300-302 (`.t-map-menu > summary` + `#u-pokretu-map-full`), `e2e/kiosk-layout.spec.ts:47` (`.ki-domains [data-layer=…]` click loop), `e2e/experience.spec.ts:43, 56-57, 87` (`getByTestId('tb')` in the per-viewport loop from :34), :137 (`.t-map-menu > summary`), :181, :184-185 (`tb`, `.day-now`, `.day-ahead`), `e2e/mobile.spec.ts:92` (`tb`), `e2e/schema.spec.ts:28, :71` and `e2e/round-f.spec.ts:224, :229` (`.t-map-menu > summary` — only :22/:92/:225 listed), `e2e/city.spec.ts:30-32` (`bike-mode` toggle and `.city-row-value` 'mjesta za povrat' assertions, removed with the handler).
- (4) `test/app/copy-guards.test.ts:207-210` `arrivalRows()` splits on `data-testid="arrival-rows"` and `<li `; the new stop detail (three `departureRow`s + 'Vozni red' rows) must keep a `data-testid="arrival-rows"` list or the helper must be rewritten — the plan only changes the assertions, not the helper. `test/app/event-location.test.ts:34-36` also asserts the Sada overview text contains `events.sources.kulturpunkt`; the nearby row's `sub` (WP1) must carry the source name or that assertion must change.
- (3) Acceptance 8 depends on `run-slot.mjs`, but `run-slot.mjs:59` still waits up to 15 s for `[data-testid=tb-lane-sada]` (swallowed by `.catch`), and :58 waits on `day-departures|.day-stop-prompt` — step 9 must also update run-slot.mjs (:58-59) or the post-deploy timing (`phoneSadaFirstContent`) is meaningless. `openKiosk(K.page, { create: true })` (:32) creates a temporary screen when `review.local/companion/screen.json` is absent/expired (DQG83CVG expires 22 Sep 17:34) — state this explicitly in the criterion (one screen via /kiosk/, documented flow).
- (3) Acceptance 'No slop strings render' grep is runnable and currently returns hits in `city/location.ts:15` and `city/next-departures.ts:14` (both deleted in steps 1-2) plus `city/strings.ts` — passes after WP4 as written, but 'Alati karte' lives in `hr.json:467` (`transport.toolsLabel`) which `--include=*.ts` never scans; the e2e text check in step 9 (2) is the real guard.
- (5) Step 6 misses `workspace.ts:439` (a second `clusterPlaces(...)` call resolving a cluster tap in `onSelect`) and :248/:1165 (`browseReturn` typed with `group: CityGroup; category: string`) — both reference state the step deletes. Step 4's note '`.ki-safety` already renders on desktop' is wrong: `statusLineMarkup` desktop branch (chrome.ts:118) has no `${safety}`; the new markup adds it, so only the comment is wrong.
- (2) Step 2 drops the `arrivals.note` foot from the Sada block, leaving the blue countdown on Sada labelled only by colour and the `t-live` aria 'uživo'. Within the owner's [O-27] and principle 5 (provenance below the fold via `provenanceBlock`), but the brief's fixed constraint says 'labelled ZET estimate': keep `provenanceBlock` crediting ZET on Sada (it does) and keep the note once in the stop detail (planned) — call this out so the coding session does not also strip the provenance credit.
- (5) Chunk C's agent brief says 'schema via the map-mode device store set in addInitScript' without the key; `review.local/companion/plan/WP4/notes.md` names it `vidikovac.map-mode`, which is wrong — the store key is `MAP_MODE_STORAGE_KEY = 'kajima:map-mode:v1'` (`app/src/core/map-mode-store.ts:4`). The view-filters key `vidikovac.view-filters` (dataOrProtocol) is correct (`core/view-store.ts:25,70`).

Corrections:

Verified as correct (no change): all other cited paths exist; `rankStops`/`routeType` (kiosk/stops.ts:61,37), `platformIds` (kiosk/arrivals.ts:57), `arrivalsAt` (shared/city/arrivals.ts:87), `lineBadge` (blocks.ts:134), `tileHref` (tiles.ts:70-73), `weatherStatus` (:32), `sunTimes` (solar.ts:27), `LJEKARNE` (ljekarne.ts:32), `vehiclePoints`/`closureLines` (u-pokretu.ts:66-76), `persistSlot` (:97-133), `renderLightweight` (:200-238, unreachable from the dashboard because `cityStore.snapshot()` always returns a CityState), `fieldZoom`/`busesVisible`/`cityWindowPoints` (mapview.ts:99,130,502-515), chrome.ts :23-24/:86-89/:108-119/:184-187, directory.ts :26-28/:35-39/:73-79/:93-97/:125-126, every workspace.ts pointer in step 6, dashboard.ts :309-317/:359-360/:378-383/:398-419/:422-449/:488-515/:846/:908-915/:1043, worker/protocol.ts ScreenStop has `routes: string[]`, worker/presentation.ts:9-16, kiosk/frame.ts:13, paired.ts:27/:786, kiosk.ts:395, city-map.ts:1428 GeolocateControl, discovery.ts:8-14 CATEGORY_TERMS, `npm run typecheck` exists, playwright projects are `chromium` and `mobile`, `/api/city/departures?operator=&stop=` (boards.ts:84) and `/data/stops.json` (core/screens.ts:28, file present) match the fixture routes, `MapSlotPassthrough` carries center/zoom/stop/theme/locale/renderer/interactive/attributionCompact/presentationProfile, `reconcile.ts` honours `data-persist-for`, `canShare` (dashboard.ts:295) = scanner ∧ live ∧ !frozen ∧ !shareDenied so the header button is absent for peers and after freeze, `kiosk.qrLabel` is missing and `kiosk.invite.qrLabel` exists (T2 fix is right), `events.countLine_*`/`count_*`/`ongoingCount_*` exist as plural keys, `session.share` = 'Podijeli grad', `ct(i18n,'map')` = 'Karta' already, all PRODUCT.md/DESIGN.md sentences listed under `supersedes` exist verbatim (PRODUCT.md :64-66, :97, :112; DESIGN.md :171-213), docs/kiosk.md :8-9 and :231 stay true, `dashboard.test.ts:24` mocks `loadStops` to `[]` so unconditional `ensureStops()` is safe in unit tests. Fixed constraints are respected (presence gate, no accounts/tracking/push/routing untouched; `docs/prijava` and `app/prijava` untouched; deploy only by git push).

Corrections to apply to the plan:
1. Step 4: replace `presentation.ts:466` with `app/src/experience/presentation.ts:40`.
2. Steps 5 and 7: use kultura.ts pointers `timeCell` :198-203, `untilText` :216-218, `eventRow` :221-227, `renderKultura` :312-345 (extract `eventsCount` from :315-325, format as :335-338). In `timeCell`, when `isAllDay(item)` and (`item.data?.precision==='range'` or `zagrebDayKey(item.until)!==zagrebDayKey(item.at)`) print `<span class="ev-allday">${untilText(i18n,item,now)}</span>` (pass `now`); in `eventRow` pass `''` as `until` for those items.
3. Step 4 CSS: `.ki-head` phone grid :24 → `auto minmax(0, 1fr) auto auto auto auto`; placements :110-112 → screen 3, share 4, session 5, safety 6; `.ki-share` modelled on `.ki-screen` :33-35 with `.ki-share > span { position: static }`; container rule at :102-108; `.ki-tabs` :117 → `repeat(3, 1fr)`; desktop block :170-206: delete :177-182 (`.ki-domains*`), `--ki-top: 6.5rem` (:171) → `3.5rem`, `scroll-padding-block: 6.5rem 0` (:172) → `3.5rem 0`, `.ki-head` (:173) → `grid-template-columns: auto minmax(0, 1fr) auto auto auto auto auto` and drop `grid-template-rows`. shell-css.test.ts :96/:185/:196 updated with the exact strings above.
4. Step 6 CSS: delete map.css :56-70 (`.t-map-chips`, `.t-toggle*`), :71-… (`.t-schema-legend`), :113-129 (`.t-map-tools*`, keep :106-111 `.t-map-status`), :124, :212 (`.t-toggle:active` in the active-state list), :265-268, :276-278, :283-286, :335-343 (`.t-map-menu*`), and city.css:73; leave city.css:2-7 for WP5 (named).
5. Step 3: do NOT delete `.day-times`/`.day-time` (overview.css :36-38, :55); move them to `app/src/ui/layers.css` beside the `.ev-*` rules (same selectors, kultura.ts unchanged) before deleting the rest of the `.day-*` rules; adjust e2e/experience.spec.ts:186 (`.day-time` count 5 → 3 on the kultura page or drop) accordingly.
6. Step 6 `modesArg()` → `kiosk() ? new Set(modes) : mapMode==='schema' ? new Set([ROUTE_TYPE_TRAM]) : null` (keep the `modes` set and :1186-1189 for the kiosk only); transport-workspace.test.ts:541-556 'trams first' stays true.
7. Step 1: `rankStops(stops, { near: ref, limit: stops.length })` for both the tram and bus scans; also state that `renderGradSada` reads `ctx.place ?? resolvePlace({ screen: ctx.screen, saved: ctx.saved, stops: ctx.stops, location: ctx.location })` so unit contexts without a dashboard (layers.test.ts, event-location.test.ts, sada.test.ts) resolve a place.
8. Step 3 slot call: add `ariaLabel: i18n.t('sada.mapBand', { place: place.name })` (required field) and keep the wrapping `<a aria-label>`.
9. Dependency handling: `app/src/city/feed.ts` must export a working fallback `selectNearby(input: NearbyInput): NearbyRow[]` (departures from `arrivalsAt`, closures by `until`, next solar event from `sunTimes`, pharmacy at night from `LJEKARNE`/`PHARMACY_POINTS`, 'uvijek' row from the place story if present) and `sentence: null` until WP1 lands, with the types defined locally and marked `// WP1 replaces`; otherwise sequence chunks B and C after WP1 and WP2 in `parallelisable`.
10. Step 2 signature: `departureRow(i18n: I18n, row: ArrivalRow, kindOf: (routeId: string) => 'tram'|'bus'|'other', frozenAt?: number): string`; keep `data-testid="arrival-rows"` on the stop detail's `<ul>` so copy-guards' `arrivalRows()` helper still slices rows (or rewrite the helper at :207-210 to split on `class="sada-departure"`).
11. testsToUpdate additions: dashboard.test.ts:1321-1367 → rewrite as 'loads the place's last-run file once and threads it into the U blizini row' (assert `loadLastRun` called with `'106_1'`, a `.nearby-row[data-kind=lastDepartures]` present after `await flush()`, no second load on `tick()`; :1351-1357 'no screen stop' becomes 'no stop within 800 m and no stops catalogue' with `loadStops` mocked to `[]`); dashboard.test.ts:547-566 → drop the two `.day-weather-now strong` lines, assert `[data-testid=sada-sentence]` text matches /21 °C/, and move the retry assertion to the provenance block (`.provenance [data-action=retry][data-module=glasnik]`) or delete it; kiosk-local.test.ts:95 `'u-pokretu': 'Karta'`; kiosk.test.ts:1416 `'Otključano do 14:42 · Karta'`; e2e/a11y-session.spec.ts:144 → `getByTestId('day-departures')`; e2e/a11y.spec.ts:271-272 → open the route via search (`transport-search` fill '6', first `[data-action=select-route]`); e2e/motion.spec.ts:289-291 same, :300-302 delete (map-full is gone); e2e/kiosk-layout.spec.ts:47 → `phone.locator('[data-action=nav][data-layer="${layer}"]:visible').first().click()` falling back to `status-more` + `dir-${layer}` for MORE layers; e2e/experience.spec.ts:43,56-57,87,181 → `getByTestId('day-departures')`/`#layer-grad-sada`, :137 delete, :184-185 → `[data-testid=nearby]` visible; e2e/mobile.spec.ts:92 → `day-departures`; e2e/schema.spec.ts:28,71 and e2e/round-f.spec.ts:224,229 → delete the `.t-map-menu > summary` clicks; e2e/city.spec.ts:30-32 → search 'BAJS' and assert the first `[data-action=select-place]` result text matches /bicikl/; event-location.test.ts:35-36 → assert `[data-testid=nearby]` text contains the event title (not the source) unless WP1 puts the source in `sub`.
12. Step 6: also delete `workspace.ts:439` (cluster resolution in `onSelect`) and drop `group`/`category` from `browseReturn` (:248, :1165).
13. Step 9 / acceptance 8: update `run-slot.mjs:58` selector to `[data-testid=day-departures], [data-testid=sada-place]` and delete :59 (`tb-lane-sada` wait); state that `openKiosk({create:true})` creates the day's one temporary screen through /kiosk/ when `screen.json` is missing or expired.
14. Chunk C brief: name the schema device store key `kajima:map-mode:v1` (`localStorage.setItem('kajima:map-mode:v1', …)` in `addInitScript`; read `core/map-mode-store.ts` for the stored shape) and correct notes.md.
15. Minor pointer fixes: analyse.mjs :23-25 and :62-79; schedules.ts :168-188; local.ts:361; layers/index.ts:37; protocol.ts:34-40; step 4 comment about `.ki-safety` (it is added by the new desktop markup, not already rendered).
