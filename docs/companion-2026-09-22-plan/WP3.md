# WP3 — Setup and settings: one "Adresa ili stajalište" field, derived place, click-toggle settings behind a long press

Part of the companion brief `docs/companion-2026-09-22.md` (§15). Line numbers refer to the working tree at build `b300af3`; re-anchor with `grep` before editing. Read-only rules of the brief §1 apply: no `wrangler deploy`, no production screen outside the documented local flow, `docs/prijava` and `app/prijava` untouched.

## 0. Reconciliation with the other packages (binding; read first)

The planner text below was written before the seven packages were compared and before the adversarial verifier ran. Where this section, the verifier verdict at the end, or the brief §15 disagree with the planner text, **this section wins, then the verdict, then the planner text**.

**Owner rulings of 22 September, evening ([O-62]–[O-74], binding over everything below).** (1) **No "zid" on the frontend** [O-66]: the setup line is `kiosk.setup.preview` = **"Na zaslonu: {place} i {count} stajališta uokolo"** (`_one` "… {count} stajalište uokolo") and `kiosk.setup.previewCity` = **"Na zaslonu: cijeli grad."** — no verb, no pronoun, gender-neutral; every planner string that said "Na zaslonu: …" is replaced accordingly (already substituted below). (2) **Empty field → place = Trg bana Jelačića** (`placeFromStop(screenStop('106_1'))`) with the whole-city map window kept [O-52], [O-65]: `POST {}` stays byte-identical and the server derives the default place on read (`screenMetadata()` enrichment), so pre-deploy records behave the same; the header names "Trg bana J. Jelačića". (3) **Frame radius measured per place** [O-68]: WP3's original `frameRadiusM(place, stops, frame)` (N-th nearest distinct-name tram stop, clamp 500–3000 m) is the shared function in `shared/city/frame.ts` (WP2 owns the file); `wallSpanM()` = `2 × frameRadiusM(place, stops, frame)`; the constant table is the fallback. (4) **Settings gain a `view` row** [O-72]: click-toggle `[data-testid=toggle-view][data-value=map|schema]` with the text "Prikaz: karta" / "Prikaz: shema"; the choice is a client preference in `prefs.ts` (`vidikovac-kiosk-view`), not a protocol field; rows are `place`, `frame`, `view`, `theme`, `rhythm`, `screen`. (5) Standard, natural Croatian, human-checked before deploy [O-66].

These decisions override the planner text below wherever they differ. Apply **every correction in the verifier verdict** (the last section of this file), with the following settlements. Canonical ids: the plan's "WP0/WP1 trust" is **WP0**; "the wall composition/timeline WP" is **WP1**; "the phone WP" is **WP4**.

**Lane and gate.** Step 0 (contract freeze) is executed by the coordinating session in **lane S** before any lane forks (seams S1 and S3 of the brief §15). Steps 1–4 are lane W-A; the `kiosk.ts` edits land **first** in the serial integrator order **WP3** → WP2 → WP1; ships in deploy **D2**.

**Seam S1 — protocol (WP3 owns; `worker/protocol.ts`).** `ScreenMetadata.place?: ScreenPlace | null`, `ScreenMetadata.frame?: FrameStops`; `screen-set` **version 2** `{ t: 'screen-set'; version: 2; place: ScreenPlaceInput | null; frame: FrameStops }` beside v1 (kept indefinitely); `SCREEN_SET_MIN_MS` moved here and **re-exported** from `worker/do/beacon-do.ts` (the workers test imports it from there); error words `'bad-place'`, `'bad-frame'` (only to a v2 frame); capability `'place-v2'` whitelisted beside `'city-v1'`; `parseClient` cast gains `place?: unknown; frame?: unknown`; `POST /api/screens` body `{}` | `{ place, frame? }` | legacy `{ area?, stopId? }`. `place: null` and `frame` are always explicit in `provisionScreen` and `screenMetadata()`; read-path enrichment derives `place` from a stored stop for pre-deploy records. The frame type is `FrameStops` from `shared/city/frame.ts` (seam S2, WP2's constant table 4/6/8 → 1.3/2/2.7 km); drop `ScreenFrame`, `SCREEN_FRAMES`, `FRAME_CIRCLE_KM`, `FRAME_RADIUS_MIN_M/MAX_M`, the measured `frameRadiusM(place, stops, frame)` and the "monotone/clamped" acceptance (brief Q10). The wall camera span for a screen with a place is `frameSpanM(frame)`.

**Seam S3 — place (WP3 owns; `shared/city/place.ts`).** `ScreenPlace { kind: 'tram' | 'bus' | 'address'; name; lon; lat; stopId?; address? }`; `ScreenPlaceInput`; `TRAM_NEAR_M = 400`; `BUS_NEAR_M = 300`; `derivePlace(anchor, stops, isTram)` (nearest tram platform within 400 m → tram; else nearest bus platform within 300 m → bus; else the address point); `placeFromStop(stop, isTram, address?)`; `nearestStops(...)`; `inZagreb(lon, lat)` over `MAP_CONFIG.bounds`; `isValidPlaceInput`. WP4's phone `resolvePlace({ screen, saved, stops, location })` (`app/src/city/place.ts`) wraps `derivePlace` for the order screen → saved → nearest; its 800 m `DEPARTURES_STOP_M` is the phone's own rule. `worker/pairing/place.ts` imports `../../app/src/data/zet-routes.json` statically (precedent `worker/pairing/stops.ts:1` for a static JSON import; update the stale comment `app/src/data/routes.ts:2-3`).

**Header and settings (WP3 owns).** `shellMarkup` header = brand **button** `[data-testid=kiosk-brand]` (`aria-label` "Kaj ima? · Postavke zaslona") · `[data-testid=kiosk-context]` (the place; "Zagreb" for the whole city — the existing testid stays the header probe) · date · clock; no `kiosk-settings` gear, no `kiosk-theme` glyph. **`LONG_PRESS_MS = 800`**, slop 12 px, Enter/Space opens at once; scheduled through `deps.setTimeout` so `k.tick()` fires; coexists with the first-tap wake-lock listener. Settings panel `[data-testid=kiosk-settings-panel]` rows `place`, `frame`, `theme`, `rhythm`, `screen` with click-toggles `[data-testid=toggle-place]`, `[data-testid=toggle-frame][data-value=4|6|8]` ("Kadar: {count} stajališta odavde"), `[data-testid=toggle-theme]`, `[data-testid=toggle-rhythm][data-value=20|30|60]` ("Ritam: {seconds} s"); no Spremi; `SendQueue` 800 ms debounce, ≥ `SCREEN_SET_MIN_MS` between sends, one in flight, refusal repaints from `deps.screen()`. `settings.ts` `SCREEN_SET_ERRORS` gains `'bad-place'`, `'bad-frame'`; `kiosk.test.ts:594` uses `'auth-required'` as the ignored probe word. `app/src/kiosk/prefs.ts` (`RHYTHMS`, `readRhythm`, `writeRhythm`, key `vidikovac-kiosk-rhythm`) is exposed to WP1's sentence sequence and timeline tick. New unit test "the invitation header has no button except the brand". `kiosk.ts` state `place`/`frame` from `screen.place ?? placeFromStop(stop)` / `screen.frame ?? 6`; `wallSpanM()` = `composition === 'handheld' ? HANDHELD_SPAN_M : place ? frameSpanM(frame) : FIELD_SPAN_M` used by `paintMap` (and by nothing else: WP1 deletes `paintTicker`, so skip the :313 edit); `configuredDistrict()` deleted; `paintContext` prints `place?.name ?? 'Zagreb'`. `app/src/kiosk/mapview.ts` is WP2's function: WP3 only passes `place`, `frame`, `spanM`.

**Setup (WP3 owns).** `[data-testid=setup-place]` (one field "Adresa ili stajalište"), `[data-testid=setup-suggestions]` > `setup-suggestion`, `[data-testid=setup-preview]` ("Na zaslonu: {place} i 6 stajališta uokolo" / "Na zaslonu: cijeli grad."), `setup-create` (existing id, label **"Pokreni"**); empty field → `POST {}` byte-identical. `app/src/kiosk/invitation.ts:45` is **WP1's** (the handheld sentence moves to `kiosk.handheld.info` and says "Pokreni"); docs/kiosk.md:36 and :331 join step 5's line list. `CITY_CENTRE` is exported from `app/src/kiosk/stops.ts`.

**Street index (step 2; brief Q3 default = build it).** Served tiles are **raw MVT** (no gzip; decode directly, detect gzip by magic bytes; the pmtiles `--archive` path gunzips per the archive header); fixtures are raw MVT as served. Add `.cache/` to `.gitignore`, `"build:streets"` to package.json, `@mapbox/vector-tile` and `pbf` to devDependencies; the script header states it is the one `scripts/*.mjs` tool with npm dependencies. **Do not add to `izvori.json` `sources`** (pinned to nine modules): add the ODbL row to `docs/izvori.md` "Statički skupovi" and a new `staticSources` key rendered by `renderStaticSources()` in `app/src/izvori-render.ts`, with a test that `sources.length` is still 9. `izvori.json:16` is edited by WP0 first (D1); WP3 rebases.

**Tests.** Legacy DO case created directly through `create({ …, stopId: '106_1', stop })` (the helpers take only an area), or add an optional `stopId` to `provision()/onlineKiosk()`. `beacon.test.ts` adds `server({ t: 'error', error: 'bad-place' })` → `onError('bad-place')`. `e2e/screen-creation.spec.ts` opens settings with `click({ delay: 900 })` on `kiosk-brand`. `scripts/audit-production.mjs` already targets the retired wizard and is never run (WP6 adds the refusal guard as lane V's first commit).

**Production verification (brief Q15).** After Cloudflare Builds is green: create **one** temporary screen for the day through `https://zagreb.aningfilm.hr/kiosk/` (field left empty), record it in `review.local/companion/screen-<date>.json`, open Postavke by long press, set Mjesto = Kvaternikov trg, sample `kiosk-context` every 20 s for 10 min (non-empty in 100 % of samples), set Cijeli grad, close; never a second screen that day. DQG83CVG expired 22 Sep 17:34 and is not used.

**Copy.** New keys only under `kiosk.setup.*` and `kiosk.settings.*` (seam S9). Owner strings byte-exact: "Adresa ili stajalište", "Pokreni", "Na zaslonu: {place} i {count} stajališta uokolo" (nominative apposition, brief Q5), "Na zaslonu: cijeli grad.", "Mjesto", "Kadar", "Tema", "Ritam". Delete `kiosk.setup.intro/validity/search/loadingStops/errorStops` and `kiosk.settings.area/areaHint/stop/stopNone/stopHint/save/saving/saved` together with their `KioskStrings` members in the same commit (typecheck).

## Goal

Replace the one-button start and the area/stop/Spremi settings panel with the §11 contract: a single field "Adresa ili stajalište" that suggests tram/bus stops (stops.json) and register streets (new offline street-geometry index), derives the place (nearest tram stop ≤ 400 m, else bus stop ≤ 250 m, else the address point) and says under the field what the wall will show ("Na zaslonu: Kvaternikov trg i 6 stajališta uokolo"); empty → whole city and POST {} exactly as today. Settings become single-button click-toggles (Mjesto, Kadar 4/6/8 "stajališta odavde", Tema, Ritam 20/30/60 s, Zaslon) reached by a 1.5 s press on the brand; gear and theme glyph leave the header (principle 8, slop #17). The screen record gains place {kind,name,lon,lat,stopId?,address?} and frame 4|6|8 on ScreenMetadata; screen-set v2 carries them, v1 stays accepted, records without a place are enriched on read. The header chip always names a place (stop or street) or "Zagreb". Presence gate, quotas, /hitno, prijava untouched; deploy only by git push to main.

## Depends on

- WP0/WP1 tram-path stability and pills (independent code; only ordering by owner rule 'trust first' [O-54])
- The wall composition/timeline WP (map framing to 6–7 stops, 'U blizini' circle, header sentence) CONSUMES this package's ScreenMetadata.place/frame, shared/city/place.ts frameRadiusM/FRAME_CIRCLE_KM and the .kiosk[data-rhythm]/prefs.readRhythm — WP3 must land its Step 0 before that WP starts
- The phone WP (Sada nearest stop automatic, Karta same frame as the wall [O-50]) reads screen.place/frame from the room 'joined' frame (worker/do/room-do.ts:374,389,593 already forward screenMetadata()) and replaces app/src/city/location.ts:7-10 defaultLocation(screen.stop) with the place — after Step 1

## Data, protocol and artefact changes

Wire (worker/protocol.ts): ScreenMetadata gains `place?: ScreenPlace | null` ({ kind: 'tram'|'bus'|'address'; name; lon; lat; stopId?; address? }) and `frame?: 4|6|8`; additive, so phones and old kiosks ignore them. Client→server: `screen-set` version 2 `{ place: ScreenPlaceInput | null, frame }` where ScreenPlaceInput is `{ kind:'stop', stopId, address? }` (the DO fills name/lon/lat/kind from its own stop table and zet-routes.json — the browser is never trusted with a stop's point) or `{ kind:'address', name, lon, lat, address? }` (bounds 15.70–16.30 × 45.50–46.02, name ≤ 80, address ≤ 120, no control chars); version 1 `{ stopId, area }` stays accepted indefinitely (open tabs up to 24 h, venue screens longer) and is mapped to place = stop-derived, area as sent. New error words 'bad-place', 'bad-frame' (only ever sent to a v2 frame); 'screen-set-rate' unchanged (SCREEN_SET_MIN_MS moved to protocol.ts and shared). `auth.capabilities` may include 'place-v2' (whitelisted in the DO beside 'city-v1'). POST /api/screens body: `{}` (unchanged whole-city path), or `{ place: ScreenPlaceInput, frame? }`; legacy `{ area?, stopId? }` still accepted; 400 fields 'place' / 'frame'. Server record (BeaconDO meta): new keys 'place' (JSON or '') and 'frame'; 'area' now derived from the place through districtOf (falls back to 'zagreb') and kept for stats, ScanOk.area and index-do; 'stop'/'stopId' kept as the place's stop or empty. Read-path enrichment: screenMetadata() and the client's adoptCredentials derive place from a stored stop when 'place' is absent, frame defaults to 6 — so records and localStorage copies made before this deploy keep working without migration. Client-only preference: rhythm 20|30|60 s in localStorage 'vidikovac-kiosk-rhythm' (theme stays in the existing theme controller storage). New static artefact: app/public/data/streets-geo.json (OSM/Protomaps-derived street centrelines: name, register id when matched, centroid, bbox, length, stops within 60 m, simplified line for streets > 700 m; ODbL, listed in docs/izvori.md and /izvori); fetched lazily like /data/stops.json. Removed from the UI (kept in the protocol for compatibility): area/district selection and the district-seat framing.

## Contracts this package supersedes

- PRODUCT.md lines 59-60: 'Settings retain unsaved input and have a bounded scroll area with visible actions.' → settings are click-toggles applied at once (one frame per 5 s), no draft, no Spremi
- PRODUCT.md lines 71-75 (19 September approval): 'Opening /kiosk/ gives one button, Pokreni zaslon, and nothing else to decide … Area, stop, theme and expiry move to an on-screen settings panel behind a gear in the header.' → one field 'Adresa ili stajalište' (optional), derived place, 'Pokreni'; settings behind a long press on the brand; area no longer a user choice
- DESIGN.md lines 93-95: 'The settings body scrolls independently of its header, errors and save action, preserving a closed unsaved draft.' → toggles, no save action
- DESIGN.md lines 129-132: 'Starting a screen is one button and no configuration; area, stop, theme and expiry belong to an on-screen settings panel, not a setup wizard and not a screen full of large district pills. A refused or rate-dropped save is stated in the panel, never swallowed.' → one field + summary line + Pokreni; refusals still stated in the panel (kept)
- DESIGN.md line 135: 'Configured area or stop, and public selections, determine framing.' → the screen's place and frame (4/6/8 stops) determine framing; whole city only without a place
- docs/kiosk.md lines 19-20 ('Postavke pamte nespremljeni unos; tijelo se pomiče unutar ograničene ploče, odvojeno od gumba za spremanje i poruke pogreške.'), lines 35-62 (steps 2-3: Pokreni zaslon with nothing to decide; gear; Područje/Stajalište/Tema/Zaslon; Spremi; district framing), lines 333-334 (troubleshooting rows about the gear and Spremi)
- app/src/kiosk/INTEGRATION.md lines 151-154 (gear opens Postavke: area, stop, theme…), 157-163 (ScreenMetadata.area read by the camera as its district), 165-173 (screen-set version 1 as the only save path)
- Brief §11 'Setup (wall)' and 'Setup / settings (wall)' are implemented as written; brief §13 rows 16-17 (Zaustavi, gear + theme icon) — this package removes the gear and the theme glyph; 'Zaustavi' belongs to the wall composition WP

## Steps

### 0. Freeze the wire contract and the pure derivation API (sequential prerequisite for every other step; ~30 min, done by the coordinating session before agents fork)  `[S]`

Files: `worker/protocol.ts`, `shared/city/place.ts`, `worker/pairing/place.ts`

worker/protocol.ts (after ScreenStop, line 32): add `export type ScreenFrame = 4 | 6 | 8; export const SCREEN_FRAMES: readonly ScreenFrame[] = [4, 6, 8]; export const DEFAULT_SCREEN_FRAME: ScreenFrame = 6; export type ScreenPlaceKind = 'tram' | 'bus' | 'address'; export interface ScreenPlace { kind: ScreenPlaceKind; name: string; lon: number; lat: number; stopId?: string; address?: string }` (name = what the header shows: the stop's name or the street's name, never the venue's [O-19]; address = the typed street+number for the Mjesto row only); `export type ScreenPlaceInput = { kind: 'stop'; stopId: string; address?: string } | { kind: 'address'; name: string; lon: number; lat: number; address?: string }`; `export const PLACE_NAME_MAX = 80; export const PLACE_ADDRESS_MAX = 120;`. Extend `ScreenMetadata` (34-40) with `place?: ScreenPlace | null; frame?: ScreenFrame`. Extend `BeaconClientMessage` (108-118) with `| { t: 'screen-set'; version: 2; place: ScreenPlaceInput | null; frame: ScreenFrame }` and keep the version-1 member (115) verbatim. Move `SCREEN_SET_MIN_MS = 5_000` from worker/do/beacon-do.ts:55 into protocol.ts (export) and re-import it in beacon-do.ts. Extend `CreateBeaconRequest` (90-96) with `place?: ScreenPlaceInput; frame?: ScreenFrame`. Add `'bad-place' | 'bad-frame'` to the documented screen-set error words (comment next to line 115). shared/city/place.ts (new, pure, imports only types from '../../worker/protocol' and `distanceM` from './geo'): `export const TRAM_NEAR_M = 400; export const BUS_NEAR_M = 250; export const FRAME_CIRCLE_KM: Record<ScreenFrame, number> = { 4: 1.3, 6: 2, 8: 2.7 }; export const FRAME_RADIUS_MIN_M = 500; export const FRAME_RADIUS_MAX_M = 3000; export const ZAGREB_BOUNDS = { west: 15.70, south: 45.50, east: 16.30, north: 46.02 }` (= MAP_CONFIG.bounds, app/src/core/contracts.ts:95, cite it); `export interface PlaceAnchor { lon: number; lat: number; name: string; address?: string }`; `export function inZagreb(lon: number, lat: number): boolean`; `export function nearestStops(point, stops: readonly ScreenStop[], opts: { limit: number; only?: (s: ScreenStop) => boolean }): (ScreenStop & { distanceM: number })[]` (dedupes platforms by name, nearest first); `export function placeFromStop(stop: ScreenStop, isTram: (routeId: string) => boolean, address?: string): ScreenPlace` (kind tram when any route isTram, else bus); `export function derivePlace(anchor: PlaceAnchor, stops, isTram): ScreenPlace` (tram platform within TRAM_NEAR_M → placeFromStop; else bus platform within BUS_NEAR_M; else `{ kind: 'address', name: anchor.name, lon, lat, address }`); `export function frameRadiusM(place: { lon; lat }, stops, frame: ScreenFrame): number` (distance to the frame-th nearest distinct-name stop, clamped to [MIN, MAX]; MAX when fewer stops); `export const frameSpanM = (r: number) => 2 * r`. worker/pairing/place.ts (new): `import routes from '../../app/src/data/zet-routes.json'` (precedent: worker/feed/modules/zet-routes.ts:19 and worker/pairing/stops.ts:1); `export const isTramRoute = (id: string) => routes[id]?.type === 0`; `export function parsePlaceInput(raw: unknown): ScreenPlaceInput | null` (shape, trims, name 1..PLACE_NAME_MAX, address ≤ PLACE_ADDRESS_MAX, no control chars, lon/lat finite and inZagreb); `export function parseFrame(raw: unknown): ScreenFrame | null`; `export function resolvePlace(input: ScreenPlaceInput): ScreenPlace | null` (stop → `screenStop(id)` from worker/pairing/stops.ts:15, overwrite name/lon/lat from the server table, kind via isTramRoute; address → as given). Commit this step alone first so steps 1–4 can fork.

### 1. Server: POST /api/screens and BeaconDO accept, validate, store and enrich place + frame; screen-set v2 beside v1  `[M]`

Files: `worker/routes/screens.ts`, `worker/pairing/provision.ts`, `worker/do/beacon-do.ts`, `worker/pairing/place.ts`, `test/pairing/screens.workers.test.ts`, `test/pairing/beacon-do.workers.test.ts`

worker/routes/screens.ts:49-61: after the body parse, `const placeInput = body.place === undefined || body.place === null ? null : parsePlaceInput(body.place); if (body.place !== undefined && body.place !== null && !placeInput) return json({ error: 'bad-request', field: 'place' }, 400);` `const frame = body.frame === undefined ? DEFAULT_SCREEN_FRAME : parseFrame(body.frame); if (frame === null) return 400 field 'frame'`; `const place = placeInput ? resolvePlace(placeInput) : null; if (placeInput && !place) return 400 field 'place'` (unknown stop); area = `body.area !== undefined ? areaSlugOf(body.area) : place ? (districtOf(place.lon, place.lat) ?? CITY_AREA.slug) : CITY_AREA.slug` (districtOf from worker/feed/geo/districts.ts:125); stopId = `place?.stopId ?? (typeof body.stopId === 'string' ? body.stopId : null)` (legacy stopId still resolves through screenStop, and when no place was sent but a stop was, `place = placeFromStop(stop, isTramRoute)`); label default `Kaj ima? · ${place?.name ?? areaName(area)}`. Keep the 512 B cap (a place body is ~120 B). Pass `place, frame` into provisionScreen. worker/pairing/provision.ts:8-15,21,24-29: `ProvisionInput.place?: ScreenPlace | null; frame?: ScreenFrame`; `screen = { kind, expiresAt, stop, area, place: input.place ?? (stop ? placeFromStop(stop, isTramRoute) : null), frame: input.frame ?? DEFAULT_SCREEN_FRAME }`; record carries `place`, `frame`. worker/do/beacon-do.ts: `BeaconCreateInput` (59-…) add `place?: ScreenPlace | null; frame?: ScreenFrame`; `create()` (344-356) `setMeta('place', input.place ? JSON.stringify(input.place) : '')`, `setMeta('frame', String(input.frame ?? 6))`; `parseClient` (104-120) accept v2: `parsed.t === 'screen-set' && parsed.version === 2 && (parsed.place === null || typeof parsed.place === 'object') && [4,6,8].includes(parsed.frame)` → `{ t:'screen-set', version:2, place, frame }`, keep the v1 branch; extend the capabilities whitelist at line 109 to `['city-v1','place-v2']`; dispatch (443-446) → `setScreen(ws, attachment, parsed)`; `setScreen` (618-634) becomes: v1 → `stop = stopId ? screenStop : null; area as given (isAreaSlug else 'bad-area'); place = stop ? placeFromStop(stop, isTramRoute) : null; frame = current meta frame ?? 6`; v2 → `const input = parsePlaceInput(msg.place) (null place allowed); const place = input ? resolvePlace(input) : null; if (msg.place !== null && !place) → error 'bad-place'; if (!parseFrame(msg.frame)) → 'bad-frame'; area = place ? districtOf(...) ?? 'zagreb' : 'zagreb'`; both write meta area, stopId, stop, place, frame in one transactionSync and answer with sendBatch (unchanged path). `screenMetadata()` (174-185): return `place` = stored JSON, or derived read-path from the stored stop (`placeFromStop(stop, isTramRoute)`) when the meta is absent (same pattern as withDistrict), else null; `frame` = Number(meta('frame')) if in SCREEN_FRAMES else 6. Rate limit unchanged (SCREEN_SET_MIN_MS imported from protocol). Tests: screens.workers.test.ts 43-61 add `expect(screen.screen).toMatchObject({ place: null, frame: 6 })` and after the v1 screen-set `after.screen.place` toMatchObject `{ kind: 'tram', stopId: '106_1' }`; new it: body `{ place: { kind: 'stop', stopId: '106_1' }, frame: 8 }` → 201, `screen.place.name` contains 'Jelačića', `screen.frame === 8`, `screen.stop.id === '106_1'`, listed operatorLabel 'Kaj ima? · Trg bana J. Jelačića'; new it: address body `{ place: { kind: 'address', name: 'Ilica', lon: 15.9700, lat: 45.8135, address: 'Ilica 25' } }` → 201, `screen.place.kind === 'address'`, `screen.stop === null`, `screen.area === districtOf(15.97, 45.8135)` (compute in the test, do not hard-code), label 'Kaj ima? · Ilica'; new it: `{ place: { kind: 'address', name: 'X', lon: 0, lat: 0 } }` → 400 field 'place'; `{ frame: 5 }` → 400 field 'frame'. beacon-do.workers.test.ts 257-300: keep the four v1 tests; add v2 tests: stop place + frame 8 → codes.screen `{ frame: 8, area: 'gornji-grad-medvescak', stop: { id: '106_1' }, place: { kind: 'tram', stopId: '106_1' } }`; address place → `{ stop: null, place: { kind: 'address' } }` and area from districtOf; `place: { kind: 'stop', stopId: 'invented' }` → error 'bad-place' and screenMetadata unchanged; `frame: 5` → 'bad-frame'; legacy: `onlineKiosk` created via the existing helper with a stopId → `runInDurableObject(..., instance => expect(instance.screenMetadata().place).toMatchObject({ stopId: '106_1' }))` and `frame === 6` without any screen-set.

### 2. Offline street geometry index from the served Protomaps tiles + client suggestion/derivation modules  `[L]`

Files: `scripts/streets-geo.mjs`, `app/public/data/streets-geo.json`, `test/fixtures/maps/README.md`, `test/scripts/streets-geo.test.ts`, `app/src/core/screens.ts`, `app/src/kiosk/stops.ts`, `app/src/kiosk/places.ts`, `test/app/kiosk-places.test.ts`, `test/city/place.test.ts`, `package.json`, `docs/izvori.md`, `app/src/data/izvori.json`

scripts/streets-geo.mjs (new; follow scripts/districts.mjs conventions: dependency-light, deterministic, fixture + `--fetch`): compute the z14 tile range covering ZAGREB_BOUNDS (≈28×36 ≈ 1,000 tiles); with `--fetch` GET `https://zagreb.aningfilm.hr/maps/zagreb-v1/14/{x}/{y}.mvt` (public same-origin tiles; the tile arrives gzip-compressed as verified on 14/8919/5841 — gunzip with node:zlib) into a gitignored `.cache/maps/zagreb-v1/`, or `--archive <path>` reads a local copy of `vidikovac-maps/zagreb-v1.pmtiles` fetched with `wrangler r2 object get` (owner login) through the `pmtiles` package with a small fs Source; decode with `@mapbox/vector-tile` + `pbf` (both present in node_modules via maplibre-gl; add them to devDependencies explicitly, BSD-3); collect `roads` features with `name`; merge segments per normalName(name) (shared/city/geo.ts:3) across tiles; per street: length-weighted centroid, bbox, total length, polyline simplified at 15 m (reuse districts.mjs's Douglas-Peucker), the stops (app/public/data/stops.json) within 60 m of the line deduped by name and ordered along it (ids of the nearest platform per name); join to the register (streets chunks listed in app/public/data/city/manifest.json) by normalName + settlement polygon containment (catalogue settlements chunk; same rule as matchStreet, shared/city/geo.ts:38-45) → `id` = UL_JID when exactly one register row matches; output `app/public/data/streets-geo.json` = `{ version: 'zagreb-v1', source: 'OpenStreetMap contributors via Protomaps zagreb-v1', licence: 'ODbL 1.0', generatedFrom: <archive etag or tile count>, streets: [{ id?, name, settlementId?, lon, lat, bbox: [w,s,e,n], lengthM, stops: string[], line?: [lon,lat][] (only when lengthM > 700) }] }` sorted by name then settlementId, coordinates rounded to 5 dp; exit 1 if > 400 KB. Print coverage: matched register rows in settlement Zagreb / 3,289. Save 3 real tiles (8919/5841, 8920/5841, 8919/5842) as fixtures under test/fixtures/maps/ with a README naming their fetch date; test/scripts/streets-geo.test.ts runs the builder on the fixtures and asserts determinism (two runs byte-identical) and that 'Ilica' resolves with a centroid inside ZAGREB_BOUNDS. app/src/core/screens.ts: add `loadStreets(fetchImpl = fetch): Promise<StreetGeo[]>` mirroring loadStops (27-34): lazy, cached, never on the entry graph. app/src/kiosk/stops.ts: export `fold` (21-29) and add `isTramStop(stop)` using routeType (37-40). app/src/kiosk/places.ts (new, pure): `export type PlaceSuggestion = { kind: 'stop'; stop: RankedStop } | { kind: 'street'; street: StreetGeo; number?: string } | { kind: 'segment'; street: StreetGeo; stop: ScreenStop }`; `export function parseAddressQuery(q): { street: string; number?: string }` (`/^(.*?)\s+(\d+[a-z]?)$/i`); `export function suggestPlaces(query, stops, streets, near = CITY_CENTRE, limit = 8)`: ≥ 2 characters; stops via rankStops({ query, near, dedupeNames: true, limit }) with tram-served stops before bus-only at equal fold rank; streets by fold-prefix then fold-substring on name; a street with lengthM > 700 is offered as up to 4 segment rows (its `stops`, deduped by name) instead of one centroid row; a typed number is kept as `number` (text only — the index has no house numbers) and shown in the row; `export function anchorOf(s: PlaceSuggestion): PlaceAnchor` (stop → the stop; street → centroid, name = street.name, address = `${name} ${number}`; segment → the stop's point, name = street.name, address = `${street.name} · ${stop.name}`); `export function placeInputOf(place: ScreenPlace): ScreenPlaceInput` (stop → { kind:'stop', stopId, address }, address → the point). Tests: test/app/kiosk-places.test.ts with the real app/public/data/stops.json and a 20-row hand-made streets fixture: 'Kvatern' → first row stop 'Kvaternikov trg' (tram platform 236_*), streets 'Kvaternikova ulica' and 'Trg Eugena Kvaternika' after it; 'Ilica 25' → street Ilica with number '25' expanded into segment rows when lengthM > 700; test/city/place.test.ts: derivePlace with the real stops.json — an anchor 100 m from tram platform 236_2 → kind 'tram', stopId name 'Kvaternikov trg'; an anchor whose nearest tram platform is > 400 m away and nearest bus platform < 250 m → 'bus' (compute the anchor from the file in the test: take a bus-only platform, assert the tram distance); an anchor > 400/250 m from both → 'address' with the anchor's name; frameRadiusM: 6 stops around 236_2 within [500, 3000] and monotone 4 ≤ 6 ≤ 8; frameRadiusM with 3 stops → FRAME_RADIUS_MAX_M. docs/izvori.md reference table (§ starting line 63) and app/src/data/izvori.json: add the row 'Ulične osi za pretragu adrese · OpenStreetMap (ODbL 1.0) preko Protomaps arhive zagreb-v1 · izvedena baza pod ODbL' and check test/app/izvori.test.ts still passes.

### 3. Start screen: one field, suggestions, summary line, Pokreni  `[M]`

Files: `app/src/kiosk/start.ts`, `app/src/kiosk/place-field.ts`, `app/src/kiosk.ts`, `app/src/core/screens.ts`, `app/src/ui/kiosk.css`, `app/src/i18n/hr.json`, `app/src/i18n/en.json`, `app/src/kiosk/strings.ts`, `test/app/kiosk.test.ts`

app/src/kiosk/place-field.ts (new, shared by start.ts and settings.ts): `mountPlaceField(host, deps: { strings; locale; loadStops; loadStreets; isTram; near?: {lon,lat}; initial?: ScreenPlace | null; onChange(place: ScreenPlace | null, unresolvedText: string): void; setTimeout; clearTimeout })` renders `<label class="k-search"><span class="k-search-label">Adresa ili stajalište</span><input type="search" role="combobox" aria-autocomplete="list" aria-expanded aria-controls="k-place-list" data-testid="setup-place" autocomplete="off" spellcheck="false"></label><ul id="k-place-list" class="k-suggest" role="listbox" data-testid="setup-suggestions" hidden>` ; loads stops+streets on first focus/input (never at mount — kiosk.test.ts:425 asserts loadStops not called on the start screen); 120 ms debounce; rows `<li role="option" data-testid="setup-suggestion" data-kind="stop|street|segment">` (name + meta: `setup.routesAt` 'linije {routes}' for stops with distance via fmtDistance when `near`; settlement name for the 177 ambiguous streets; `setup.streetNear` for segments); ArrowUp/Down/Enter/Escape and click pick; Enter on unmatched text tries an exact fold match; picking sets the input text to the row's name (+ typed number) and calls onChange(derivePlace(anchorOf(row), stops, isTram)); clearing the field calls onChange(null, ''); exposes `{ element, value(): ScreenPlace | null, unresolved(): string, focus(), destroy() }`. app/src/kiosk/start.ts: `StartDeps` (16-23) gains `loadStops, loadStreets, isTram`, and `createScreen: (input: CreateScreenInput) => Promise<CreateBeaconResponse>`; `startMarkup` (56-67) becomes kicker · h1 `setup.title` · place field host · `<p class="k-setup-summary" data-testid="setup-summary" aria-live="polite">` · error · actions (`setup-create` button text `setup.create` = 'Pokreni', `setup-retry` unchanged); drop the intro and validity paragraphs (keys `kiosk.setup.intro`, `kiosk.setup.validity` deleted in hr/en and in strings.ts:411-418); summary text: no place → `setup.showsCity` 'Na zaslonu: cijeli grad.'; place → `fill(setup.shows, { place: place.name, count: 6 })` = 'Na zaslonu: {place} i {count} stajališta uokolo' (owner's line, brief §11 [O-52]); unresolved text on submit → `setup.noMatch` 'Nema takvog stajališta ni ulice. Odaberi prijedlog ili ostavi prazno za cijeli grad.' and no POST; `create()` (120-141) posts `deps.createScreen(place ? { place: placeInputOf(place), frame: DEFAULT_SCREEN_FRAME } : {})` — the empty field must produce exactly `{}` (landing e2e and audit scripts keep pressing setup-create with an empty field). app/src/kiosk.ts:177 `const createScreen = deps.createScreen ?? ((input: CreateScreenInput) => createTemporaryScreen(input))`; `KioskDeps.createScreen` type (102) updated; `mountStartPhase` (886-894) passes `loadStops` (the existing wrapper that also fills `stops`), `loadStreets: deps.loadStreets ?? loadStreetsImpl`, `isTram: (id) => routeType(id) === 0`. CSS (app/src/ui/kiosk.css): reuse `.k-search`, `.k-search-input` (613-616) and the existing unused `.k-setup-summary` (627-628); add `.k-suggest` (position: absolute under the input, `max-height: calc(var(--k-control) * 6)`, `grid-auto-rows: var(--k-control)` so only whole rows show, `overflow-y: auto`, `[aria-selected=true]` uses `--k-action-soft`), `.k-suggest-meta` in `--k-hint-size`; handheld: `.kiosk[data-size='handheld'] .k-suggest { position: static }`. Strings (hr.json `kiosk.setup` 1087-1108; mirror en.json 1051; strings.ts `setup` type 267-286 and group 411-418): add `place` 'Adresa ili stajalište', `shows`, `showsCity`, `noMatch`, `streetNear` '{street} · stajalište {stop}', `loadingPlaces` 'Učitavanje adresa i stajališta…', `errorPlaces` 'Popis adresa i stajališta nije dostupan.'; change `create` to 'Pokreni'; delete `intro`, `validity`, `search` (superseded by `place`), `loadingStops`, `errorStops` (replaced). test/app/kiosk.test.ts: 415-433 → assert `setup-place` present, `setup-summary` text 'Na zaslonu: cijeli grad.', no `select`, `loadStops` not called before focus, header has no `kiosk-settings`/`kiosk-theme` (now `kiosk-brand` only); 434-449 → `createScreen` toHaveBeenCalledWith({}) and header chip 'Zagreb'; new it 'typing a stop name suggests it, picking it derives the place and writes the line': focus + input 'Zapr' → k.tick(120) → one `setup-suggestion` 'Zapruđe' → click → summary 'Na zaslonu: Zapruđe i 6 stajališta uokolo' → submit → createScreen called with `{ place: { kind: 'stop', stopId: '200_1' }, frame: 6 }`; new it 'unmatched text does not create a screen' → noMatch sentence, createScreen not called; error-ladder test (450-473) unchanged in substance.

### 4. Settings as click-toggles behind a long press; header loses gear and theme glyph; the wall reads place and frame  `[L]`

Files: `app/src/kiosk/settings.ts`, `app/src/kiosk/prefs.ts`, `app/src/kiosk.ts`, `app/src/beacon.ts`, `app/src/kiosk/credentials.ts`, `app/src/kiosk/mapview.ts`, `app/src/ui/kiosk.css`, `app/src/i18n/hr.json`, `app/src/i18n/en.json`, `app/src/kiosk/strings.ts`, `test/app/kiosk.test.ts`, `test/app/beacon.test.ts`, `test/app/kiosk-css.test.ts`

app/src/beacon.ts: `BeaconClient.setScreen` (85, 197-199) becomes `setScreen(input: { place: ScreenPlaceInput | null; frame: ScreenFrame }): void` sending `{ t: 'screen-set', version: 2, ...input }`; `capabilities` default stays caller-provided; app/src/kiosk.ts:957 `capabilities: ['city-v1', 'place-v2']`. app/src/kiosk/prefs.ts (new): `export const RHYTHMS = [20, 30, 60] as const; export type Rhythm = 20|30|60; export const RHYTHM_STORAGE_KEY = 'vidikovac-kiosk-rhythm'; readRhythm(storage): Rhythm (default 20); writeRhythm(storage, r)` with try/catch like credentials.ts. app/src/kiosk/settings.ts rewrite: `SettingsDeps` (47-64) → `{ strings; locale; loadStops; loadStreets; isTram; screen: () => { place: ScreenPlace | null; frame: ScreenFrame; expiresAt: number | null }; themePreference; cycleTheme; rhythm: () => Rhythm; setRhythm(r); save: (input: { place: ScreenPlaceInput | null; frame: ScreenFrame }) => boolean; forget; onOpen?; onClose?; now; setTimeout; clearTimeout }`; `settingsMarkup` (93-133) → header (title `settings.title` 'Postavke', hint, `kiosk-settings-close`) and rows `data-row` = `place`, `frame`, `theme`, `rhythm`, `screen`: place row = label `settings.place` 'Mjesto', current value `<p data-testid="settings-place">` (place.name + address, or `settings.areaWhole` 'Cijeli grad') and a ghost button `settings-place-change` `settings.placeChange` 'Promijeni' that reveals the shared place field (mountPlaceField, `near` = current place or CITY_CENTRE) plus a ghost button `settings-place-city` 'Cijeli grad' that sets place null; frame row = one button `settings-frame` whose text is `fill(settings.frameValue, { count })` 'Kadar: {count} stajališta odavde' cycling 4 → 6 → 8 → 4 on click; theme row = existing `settings-theme` button (label `header.theme` 'Tema: {pref}' + THEME_ICON glyph moved here from kiosk.ts:131,339-346); rhythm row = button `settings-rhythm` `fill(settings.rhythmValue, { seconds })` 'Ritam: {seconds} s' cycling 20 → 30 → 60, applied at once via deps.setRhythm; screen row unchanged (expiry sentence, `settings-forget` two-press). No Spremi button (delete `settings.save/saving/saved`, `.k-settings .k-setup-actions`). Sending: a `SendQueue` inside settings.ts — `SETTINGS_SEND_DELAY_MS = 800` trailing debounce after the last place/frame change, `SCREEN_SET_MIN_MS` (protocol) between consecutive sends, one frame in flight, `SAVE_TIMEOUT_MS` (28) still gives up waiting after 8 s and shows `settings.saveOffline`; latest state wins, a state equal to the DO's current screen is never sent; `applied()` (323-330) compares place/frame instead of stopId/area and does NOT close the panel (toggles stay open; 90 s idle and Escape/close as before); `refused(error)` (331-336) maps `bad-place`/`bad-stop` → `settings.saveRefused` 'Poslužitelj nije prihvatio mjesto. Odaberi ponovno.', `bad-frame` → same, `screen-set-rate` → `settings.saveBusy` (existing), and repaints the toggles from deps.screen() (the DO's truth). Optimistic text: the toggle shows the chosen value at once; `.kiosk` dataset gets `data-frame`, `data-rhythm`, `data-place-kind` painted by kiosk.ts from the applied screen (test hooks). app/src/kiosk.ts: shellMarkup (136-152) header line 141 → `<button type="button" class="k-brand" data-testid="kiosk-brand" aria-label="${s.appName} · ${s.settings.open}">${s.appName}</button>` and the clock row keeps only `k-date` and `kiosk-clock` (remove `kiosk-settings` and `kiosk-theme` buttons, `settingsBtn`/`themeBtn` consts 198, `paintTheme` header glyph 339-346 → keep `settings?.paint()` in the theme onChange, `cycleTheme` 347-350 stays for the panel, listeners 1222-1223 removed); add `export const LONG_PRESS_MS = 1500; const LONG_PRESS_SLOP_PX = 12;` and on the brand: pointerdown arms a oneShot(LONG_PRESS_MS) → openSettings(); pointerup/pointercancel/pointerleave or pointermove beyond the slop disarms; `keydown` Enter/Space on the focused brand opens at once (a keyboard is an operator's tool); openSettings (899-925) guard `phase === 'invitation'` unchanged, deps as above with `save: (input) => { if (!beacon || beacon.status() !== 'live') return false; beacon.setScreen(input); return true; }`, `rhythm/setRhythm` through prefs.ts on `storage`. State: add `let place: ScreenPlace | null = null; let frame: ScreenFrame = DEFAULT_SCREEN_FRAME;` set in `adoptCredentials` (932-949) and `applyScreen` (1056-1071) from `screen.place ?? (screen.stop ? placeFromStop(screen.stop, isTram) : null)` and `screen.frame ?? 6`; keep `stop = screen.stop` (arrivals, paired compositions untouched); `paintContext` (364-371): `contextEl.textContent = !credentials ? '' : (place?.name ?? CITY_AREA.name)`; `configuredDistrict()` (357-359) deleted; requestKioskMap call (514-532): `district: null` removed, add `place, frame`, `spanM: composition === 'handheld' ? HANDHELD_SPAN_M : frameSpanM(frameRadiusM(place ?? stop ?? CITY_CENTRE_POINT, stops ?? [], frame))` when a place exists, else FIELD_SPAN_M. app/src/kiosk/mapview.ts: `FieldInput` (≈605-616) gains `place?: ScreenPlace | null` and `fieldView` (674-689) orders place → stop → city window (the district branch and DISTRICT_SPAN_M stay exported for now; the kvart outline code 515-540 becomes unreachable from the invitation — leave it, flag for the wall WP). app/src/kiosk/credentials.ts: unchanged (withScreen copies the whole ScreenMetadata). CSS: `.k-brand` gains button reset + `cursor: default; user-select: none; touch-action: none`; keep `.k-theme` (kiosk-css.test.ts:126) for the panel's toggle buttons; replace `.k-settings .k-stop-list` rules (850-852) with `.k-settings .k-suggest` whole-row rules; `.k-settings-rows` (837) stays a 2-column grid with `place` spanning 2 rows. Strings: hr.json `kiosk.settings` (1109-1137) add `place` 'Mjesto', `placeChange` 'Promijeni', `frame` 'Kadar', `frameValue` 'Kadar: {count} stajališta odavde', `rhythm` 'Ritam', `rhythmValue` 'Ritam: {seconds} s'; reword `saveRefused`; delete `area`, `areaHint`, `stop`, `stopNone`, `stopHint`, `save`, `saving`, `saved`; keep `areaWhole` 'Cijeli grad', `open` 'Postavke zaslona', `title`, `hint`, `close`, `saveOffline`, `saveBusy`, `screen`, `expiry*`, `forget*`; mirror en.json; update strings.ts `settings` type (289-…) and group (419-423). Tests: test/app/kiosk.test.ts settings block 476-706 → `open` helper = `brand.dispatchEvent(new PointerEvent('pointerdown', { bubbles: true })); k.tick(LONG_PRESS_MS)`; rows toEqual ['place','frame','theme','rhythm','screen']; new 'a short press or a moved finger does not open settings' (pointerup at 800 ms; pointermove 20 px); 'Kadar click changes the text at once and sends one v2 frame after the debounce' → click `settings-frame` → text 'Kadar: 8 stajališta odavde' → `k.tick(SETTINGS_SEND_DELAY_MS)` → `beacon.setScreen` calledWith `{ place: { kind: 'stop', stopId: '106_1' }, frame: 8 }` exactly once; 'three quick clicks send one frame carrying the last value'; 'a second change inside five seconds waits for the window' (tick SCREEN_SET_MIN_MS → second send); 'a refusal repaints the toggle from the DO's truth' (onError 'bad-place' → text back to 'Kadar: 6 …', error sentence); 'the DO's answer sets data-frame on the shell' (onContext with frame 8 → `.kiosk[data-frame="8"]`); place change via the field: input 'Zapr' → pick → `setScreen` with `{ place: { kind: 'stop', stopId: '200_1' }, frame: 6 }`; 'Cijeli grad' → `{ place: null, frame: 6 }` and chip 'Zagreb' after onContext; offline (568-580) and timeout (601-627) tests adapted to the toggle flow; forget (658-683), idle/Escape (647-657), expiry (629-646), theme cycle (698-706) adapted to the new open helper; 1723-1760 → 'frames the invitation on the screen's place at its frame': stored screen `{ ...CITY_SCREEN, place: { kind:'tram', name:'Zapruđe', lon:15.99, lat:45.77, stopId:'200_1' }, frame: 6 }` → factory center [15.99,45.77], zoom `fieldZoom(FIELD_DESIGN_WIDTH.wide, 45.77, frameSpanM(frameRadiusM(place, STOPS, 6)))`, chip 'Zapruđe'; whole-city test chip 'Zagreb' (twice); re-frame test: onContext with a place → setView center = place; 1781 → long-press helper; 1893-1930 'T5.3' → the panel's `settings-theme` glyph/aria cycle (open the panel first), listener-count test unchanged; fake beacon (100) `setScreen: vi.fn()` unchanged in shape. test/app/beacon.test.ts:90-106 → `client.setScreen({ place: { kind: 'stop', stopId: '106_1' }, frame: 6 })` expects `{ t: 'screen-set', version: 2, place: { kind: 'stop', stopId: '106_1' }, frame: 6 }`; `client.setScreen({ place: null, frame: 8 })`; error words `bad-place` surfaced. test/app/kiosk-css.test.ts:43-61 → retarget to `.k-settings .k-suggest` whole-row invariants (grid-auto-rows var, flex none, no bare rem max-height).

### 5. Docs, integration notes, e2e; supersede the paragraphs the plan changes  `[M]`

Files: `docs/kiosk.md`, `PRODUCT.md`, `DESIGN.md`, `app/src/kiosk/INTEGRATION.md`, `e2e/screen-creation.spec.ts`, `test/docs/docs.test.ts`

docs/kiosk.md: rewrite step 2–3 of 'Postavljanje stvarnog zaslona' (lines 35-62): one field 'Adresa ili stajalište', suggestions, derivation rule (tramvajsko stajalište do 400 m, inače autobusno do 250 m, inače sama adresa), the line 'Na zaslonu: …', prazno polje → cijeli grad, then 'Pokreni'; `POST /api/screens` with `{}` or `{ place, frame }`, 24 sata (keep the literal strings 'POST /api/screens' and '24 sata' — test/docs/docs.test.ts:51,56 assert them); settings paragraph: dugi pritisak (1,5 s) na 'Kaj ima?' opens Postavke; click-toggles Mjesto / Kadar 4-6-8 stajališta odavde / Tema / Ritam 20-30-60 s / Zaslon; `screen-set` verzija 2 (place, frame), verzija 1 still accepted; DO derives area from the place; line 19-20 ('Postavke pamte nespremljeni unos…') replaced by 'Postavke su prekidači: svaki klik odmah mijenja stanje; jedan okvir prema poslužitelju najviše svakih pet sekundi'; troubleshooting rows 333-334 reworded (dugi pritisak; refusal words bad-place/bad-frame). PRODUCT.md: mark lines 59-60 ('Settings retain unsaved input and have a bounded scroll area with visible actions.') and 71-75 ('Opening /kiosk/ gives one button … behind a gear in the header') as superseded by a new dated paragraph (22 September 2026: one field, derived place, click-toggle settings behind a long press, header names the place or Zagreb). DESIGN.md: same for 93-95 ('The settings body scrolls independently…'), 129-132 ('Starting a screen is one button and no configuration; area, stop, theme and expiry belong to an on-screen settings panel…') and 135 ('Configured area or stop … determine framing' → 'The screen's place and frame determine framing'). app/src/kiosk/INTEGRATION.md 151-154, 157-163, 165-173: describe the brand long press, `screen-set` v2, `ScreenMetadata.place/frame`, the read-path enrichment for old records, and that area is derived. e2e/screen-creation.spec.ts (7-46) rewrite: goto /kiosk/ → `setup-place` fill 'Kvatern' → first `setup-suggestion` text 'Kvaternikov trg' → click → `setup-summary` toHaveText('Na zaslonu: Kvaternikov trg i 6 stajališta uokolo') → `setup-create` click → `kiosk-invitation` visible → `kiosk-context` toHaveText('Kvaternikov trg', 30 s) → `page.getByTestId('kiosk-brand').click({ delay: 1600 })` → `kiosk-settings-panel` visible → `settings-frame` click → toHaveText('Kadar: 8 stajališta odavde') → `expect(page.locator('.kiosk')).toHaveAttribute('data-frame', '8', { timeout: 30_000 })` → Escape → then the existing second-browser pairing half (29-45) unchanged. e2e/landing.spec.ts:152 and scripts/audit-production.mjs:449-450 / capture-landing.mjs:147-148 keep working unchanged (empty field, `setup-create`) — do not run the two scripts during development (they create production screens).

## Parallel lanes

Step 0 (contract freeze, one commit) is sequential and first. Then four agents in parallel on disjoint files: A = Step 1 (worker/routes/screens.ts, worker/pairing/provision.ts, worker/pairing/place.ts, worker/do/beacon-do.ts, test/pairing/*); B = Step 2 (scripts/streets-geo.mjs + data file + fixtures, app/src/core/screens.ts loadStreets, app/src/kiosk/stops.ts fold export, app/src/kiosk/places.ts, shared/city/place.ts tests, docs/izvori.md row); C = Step 3 (app/src/kiosk/start.ts, app/src/kiosk/place-field.ts, setup strings, start tests); D = Step 4 (app/src/kiosk/settings.ts, prefs.ts, kiosk.ts header/long-press/state, beacon.ts setScreen, mapview.ts fieldView, settings strings, settings/framing/theme tests). C and D both touch app/src/kiosk.ts (C: lines 102,177,886-894; D: 131-152,198,339-372,514-532,899-960,1056-1071,1208-1223) and hr.json/en.json/strings.ts in different key groups — assign D ownership of kiosk.ts and have C hand D a patch for its three hunks, or run C then D. C depends on B's `suggestPlaces/anchorOf/placeInputOf` signatures: freeze them in Step 0's commit as a stub module (types + throw) so C can mock them. Step 5 (docs, INTEGRATION.md, e2e) runs last, after A–D merge; it can be drafted in parallel by agent E against the testids fixed above.

## Tests to update

- test/app/kiosk.test.ts:415-473 (start: field, summary, createScreen called with {} / {place, frame}, chip 'Zagreb', no header gear/theme)
- test/app/kiosk.test.ts:476-706 (settings: open by long press on kiosk-brand + tick(LONG_PRESS_MS); rows place/frame/theme/rhythm/screen; toggles send one screen-set v2 after SETTINGS_SEND_DELAY_MS; SCREEN_SET_MIN_MS spacing; refusal repaints; timeout/offline/forget/idle/theme adapted)
- test/app/kiosk.test.ts:709-722 (invitation: chip stays 'Trg bana J. Jelačića' — place derived from stop; remove any header-glyph assertion)
- test/app/kiosk.test.ts:1723-1760 (district framing → place framing with frameRadiusM; whole-city chip 'Zagreb')
- test/app/kiosk.test.ts:1781 (gear click → long-press helper)
- test/app/kiosk.test.ts:1893-1930 (T5.3 theme button → the panel's settings-theme glyph/aria cycle)
- test/app/beacon.test.ts:90-106 (setScreen sends version 2 {place, frame}; bad-place surfaced)
- test/app/kiosk-css.test.ts:43-61 (stop-list whole-row rule → .k-settings .k-suggest); line 126 (.k-theme) unchanged
- test/app/i18n.test.ts:10-11 (hr/en key parity: mirror every added/removed kiosk.setup and kiosk.settings key in en.json)
- test/app/copy-guards.test.ts:72 (no 'Vi' in new Croatian strings)
- test/pairing/screens.workers.test.ts:43-80 (place/frame in the response; address body derives area; 400 field 'place'/'frame'; legacy stopId path still produces a place)
- test/pairing/beacon-do.workers.test.ts:257-300 (keep v1 cases; add v2 stop/address/bad-place/bad-frame; legacy record → screenMetadata().place derived, frame 6)
- test/docs/docs.test.ts:48-58 (unchanged assertions; docs/kiosk.md must keep 'POST /api/screens', '24 sata', 'git push', 'APP_ENV=test', 'E2E_ADMIN_BYPASS', 'fizička matrica')
- test/app/izvori.test.ts (new ODbL streets-geo source row must satisfy it)
- e2e/screen-creation.spec.ts:7-46 (rewrite to the field → summary → Pokreni → long press → Kadar flow)
- new: test/city/place.test.ts, test/app/kiosk-places.test.ts, test/scripts/streets-geo.test.ts

## Acceptance (executable)

| # | Criterion | How (command / threshold) |
|---|---|---|
| A1 | Unit suite green for every touched client module and guard | npx vitest run --project unit test/app/kiosk.test.ts test/app/kiosk-places.test.ts test/city/place.test.ts test/app/beacon.test.ts test/app/kiosk-css.test.ts test/app/kiosk-local.test.ts test/app/i18n.test.ts test/app/copy-guards.test.ts test/app/izvori.test.ts test/docs/docs.test.ts test/scripts/streets-geo.test.ts → 0 failed |
| A2 | Worker suite green: v1 and v2 screen-set both accepted, area derived from an address place, legacy records enriched on read | npx vitest run --project workers test/pairing/screens.workers.test.ts test/pairing/beacon-do.workers.test.ts → 0 failed, and the run includes the named new cases (grep the vitest output for 'bad-place', 'version: 2', 'legacy') |
| A3 | Types clean across worker and app | npm run typecheck → exit 0 |
| A4 | No operator chrome in the header (principle 8, slop #17) | grep -c -E "data-testid=\"kiosk-(theme\|settings)\"" app/src/kiosk.ts → 0; and the unit test 'the invitation header has no button except the brand' passes (querySelectorAll('.k-head button') length 1, testid kiosk-brand) |
| A5 | Empty field keeps the one-button contract byte-for-byte (landing trial and audit scripts unchanged) | unit test asserts createScreen toHaveBeenCalledWith({}); npx playwright test e2e/landing.spec.ts → pass locally |
| A6 | End-to-end setup → place → long press → Kadar on the local flow | npx playwright test e2e/screen-creation.spec.ts (local wrangler dev with APP_ENV=test and .dev.vars) → pass; the spec asserts setup-summary 'Na zaslonu: Kvaternikov trg i 6 stajališta uokolo', kiosk-context 'Kvaternikov trg' within 30 s, panel opens only via click({ delay: 1600 }) on kiosk-brand, and .kiosk[data-frame="8"] after the DO answer |
| A7 | Offline street index exists, is reproducible and covers the register | node scripts/streets-geo.mjs (from cached tiles) run twice → identical sha256sum app/public/data/streets-geo.json; file ≤ 400 KB (stat -c %s); the script's coverage line reports matched register rows in settlement Zagreb ≥ 2,800 of 3,289 (≥ 85 %); test/scripts/streets-geo.test.ts asserts 'Ilica' resolves with a centroid inside ZAGREB_BOUNDS |
| A8 | Derivation thresholds behave as the owner ruled [O-26] | test/city/place.test.ts: anchor 100 m from tram platform 236_2 → kind 'tram' named 'Kvaternikov trg'; anchor with nearest tram > 400 m and nearest bus < 250 m → 'bus'; anchor beyond both → 'address' keeping the street name; frameRadiusM monotone for 4 ≤ 6 ≤ 8 and clamped to [500, 3000] |
| A9 | Fresh screen always names a place in the header (backlog P1.6) | unit tests: chip 'Zapruđe' with a place, 'Zagreb' for a whole-city screen; after deploy, on the session's own screen DQG83CVG (review.local/companion/screen.json) set Mjesto = Kvaternikov trg via its settings and capture the header at two hours: kiosk-context non-empty in 100 % of a 10-minute S8-style sample (node review.local/companion/… sampler or a Playwright script reading textContent every 20 s), then restore Cijeli grad; no new production screen is created |
| A10 | Old kiosk JavaScript keeps working against the new DO for its remaining life | beacon-do.workers.test.ts v1 cases pass unchanged (stores stop, answers codes+screen; screen.place derived); manual: an open pre-deploy /kiosk/ tab saves a stop from the old panel after deploy and the header re-frames |
| A11 | Rate discipline: toggles never exceed one screen-set per 5 s per socket and never loop | unit tests 'three quick clicks send one frame' and 'a second change inside five seconds waits for the window'; beacon-do test 'drops a second frame inside the five-second window' still passes |
| A12 | Deployment path unchanged | git log origin/main shows the WP3 commits; no `wrangler deploy` in the shell history of the session; Cloudflare Builds status green; curl -s https://zagreb.aningfilm.hr/api/health → ok |

## Risks

- Grammar of the setup line: resolved by the owner [O-66] — the gender-neutral wording "Na zaslonu: {place} i {count} stajališta uokolo" carries no pronoun, so no declension question remains; never build a declension engine.
- The street register has no geometry (shared/city/types.ts:42-49; rpj_ulica.csv). Street suggestions therefore rest on an OpenStreetMap-derived index (ODbL) extracted from the self-hosted Protomaps archive: OSM already powers the basemap with attribution (app/src/map/basemap.ts:497), but streets-geo.json is a derivative database published under ODbL share-alike — record it in docs/izvori.md and /izvori (Step 2). Minor streets missing in OSM or named differently from the register (e.g. register 'Trg Eugena Kvaternika' vs colloquial/stop 'Kvaternikov trg') lower coverage; 177 names exist in several settlements and are disambiguated by the settlement polygons only when the OSM segment falls inside one.
- House numbers are not available offline: the served z14 archive carries no address points (verified on tile 14/8919/5841: roads with names, no addr_housenumber; MAP_CONFIG.maxzoom 14) and data.zagreb.hr publishes no house-number dataset. A typed number is kept as text; a long street is offered as segments named by its stops so the operator picks the part. Optional later tier: an OSM addr:housenumber extract (Geofabrik/Overpass, ODbL, ~60k points, ≈1 MB) or the Protomaps archive rebuilt to z15 with the buildings layer's kind=address. External fallback named only: Nominatim (OSMF usage policy: no autocomplete, ≤ 1 req/s, attribution, identifiable User-Agent) or Photon (komoot, no guarantee), both ODbL; both send the venue's typed address to a third party and would need a same-origin Worker proxy (/api/geocode) — an owner decision on licence/policy and privacy; WP3 does not wire it (no FLAGS entry).
- Click-toggles vs the DO's one screen-set per 5 s per socket (SCREEN_SET_MIN_MS): without the SendQueue a fast operator gets 'screen-set-rate' and a stale wall; with it, a refused or timed-out send must repaint the toggles from deps.screen() and never re-send by itself beyond the one pending latest state (start.ts's 'nothing retries on its own' rule).
- Old kiosk JavaScript stays open in browsers for up to 24 h (temporary) or indefinitely (venue screens) and sends screen-set v1 and lacks place-v2 — the DO must keep v1 forever; stored records without a place meta must be enriched on read (screenMetadata) and on the client (adoptCredentials) so the header never goes blank on a pre-deploy record.
- Metrics keyed by area (kiosk_online, session counts, ScanOk.area, index-do beacons.area) now receive an area derived from the place via districtOf; a place outside every polygon (snap 50 m) falls to 'zagreb' — continuity of /stats by district changes meaning slightly; note it in docs/kiosk.md.
- The long press has no touch affordance by design; Playwright reproduces it with click({ delay: 1600 }); the first-tap fullscreen/wake-lock listener (kiosk.ts:1208-1216) also fires on pointerdown — both must coexist (do not stopPropagation). Keyboard operators open the panel with Enter/Space on the focused brand.
- kiosk.ts is edited by two agents (Steps 3 and 4); merge conflicts are likely unless D owns the file and C submits its three hunks as a patch.
- Body cap 512 B on POST /api/screens (worker/routes/screens.ts:49): an address of 120 chars plus a street name of 80 stays under it; PLACE_ADDRESS_MAX must be enforced client-side too or the DO answers bad-request.
- Running scripts/audit-production.mjs or scripts/capture-landing.mjs creates real production screens (they press setup-create); WP3 development and e2e run only against local wrangler dev (APP_ENV=test, .dev.vars); production verification uses the session's own screen DQG83CVG through its settings and restores it.

## Agent briefs (self-contained; one per lane)

### Brief 1

AGENT A — server (after Step 0 is committed). Files: worker/routes/screens.ts, worker/pairing/provision.ts, worker/pairing/place.ts, worker/do/beacon-do.ts, test/pairing/screens.workers.test.ts, test/pairing/beacon-do.workers.test.ts. Read first: worker/protocol.ts (ScreenPlace, ScreenPlaceInput, ScreenFrame, SCREEN_SET_MIN_MS now exported), shared/city/place.ts (placeFromStop), worker/pairing/stops.ts (screenStop, withDistrict pattern), worker/feed/geo/districts.ts:125 (districtOf). Do: (1) screens.ts:49-61 parse body.place via parsePlaceInput/resolvePlace and body.frame via parseFrame → 400 with field 'place'/'frame'; area = body.area given ? areaSlugOf : place ? districtOf(place.lon, place.lat) ?? 'zagreb' : 'zagreb'; legacy stopId still resolves and yields place = placeFromStop(stop, isTramRoute); label 'Kaj ima? · ' + (place?.name ?? areaName(area)); keep readCappedBody(request, 512). (2) provision.ts: ProvisionInput.place/frame; screen and record carry place (or derived from stop) and frame (default DEFAULT_SCREEN_FRAME). (3) beacon-do.ts: BeaconCreateInput.place/frame; create() writes meta 'place' (JSON or '') and 'frame'; parseClient accepts screen-set version 2 {place: object|null, frame ∈ 4|6|8} beside version 1 and whitelists capability 'place-v2' next to 'city-v1' (line 109); setScreen(ws, attachment, msg) handles v1 (stop → place derived, area as sent) and v2 (place validated/resolved → area derived; errors 'bad-place', 'bad-frame'); one transactionSync writes area, stopId, stop, place, frame; screenMetadata() returns place (stored, else derived from stored stop via placeFromStop, else null) and frame (stored, else 6). Rate limit unchanged. Tests: extend the named cases exactly as Step 1 lists (v1 kept, v2 stop/address/bad-place/bad-frame, legacy enrichment, 400 fields). Run: npx vitest run --project workers test/pairing/screens.workers.test.ts test/pairing/beacon-do.workers.test.ts; npm run typecheck. Do not touch app/, docs/, or the quota/principal code (lines 36-48, 62-64).

### Brief 2

AGENT B — offline street index and pure client modules (after Step 0). Files: scripts/streets-geo.mjs (new), app/public/data/streets-geo.json (new, committed), test/fixtures/maps/{README.md, 14-8919-5841.mvt, 14-8920-5841.mvt, 14-8919-5842.mvt}, test/scripts/streets-geo.test.ts (new), app/src/core/screens.ts (loadStreets beside loadStops:27-34), app/src/kiosk/stops.ts (export fold:21; add isTramStop), app/src/kiosk/places.ts (new), test/app/kiosk-places.test.ts (new), test/city/place.test.ts (new), package.json devDependencies (+ @mapbox/vector-tile, pbf — already in node_modules via maplibre-gl), docs/izvori.md (+ row in the reference table starting line 63), app/src/data/izvori.json (+ same source; keep test/app/izvori.test.ts green). Facts: the register (catalogue 'streets' chunks, manifest app/public/data/city/manifest.json) has 3,795 rows, no geometry; the served tiles https://zagreb.aningfilm.hr/maps/zagreb-v1/14/{x}/{y}.mvt are gzip'd MVT with a 'roads' layer carrying 'name' and no address points; MAP_CONFIG.bounds [15.70,45.50,16.30,46.02] (app/src/core/contracts.ts:95); stops.json has 2,529 platforms/1,199 names; zet-routes.json types 0=tram (19), 3=bus (135). Build the script as Step 2 specifies (tile range, gunzip, decode, merge by normalName, centroid/bbox/length, 15 m DP polyline only for streets > 700 m, stops within 60 m ordered along the line, register join by name + settlement polygon, sorted deterministic output ≤ 400 KB, coverage line, --fetch to .cache/, --archive for a local pmtiles, fixtures mode for the test). Never call data.zagreb.hr/api/ (robots guard test/feed/robots.test.ts). Then places.ts (suggestPlaces, parseAddressQuery, anchorOf, placeInputOf) and the tests named in Step 2, using the REAL app/public/data/stops.json for the derivation tests (compute expected anchors from the file; do not hard-code distances). Run: node scripts/streets-geo.mjs --fetch once, then twice without → identical sha256; npx vitest run --project unit test/scripts/streets-geo.test.ts test/app/kiosk-places.test.ts test/city/place.test.ts test/app/izvori.test.ts; npm run typecheck.

### Brief 3

AGENT C — start screen (after Step 0; B's places.ts API is frozen as a stub). Files: app/src/kiosk/start.ts (rewrite), app/src/kiosk/place-field.ts (new, shared with settings), app/src/i18n/hr.json kiosk.setup (1087-1108) and en.json (1051-…), app/src/kiosk/strings.ts setup type (267-286) + group (411-418), app/src/ui/kiosk.css (.k-suggest rules; reuse .k-search 613-616 and .k-setup-summary 627-628), test/app/kiosk.test.ts 415-473 (+ two new its). Three hunks in app/src/kiosk.ts belong to this brief but are applied by Agent D's file ownership: KioskDeps.createScreen type (line 102) → (input: CreateScreenInput) => Promise<CreateBeaconResponse>; line 177 default → createTemporaryScreen(input); mountStartPhase (886-894) passes loadStops, loadStreets, isTram = (id) => routeType(id) === 0. Contract: field label 'Adresa ili stajalište'; suggestions from suggestPlaces (stops first, streets, segments for streets > 700 m; typed number kept as text); pick → derivePlace(anchorOf(row), stops, isTram) → summary fill('Na zaslonu: {place} i {count} stajališta uokolo', { place: place.name, count: 6 }); empty → 'Na zaslonu: cijeli grad.' and POST {} exactly; unresolved text → 'Nema takvog stajališta ni ulice. Odaberi prijedlog ili ostavi prazno za cijeli grad.' and no POST; button text 'Pokreni' (testid setup-create unchanged); intro and validity paragraphs removed with their keys; loadStops/loadStreets only on first focus or input (kiosk.test.ts:425 asserts no load on the start screen); keyboard: ArrowUp/Down/Enter/Escape; a11y: role=combobox/listbox/option, aria-live summary; handheld: list static below the input; lagano: works without the map. Error ladder (403/429/network) unchanged. Run: npx vitest run --project unit test/app/kiosk.test.ts test/app/i18n.test.ts test/app/copy-guards.test.ts; npm run typecheck.

### Brief 4

AGENT D — settings toggles, long press, header, wall state (after Step 0; owns app/src/kiosk.ts). Files: app/src/kiosk/settings.ts (rewrite), app/src/kiosk/prefs.ts (new), app/src/kiosk.ts, app/src/beacon.ts (setScreen v2, lines 85 and 197-199), app/src/kiosk/mapview.ts (FieldInput.place, fieldView place → stop → city), app/src/ui/kiosk.css (.k-brand button reset; .k-settings .k-suggest replacing .k-stop-list rules 850-852; keep .k-theme), app/src/i18n/hr.json kiosk.settings (1109-1137) + en.json, app/src/kiosk/strings.ts settings type + group (419-423), test/app/kiosk.test.ts 476-706, 709-722, 1723-1760, 1781, 1893-1930, test/app/beacon.test.ts 90-106, test/app/kiosk-css.test.ts 43-61. Contract: header = brand button (testid kiosk-brand, aria-label 'Kaj ima? · Postavke zaslona') + date + clock; no kiosk-settings, no kiosk-theme; LONG_PRESS_MS = 1500 exported, slop 12 px, Enter/Space opens at once; openSettings guard unchanged (invitation phase only). Panel rows data-row place/frame/theme/rhythm/screen; strings 'Mjesto', 'Promijeni', 'Cijeli grad' (existing areaWhole), 'Kadar: {count} stajališta odavde', 'Tema: {pref}' (existing header.theme + THEME_ICON glyph moved from the header), 'Ritam: {seconds} s', 'Postavke zaslona' (existing open), 'Poslužitelj nije prihvatio mjesto. Odaberi ponovno.' (saveRefused), saveBusy/saveOffline/expiry/forget* unchanged; delete area/areaHint/stop/stopNone/stopHint/save/saving/saved. Place change uses Agent C's mountPlaceField (import; if not merged yet, code against the signature in Step 3). Sending: SendQueue with SETTINGS_SEND_DELAY_MS = 800 trailing debounce, ≥ SCREEN_SET_MIN_MS (from worker/protocol.ts) between sends, one in flight, SAVE_TIMEOUT_MS → saveOffline; latest wins; nothing equal to deps.screen() is sent; applied() compares place+frame and keeps the panel open; refused() repaints toggles from deps.screen(). Rhythm: prefs.ts localStorage key 'vidikovac-kiosk-rhythm', RHYTHMS [20,30,60], default 20; kiosk.ts paints .kiosk[data-frame], [data-rhythm], [data-place-kind] and exposes rhythm to the composition (deps/handle) for the wall WP. State in kiosk.ts: place/frame from screen.place ?? placeFromStop(screen.stop, isTram) / screen.frame ?? 6 in adoptCredentials and applyScreen; paintContext → place?.name ?? CITY_AREA.name ('Zagreb') when credentials exist; remove configuredDistrict; requestKioskMap gets place, frame and spanM = frameSpanM(frameRadiusM(...)) when a place exists (FIELD_SPAN_M otherwise; HANDHELD_SPAN_M unchanged); capabilities ['city-v1','place-v2'] at line 957. Apply Agent C's three kiosk.ts hunks (Step 3). Tests exactly as Step 4 lists. Run: npx vitest run --project unit test/app/kiosk.test.ts test/app/beacon.test.ts test/app/kiosk-css.test.ts test/app/kiosk-local.test.ts test/app/i18n.test.ts; npm run typecheck.

### Brief 5

AGENT E — docs and e2e (after A–D merge; draft in parallel against the testids above). Files: docs/kiosk.md (lines 19-20, 35-62, 331-334), PRODUCT.md (59-60, 71-75 → superseded paragraph dated 22 September 2026), DESIGN.md (93-95, 129-132, 135), app/src/kiosk/INTEGRATION.md (151-154, 157-163, 165-173), e2e/screen-creation.spec.ts (7-46). Keep the literal strings test/docs/docs.test.ts asserts ('POST /api/screens', '24 sata', 'git push', 'APP_ENV=test', 'E2E_ADMIN_BYPASS', 'fizička matrica', 'Isti Wi-Fi je dopušten.'). Write the setup steps in Croatian in the doc's register (ti/impersonal, no Vi): jedno polje 'Adresa ili stajalište'; prijedlozi; pravilo (tramvajsko stajalište do 400 m, inače autobusno do 250 m, inače adresa); redak 'Na zaslonu: …'; prazno → cijeli grad; 'Pokreni'; dugi pritisak (1,5 s) na 'Kaj ima?' otvara Postavke; prekidači Mjesto / Kadar 4–6–8 stajališta odavde / Tema / Ritam 20–30–60 s / Zaslon; screen-set verzija 2 i zadržana verzija 1; područje izvedeno iz mjesta. INTEGRATION.md: describe ScreenMetadata.place/frame, read-path enrichment for records without a place, capability 'place-v2', error words bad-place/bad-frame. e2e: the flow in Step 5 (fill 'Kvatern' → suggestion 'Kvaternikov trg' → summary → Pokreni → chip → click({ delay: 1600 }) on kiosk-brand → settings-frame → data-frame 8 → Escape → the unchanged pairing half). Run locally only: npx playwright test e2e/screen-creation.spec.ts e2e/landing.spec.ts; npx vitest run --project unit test/docs/docs.test.ts. Never run scripts/audit-production.mjs or scripts/capture-landing.mjs (they create production screens).

## Verifier verdict (adversarial pass, 22 Sep)

Refuted as written: **yes**. Problems found:

- ACCEPTANCE NOT EXECUTABLE — izvori: Step 2 adds a source row to app/src/data/izvori.json `sources` and claims test/app/izvori.test.ts 'still passes'. That test pins `sources` to exactly the nine feed modules (izvori.test.ts:57 `lists exactly the nine modules`, :86 parity with worker/feed/registry MODULES, :93 and :108 exactly nine `<article class="izvor">`). A tenth `sources` entry fails four assertions; the /izvori page has no static-datasets section (app/src/izvori-render.ts:69-96 renders `sources` + `renderCitySources()` from worker/city/sources.ts REFERENCE_SOURCES/LIVE_SOURCES only).
- FACT WRONG — tile compression: Step 2 says the served tile 'arrives gzip-compressed as verified on 14/8919/5841 — gunzip with node:zlib'. Verified now: GET https://zagreb.aningfilm.hr/maps/zagreb-v1/14/8919/5841.mvt answers 200, content-type application/x-protobuf, NO content-encoding, 117,661 B, first bytes 1a ca e6 01 (raw MVT, not 1f 8b). worker/routes/maps.ts:14-18 decompresses server-side. Only the --archive (pmtiles) path sees gzip (per the archive header's tile compression). An unconditional gunzip of --fetch output throws.
- COMPILE BREAK — SCREEN_SET_MIN_MS move: Step 0 moves it from worker/do/beacon-do.ts:55 to protocol.ts and 're-imports' it in beacon-do.ts. test/pairing/beacon-do.workers.test.ts:3 imports SCREEN_SET_MIN_MS from '../../worker/do/beacon-do' (used at :304). Unless beacon-do.ts re-exports it, the acceptance 'drops a second frame inside the five-second window still passes' fails at compile.
- ACCEPTANCE NOT EXECUTABLE — production screen DQG83CVG: acceptance 'Fresh screen always names a place' says 'after deploy, on the session's own screen DQG83CVG … set Mjesto'. review.local/companion/screen.json expiresAt 1790091252546 = 2026-09-22 17:34:12 Zagreb; it is 16:13 now and WP3 lands after WP0–WP2 [O-54], so the screen will be expired (BeaconDO isRevoked() → no codes, settings never open past 'invitation' phase). Brief §17 Q15 already rules: one temporary screen per verification day, created through /kiosk/, never audit-production.mjs.
- OWNER-DECISION CONFLICT — frame radius: brief §17 Q10 default and ledger [D-3]/[A-P1] fix ONE constant table (4/6/8 → 1.3/2/2.7 km, module named `shared/city/frame.ts`) 'so the map frame and the 2 km pill are one number'. Step 4 frames the wall map with the MEASURED `frameRadiusM(place, stops, frame)` (distance to the N-th stop, clamped 500–3000 m) and puts FRAME_CIRCLE_KM in `shared/city/place.ts`. The map frame and the 'U blizini · 2 km' pill then disagree in the dense centre and on the edges, exactly what Q10 forbids; and two WPs will look for the constant under two module names.
- COMPILE BREAK — missed call site: app/src/kiosk.ts:313 (`paintTicker`) also calls `fieldView({stop, district: configuredDistrict(), …, spanM: … FIELD_SPAN_M})`. Step 4 deletes `configuredDistrict()` (357-359) and lists only 514-532; line 313 is not in the plan's hunk list (131-152, 198, 339-372, 514-532, 899-960, 1056-1071, 1208-1223). It must be updated with the same place/frame camera as paintMap or the highlight bounds are computed on the wrong frame.
- MISSED STRING — app/src/kiosk/invitation.ts:45 hard-codes (not in hr.json) 'Za javni zaslon otvori /kiosk/ na tom uređaju i odaberi Pokreni zaslon.' / '…choose Start screen.' Step 3 renames the button to 'Pokreni' but does not list this file; the handheld sentence would name a button that no longer exists. docs/kiosk.md:331 (troubleshooting row 'pritisnuti **Pokreni zaslon**. Područje i stajalište nisu potrebni.') is likewise outside the plan's 333-334 range.
- TEST PREMISE CHANGES — test/app/kiosk.test.ts:594 uses `onError('bad-frame')` as 'an error word that belongs to something else on the socket' and asserts the panel ignores it. Step 1/4 make 'bad-frame' a real screen-set refusal. The plan's 476-706 rewrite must replace that probe word (and update settings.ts:36 SCREEN_SET_ERRORS to add 'bad-place', 'bad-frame'); it is not called out.
- TEST HELPER MISMATCH — Step 1's legacy case says '`onlineKiosk` created via the existing helper with a stopId'. test/pairing/helpers.ts:196 `provision(area='donji-grad')` and :212 `onlineKiosk(area)` take only an area and hard-code `stopId: null` (:204); no stop can be passed. The 'legacy record → screenMetadata().place derived' case cannot be written as described.
- MIS-CITED PRECEDENT — Step 0 cites 'worker/feed/modules/zet-routes.ts:19 and worker/pairing/stops.ts:1' as precedent for `import routes from '../../app/src/data/zet-routes.json'`. zet-routes.ts:19 is a dynamic `await import()` in try/catch; stops.ts:1 imports `../data/zet-stops.json` from worker/data, not app/src/data. (A static JSON import does work under moduleResolution 'Bundler' + wrangler; route `type` is numeric — 19×0, 135×3 verified — so `=== 0` is right. app/src/data/routes.ts:2-3 comment 'the only module that imports the JSON' becomes stale.)
- INACCURATE CLAIM — Step 5 says scripts/audit-production.mjs:449-450 'keep working unchanged (empty field, setup-create)'. Lines 442-448 already click `setup-next`, wait for `setup-stops`, fill `setup-search`, check `input[name=stop][value=106_1]` — the retired wizard; the script is already broken against the current one-button start. The 'never run it' rule stands, but the claim is false and could mislead Agent E.
- MISSING HOUSEKEEPING — .gitignore (node_modules/, dist/, .dev.vars, .wrangler/, test-results/, playwright-report/, *.local, .superpowers/, recordings/) has no `.cache/` entry, so the plan's 'gitignored .cache/maps/zagreb-v1/' is not true until added. scripts/*.mjs are documented dependency-free (scripts/districts.mjs:6-7, 34-37); streets-geo.mjs importing @mapbox/vector-tile + pbf + pmtiles is a convention break the script header must declare. package.json has no `build:streets` script (build:city/network/schema/trips exist).
- MINOR — `CITY_CENTRE` (settings.ts:33) is module-private; Step 2's `suggestPlaces(..., near = CITY_CENTRE)` and Step 4's `CITY_CENTRE_POINT` need an exported constant (none exists in app/src). i18n parity test is at test/app/i18n.test.ts:49-51, not 10-11. `grep -c … → 0` exits 1 on zero matches; use `! grep -q`. The test 'the invitation header has no button except the brand' named in the acceptance does not exist and must be written (Step 3/4). `k.tick(ms)` (kiosk.test.ts:150) fires only timers registered with exactly that ms through the injected setInterval/oneShot — the 120 ms debounce and LONG_PRESS_MS must use deps.setTimeout, never globalThis.
- VERIFIED CORRECT (for the record): every cited file exists; line pointers for worker/protocol.ts (32, 34-40, 90-96, 108-118, 115), beacon-do.ts (55, 59, 104-124, 109, 174-185, 346-358, 443-446, 618-637), screens.ts 49-61, provision.ts 8-15/21/24-29, stops.ts:15, districts.ts:125, kiosk.ts (102, 131, 136-152, 177, 198, 339-350, 356-371, 514-532, 886-894, 899-925, 932-948, 957, 1056-1071, 1208-1223), start.ts 16-23/56-67/125-145, settings.ts 28/47-64/93-133/323-336, stops.ts 21-29/37-40, mapview.ts 605-616/674-689/515-540, beacon.ts 85/197-199, core/screens.ts 27-34, contracts.ts:95, hr.json 1087/1109, en.json 1051/1072, strings.ts 267-286/289-…/411-423, kiosk.css 613-616/627-628/850-852, kiosk.test.ts 415-473/476-706/709-722/1723-1760/1781/1893-1930, beacon.test.ts 90-105, kiosk-css.test.ts 43-61/126, screens.workers.test.ts 43-80, beacon-do.workers.test.ts 257-322, docs.test.ts 48-58, copy-guards 71-73, docs/kiosk.md 19-20/36-62/333-334, PRODUCT.md 59-60/71-75, DESIGN.md 93-95/129-132/135, INTEGRATION.md 151-154/157-163/165-173, e2e/screen-creation 7-46, landing.spec:152, capture-landing 147-148 are right. Data facts hold: stops.json 2,529 platforms/1,199 names, routes only (no routeType); register 3,795 rows, 3,289 in Zagreb, 177 ambiguous names, 'Trg Eugena Kvaternika'/'Kvaternikova ulica'/'Ilica' present, 'Kvaternikov trg' absent; 236_2 = Kvaternikov trg with tram routes; @mapbox/vector-tile 3.0.0 and pbf 5.1.2 in node_modules, pmtiles 4.5.0 a dependency; vitest 'unit' glob test/**/*.test.ts covers new test/city and test/scripts files; districts.mjs exports douglasPeucker (:122) behind an invokedDirectly guard (:373); worker/routes/maps.ts R2 key vidikovac-maps/zagreb-v1.pmtiles; MAP_ATTRIBUTION_HTML basemap.ts:496-497; kiosk.test fixture 200_1 'Zapruđe' routes ['7'] (tram) and 106_1 tram routes match the expected place kinds; parseClient 512-char cap fits a max-size address place (~330 chars); fixed constraints (presence gate, quotas, /hitno, prijava, git-push deploy) are not touched by any step.

Corrections:

1) izvori (Step 2 + acceptance): do NOT add to izvori.json `sources`. Add the ODbL row to docs/izvori.md 'Statički skupovi' table (after line 65): `| Ulične osi za pretragu adrese | OpenStreetMap contributors preko Protomaps arhive zagreb-v1 (posluženi z14 tile-ovi /maps/zagreb-v1) | ODbL 1.0 (izvedena baza, share-alike) | scripts/streets-geo.mjs → app/public/data/streets-geo.json | © OpenStreetMap contributors · Protomaps |`. For /izvori add a NEW key `"staticSources": [{ id: 'streets-geo', naziv: 'Ulične osi za pretragu adrese', url: 'https://www.openstreetmap.org/copyright', text: '© OpenStreetMap contributors · Protomaps', licence: 'ODbL 1.0' }]` to izvori.json and a `renderStaticSources()` section in app/src/izvori-render.ts (beside renderCitySources, line 88) with its own `<section aria-labelledby="static-sources-title">`, NOT `<article class="izvor">`; add one test in test/app/izvori.test.ts asserting the section renders and that `sources.length` is still 9. Rewrite the acceptance to: `npx vitest run --project unit test/app/izvori.test.ts test/docs/docs.test.ts → 0 failed; grep -c 'streets-geo' docs/izvori.md ≥ 1`. 2) Tile decoding (Step 2, Agent B): '--fetch' tiles are raw MVT — decode directly with new VectorTile(new Pbf(buf)); detect gzip only by magic bytes (buf[0]===0x1f && buf[1]===0x8b) and gunzip then; '--archive' path: read header via pmtiles PMTiles(source).getHeader() and gunzip only when header.tileCompression === Compression.Gzip. Fixture README must state the fixtures are raw MVT as served (117,661 B for 8919/5841). 3) SCREEN_SET_MIN_MS: in worker/do/beacon-do.ts replace line 55 with `import { SCREEN_SET_MIN_MS } from '../protocol'; export { SCREEN_SET_MIN_MS };` (or change test/pairing/beacon-do.workers.test.ts:3 to import it from '../../worker/protocol'); keep both paths type-clean. 4) Production verification acceptance: replace 'on the session's own screen DQG83CVG' with 'per brief §17 Q15: after Cloudflare Builds is green, create ONE temporary screen for the verification day through https://zagreb.aningfilm.hr/kiosk/ (field left empty), record it in review.local/companion/screen-<date>.json (same shape as screen.json), open its Postavke by long press, set Mjesto = Kvaternikov trg, sample kiosk-context every 20 s for 10 min (Playwright textContent) → non-empty in 100 % of samples, then set Cijeli grad and close; never a second screen that day; audit-production.mjs/capture-landing.mjs never run'. 5) Frame radius: pick ONE rule and write it into both Q10 and Step 0. Recommended (honours [D-3]/[O-30]): `export const FRAME_CIRCLE_KM = {4:1.3, 6:2, 8:2.7}` in shared/city/place.ts (rename Q10's default to this path, or create shared/city/frame.ts and have place.ts re-export it), wall map spanM = `2 * FRAME_CIRCLE_KM[frame] * 1000` for a screen with a place (constant; no frameRadiusM in the camera path); keep `frameRadiusM` only if the wall WP asks for a diagnostic, and drop the 'monotone/clamped frameRadiusM' acceptance in favour of `fieldZoom(FIELD_DESIGN_WIDTH.wide, place.lat, 2 * FRAME_CIRCLE_KM[6] * 1000)` in test/app/kiosk.test.ts 1723-1760. If the owner instead wants measured framing, strike Q10's default and [D-3]'s 'one constant' note in the brief in the same commit. 6) kiosk.ts:313 — add to Step 4's hunk list: `const camera=fieldView({stop, place, district:null, widthPx:width, heightPx:height, spanM: wallSpanM()})` where `wallSpanM()` is the same helper paintMap uses (handheld → HANDHELD_SPAN_M; place → frame span; else FIELD_SPAN_M). 7) Add app/src/kiosk/invitation.ts:45 to Step 3 (Agent C): change 'odaberi Pokreni zaslon' → 'odaberi Pokreni' and 'choose Start screen' → 'choose Pokreni' (or move the sentence into hr/en `kiosk.invite.handheldInfo` and mirror in en.json — then add the key to strings.ts and i18n parity). Add docs/kiosk.md:331 and :36 to Step 5's line list. 8) Step 4 tests: in the rewrite of kiosk.test.ts:585-605 use a non-screen-set word (e.g. 'auth-required') as the ignored probe, and extend settings.ts SCREEN_SET_ERRORS to `['bad-area','bad-stop','bad-place','bad-frame','screen-set-rate']`; beacon.test.ts:99-102 add `server({ t:'error', error:'bad-place' })` → onError('bad-place'). 9) Legacy DO case (Step 1, Agent A): create the record directly — `const stop = screenStop('106_1')!; await beaconStub(testEnv, id).create({ beaconId:id, venueType:'kafic', area:'donji-grad', operatorLabel:'x', stopId:'106_1', stop, secret })` then connectBeaconDirect + authKiosk, and assert `instance.screenMetadata()` toMatchObject `{ place:{kind:'tram', stopId:'106_1'}, frame:6 }`; or add an optional `stopId` parameter to provision()/onlineKiosk() in test/pairing/helpers.ts:196/212 (touching test/ is allowed). 10) Step 0 wording: cite worker/pairing/stops.ts:1 as the static-JSON-import precedent (path worker/data) and state that worker/pairing/place.ts imports `../../app/src/data/zet-routes.json` statically (moduleResolution Bundler; wrangler bundles JSON); update the stale comment at app/src/data/routes.ts:2-3. 11) Step 5: reword the audit-production note to 'scripts/audit-production.mjs already targets the retired wizard (setup-next, setup-stops, setup-search at 442-448) and is not exercised by this WP; never run it'. 12) Housekeeping: add `.cache/` to .gitignore in Step 2; add `"build:streets": "node scripts/streets-geo.mjs"` to package.json scripts; write in streets-geo.mjs's header that it is the one scripts/*.mjs tool with npm dependencies (@mapbox/vector-tile, pbf, pmtiles) and why; add @mapbox/vector-tile and pbf to devDependencies with a package-lock update. 13) Minor: export `CITY_CENTRE` from app/src/kiosk/stops.ts (move it from settings.ts:33) and import it in places.ts, settings.ts and kiosk.ts; fix the i18n cite to test/app/i18n.test.ts:49-51 (and optionally add the deleted kiosk.setup/settings keys to DEAD_KEYS at :21-43); write the acceptance grep as `! grep -q -E 'data-testid="kiosk-(theme|settings)"' app/src/kiosk.ts`; add the new unit test 'the invitation header has no button except the brand' to Step 4's list (`k.root.querySelectorAll('.k-head button')` length 1, dataset.testid 'kiosk-brand'); state in Step 3/4 that mountPlaceField and the long press schedule through deps.setTimeout (kiosk's oneShot) so `k.tick(120)` / `k.tick(LONG_PRESS_MS)` fire. 14) Keep `place: null` explicit (never omitted) in both worker/pairing/provision.ts's `screen` and BeaconDO.screenMetadata(), and `frame` always present, so screens.workers.test.ts:33/36 `toEqual(screen.screen)` keeps holding across the create response, the scan grant and the room 'joined' frame.
