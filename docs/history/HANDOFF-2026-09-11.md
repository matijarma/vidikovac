# Hand-off, Friday 11 September 2026 (evening)

What exists, what Matija does next, in order. Croatian documents for the filing are in `docs/prijava/`.

## What is live

- https://zagreb.aningfilm.hr (Worker `vidikovac`, account KompMajstor, deploys from `main` on GitHub `matijarma/vidikovac`). Behind Cloudflare Access for now.
- Open tier `/hitno`, open data `/open/` and `/open/catalog.json`, teaser `/api/teaser`, data `/api/data/:module` (session token for session modules), scan `/s/`, dashboard `/d/`, kiosk `/kiosk/`, `/izvori`, `/privatnost`, `/pristupacnost`, operator `/stats` (Access-gated).
- All seven live modules answer with real data (ZET GTFS-Realtime, road closures, DHMZ warnings, observation and forecast, EMSC quakes within 1.5 degrees over 7 days, HRT headlines, city GeoJSON layers; Službeni glasnik as a session module).
- The pairing mechanic verified in production: beacon socket challenge and HMAC auth, batches of twenty 30-second codes, keepalive, and a scan from the kiosk's own network rejected with `same-network`.
- Test state on `main`: 81 vitest files, 663 tests, `npm run typecheck` clean, `vite build` clean, `npm run check:izvori` answers on all 27 source URLs.

## Do this before the Monday café test

1. **Narrow Cloudflare Access.** In Zero Trust the application on `zagreb.aningfilm.hr` currently covers everything. Change its path scope to `/stats*` and `/api/admin/*` only (or make two applications). Public paths must be reachable without login for the call's "nenaplatno dostupni javnosti" and for the demo. The Worker verifies the Access JWT itself on those two paths (vars `CF_ACCESS_TEAM_DOMAIN` and `CF_ACCESS_AUD` are set).
2. **Switch off Cloudflare Web Analytics injection** for the hostname (Analytics & Logs, Web Analytics). Our CSP already blocks the beacon, but the privacy statement promises no third-party script at all.
3. **Provision the demo screen on the kiosk device.** A screen `JF1RC8SE` ("Demo zaslon", kafić, Donji grad) already exists; its one-time provisioning URL is in `.superpowers/sdd/2026-09-11-vidikovac-stage1/beacon-demo.json` (never commit it). Open that URL once in the kiosk browser (Chromium with `--kiosk --noerrdialogs --disable-infobars --incognito` is documented in `docs/kiosk.md`); the secret is stored in that browser. New screens: `POST /api/admin/beacons` with an Access session, body `{"venueType":"kafic","area":"donji-grad","operatorLabel":"..."}`; area slugs are the 17 gradske četvrti (`donji-grad`, `trnje`, `maksimir`, ...).
4. **Café test.** Kiosk on the venue Wi-Fi, phone on mobile data. Scan the QR with the phone camera (native app), confirm card "Zaslon: kafić, Donji grad, 10 minuta", tap Otključaj; both screens switch; the kiosk mirrors the phone's layer; try "Podijeli grad" to a second phone (5 minutes, one hop); wait for the 60 s and 20 s announcements and the freeze at ten minutes. Film it (shot list in `docs/video/shot-list.md`). With the phone on the same Wi-Fi you must see the Croatian same-network message; that is the rule working.

## Filing (Wednesday 16 September, e-Pisarnica + NIAS, by early afternoon)

- `docs/prijava/prijedlog-projekta.md` (paste into Word): fill the five `[[POPUNITI]]` fields (OIB, address, video link and similar), then run `npm run check:izvori -- --filing`, which exits non-zero while any field is unfilled.
- `docs/prijava/obrazac-3-financijski-plan.md`: the five rows 6,300 / 11,090 / 1,200 / 320 / 1,090 = 20,000 EUR without VAT, hourly formula stated; confirm the rate against payroll.
- `docs/prijava/plan-provedbe.md`, `docs/prijava/rizici-i-odgovori.md` support the criteria sections.
- Attachments per `docs/prijava/petak-11-rujna.md`: BON-1, BON-2, Porezna, GSKG (all after 1 Sept), five statements, Obrasci 1, 2.1, 2.2, 3.
- Video: `video/out/title.mp4` and `video/out/end.mp4` are rendered; cut them around the café footage (90 s, shot list), upload unlisted, put the link in the proposal.

## Where the record is

- Plan `C:\Users\MatijaRadeljak\.claude\plans\lets-work-on-this-ethereal-beacon.md`; spec `docs/superpowers/specs/2026-09-11-vidikovac-design.md`; implementation plan `docs/superpowers/plans/2026-09-11-vidikovac-stage1.md`.
- Ledger and rulings (git-ignored): `.superpowers/sdd/2026-09-11-vidikovac-stage1/progress.md`, `rulings.md` (R-01 to R-49), per-task reports `task-*-report.md`, final review `final-review.md` when written.
- Local dev: `.dev.vars` exists (NETWORK_CHECK=off, test bypass); `npm run dev`, `npm test`, `npm run e2e`.
