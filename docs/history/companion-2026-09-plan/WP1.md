# WP1 — Wall composition: header sentence, "U blizini" timeline, QR card, footer, night, outage

Part of the companion brief `docs/companion-2026-09-22.md` (§15). Line numbers refer to the working tree at build `b300af3`; re-anchor with `grep` before editing. Read-only rules of the brief §1 apply: no `wrangler deploy`, no production screen outside the documented local flow, `docs/prijava` and `app/prijava` untouched.

## 0. Reconciliation with the other packages (binding; read first)

The planner text below was written before the seven packages were compared and before the adversarial verifier ran. Where this section, the verifier verdict at the end, or the brief §15 disagree with the planner text, **this section wins, then the verdict, then the planner text**.

**Owner rulings of 22 September, evening ([O-62]–[O-74], binding over everything below).** (1) A screen without an address takes **Trg bana Jelačića** as its place for the list and the departures [O-65]: `selectNearby` always has a place; the departure-less whole-city case is gone; the head shows the place. (2) The circle is **measured per place** [O-68]: `NearbyInput.radiusM` (from `frameRadiusM(place, stops, frame)` in `shared/city/frame.ts`) replaces `frame`; `nearbyHead(i18n, radiusM)` prints the measured distance with a decimal comma and its walking minutes at 7.5 min/km, e.g. "U blizini · 2,2 km · ~16 min"; "2 km · ~15 min" is what a 2.0 km radius prints, not a constant. (3) No "zid" in any UI string [O-66]; standard, natural Croatian; every new Croatian string gets the owner's read-through before deploy.

These decisions override the planner text below wherever they differ. Apply **every correction in the verifier verdict** (the last section of this file), with the following settlements. Canonical package ids: the plan's "WP2 (setup …)" is **WP3**, its "WP map" is **WP2**, its "Phone WP" is **WP4** (brief §15 id table).

**Lane and gate.** WP1 is lane W-C (the longest wall lane). It starts after the shared seams (lane S) exist and after D1 for anything in `app/src/kiosk.ts`; its integrator pass is the **last** of the serial order WP3 → WP2 → WP1 on `kiosk.ts`, `kiosk/mapview.ts`, `test/app/kiosk.test.ts`, `kiosk.css`; the wall ships as **one deploy D2** together with WP2 and WP3.

**Module names and locations (seams S4–S6, built first by the coordinating session, filled by WP1).**
1. `shared/kiosk/sentence.ts` holds the wire types `SentenceKicker`, `SentenceFact`, `WrittenSentence`, `SentenceRequest`, `SentenceResponse` and `acceptSentence(...)`; the Worker route and the app import from there (replaces `shared/kiosk/sentence-accept.ts`).
2. **`app/src/city/nearby.ts`** (pure, no DOM) replaces `app/src/kiosk/nearby.ts`: `selectNearby(input: NearbyInput): NearbyRow[]`, `nearbyHead(i18n, frame): string`, `rowBudget(availablePx, count)`. `NearbyKind = 'departure'|'closure'|'event'|'solar'|'last'|'first'|'opening'|'always'|'pharmacy'`; `NearbyRow { id; kind; atMs: number | null; always: boolean; title; sub; live; source; selection?; map?: MapHighlight }` (`map = { id: item.id, geometry: item.geo }`). `NearbyInput { place: ScreenPlace | null; frame: FrameStops; now; boards; fixes; snapshots; city; lastRun; locale; i18n }`. The builders a–h of plan step 2 fill it; `timeline.ts` derives `timeLabel`/`dayLabel`/lead markup from `atMs`, `kind`, `live`. The radius is `frameRadiusM(input.frame)` from `shared/city/frame.ts` (seam S2), never a private `NEARBY_RADIUS_M`.
3. **`app/src/city/sentence.ts`** replaces `app/src/kiosk/sentence.ts`: `sentenceFacts(...)`, `templateSentences(...)`, `createSentenceSequence({ rhythmMs, noRepeatMs: 600_000 })`; `fetchSentences` in `app/src/api.ts`. The phone (WP4) reuses all three.
4. **Rhythm**: the 20 s cadence is `prefs.readRhythm()` from WP3's `app/src/kiosk/prefs.ts` (20 | 30 | 60), passed into `createSentenceSequence` and the timeline tick; no hard-coded 20 s.

**Ownership inside `app/src/kiosk.ts` and its neighbours.**
5. WP1 owns **only `.k-head-mid`** (the sentence, its kicker, the visually-hidden overflow probe span, `data-valid-until`, the pairing notice and session label unchanged). The brand button, long press, removal of the gear and theme glyph, and the settings panel are **WP3's** (strike them from step 7). The wall's "Vrati pregled grada" button is **WP0's** (D1).
6. WP1 owns `app/src/kiosk/invitation.ts` (full rewrite), `timeline.ts`, `frame.ts` (footer: green cross · 24/7 · address; sources without a time; moves `SAFETY_ICON`/`safetyVerdict` into the existing `app/src/experience/safety-state.ts` and removes the `kiosk.safety.confirmed` and `kiosk.safety.pharmacy` keys with their `KioskStrings` members), `markup.ts` `codeBlockMarkup`, `highlights.ts`/`ticker.ts` deletion with every kiosk dead path of ia-map §7 #1, #10, #28 (corner-qr, join-code, `kiosk.ticker.*`, `kiosk.invite.codeWaiting`), `teaser.ts:47` (`body: s.invitation.lead`, card id stays `invitation`), `test/city/readable.test.ts:6,42-83` deletion. WP5's A2 is reduced accordingly.
7. **`app/src/kiosk/mapview.ts` is WP2's function.** WP2 adds `vehiclesVisible?: boolean` (→ `points = []`, never `setModes(new Set())` — verdict 7j), `place`, `frame` to `KioskMapInput`; WP1 only passes `vehiclesVisible: feedStateOf(snapshots['zet-rt']) !== 'down'` and `setHighlight(sentence?.refs)` from `paintMap`. The camera is WP3's `wallSpanM()`; `paintTicker` is deleted by WP1 (WP3 skips its :313 edit).
8. **Legend keys are WP2's** (`kiosk.legend.{tram,bikes,culture}`, culture = "Kultura večeras", brief Q6); WP1 mounts them (only when `!lightweight`). Handheld sentence from `kiosk.handheld.info` with the word **"Pokreni"** (WP3 renames the button).
9. `app/src/city/strings.ts`: WP1 does not touch it (the `quiet` deletion moves to WP4).

**Probe contract (brief §15 table; WP6 reads it).** `[data-testid=kiosk-sentence][data-kicker][data-valid-until]` on the container with children `kiosk-sentence-kicker` / `kiosk-sentence-text`; `[data-testid=nearby]` > `[data-testid=nearby-head]` (text exactly "U blizini · 2 km · ~15 min" for frame 6) + `ol[data-testid=nearby-rows]` > `li.nearby-row[data-id][data-kind][data-when=<ISO> | data-always="1"][data-live="1"?][data-source]` with `.nearby-title`, `.nearby-when` (`<time>`), `.nearby-sub` (`data-key` may stay as a duplicate attribute for `reconcile.ts`); outage note `[data-testid=map-note]`; footer `[data-testid=strip-pharmacy] [data-symbol=pharmacy]`, `strip-sources` without HH:MM; QR `kiosk-qr` SVG ≥ 240 CSS px at 1920, 1366 and 1080×1920 (kiosk.css:118/:149 and kiosk-city.css:27 → 264 px plate, `.k-qr .qr` padding 12 px; handheld stays 240).

**Legibility tiers (settled here so the CSS is written once; WP6 asserts them).** Row title and time at `--k-main-size` (40 px, read tier ≥ 38.9 px); `.nearby-sub` at `--k-sup-size` (28 px, walk-up tier); `--k-card-hint` 26 → 28 px; `.k-lead` 28 px (walk-up); `.k-map-legend` walk-up. Rows grow from 64 to 92 px by count; type scales with `--k-nearby-scale` but never below the tier floors. Kicker colours: `bicikli` uses `--k-green` (the bike token fails AA); contrast pairs written as palette roles.

**Whole-city wall (brief Q4).** With `place === null` the head reads "Zagreb" and the pill is omitted; no departures. Frame 4/8 head copy defaults (Q9-type, flagged): `{ 4: '1,3 km · ~10 min', 6: '2 km · ~15 min', 8: '2,7 km · ~20 min' }`.

**Instrument.** Plan step 12's `scripts/kiosk-rotation.mjs` is **dropped**; the ten-minute rotation, calm-motion and outage checks are WP6's `e2e/wall.ts` + `e2e/accept/wall.spec.ts` scenes; WP1's acceptance cites that spec. WP1 must ship the deterministic template fallback (≥ 3 distinct sentences per 10 min without Workers AI). Night-palette artefact: `e2e/readable-city.spec.ts` wall screenshot at 1920×1080 under `?tema=tamna` with `page.clock` at 21:30.

**Copy.** New keys only under `kiosk.nearby.*`, `kiosk.sentence.*`, `kiosk.handheld.*` (seam S9); byte-exact owner strings: "U blizini", "{km} km · ~{min} min", "uvijek", "Skeniraj za 10 minuta grada.", "24/7", "sutra", the six kickers; the outage note "ZET trenutačno ne šalje položaje vozila; polasci su po voznom redu."; the copy-guards rule is scoped to those groups. Stale closures stay on the wall (Q7).

## Goal

Replace the wall's aside (weather panel + 20 s highlight + QR card) and its dead header middle with the composition the owner approved through mocks v1–v5 (brief §4, §11, §12): one header (brand · place · one model-written sentence with a coloured kicker · date · clock), a time-ordered "U blizini · 2 km · ~15 min" list beside the map (≤ 3 departures blue/grey, timed rows, last-departures row from T−4 h, first tram from 22:00, one solar event, 'uvijek' rows alternating place story / heritage every 20 min, whole rows only, 64–92 px rows sized from the item count, rows keep their DOM nodes and enter at their time position), a QR card reduced to lead + code + typed address with a real ≥ 240 px code, a footer with the green cross · 24/7 · address and sources without times, no pause/copy/theme/gear on the visible wall (settings behind a 1.5 s long press on the brand, still reachable by an accessibly-named hidden button), the existing solar night palette verified for the new elements, and an outage mode (zet-rt down) in which the map keeps everything but vehicles, one quiet note sits on the map, every departure is a grey timetable time, the header sentence says what is known and nothing reads "nedostupno" as a headline.

## Depends on

- Nothing blocks WP1 from starting: it changes only app/src/kiosk/*, app/src/kiosk.ts, worker/routes/kiosk.ts (+ index registration), worker/feed/sentences.ts, shared/kiosk/sentence-accept.ts, scripts/gtfs-lastrun.mjs, app/public/data/lastrun/*, i18n JSON, CSS, tests, docs.
- WP2 (setup: address or any stop → place; Kadar 4/6/8 'stajališta odavde'; click-toggles Mjesto/Kadar/Tema/Ritam) supplies `place` and `radiusM` to `nearbyItems`/`InvitationModel`; until it lands a whole-city screen has no departures and the pill is the constant '2 km · ~15 min'. WP1 exposes `NEARBY_RADIUS_M` and `place` as the seams WP2 fills.
- WP map (framing 6–7 stops, no clusters, BAJS counts, full-number pills) is independent; WP1's outage plumbing (`vehiclesVisible`) and the sentence-linked `setHighlight` ring use the existing mapview API only.
- Phone WP (Sada first viewport) should reuse `nearbyItems` and `sentenceFacts`/`templateSentences` from WP1 for 'U blizini' on the phone; therefore keep those modules free of kiosk DOM (they are pure).

## Data, protocol and artefact changes

1) app/public/data/lastrun/<stopId>.json (committed, generated by scripts/gtfs-lastrun.mjs) gains an optional sibling field `first: Record<routeId, Record<serviceDate, 'HH:MM'>>` (earliest departure per line per GTFS service date); `routes` (latest) unchanged; app/src/core/lastrun.ts `LastRunLive.first?` and `firstDeparture()`. 2) New Worker route `POST /api/kiosk/sentences` (worker/routes/kiosk.ts): request `{ locale: 'hr'|'en'; budget: 40..80; facts: { id: string; kind: 'promet'|'kultura'|'vrijeme'|'bicikli'|'nocas'|'radovi'; text: string (≤160); validUntil: number|null }[] (≤16) }`, response `{ generatedAt: string; sentences: { kicker; text; refs: string[]; validUntil: number|null; origin: 'model' }[] }`, `cache-control: private, no-store`; 405/400/429; KV keys `sentence:v1:<sha256>` in env.FEED (TTL 1200 s, negative 300 s); no AI call when `!env.AI || isTestEnvironment(env)`; rate-limited by env.RL_OPEN per client IP; same-origin only. 3) New map request option `vehiclesVisible?: boolean` on `KioskMapInput` (mapview.ts) ⇒ `modes: new Set()` when false. 4) i18n: add `kiosk.nearby.*`, `kiosk.sentence.*`, `kiosk.legend.*`, `kiosk.handheld.info`; remove `kiosk.ticker.*`, `kiosk.invite.support`, `kiosk.invite.codeWaiting`, `kiosk.safety.confirmed`; `city/strings.ts` drops `quiet`. 5) DOM testids the instruments read: `kiosk-sentence`, `kiosk-sentence-kicker`, `kiosk-sentence-text` (with `data-kicker`, `data-valid-until`), `kiosk-nearby`, `kiosk-nearby-pill`, `kiosk-nearby-rows`, `li.k-nearby-row[data-key][data-kind]`, `.k-nearby-time[data-live]`, `kiosk-map-note`, `kiosk-brand`, `strip-pharmacy`, `strip-sources`; removed: `kiosk-highlight`, `kiosk-panel-weather` (invitation only), `kiosk-theme`, `pair-copy`, `pair-copy-status`, `kiosk-ticker`, `corner-qr`, `join-code`. 6) Deployment unchanged: git push to main → Cloudflare Builds; wrangler.jsonc already binds AI, FEED, RL_OPEN.

## Contracts this package supersedes

- PRODUCT.md, approval of 20 September 2026 ('passive-view refinement'): the sentences 'one anchored geographic map, compact current conditions, one map-linked highlight held for 20 seconds, and a permanently reserved QR invitation. Mobility and city-life subjects alternate when both are available.' and 'Highlighting can be paused and does not send a presentation command…' — replaced by the timeline + sentence contract (no conditions card, no rotating highlight, no pause control).
- PRODUCT.md, approval of 19 September 2026: 'Area, stop, theme and expiry move to an on-screen settings panel behind a gear in the header.' (settings are behind a long press; no gear or theme glyph in the header) and 'The header carries one line of city news at a time, whose long source texts are condensed once, server-side, into a single machine-written sentence…' (the header sentence is now written from the wall's own candidates with an 80-character budget; still a derived reading, never a republished source).
- DESIGN.md › Public screen › '20 September passive-view contract': 'Conditions, one attributed highlight and the QR occupy independent grid regions… Highlights hold for 20 seconds, alternate mobility and city life, and use real cultural/heritage records when events are quiet… Presentation suspends the sequence' and 'pause/resume and reduced motion preserve useful information' — replaced by: the aside is 'U blizini' (time-ordered rows, whole rows, 64–92 px, nodes kept) over the QR card; heritage/place-story rows appear as 'uvijek' rows without caveats; presentation suspends the sentence and the list.
- DESIGN.md › Public screen (historical body): 'The other column carries weather, a transit-exceptions card, an events card that fills the room it is given, and the pairing card…' and 'The header carries one line of city news at a time: a coloured kicker and one sentence, replaced on a fixed period…' and 'Keep location/date/time in the header and safety/on-duty pharmacy in the footer.' — amended: pharmacy as green cross · 24/7 · address; footer sources without a confirmation time; the header's kicker set is Promet/Kultura/Vrijeme/Bicikli/Noćas/Radovi at a 20 s cadence.
- DESIGN.md › Public screen: 'nothing on the picture but MapLibre's own attribution' (kiosk.css header comment and the map paragraph) — amended by the one quiet outage note on the map (§4.8).
- docs/kiosk.md :3-24 'Pasivni pregled, odobrena izmjena 20. rujna 2026.' (highlight every 20 s with a stop button; 240 px QR wording), :41-57 (zupčanik u zaglavlju otvara Postavke), :111-133 ('Desni stupac nosi tri ploče i pozivnicu…'), :135-142 (traka gradskih vijesti, 8 s, VRIJEME/PROMET/RADOVI/VEČERAS/GRAD), :144-146 (sigurnosna traka imenuje dežurnu ljekarnu — now as the green cross · 24/7 · address), :226-231 ('Rečenice u traci zaglavlja strojno su sažete…' — the sentence is written from the wall's facts, not condensed source texts).
- app/src/kiosk/INTEGRATION.md :88-96 (front page carries weather, promet, tonight; `kiosk-ticker`) and :139-155 (§Header: ticker built by kiosk/ticker.ts).
- review.local/companion/code-reality/kiosk.md §2 (the aside structure `['weather','k-highlight','k-panel--card']` and the highlight rotation) becomes historical once WP1 lands; the brief's §16 verification plan should cite scripts/kiosk-rotation.mjs as the instrument.

## Steps

### 1. Data: first departure per line per service date beside the last one  `[M]`

Files: `scripts/gtfs-lastrun.mjs`, `app/src/core/lastrun.ts`, `app/public/data/lastrun/*.json`, `test/app/lastrun.test.ts`

scripts/gtfs-lastrun.mjs currently writes {generatedAt, validUntil, routes: {routeId: {serviceDate: 'HH:MM' (latest)}}}. Extend the second stop_times pass to also keep the EARLIEST departure per (route, service date) and write it as a sibling field `first` with the same shape; keep `routes` unchanged so the phone's tiles and the paired Promet foot read the same bytes. app/src/core/lastrun.ts: `LastRunFile` and `LastRunLive` gain `first?: LastRunRoutes`; `parseFile` (:109-122) validates `first` with the same per-day string check when present and still accepts a file without it; add `export function firstDeparture(snapshot, routeId, now): { at: number } | null` mirroring `lastDeparture` (:89-102) but reading `first` for the NEXT service date whose instant lies ahead of now (today's service date first, then shiftDayKey(today,+1)); `serviceInstant` already handles GTFS hours ≥ 24. Run `node scripts/gtfs-lastrun.mjs` (zip download documented in the script header) and commit app/public/data/lastrun/. Tests: in test/app/lastrun.test.ts add `firstDeparture` cases (Fri 22:40 → Sat 04:16 from Saturday's service date; Sat 00:10 → same day 04:16; null without `first`) and update the committed-artefact shape test at :153 to accept the `first` key.

### 2. Selection layer: app/src/kiosk/nearby.ts (candidates, bounds, order, row budget)  `[L]`

Files: `app/src/kiosk/nearby.ts`, `test/app/kiosk-nearby.test.ts`

New pure module (no DOM, no fetch, no Date.now). Exports: `export type NearbyKind = 'departure'|'closure'|'event'|'solar'|'last'|'first'|'opening'|'always'`; `export interface NearbyItem { key: string; kind: NearbyKind; at: number | null; live?: boolean; dayLabel?: string; timeLabel: string; leadMarkup?: string; title: string; sub?: string; map?: MapHighlight; source: string }` (`at === null` ⇒ timeLabel is `s.nearby.always` = 'uvijek'); `export const NEARBY_RADIUS_M = 2000` (WP2 scales it 1300/2000/2700 for Kadar 4/6/8); `MAX_DEPARTURES = 3`; `LAST_DEPARTURES_AHEAD_MS = 4 * 3_600_000`; `FIRST_TRAM_FROM_HOUR = 22`; `ALWAYS_ALTERNATE_MS = 20 * 60_000`; `ROW_MIN_PX = 64`, `ROW_MAX_PX = 92`; `export function rowBudget(availablePx: number, count: number): { rowPx: number; rows: number }` = rowPx clamp(64, floor(available/max(count,1)), 92), rows = floor(available/rowPx) (Infinity available ⇒ rows = count, rowPx 64). `export function nearbyItems(input: { place: {lon,lat,name} | null; stop: ScreenStop | null; stops: readonly ScreenStop[] | null; arrivals?: StopArrivals; modules: readonly ModuleSnapshot[]; city: CityState; lastRun: LastRunSnapshot | null; now: number; radiusM: number; strings: KioskStrings; i18n: I18n; locale: string; bounds?: HighlightBounds }): NearbyItem[]`. Builders, each reusing existing readers: (a) departures — `input.arrivals.rows.slice(0, 3)` (rows come from shared/city/arrivals.ts `arrivalsAt` via kiosk.ts `arrivalsAtStop`); key `dep:${tripId}`; leadMarkup `kBadge(routeName, kindOfRoute(routeId))` (kiosk/markup.ts, kiosk/exceptions.ts); title = headsign || routeName; live = row.live; timeLabel = live && minutes !== null ? (minutes === 0 ? s.arrivals.now : fill(s.arrivals.inMinutes,{n})) : clock(atMs); NO 'uživo'/'po redu vožnje' words; when the board answered live and has zero future rows, the timetable still exists ⇒ no row is invented (principle 9 is served by the boards themselves; the first-tram row covers the night). (b) closures — `closuresByDistance(byModule(modules).prometnice, placeAsStop, now)` (kiosk/local.ts:338) filtered `distanceM !== null && distanceM <= radiusM` (place without geometry ⇒ none); key `closure:${item.id}`; at = Date.parse(item.until) or null-safe skip when no end; timeLabel = sameZagrebDay(until, now) ? fill(s.paired.untilTime,{time: clock(until)}) : `${s.nearby.until} ${dayMonth(until)}` ('do 25. 9.' — slop #22 rule); title = item.title (street); sub = item.brief ?? '' (one line, never a caveat); map = item.geo as MapHighlight; stale snapshots are included (a closure does not reopen because our fetch failed) but nothing prints 'zastarjelo'. (c) events — `locatedEvents(dogadanja.items, city.places, now, 'week')` (shared/city/events.ts:72) filtered `dataText(item,'precision')==='time'`, start ≥ now, venue located within radiusM; key `event:${item.id}`; at = start; dayLabel = sameZagrebDay ? '' : dayKey === zagrebDayAfter(now,1) ? s.say.tomorrow ('sutra') : weekdayDayMonth(locale, at); timeLabel = clock(at); title = item.brief ?? item.title; sub = venue name + (tramTo ? ` · ${s.lines.tram} ${route}` : ''); `tramTo(venue, stop, stops)` = nearest ScreenStop within 400 m of the venue whose `routes` intersects `stop.routes`, first tram route by `sortRouteIds`; map = point. (d) solar — `nextSolarEvent(now)` using `sunTimes` (app/src/ui/solar.ts): before sunrise ⇒ today's sunrise; before sunset ⇒ today's sunset; else tomorrow's sunrise (never both, [O-48]); key `solar:${sunrise|sunset}:${dayKey(at)}`; title s.nearby.sunset/sunrise; leadMarkup iconMarkup('sunset'|'sunrise'); timeLabel clock; dayLabel 'sutra' when tomorrow. (e) last departures — for each tram route of `stop.routes` (`kindOfRoute==='tram'`) `lastDeparture(lastRun, routeId, now)` (core/lastrun.ts:89); keep those with `at - now <= LAST_DEPARTURES_AHEAD_MS`; ONE row: key `last:${dayKey(now)}`; at = min(at); title s.nearby.lastTrams; sub = lines with their times sorted soonest first, `6 23:52 · 11 23:58 · 12 00:04` (badges via kBadge, times via clock); timeLabel = clock(min at); the row disappears when the LAST of them has left (each departure drops out of the sub as it passes). (f) first tram — from FIRST_TRAM_FROM_HOUR (zagrebHour ≥ 22 or < 6): for tram routes `firstDeparture(lastRun, routeId, now)` (step 1); one row key `first:${dayKey(at)}`; at = min; title s.nearby.firstTram; sub = routes sorted with times; dayLabel 'sutra' when not today; stays until it leaves. (g) openings (evening horizon, §4.5) — places of category 'market' or 'culture' within radiusM whose `Place.hours` matches a conservative opener regex `/(?:^|[^\d])(\d{1,2})[:.](\d{2})\s*[–-]/` giving tomorrow's first opening; key `open:${place.id}:${dayKey}`; dayLabel 'sutra'; timeLabel opener; title place.name; sub category word from `ct(i18n, category)`; skip on any parse doubt (unit-tested against 10 real catalogue `hours` samples). (h) always — `alwaysRows(...)`: index = floor(now / ALWAYS_ALTERNATE_MS) % 2; 0 ⇒ place story: `matchStreet(place.name, place, city.streets, city.settlements)` (shared/city/geo.ts:38) else the nearest StreetStory whose settlement polygon contains the place; title story.name, sub = first sentence of story.description cut at the first '. ' after ≥ 40 chars, ≤ 140 chars, never mid-word; 1 ⇒ heritage: nearest `category==='heritage'` place within radiusM by `distanceM`; title place.name, sub = place.address ?? place.subtype ?? ''; NEVER ct('siteNote') or ct('reference') (slop #12/#13); key `always:story:${id}` / `always:heritage:${id}`; map = polygons ? MultiPolygon : point; at null, timeLabel s.nearby.always ('uvijek'). Between 22:00 and 06:00 the always row is the pharmacy: key `always:pharmacy`, leadMarkup iconMarkup('cross') (step 8 adds the symbol), title `24/7`, sub `nearestPharmacy(stop).label` (kiosk/local.ts:382). Order: `items.sort((a,b) => (a.at ?? Infinity) - (b.at ?? Infinity))` with departures naturally first; ties by kind order departure < closure < event < solar < last < first < opening. Bounds: departures ≤ 3, one solar, one last, one first, one always; then the caller slices to `rowBudget(...).rows`. Tests (test/app/kiosk-nearby.test.ts, node env, reuse the fixture style of test/app/kiosk-local.test.ts:30-60): 3-departure cap; blue/grey labels without words; closure within 2 km in, 3 km out, 'do 18:00' vs 'do 25. 9.'; only one solar event and it flips at sunset; last-departures row appears at T−4h and its sub shrinks as trams leave, row gone after the last; first tram row from 22:00 with 'sutra'; always alternation at 20-minute boundaries; pharmacy row at 03:00 with '24/7'; no row text ever contains 'registra', 'nije provjera', 'Obuhvat', 'Dohvaćeno', 'zastarjelo', 'nepotvrđeno'; rowBudget clamps (600/3 ⇒ 92 px 6 rows; 600/12 ⇒ 64 px 9 rows; Infinity ⇒ count).

### 3. Timeline component: app/src/kiosk/timeline.ts + CSS (rows keep nodes, enter at their time position, whole rows, 64–92 px)  `[M]`

Files: `app/src/kiosk/timeline.ts`, `app/src/ui/kiosk-city.css`, `app/src/ui/kiosk.css`, `test/app/kiosk-timeline.test.ts`, `test/app/kiosk-css.test.ts`

`export function mountTimeline(host: HTMLElement, deps: { strings: KioskStrings; locale: string; reduced: boolean; designHeightPx: number }): TimelineHandle` where `TimelineHandle = { element: HTMLElement; update(items: readonly NearbyItem[], radiusM: number): void; measureHeight(): number; shown(): number; destroy(): void }`. Markup: `<section class="k-nearby" data-testid="kiosk-nearby"><header class="k-nearby-head"><h2 class="k-nearby-title">U blizini</h2><p class="k-nearby-pill" data-testid="kiosk-nearby-pill"><svg radius icon/> 2 km · <svg figure icon/> ~15 min</p></header><ol class="k-nearby-rows" data-testid="kiosk-nearby-rows"></ol></section>` — title from `s.nearby.title` ('U blizini'), pill from `fill(s.nearby.pill, {km: 2, min: 15})` ('{km} km · ~{min} min' ⇒ exactly '2 km · ~15 min'). update(): available = measureHeight() || designHeightPx; `{rowPx, rows} = rowBudget(available, items.length)`; `shown = items.slice(0, rows)`; set `element.style.setProperty('--k-nearby-row', rowPx+'px')` and `--k-nearby-scale` = rowPx/64; build a detached `<ol>` with one `<li class="k-nearby-row" data-key="${item.key}" data-kind="${kind}">` per item: `<span class="k-nearby-time" data-live="true|false">[<span class="k-nearby-day">sutra</span>]{timeLabel}</span><span class="k-nearby-lead">{leadMarkup}</span><span class="k-nearby-main"><span class="k-nearby-title">{title}</span>[<span class="k-nearby-sub">{sub}</span>]</span>`; then `reconcile(rowsEl, next)` (app/src/ui/dom/reconcile.ts:143, keyed on data-key) so an unchanged row keeps its node and only changed text nodes are touched; before reconciling record the existing keys; after it, for each li whose key was not present before set `data-enter="1"` and clear it on `animationend` (or after 260 ms); nothing else re-animates (principle 7). Rows never get `hidden`; whole rows only by slicing (no half row: the list's overflow is hidden and its height is exactly rows × rowPx via `grid-auto-rows: var(--k-nearby-row)`). CSS (kiosk-city.css: delete the `.k-highlight*` rules at lines 11-18, 28-35, 38-39, 48-49, 51 and add): `.kiosk .k-city-window .k-overview{grid-template-rows:minmax(0,1fr) auto}` (two regions now: list, card); `.k-nearby{display:grid;grid-template-rows:auto minmax(0,1fr);min-height:0;padding:var(--k-gap);background:var(--k-surface);border-radius:var(--k-radius-s)}`; `.k-nearby-head{display:flex;align-items:baseline;justify-content:space-between}`; `.k-nearby-title{font-size:var(--k-label-size);font-weight:700;letter-spacing:.04em;text-transform:uppercase;color:var(--k-ink-3)}`; `.k-nearby-pill{font-size:var(--k-hint-size);color:var(--k-ink-2)}`; `.k-nearby-rows{display:grid;grid-auto-rows:var(--k-nearby-row,64px);align-content:start;overflow:hidden;min-height:0;margin:0;padding:0;list-style:none}`; `.k-nearby-row{display:grid;grid-template-columns:minmax(7.5ch,auto) auto minmax(0,1fr);column-gap:var(--k-gap);align-items:center;border-top:1px solid var(--k-line);min-height:var(--k-nearby-row)}`; `.k-nearby-row:first-child{border-top:0}`; `.k-nearby-time{font-size:calc(var(--k-sup-size) * 1.15 * var(--k-nearby-scale,1));font-weight:700;font-variant-numeric:tabular-nums;color:var(--k-ink-2)}`; `.k-nearby-time[data-live='true']{color:var(--k-action)}`; `.k-nearby-row[data-kind='always'] .k-nearby-time{color:var(--k-ink-3);font-weight:600}`; `.k-nearby-day{display:block;font-size:var(--k-label-size);text-transform:uppercase;letter-spacing:.04em;color:var(--k-ink-3)}`; `.k-nearby-title{font-size:calc(var(--k-sup-size) * var(--k-nearby-scale,1));font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}`; `.k-nearby-sub{display:block;font-size:calc(var(--k-hint-size) * var(--k-nearby-scale,1));color:var(--k-ink-2);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}`; `.k-nearby-row[data-enter='1']{animation:k-row-in 220ms var(--ease-enter) both}@keyframes k-row-in{from{opacity:0}}`; reduced motion and lagano: add `.kiosk .k-nearby-row[data-enter='1']{animation:none}` to the two existing blocks (kiosk.css:712-719). Portrait (`.kiosk[data-portrait='1'] .k-overview{grid-template-rows:minmax(0,1fr) auto}` replaces :37-40) and handheld (`.kiosk[data-size=handheld] .k-nearby{order:2}` replaces :48-49; handheld list height auto ⇒ designHeightPx Infinity ⇒ all rows in flow). Tests: test/app/kiosk-timeline.test.ts (happy-dom): two updates with the same keys keep the same `li` node objects and set no data-enter; a new key gets data-enter, others do not; rows = min(count, budget) with designHeightPx 520 (no layout in happy-dom ⇒ measureHeight 0); `--k-nearby-row` is '92px' for 3 items and '64px' for 12; pill text is exactly '2 km · ~15 min'; no `hidden` attribute on any row. test/app/kiosk-css.test.ts:28-39 update the aside rule to `grid-template-rows:minmax(0,1fr) auto` and assert `grid-auto-rows:var(--k-nearby-row` and the absence of `.k-highlight` in both sheets.

### 4. Sentence writer, client side: app/src/kiosk/sentence.ts (facts, templates hr/en, acceptance, 20 s sequence, validity, no verbatim repeat within 10 min, overflow skip)  `[M]`

Files: `app/src/kiosk/sentence.ts`, `test/app/kiosk-sentence.test.ts`, `test/app/ticker.test.ts`

Pure module. Types (also the wire contract for step 5): `export type SentenceKicker = 'promet'|'kultura'|'vrijeme'|'bicikli'|'nocas'|'radovi'`; `export interface SentenceFact { id: string; kind: SentenceKicker; text: string; validUntil: number | null }`; `export interface WrittenSentence { kicker: SentenceKicker; text: string; refs: string[]; validUntil: number | null; origin: 'model'|'template' }`. `export const SENTENCE_HOLD_MS = 20_000`, `SENTENCE_NO_REPEAT_MS = 600_000`, `SENTENCE_BUDGET: Record<Composition, number> = { wide: 80, compact: 64, portrait: 64, handheld: 64 }`, `SENTENCE_REFRESH_MS = 600_000`, `MAX_FACTS = 16`. `export function sentenceFacts(input: { items: readonly NearbyItem[]; modules: readonly ModuleSnapshot[]; city: CityState; place; stop; now; outage: boolean; locale; strings }): SentenceFact[]` — from the SAME candidates as the timeline plus weather, closures, bikes: departures (each of the ≤ 3 rows: `Tramvaj 6 prema Črnomerec za 3 min` / `… u 14:52`, validUntil = atMs, kind promet — these are volatile and are NOT sent to the model, only templated), closures (`Ilica zatvorena do 18:00` / `do 25. 9.`, kind radovi, validUntil = until), weather (`weatherNow` kiosk/local.ts:60 + forecast row: `21 °C, vedro; danas do 24 °C`, kind vrijeme, validUntil = now + 3 h), bikes (nearest `dynamicPlaces` BAJS with fresh bikes > 0 within radius: `BAJS Kvaternikov trg: 7 bicikala` via `bikeCount`, kind bicikli, validUntil now + 15 min), solar (`Sunce zalazi u 19:05`, vrijeme, validUntil = at), last trams (`Zadnji tramvaj 6 večeras u 23:52` per line, kind nocas between 20:00–05:00 else promet, validUntil = at — this is [O-41]), first tram (`Prvi tramvaj 6 ujutro u 04:16`, nocas/promet, validUntil = at), events (`19:30 Intersonus, Kino Europa`, kultura, validUntil = start), pharmacy at night (`Dežurna ljekarna 24/7: Trg bana J. Jelačića 3`, nocas, validUntil sunrise), heritage/story always row (`{name}: {sub}`, kultura, validUntil = next 20-min boundary), outage (`ZET ne šalje položaje vozila; polasci su po voznom redu`, promet, validUntil now + 10 min; the word 'nedostupn' is banned in every template). `export function templateSentences(facts, budget): WrittenSentence[]` — the fact text is itself the sentence (facts are written as sentences, terminal '.' added); any filled text longer than budget is DROPPED, never cut. `export function acceptSentence(raw: string, facts: readonly SentenceFact[], budget: number): { text: string; refs: string[] } | null` — trim, strip a wrapping quote pair or a stray one (port `unquote` from worker/feed/brief.ts:171), reject if '' or length > budget or contains '…' or '...' or '\n' or /[*_`#[\]]/ ; reject if any number sequence `/\d+(?:[.,:]\d+)?/g` in the answer is not a substring of some referenced fact text (no invented times/temps); reject if no significant token (≥ 5 letters, hr-lowercased, or a digit run) is shared with the referenced facts; refs = facts whose text shares a token. `export function createSentenceSequence(): { read(sentences: readonly WrittenSentence[], now: number, suspended: boolean, overflowed: (s) => boolean): WrittenSentence | null }` — holds the current sentence SENTENCE_HOLD_MS, skips sentences with `validUntil !== null && validUntil <= now`, skips those shown within SENTENCE_NO_REPEAT_MS (ring of {text, shownAt}), skips ones the caller reports as overflowing, prefers the least-recently-shown kicker, keeps identity while suspended (presentation, pairing notice), never returns null while any valid template exists (the solar template always exists). Localisation: templates in both hr and en in `kiosk.sentence.*` (step 9) with `fill()`. Kicker words `kiosk.sentence.kicker.{promet,kultura,vrijeme,bicikli,nocas,radovi}` = 'Promet','Kultura','Vrijeme','Bicikli','Noćas','Radovi' (en 'Transit','Culture','Weather','Bikes','Tonight','Works'). Tests (test/app/kiosk-sentence.test.ts, replacing test/app/ticker.test.ts which is deleted): every template ≤ 80 chars for the fixture; a 90-char fill is dropped, not truncated; acceptSentence refuses '…', '...', a newline, markdown, a temperature not in the facts, and accepts a model line that shares 'zatvorena' and '18:00'; sequence holds 20 s, never repeats a text within 10 min across 30 reads, drops the last-tram sentence at 23:52:01, keeps identity while suspended, skips an overflowed sentence; nocas kicker only 20:00–05:00; outage sentence never contains 'nedostupn'.

### 5. Sentence writer, Worker side: POST /api/kiosk/sentences (Workers AI, KV cache, rate limit, test no-op) + client seam  `[M]`

Files: `worker/feed/sentences.ts`, `worker/routes/kiosk.ts`, `worker/index.ts`, `app/src/api.ts`, `test/feed/sentences.test.ts`, `test/feed/routes.workers.test.ts`

worker/feed/sentences.ts (model on worker/feed/brief.ts): `SENTENCE_MODEL = BRIEF_MODEL_AKT` ('@cf/meta/llama-3.3-70b-instruct-fp8-fast' — the owner named the small model's wording as the failure [O-13]; one call per fact-set per 20 min is a rounding error), `SENTENCE_KEY_PREFIX = 'sentence:v1:'`, `SENTENCE_TTL_SECONDS = 1200`, `SENTENCE_NEGATIVE_TTL_SECONDS = 300`, `SENTENCE_TIMEOUT_MS = 6000`, `SENTENCE_MAX_FACTS = 16`, `SENTENCE_MAX_FACT_CHARS = 160`, `SENTENCE_MAX_OUT = 8`. `SENTENCE_SYSTEM_PROMPT_HR` = 'Ti si urednik javnog gradskog zaslona u Zagrebu koji ljudi čitaju s tri metra. Iz popisa činjenica napiši do 8 različitih rečenica na hrvatskom, svaku najviše {budget} znakova, svaku o jednoj ili dvije činjenice iz popisa. Samo činjenice s popisa: ne dodaj brojeve, mjesta ni razloge kojih nema. Bez uvoda, bez navodnika, bez trotočja, bez markdowna, bez riječi „nedostupno”. Svaki redak: <id činjenice>|<rečenica>.'; EN twin. `writeSentences(env, req: SentenceRequest, waitUntil?)`: no `env.AI` or `isTestEnvironment(env)` ⇒ `[]` (tests and E2E never bill AI); key = sha256 over locale + budget + model + facts (ids and texts, in order); KV hit ⇒ parse; miss ⇒ one `ai.run` with `max_tokens: 400, temperature: 0.3`; parse lines `id|text`, run the SAME `acceptSentence` (import from app/src/kiosk/sentence.ts through a shared module `shared/kiosk/sentence-accept.ts` so worker and app agree — move acceptSentence there in step 4), validUntil = min over refs' validUntil; store `{ sentences }` (or `{ sentences: [] }` with the negative TTL when everything was refused). worker/routes/kiosk.ts `handleKiosk(request, env, ctx, url)`: `POST /api/kiosk/sentences` only (405 otherwise), same-origin (`Origin`/`Sec-Fetch-Site` same-origin or absent as screens.ts does), `env.RL_OPEN.limit({ key: 'kiosk-sentences:' + clientIp(request) })` ⇒ 429, body ≤ 8 KB, validate `{ locale: 'hr'|'en', budget: 40..80, facts: SentenceFact[] ≤ 16, each text ≤ 160, kind in the six }` ⇒ 400 `{error:'bad-request'}`; reply `{ generatedAt, sentences }` with `cache-control: private, no-store`. Register `handleKiosk` in worker/index.ts ROUTES (:39) after handleScreens. app/src/api.ts: `export async function fetchSentences(req: SentenceRequest, fetchImpl = fetch): Promise<WrittenSentence[]>` (POST JSON; any non-200 or network error ⇒ [] — the templates cover it). Tests: test/feed/sentences.test.ts mirrors test/feed/brief.test.ts (calls the model once on a miss, serves the second call from KV, refuses over-budget/ellipsis/invented-number lines, drops everything under APP_ENV=test, times out at 6 s); test/feed/routes.workers.test.ts adds 405 on GET, 400 on 17 facts, 200 with `sentences: []` in the test environment.

### 6. Invitation recomposition: aside = timeline + QR card; legend without '?'; outage note on the map; card without benefit line and copy button; strings out of TS  `[M]`

Files: `app/src/kiosk/invitation.ts`, `app/src/kiosk/markup.ts`, `app/src/ui/kiosk.css`, `app/src/ui/kiosk-city.css`

Rewrite app/src/kiosk/invitation.ts (98 lines): imports drop `weatherPanel, panelMarkup, FrontRow` (front.ts stays alive for paired.ts), `BoardSubject`, `KioskHighlight`; add `mountTimeline`, `NearbyItem`, `iconMarkup`. `export interface InvitationModel { items: readonly NearbyItem[]; radiusM: number; outage: boolean; stop: ScreenStop | null; modules: readonly ModuleSnapshot[]; now: number; composition: Composition }` (`lastRun`, `prometRows`, `prometBoard`, `city` leave the model; `FRONT_PANEL_IDS` is deleted). `InvitationHandle`: drop `highlight()`; keep `update`, `measureWidth/Height`, `setMajorLabels`, `fit`, `destroy`. Markup: `<p class="k-handheld-info">${e(s.handheld.info)}</p><div class="k-geography"></div><aside class="k-overview"><div class="k-nearby-host"></div><div class="k-panel--card">${cardMarkup(s, codeBase)}</div></aside>`; geography = mountField(...) + legend `<p class="k-map-legend"><span><b class="k-legend-tram">6</b> ${s.legend.tram}</span><span><b class="k-legend-bike">●</b> ${s.legend.bikes}</span><span><b class="k-legend-events">●</b> ${s.legend.events}</span></p>` appended ONLY when `!lightweight` (today it is appended under a field with no map); plus `<p class="k-map-note" data-testid="kiosk-map-note" hidden>${e(s.nearby.outageNote)}</p>` inside `.k-geography` (CSS: absolute bottom-left over the map, `--k-hint-size`, `--k-surface` plate at 0.92 opacity, radius-s; the one thing besides MapLibre's attribution allowed on the picture, §4.8). `cardMarkup`: `<article class="k-invite" data-testid="kiosk-invite"><div class="k-invite-side"><h1 class="k-lead">${e(s.invitation.lead)}</h1>${hintMarkup(s, codeBase)}${codeBlockMarkup(s)}</div><div class="k-qr" data-testid="kiosk-qr">…</div></article>` — the `k-invite-benefit` paragraph goes (slop #19: lead + code + address). markup.ts `codeBlockMarkup` (:50-60): remove the `pair-copy` button (:54) and `pair-copy-status` (:56); `.k-code-row` becomes one column. QR floor (T4): kiosk-city.css:27 `--k-qr: max(264px, calc(264px * var(--k-sign-zoom)))` and kiosk.css `.k-qr .qr{padding:12px}` so the SVG itself is ≥ 240 px; handheld keeps 264. update(model): `field.update(...)`; `timeline.update(model.items, model.radiusM)`; `note.hidden = !model.outage`; `fit()` = `timeline.update` re-run when `measureHeight()` changed since the last paint (fonts arriving, resize). `mountTimeline(host, { strings, locale, reduced: reducedMotion||lightweight, designHeightPx: NEARBY_DESIGN_HEIGHT[composition] })` with `NEARBY_DESIGN_HEIGHT = { wide: 520, compact: 330, portrait: 560, handheld: Infinity }` exported from invitation.ts (measured boxes replace it after the first layout, as FIELD_DESIGN_WIDTH does). InvitationDeps gains `reducedMotion: boolean`. Handheld order (kiosk-city.css:42-52): info, card, geography, nearby. kiosk.css: delete the invitation-only weather rules that no longer apply on the wall? NO — `.k-panel[data-panel='weather']` (:374-393) is still drawn by paired renderSada; leave. Delete `.k-code-copy*` (:429-433) and `.k-code-row` two-column grid (:428 ⇒ `grid-template-columns:minmax(0,1fr)`).

### 7. Controller: header (sentence in the middle, no gear/theme glyphs, long-press settings), paintWall (items → timeline, sentence sequence, map highlight from the sentence's refs), sentence refresh, outage plumbing, alert policy, code painter without copy  `[L]`

Files: `app/src/kiosk.ts`, `app/src/kiosk/mapview.ts`, `app/src/kiosk/settings.ts`

app/src/kiosk.ts edits by current line: (a) :67 delete `TICKER_SWAP_MS`; add `export const LONG_PRESS_MS = 1500` and `export const SENTENCE_REFRESH_MS` re-export from sentence.ts. (b) :131 delete `THEME_ICON`. (c) shellMarkup :136-142: header becomes `<header class="k-head"><div class="k-head-brand" data-testid="kiosk-brand"><p class="k-brand">Kaj ima?</p><p class="k-context" data-testid="kiosk-context"></p><button type="button" class="k-visually-hidden" data-testid="kiosk-settings" aria-label="${s.settings.open}" hidden></button></div><div class="k-head-mid" data-testid="kiosk-head-mid"><p class="k-ticker" data-testid="kiosk-sentence" hidden><span class="k-ticker-kicker" data-testid="kiosk-sentence-kicker"></span><span class="k-ticker-text" data-testid="kiosk-sentence-text"></span></p></div><div class="k-head-when"><p class="k-date" data-testid="kiosk-date"></p><time class="k-clock" data-testid="kiosk-clock"></time></div></header>` — the theme glyph button is gone; the settings button stays in the DOM, visually hidden but focusable and named (a11y path), and is `hidden` outside the invitation exactly as today (:370). (d) :249-255 delete the `pause-highlights` click handler; add long press on `.k-head-brand`: pointerdown starts `oneShot(openSettings, LONG_PRESS_MS)`, pointerup/pointercancel/pointerleave or a move > 12 px clears it; ignored when `phase !== 'invitation'` or `presentation?.target`. (e) state :236-242: replace `highlightSequence/highlightsPaused/ambient/highlightItems/highlightDataKey/highlightModules/highlightCity` with `const sentenceSequence = createSentenceSequence(); let items: NearbyItem[] = []; let itemsKey = ''; let facts: SentenceFact[] = []; let factsKey = ''; let modelSentences: WrittenSentence[] = []; let sentencesFetchedAt = -Infinity; let sentenceFetchSeq = 0; let currentSentence: WrittenSentence | null = null`. (f) `paintTicker` :307-329 becomes `paintWall()`: keep the camera/bounds computation (:310-316) for the radius filter; `const dataKey = JSON.stringify([Math.floor(now()/60_000), stop?.id, boardTimes, bounds, lastRun?.status, outage()])`; when it or `teaser`/`cityStore.snapshot()` changed: `items = nearbyItems({ place: stop ?? null (WP2 supplies an address place; until then a whole-city screen has no departures), stop, stops, arrivals: stop ? arrivalsAtStop(platformIds(stop, stops)) : undefined, modules: teaser, city, lastRun, now: now(), radiusM: NEARBY_RADIUS_M, strings: s, i18n, locale, bounds })`; `facts = sentenceFacts({...})`; `invitation?.update(invitationModel())`. Then `const suspended = phase !== 'invitation' || Boolean(presentation?.target) || headMid.getAttribute('role') === 'status'`; `const pool = [...modelSentences, ...templateSentences(facts, SENTENCE_BUDGET[composition])]`; `currentSentence = sentenceSequence.read(pool, now(), suspended, (sentence) => overflows(sentence))` where `overflows` writes the text into the (hidden) `k-ticker-text` and compares `scrollWidth > clientWidth` (0/0 in happy-dom ⇒ false); `paintSentence()` sets kicker `data-kicker` + word from `s.sentence.kicker[kind]`, text, `hidden = suspended || !currentSentence`, and crossfades with `data-swap` only when the text changed (the `.k-ticker[data-swap]` 220 ms rule at kiosk.css:224 already exists; instant under reduced motion/lagano); `mapAdapter.handle()?.setHighlight?.(suspended ? null : items.find(i => currentSentence?.refs.includes(i.key))?.map ?? null)` — the map ring follows the sentence, never a camera move. (g) `ensureSentences()` called from `paintLocal` and on stop change: `const key = JSON.stringify(facts.filter(f => f.kind !== 'promet' || !/za \d+ min| u \d\d:\d\d\.$/.test(f.text)).map(f => [f.id, f.text]))` (departure countdown facts stay client-side); if `key !== factsKey || now() - sentencesFetchedAt >= SENTENCE_REFRESH_MS` ⇒ `fetchSentences({ locale, budget: SENTENCE_BUDGET[composition], facts: stableFacts.slice(0, 16) })` (dep `deps.fetchSentences ?? fetchSentencesImpl`; tests fake it) ⇒ on resolve with the latest seq set `modelSentences`, `paintWall()`; never in lightweight (templates only). (h) `paintTheme` :339-346 reduces to `settings?.paint()`; `cycleTheme` stays for the settings panel; :1222-1223 drop `themeBtn` wiring, keep `settingsBtn.addEventListener('click', openSettings)`. (i) `ensureLastRun` :580-602 stays (it finally has a reader); `configuredBoard` :660-667 deleted; `invitationModel()` :669-672 ⇒ `{ items, radiusM: NEARBY_RADIUS_M, outage: outage(), stop, modules: teaser, now: now(), composition }` with `outage = () => feedStateOf(byModule(teaser)['zet-rt']) === 'down'`. (j) `paintLocal` :688-700: `ensureLastRun(); paintWall(); paintStrip(); paintMap(); …; ensureSentences()`; `paintMap` :514-534 passes `vehiclesVisible: !outage()` to `requestKioskMap` (mapview.ts `KioskMapInput` gains `vehiclesVisible?: boolean`; in `requestKioskMap` (:806-952) the modes passed at ~:939 become `new Set()` when `vehiclesVisible === false`, so a down feed shows the map without vehicles instead of frozen ghosts; `setFeedState('down')` still stops the loop). (k) `paintCode` :711-754 drop the copy (:720-728), `corner`/`joinCode` (:718-719, 735-736, 750-752 — dead testids); delete the stage click handler :755-780. (l) `loadTeaser` catch :1190-1200: keep the stale/down bookkeeping, drop `showAlert(s.status.dataDown, 'teaser')` while `phase === 'invitation'` (the map note + grey rows + sentence say it; principle 9); the beacon alerts (:977-979) stay. (m) `startBeacon.onStatus` :980: `cityStore.ensure(['heritage','streets','settlements'])`; and after `adoptCredentials` when `stop` exists call `void ensureStops()` so `tramTo` and `platformIds` have the catalogue. (n) codeTimer :1259-1279: `paintTicker()` ⇒ `paintWall()`; the minute branch keeps `invitation.update(invitationModel())`. (o) `setPhase` :854 passes `reducedMotion` into `mountInvitation`. (p) `pairedContext` unchanged (paired compositions untouched). settings.ts: no structural change in WP1 (Mjesto/Kadar/Tema/Ritam click-toggles are WP2); only the opener moved.

### 8. Footer: sources without a time, green cross · 24/7 · address; cross icon  `[S]`

Files: `app/src/kiosk/frame.ts`, `app/src/ui/icons.ts`, `app/src/ui/kiosk.css`, `app/src/kiosk/strings.ts`, `test/app/kiosk-frame.test.ts`, `test/app/contrast.test.ts`

frame.ts `stripMarkup` (:49-66): `const sources = strings.safety.sources` (drop `confirmedAt` and `strings.safety.confirmed` — no fetch/confirmation times on the wall, principle 5; `FrameStrip.confirmedAt` may stay for the paired strip but the invitation never prints it — simpler: remove it and the `confirmed` key); pharmacy cell ⇒ `<span class="k-strip-item k-strip-pharmacy" data-testid="strip-pharmacy" aria-label="${strings.safety.pharmacy}: ${label}">${iconMarkup('cross', undefined, 'icon k-icon k-cross')}<span class="k-247">24/7</span> <strong>${escapeHtml(strip.parts.pharmacy.label)}</strong></span>` (label = the City's short address, e.g. 'Trg bana J. Jelačića 3'); keep shield label, verdict button (Osnovno, unchanged), trail, `/hitno` pill. icons.ts: add `'cross'` to ICON_NAMES and `cross: '<path d="M9 3h6v6h6v6h-6v6H9v-6H3V9h6z"/>'` (a Greek cross; base.css `.icon` strokes it, `.k-cross{color:var(--k-green);fill:currentColor}` fills it). kiosk.css: `.k-cross{color:var(--k-green)} .k-247{font-weight:800;letter-spacing:.02em;color:var(--k-green);margin:0 .35em}`. strings.ts: remove `confirmed` from `KioskStrings.safety` (:185) and the build list (:387). Tests: kiosk-frame.test.ts:62-83 ⇒ trail equals 'DHMZ · EMSC' with no /\d\d:\d\d/, `strip-pharmacy` text matches /24\/7/ and contains 'Trg bana J. Jelačića 3', contains an `svg.k-cross`; :85-93 unchanged. contrast.test.ts: add the pairs (`--tone-live` on `--tone-surface-1`, `--tone-action-brand` on `--tone-surface-1` for the live time, `--tone-text-muted` on `--tone-surface-1` for the timetable time) in both faces.

### 9. Copy: hr.json/en.json keys and the typed KioskStrings tree  `[S]`

Files: `app/src/i18n/hr.json`, `app/src/i18n/en.json`, `app/src/kiosk/strings.ts`, `app/src/city/strings.ts`, `test/app/i18n.test.ts`, `test/app/copy-guards.test.ts`

hr.json under `kiosk` (en.json in parity, the i18n test enforces identical key sets): ADD `nearby: { title: 'U blizini', pill: '{km} km · ~{min} min', always: 'uvijek', until: 'do', sunrise: 'Izlazak sunca', sunset: 'Zalazak sunca', lastTrams: 'Zadnji tramvaji', firstTram: 'Prvi tramvaj', outageNote: 'ZET trenutačno ne šalje položaje vozila; polasci su po voznom redu.' }` (en: 'Nearby', '{km} km · ~{min} min', 'always', 'until', 'Sunrise', 'Sunset', 'Last trams', 'First tram', 'ZET is not sending vehicle positions right now; departures follow the timetable.'); `sentence: { kicker: { promet: 'Promet', kultura: 'Kultura', vrijeme: 'Vrijeme', bicikli: 'Bicikli', nocas: 'Noćas', radovi: 'Radovi' }, departureIn: 'Tramvaj {route} prema {to} za {n} min.', departureAt: 'Tramvaj {route} prema {to} u {time}.', busIn: 'Autobus {route} prema {to} za {n} min.', busAt: 'Autobus {route} prema {to} u {time}.', closureUntil: '{street} zatvorena do {until}.', weather: '{temp}, {condition}; danas do {max} °C.', weatherNoRange: '{temp}, {condition}.', bikes: 'BAJS {station}: {bikes}.', sunset: 'Sunce zalazi u {time}.', sunrise: 'Sunce izlazi u {time}.', lastTram: 'Zadnji tramvaj {route} večeras u {time}.', firstTram: 'Prvi tramvaj {route} ujutro u {time}.', event: '{time} {title}, {venue}.', pharmacy: 'Dežurna ljekarna 24/7: {address}.', always: '{name}: {text}', outage: 'ZET ne šalje položaje vozila; polasci su po voznom redu.' }` (en twins; the model prompt lives in the worker, not here); `legend: { tram: 'Tramvajska linija', bikes: 'BAJS · broj bicikala', events: 'Događanja' }`; `handheld: { info: 'Za javni zaslon otvori /kiosk/ na tom uređaju i odaberi Pokreni zaslon. Ovaj kod otvara osobnu sesiju; skeniranje ne mijenja javni prikaz.' }` (moved verbatim from invitation.ts:45). REMOVE `kiosk.ticker.*` (:964-970), `kiosk.invite.support`, `kiosk.invite.codeWaiting`, `kiosk.safety.confirmed`. Owner-decided strings that must stay byte-exact: 'U blizini', '2 km · ~15 min' (rendered from the pill template), 'uvijek', 'Skeniraj za 10 minuta grada.', '24/7', 'sutra', 'za {n} min' (arrivals.inMinutes), the six kicker words. Everything else under `kiosk.nearby.*`/`kiosk.sentence.*` is a proposal for the strike-through round and is deliberately kept in one place. strings.ts: `KioskStrings` gains `nearby`, `sentence: { kicker: Record<SentenceKicker,string>; … templates }`, `legend`, `handheld`; drops `ticker` (:136-144, :379), `invitation.support`/`codeWaiting` (:33-40, :356), `safety.confirmed`. app/src/city/strings.ts: delete `quiet` (only highlights.ts read it) from hr and en objects. copy-guards.test.ts: add a rule that no `kiosk.*` value contains '…' or 'nedostupn' except `kiosk.safety.warningsUnknown`/`closuresUnknown` (footer honesty) and `kiosk.weather.unavailable`/`kiosk.lines.unavailable` (paired).

### 10. Delete the dead modules and their tests; prune orphans  `[S]`

Files: `app/src/kiosk/highlights.ts`, `app/src/kiosk/ticker.ts`, `test/app/ticker.test.ts`, `app/src/kiosk.ts`, `app/src/kiosk/front.ts`, `app/src/ui/kiosk.css`

Delete app/src/kiosk/highlights.ts (rotation, register fillers, 'Upoznaj ovo mjesto', disclaimer captions) and app/src/kiosk/ticker.ts (no production import; TICKER_PERIOD_MS is imported only by test/app/kiosk.test.ts:20 — remove that import) and test/app/ticker.test.ts. kiosk.ts: remove the import at :51 and every `highlight*` symbol (step 7). front.ts stays (paired renderSada/renderPromet read `frontPanels`, `weatherPanel`, `prometPanel`, `exceptionRows`, `eventCardRows`); only drop the header comment claim 'kiosk/invitation.ts mounts the panels' (:5-6) and the `prometRows/prometBoard` wiring stays for paired. kiosk.css: `.k-ticker*` (:216-225) is REUSED by the sentence — change `.k-ticker-text` `text-overflow: ellipsis` ⇒ `text-overflow: clip` (principle 4: never an ellipsis; the sequencer skips what overflows) and add `.k-ticker-kicker[data-kicker='kultura']{color:var(--k-violet)} [data-kicker='vrijeme']{color:var(--k-amber)} [data-kicker='bicikli']{color:var(--tone-bike)} [data-kicker='radovi']{color:var(--k-amber)} [data-kicker='nocas']{color:var(--k-ink-2)}` (promet keeps `--k-action`). Verify with `grep -rn "kiosk/ticker\|kiosk/highlights\|pause-highlights\|k-highlight\|kiosk\.ticker\.\|pair-copy\|corner-qr\|join-code" app/src test e2e scripts` ⇒ only paired.ts's own `.k-join*` classes remain.

### 11. Unit tests: rewrite the assertions that pin the old aside, header and chrome  `[M]`

Files: `test/app/kiosk.test.ts`, `test/app/kiosk-css.test.ts`, `test/app/kiosk-frame.test.ts`, `test/app/kiosk-local.test.ts`, `test/app/lastrun.test.ts`, `test/app/i18n.test.ts`, `test/app/contrast.test.ts`

test/app/kiosk.test.ts: mount() gains `fetchSentences: opts.fetchSentences ?? vi.fn(async () => [])` and passes `createBoards` a fixture cache when a test needs departures (use `createBoardCache({ fetchImpl })` answering a DepartureBoard for '106_1'/'106_2' with 5 future ZET rows so ≤ 3 rows can be asserted). :165-187 'passive public city' ⇒ 'the wall: no touch discovery, rows keep their nodes across 20 s, the sentence turns, presentation suspends both': no `[data-action=pause-highlights]`; capture `li` node refs, tick 20 s, same refs, `kiosk-sentence-text` changed, `data-enter` absent on old rows; presentation ⇒ `kiosk-nearby` null and `city-detail` shows 'Gavella'. :189-… 'clipboard copy' describe deleted. :427-449 gear assertions ⇒ `kiosk-settings` has class `k-visually-hidden`, `hidden` before a screen exists and not after; new test: pointerdown on `[data-testid=kiosk-brand]` + firing the LONG_PRESS_MS one-shot opens `kiosk-settings-panel`; pointerup before it does not. :709-764 ⇒ aside children `['k-nearby-host'...]` — assert `[...column.children].map(className)` = ['k-nearby-host','k-panel--card'] and `q(column,'.k-nearby')` present; remove :746-748 and :755-756 (weather); :742-745 ⇒ a `li[data-key^="closure:"]` row contains 'Ilica' and no row contains 'Dohvaćeno'; :753 ⇒ `kiosk-sentence` visible with a `data-kicker` and text ≤ 80 chars; :757-760 unchanged; add 'the invitation has no interactive control except the QR': `front.querySelectorAll('button, a:not([data-testid=pair-url])').length === 0`. :789-813 ⇒ timers unchanged; the sentence changes after 20 s while `[data-testid=kiosk-nearby-rows]`.innerHTML is unchanged and no `kiosk-ticker` testid exists. :814-833 ⇒ during the pairing notice `kiosk-sentence` is hidden; after 4.5 s it is back with a text. :1128-1139 cold screen ⇒ `kiosk-nearby-rows` has 0 li (offline boards), `kiosk-sentence-text` equals the solar template for NOW ('Sunce zalazi u 19:1x.' — compute via sunTimes in the test), no 'Učitavanje'/'Čekamo' in the invitation. :1160-1184 stale teaser ⇒ the 'Ilica' row stays while stale (rows never say 'zastarjelo'); `kiosk-alert` stays hidden for a teaser failure in the invitation; the map still hears 'stale'. :1185-1196 all down ⇒ `kiosk-map-note` not hidden, `.k-nearby-time[data-live='true']` count 0, header text does not match /nedostupn/, `kiosk-alert` hidden. :1290-1303 ⇒ beacon 'offline' alert still shows; the teaser failure alone leaves the alert hidden. :1324-1330 EN ⇒ `kiosk-sentence-kicker` is one of Transit/Culture/Weather/Bikes/Tonight/Works and `.k-map-legend` contains 'Tram route'. Any `kiosk-panel-weather` assertion inside an invitation-phase test goes; the paired ones (:918-937, :965) stay. test/app/kiosk-local.test.ts: unchanged except `stories`/`lastDeparturesAhead` stay (paired). test/app/i18n.test.ts parity passes once both JSONs change together. Run: `npx vitest run --project unit`.

### 12. e2e and the ten-minute rotation instrument  `[M]`

Files: `e2e/kiosk-layout.spec.ts`, `e2e/readable-city.spec.ts`, `e2e/a11y.spec.ts`, `e2e/redesign.spec.ts`, `e2e/experience-fixtures.ts`, `scripts/kiosk-rotation.mjs`

e2e/kiosk-layout.spec.ts:67-77 (asserts `kiosk-panel-promet` on the invitation — dead path #24) ⇒ under the 'stale' fixture: `[data-testid=kiosk-nearby] li` count ≥ 1 and ≤ 3 for `data-kind=departure`, every `.k-nearby-time[data-live=true]` count 0 when the fixture carries no vehicles, no row text matches /zastarjelo|Dohvaćeno/; the Osnovno round trip stays. e2e/readable-city.spec.ts:116-150 ⇒ `check()` measures `.k-nearby` (no vertical overflow of `.k-nearby` and `.k-nearby-rows`; rows × rowPx ≤ list height; `k-nearby-rows` has no `hidden` li), `safe` (list bottom ≤ card top, card bottom ≤ footer top), `qr` = width of `[data-testid=kiosk-qr] svg` ≥ 240, `sentence` = `kiosk-sentence-text` textContent length ≤ 80 and `scrollWidth <= clientWidth` and no '…'; the seven 20 s cycles collect distinct sentence texts (≥ 3 distinct over 140 s with the fixture) and assert no verbatim repeat within the window; drop the pause clicks (:141-145); outage (:146-150) ⇒ `kiosk-map-note` visible, `.k-nearby-time[data-live=true]` count 0, `.k-head` text !~ /nedostupn/, `kiosk-alert` hidden, `kiosk-map` `data-feed=down`. e2e/a11y.spec.ts:235-242 ⇒ keep `kiosk-essentials-open` visible; replace the 'Zaustavi izmjenu' button expectation with `page.getByRole('button', { name: 'Postavke zaslona' })` attached (hidden visually, named) and `page.locator('[data-testid=kiosk-invitation] button')` count 0; keep the legend text. e2e/redesign.spec.ts:81-90 ⇒ iterate `.k-nearby, .k-panel--card` for overflow and add `if (document.querySelectorAll('.k-nearby-row[hidden]').length) out.push('hidden rows')`; the `.k-panel[data-panel]` loop may stay (no-op). experience-fixtures.ts `installKioskFeedFixture(page, state)`: also route `**/api/city/departures**` to a fixture board with 6 future rows for FIXTURE_STOP (so departures exist locally) and `**/api/kiosk/sentences` ⇒ `{sentences:[]}`. NEW scripts/kiosk-rotation.mjs (moved out of review.local/companion/walkthroughs/1720-mon/lib.mjs `kioskSample/startKioskLog/summariseRotation` and adapted): args `--url` (default http://127.0.0.1:8787, refuses non-local unless `--production` AND `review.local/companion/screen.json` exists — production only through the documented reuse of our own screen, never a new one), `--minutes 10`, `--at 2026-09-22T04:30+02:00` (installs `page.clock` at that instant against the local server, fixtures via experience-fixtures), `--out review.local/companion/rotation/<slot>.jsonl`; installs in-page a MutationObserver on `[data-testid=kiosk-invitation]` counting mutations and a WeakMap-based node id on each `.k-nearby-row`; samples every 2 s: sentence {kicker,text,length,overflow: scrollWidth>clientWidth,hasEllipsis}, rows [{key,kind,live,time,title,sub,nodeId}], mapNote hidden?, strip text, qr svg size, header text, `data-feed`, mutations since last sample; summary JSON: `departures: {min,max}`, `sentences: {distinct, over80, ellipsis, overflow, verbatimRepeatWithin10min, expiredShown (validUntil read from a `data-valid-until` attribute the painter writes)}`, `rows: {caveat: count of rows matching /registra|nije provjera|Obuhvat|Dohvaćeno|zastarjelo|nepotvrđeno/, nodeReuse: share of (sample_i key ∩ sample_{i+1} key) pairs with identical nodeId, solarPerSample max, lastTramAfterLeft: rows kind last whose min time < sample time}`, `mutationsPerMinute`, `contentChangesPerMinute` (distinct row-key set or sentence text changes), `hiddenRows`. Exit code 1 when any threshold in the acceptance list fails so it can run in CI against the local server.

### 13. Documents: supersede the paragraphs the new wall replaces  `[S]`

Files: `PRODUCT.md`, `DESIGN.md`, `docs/kiosk.md`, `app/src/kiosk/INTEGRATION.md`

Add a dated approval paragraph (22 September 2026) at the top of the approval history in PRODUCT.md and the Public screen section of DESIGN.md that states the new wall contract in the brief's words (§11 wall, §4 ladder, principles 1–9) and names what it supersedes; in docs/kiosk.md replace the 20. 9. 'Pasivni pregled' block (:3-24), the gear sentences (:41-57), the right-column paragraph (:111-133), the ticker paragraph (:135-142), the strip sentence (:144-146) and the 'Rečenice u traci zaglavlja' bullet (:226-231) with the new composition (header sentence written from the wall's own facts by Workers AI with an 80-character budget and a template fallback; U blizini; QR card; footer; long-press settings; outage). INTEGRATION.md: rewrite §Header (:139-155) and the front-page lines (:88-96). No change to docs/prijava or app/prijava.

## Parallel lanes

Four agents can start at once on non-overlapping files, then one integrator, then one verifier. A: steps 1+2 (scripts/gtfs-lastrun.mjs, core/lastrun.ts, data, kiosk/nearby.ts, its tests). B: step 3 (kiosk/timeline.ts, the .k-nearby CSS in kiosk-city.css/kiosk.css, kiosk-timeline.test.ts, kiosk-css.test.ts) — B codes against the `NearbyItem` interface fixed in step 2's text and a local stub. C: steps 4+5 (kiosk/sentence.ts, shared/kiosk/sentence-accept.ts, worker/feed/sentences.ts, worker/routes/kiosk.ts, worker/index.ts registration, app/src/api.ts, their tests; deletes test/app/ticker.test.ts). D: steps 8+9+10 (frame.ts, icons.ts, strings.ts, hr/en.json, city/strings.ts, deletion of highlights.ts/ticker.ts, the .k-ticker CSS tweak, copy-guards/contrast/i18n tests) — D must land its hr/en/KioskStrings changes before E compiles. Sequential after A–D: E = steps 6+7+11 (invitation.ts, markup.ts, kiosk.ts, mapview.ts vehiclesVisible, kiosk.test.ts rewrite). Then F = steps 12+13 (e2e, scripts/kiosk-rotation.mjs, docs) — F can draft the rotation script and the doc paragraphs in parallel with E and finish once E is green. Contract for parallel work: file ownership above is exclusive; the interfaces named in steps 2, 4 and 6 (NearbyItem, rowBudget, SentenceFact, WrittenSentence, SentenceRequest/Response, InvitationModel, TimelineHandle) are frozen by this plan.

## Tests to update

- test/app/kiosk.test.ts:20 (drop TICKER_PERIOD_MS import); :165-187 rewrite (no pause button; row node identity; sentence turns; presentation suspends); :189-215 'clipboard copy' delete; :427-449 gear ⇒ visually-hidden settings button + long-press test; :709-764 aside children ['k-nearby-host','k-panel--card'], remove weather (:746-748, :755-756) and highlight (:742-745) assertions, add closure row + sentence + 'no interactive control except QR'; :789-813 sentence changes at 20 s while rows' innerHTML is unchanged; :814-833 sentence hidden during the pairing notice; :1128-1139 cold screen ⇒ 0 rows, solar template sentence, no 'Čekamo'; :1160-1184 stale ⇒ Ilica row stays, no teaser alert; :1185-1196 down ⇒ map note visible, no live time, header !~ /nedostupn/; :1290-1303 alert policy; :1324-1330 EN kicker words; any invitation-phase `kiosk-panel-weather` assertion removed (paired ones stay)
- test/app/ticker.test.ts — delete (replaced by test/app/kiosk-sentence.test.ts)
- test/app/kiosk-css.test.ts:28-39 aside grid ⇒ 'grid-template-rows:minmax(0,1fr) auto', assert `.k-nearby-rows` grid-auto-rows var and no `.k-highlight` in either sheet; :63-73 `.k-ticker-text` must contain 'text-overflow: clip' (not ellipsis), keep the 220 ms rule and the reduced-motion `animation: none`; :74-80 QR floor ⇒ 264 px plate with 12 px padding (SVG ≥ 240); :101-104 'never hides useful rows' ⇒ also assert timeline.ts has no `.hidden = true`
- test/app/kiosk-frame.test.ts:62-83 trail 'DHMZ · EMSC' without a time; pharmacy cell '24/7' + 'Trg bana J. Jelačića 3' + svg.k-cross
- test/app/lastrun.test.ts:147-170 committed artefact shape accepts `first`; add firstDeparture cases
- test/app/i18n.test.ts — parity holds once hr/en change together (new kiosk.nearby/sentence/legend/handheld, removed kiosk.ticker/invite.support/invite.codeWaiting/safety.confirmed)
- test/app/copy-guards.test.ts — add the '…'/'nedostupn' rule for kiosk.* values (with the four footer/paired exceptions)
- test/app/contrast.test.ts — add live-time blue, timetable grey and green-cross pairs on surface-1 in both faces
- test/feed/routes.workers.test.ts — add POST /api/kiosk/sentences 405/400/200-empty cases
- e2e/kiosk-layout.spec.ts:67-77 stale test ⇒ timeline assertions (1–3 departure rows, no live time, no 'zastarjelo')
- e2e/readable-city.spec.ts:116-150 ⇒ nearby/sentence/qr-svg checks, distinct sentences over 7 cycles, outage note + grey rows + no 'nedostupn'; drop pause clicks
- e2e/a11y.spec.ts:235-242 ⇒ no 'Zaustavi izmjenu'; named hidden settings button; zero buttons inside the invitation
- e2e/redesign.spec.ts:81-90 ⇒ include .k-nearby in the overflow/hidden-row sweep
- e2e/experience-fixtures.ts installKioskFeedFixture ⇒ also stub /api/city/departures and /api/kiosk/sentences
- New: test/app/kiosk-nearby.test.ts, test/app/kiosk-timeline.test.ts, test/app/kiosk-sentence.test.ts, test/feed/sentences.test.ts

## Acceptance (executable)

| # | Criterion | How (command / threshold) |
|---|---|---|
| A1 | Typecheck and the whole unit suite pass with the old modules gone | `npm run typecheck` exit 0 && `npx vitest run` 0 failures && `grep -rn "kiosk/ticker\\|kiosk/highlights\\|pause-highlights\\|k-highlight\\|kiosk\.ticker\.\\|pair-copy\\|Upoznaj ovo mjesto" app/src test e2e scripts \| wc -l` = 0 |
| A2 | Aside composition: timeline + QR card only, no interactive control except the QR, no disclaimer/freshness unit | `npx vitest run test/app/kiosk.test.ts -t "invitation"` passes: aside children = ['k-nearby-host','k-panel--card']; `front.querySelectorAll('button, a:not([data-testid=pair-url])').length === 0`; no row or card text matches /registra\|nije provjera\|Obuhvat\|Dohvaćeno\|zastarjelo\|nepotvrđeno\|Čekamo/ |
| A3 | Bounded departures with the two-colour rule, whole rows only | `node scripts/kiosk-rotation.mjs --minutes 10 --at <slot>` for slots 04:30, 07:45, 12:30, 17:45, 21:30 (local server, fixtures): summary.departures.min ≥ 1 && .max ≤ 3; summary.hiddenRows === 0; every `.k-nearby-time[data-live=true]` text matches /^(sada\|za \d+ min\|\d\d:\d\d)$/ and every non-live time matches /^(\d\d:\d\d\|do .+\|uvijek)$/; solarPerSample ≤ 1; lastTramAfterLeft === 0 |
| A4 | Header sentence: ≤ 80 characters, never an ellipsis, never overflowing at 1920, 20 s cadence, no verbatim repeat within ten minutes, time-aware | rotation summary at 1920×1080: sentences.over80 === 0, sentences.ellipsis === 0, sentences.overflow === 0, sentences.verbatimRepeatWithin10min === 0, sentences.expiredShown === 0, sentences.distinct ≥ 6 over 10 min with the fixture; `npx vitest run test/app/kiosk-sentence.test.ts` passes (hold 20 s, last-tram sentence dropped at 23:52:01, budget drop not cut) |
| A5 | Calm motion: rows keep their DOM nodes; nothing re-animates without a change | rotation summary: rows.nodeReuse === 1.0; mutationsPerMinute ≤ contentChangesPerMinute + 5; `npx vitest run test/app/kiosk-timeline.test.ts` passes the node-identity and data-enter cases |
| A6 | Rows grow when fewer: 64–92 px from the item count | `npx vitest run test/app/kiosk-nearby.test.ts -t rowBudget` passes (600/3 ⇒ 92 px, 600/12 ⇒ 64 px); e2e readable-city measures `getComputedStyle(row).minHeight` ∈ [64, 92] at 1366 and 1920 |
| A7 | Operator chrome gone; settings behind a long press but accessibly reachable | `npx vitest run test/app/kiosk.test.ts -t "long press"` passes (1500 ms pointerdown opens Postavke, a shorter press does not); `npx playwright test e2e/a11y.spec.ts -g kiosk`: no visible button inside the invitation, `getByRole('button',{name:'Postavke zaslona'})` attached, no 'Zaustavi izmjenu', no `kiosk-theme` testid anywhere |
| A8 | QR code itself ≥ 240 CSS px at the three wall sizes | `npx playwright test e2e/readable-city.spec.ts e2e/redesign.spec.ts`: `[data-testid=kiosk-qr] svg` bounding width ≥ 240 at 1920×1080, 1366×768, 1080×1920 |
| A9 | Footer: green cross · 24/7 · address; sources without a time | `npx vitest run test/app/kiosk-frame.test.ts` passes: `strip-pharmacy` text matches /24\/7/ and contains the address, contains svg.k-cross; `strip-sources` text === 'DHMZ · EMSC' with no /\d\d:\d\d/ |
| A10 | Night palette holds contrast for the new elements | `npx vitest run test/app/contrast.test.ts` passes with the added pairs (live time, timetable time, green cross, kicker colours) in the dark face; `npx playwright test e2e/kiosk-layout.spec.ts` dark screenshots at 21:30 fixture written to test-results/kiosk-*-dark-*.png for the visual review gate |
| A11 | Outage: map without vehicles + one note, grey rows, sentence says what is known, nothing shouts | `npx playwright test e2e/readable-city.spec.ts -g outage`: `kiosk-map` `data-feed=down`, zero vehicle pills (`data-pills` '0' or absent), `kiosk-map-note` visible exactly once, `.k-nearby-time[data-live=true]` count 0, `.k-head` text !~ /nedostupn/, `kiosk-alert` hidden; `npx vitest run test/app/kiosk.test.ts -t down` passes the same in happy-dom |
| A12 | Sentence endpoint is safe and free in tests | `npx vitest run --project workers test/feed/routes.workers.test.ts test/feed/sentences.test.ts`: GET ⇒ 405; 17 facts ⇒ 400; valid POST under APP_ENV=test ⇒ 200 {sentences:[]} with zero ai.run calls; acceptSentence refuses >budget/ellipsis/invented numbers; KV hit on the second identical request |
| A13 | First-tram data present and the wall's data files still parse | `node scripts/gtfs-lastrun.mjs` run and committed; `node -e "const f=require('./app/public/data/lastrun/106_1.json');if(!f.first\|\|!f.routes)process.exit(1)"`; `npx vitest run test/app/lastrun.test.ts` passes |
| A14 | Nothing else regressed on the kiosk surface | `npx playwright test e2e/kiosk-layout.spec.ts e2e/kiosk-recovery.spec.ts e2e/lagano.spec.ts e2e/screen-creation.spec.ts e2e/redesign.spec.ts e2e/a11y.spec.ts e2e/readable-city.spec.ts` all pass locally; `npm run review:visual` writes kiosk-window.png with overflow ≤ 1 and 0 serious axe violations |

## Risks

- Workers AI wording and latency: the 70b-fast model may return fewer than 8 acceptable lines or time out; mitigated by acceptSentence, a 6 s timeout, a 20-minute KV cache per fact-set and the always-present client templates (the solar template guarantees a sentence at every hour). Cost is bounded to one call per distinct fact-set per 20 min per locale.
- Croatian copy that the owner has not ruled on (row titles 'Zadnji tramvaji'/'Prvi tramvaj'/'Izlazak sunca'/'Zalazak sunca', the outage note, the template sentences) is a proposal for the strike-through round; it is confined to hr.json `kiosk.nearby.*`/`kiosk.sentence.*` so one edit changes it. Decided strings ('U blizini', '2 km · ~15 min', 'uvijek', 'Skeniraj za 10 minuta grada.', '24/7', 'sutra', kickers) are byte-exact.
- Regenerating app/public/data/lastrun/*.json for 3,805 stops with the `first` field is a large committed diff; the file shape is additive and `parseFile` accepts both, so a stale file cannot break the wall (first-tram row simply absent).
- Character budget ≠ pixel width: 80 Manrope characters at 26 px fit ~1,150 px at 1920 but not the compact header; the per-composition budget (64 at compact/portrait/handheld) and the runtime scrollWidth check (skip, never ellipsis) cover it; happy-dom cannot measure, so the pixel rule is proven only in e2e.
- Rows growing to 92 px scale type by 1.44 (28 → 40 px titles); at compact (aside 560 px wide) a long headsign may ellipsise mid-word in the title cell — acceptable per the row grammar (one line, ellipsis in a cell is not the header sentence) but review at 1366 in the visual gate.
- Stale closures now stay on the wall (a closure does not reopen because our fetch failed); the phone and /izvori keep provenance. If the owner disagrees, gate on `status === 'live'` in nearby.ts (one line).
- Whole-city screens (today's default) have no stop until WP2 lands: the timeline then shows only closures/events/solar/always rows and the sentence leans on weather/solar/closures; principle 3's 'a departure row always exists' is fully met only with a place. Sequence WP2 right after WP1.
- `cityStore.ensure(['streets','settlements'])` loads the street-register chunks on the wall; verify chunk sizes in app/public/data/city/manifest.json before enabling; fall back to `ensure(['heritage'])` only if the streets chunk exceeds ~1 MB gzipped and gate the story row on `city.streets.length`.
- reconcile keyed on data-key preserves nodes only when keys are stable per item: departure keys use tripId (stable), closure/event keys use feed ids (synthesised `slug(street):start` for prometnice — stable within a day). A source that re-ids items would re-enter rows; the rotation instrument's nodeReuse metric catches it.
- Removing the header theme glyph leaves theme control only in Postavke (behind a long press or the hidden named button); `?tema=` at boot still works. Removing the copy button also removes it from the paired join card (cardMarkup is shared) — intended, but note in the paired review.
- The map note is a second thing on the picture besides MapLibre's attribution; DESIGN.md is amended (step 13) so the 'nothing on the picture' rule names the one exception.

## Agent briefs (self-contained; one per lane)

### Brief 1

AGENT A — data + selection layer (steps 1, 2). Repo /home/matija/projects/kajima/working-deployed. Files you own: scripts/gtfs-lastrun.mjs, app/src/core/lastrun.ts, app/public/data/lastrun/**, app/src/kiosk/nearby.ts (new), test/app/lastrun.test.ts, test/app/kiosk-nearby.test.ts (new). Read first: docs/companion-2026-09-22.md §4 and §12, app/src/kiosk/local.ts (closuresByDistance :338, nearestPharmacy :382, sunToday :123, lastDeparturesAhead :642), shared/city/arrivals.ts (ArrivalRow), shared/city/events.ts (locatedEvents :72), shared/city/geo.ts (matchStreet :38, distanceM :7), app/src/ui/solar.ts (sunTimes), app/src/kiosk/markup.ts (kBadge :25), app/src/kiosk/exceptions.ts (kindOfRoute :32), app/src/kiosk/format.ts (clock, dayMonth, sameZagrebDay, weekdayDayMonth, zagrebDayAfter, dayKey), app/src/format.ts (zagrebHour), app/src/kiosk/stops.ts (sortRouteIds, stopDistanceM), app/src/kiosk/highlights.ts (HighlightBounds/inHighlightBounds — copy the two functions into nearby.ts as `highlightBounds`/`inBounds`; highlights.ts is being deleted by agent D), test/app/kiosk-local.test.ts:30-60 (fixture style). Deliver exactly the exports and rules written in plan steps 1 and 2 (types NearbyItem/NearbyKind, constants, rowBudget, nearbyItems with builders a–h, ordering, bounds). Strings come from `KioskStrings.nearby.*`, `.say.tomorrow`, `.paired.untilTime`, `.arrivals.now/inMinutes`, `.lines.tram` — agent D adds `nearby` to strings.ts; until then compile against a local `Pick<KioskStrings,...>` type and the literal values 'U blizini', 'uvijek', 'do', 'Zalazak sunca', 'Izlazak sunca', 'Zadnji tramvaji', 'Prvi tramvaj'. Forbidden in any row text: 'registra', 'nije provjera', 'Obuhvat', 'Dohvaćeno', 'zastarjelo', 'nepotvrđeno', 'uživo', 'po redu vožnje', 'Procjena'. Do not touch kiosk.ts, invitation.ts or CSS. Run `node scripts/gtfs-lastrun.mjs` (the script header documents the GTFS zip) and commit the regenerated directory. Done when `npx vitest run test/app/lastrun.test.ts test/app/kiosk-nearby.test.ts` passes and `npm run typecheck` is clean.

### Brief 2

AGENT B — timeline DOM + CSS (step 3). Files you own: app/src/kiosk/timeline.ts (new), the `.k-nearby*` rules in app/src/ui/kiosk-city.css (replace the `.k-highlight*` rules at :11-18, :28-35, :38-39, :48-49, :51 and the `.k-overview` rows at :5 and :37) and the two reduced-motion/lagano blocks in app/src/ui/kiosk.css (:712-719), test/app/kiosk-timeline.test.ts (new), test/app/kiosk-css.test.ts (:28-39 and the new assertions). Read first: brief §11 wall contract and principles 5, 7; app/src/ui/dom/reconcile.ts (keyed reconciliation you MUST use so rows keep their nodes); app/src/kiosk/invitation.ts (how a handle is shaped); app/src/kiosk/paired.ts:217-260 fitRows (what NOT to do: rows are never `hidden`, they are sliced); ledger [O-22], [O-24], [O-33], [O-42]. Implement `mountTimeline`/`TimelineHandle`, the markup, the CSS (row band via `--k-nearby-row` 64–92 px and `--k-nearby-scale`, grid-auto-rows, overflow hidden, enter animation 220 ms opacity only, none under reduced motion/lagano), the pill rendered as exactly '2 km · ~15 min' from the template '{km} km · ~{min} min', title 'U blizini'. Import `NearbyItem` and `rowBudget` from app/src/kiosk/nearby.ts (agent A); if it is not merged yet, code against the interface text in plan step 2 and a local stub, then delete the stub. Blue live time = `--k-action`, timetable = `--k-ink-2`, 'uvijek' rows = `--k-ink-3`. Done when `npx vitest run test/app/kiosk-timeline.test.ts test/app/kiosk-css.test.ts` passes; do not edit kiosk.ts or invitation.ts (agent E wires you in).

### Brief 3

AGENT C — sentence writer, client and worker (steps 4, 5). Files you own: app/src/kiosk/sentence.ts (new), shared/kiosk/sentence-accept.ts (new; acceptSentence shared by app and worker), worker/feed/sentences.ts (new), worker/routes/kiosk.ts (new), worker/index.ts (add handleKiosk to ROUTES at :39), app/src/api.ts (add fetchSentences), test/app/kiosk-sentence.test.ts (new), test/feed/sentences.test.ts (new), test/feed/routes.workers.test.ts (add cases), and delete test/app/ticker.test.ts. Read first: brief §10 principle 4, §12 'Sentence', §13 #31, ledger [O-13], [O-17], [O-41], [O-47]; worker/feed/brief.ts (your model: model ids, KV key hashing, negative TTL, timeout, `unquote`, `acceptBrief`, `isTestEnvironment` guard, waitUntil pattern) and test/feed/brief.test.ts; worker/routes/feed.ts and worker/routes/city.ts:16 (RL_OPEN by CF-Connecting-IP), worker/routes/screens.ts (same-origin check), worker/http.ts (json, clientIp); app/src/kiosk/ticker.ts (the retired writer whose kicker/ordering ideas you replace — do not import it); app/src/kiosk/local.ts weatherNow :60; app/src/city/discovery.ts dynamicPlaces :21 and app/src/city/strings.ts bikeCount. Implement the types, constants, `sentenceFacts`, `templateSentences`, `acceptSentence` (rules: trim/unquote; reject '', > budget, '…', '...', newline, markdown; reject any number not present in a referenced fact; require a shared ≥5-letter token or digit run), `createSentenceSequence` (20 s hold, skip expired validUntil, no verbatim repeat within 600 s, skip overflowed, prefer least-recent kicker, keep identity while suspended, never null while a template exists), the Worker endpoint POST /api/kiosk/sentences with the Croatian/English prompts in plan step 5, KV `sentence:v1:` 1200 s / negative 300 s, RL_OPEN, ≤ 8 KB body, no AI call when `!env.AI || isTestEnvironment(env)`. Template strings read from `KioskStrings.sentence.*` (agent D adds them; until then use the literals in plan step 9). Kickers exactly: Promet, Kultura, Vrijeme, Bicikli, Noćas, Radovi. No template may contain 'nedostupn' or '…'. Done when `npx vitest run test/app/kiosk-sentence.test.ts test/feed/sentences.test.ts` and `npx vitest run --project workers test/feed/routes.workers.test.ts` pass and `npm run typecheck` is clean.

### Brief 4

AGENT D — footer, strings, deletions (steps 8, 9, 10). Files you own: app/src/kiosk/frame.ts (stripMarkup :49-66), app/src/ui/icons.ts (add 'cross'), app/src/kiosk/strings.ts, app/src/i18n/hr.json, app/src/i18n/en.json, app/src/city/strings.ts (delete `quiet`), app/src/ui/kiosk.css (:216-225 `.k-ticker*`: `text-overflow: clip` + per-kicker colours; add `.k-cross`/`.k-247`; delete `.k-code-copy*` :429-433), delete app/src/kiosk/highlights.ts and app/src/kiosk/ticker.ts, tests test/app/kiosk-frame.test.ts, test/app/copy-guards.test.ts, test/app/contrast.test.ts, test/app/i18n.test.ts. Read first: brief §11 footer sentence, §13 #15, #29, #17, #19, principles 5 and 8; ledger [O-39], [O-43]; app/src/kiosk/strings.ts (the typed adapter and its `group/forms` builders), the current hr.json `kiosk.invite` (:856-863), `kiosk.ticker` (:964-970), `kiosk.safety` (:971-988). Make exactly the key additions/removals in plan step 9 (both locales, parity), the KioskStrings tree changes (add `nearby`, `sentence`, `legend`, `handheld`; remove `ticker`, `invitation.support`, `invitation.codeWaiting`, `safety.confirmed`), the footer per plan step 8 (trail = 'DHMZ · EMSC' without time; pharmacy = svg cross + '24/7' + short address; aria-label with `safety.pharmacy`), and the deletions. Owner-decided strings are byte-exact: 'U blizini', '{km} km · ~{min} min', 'uvijek', 'Skeniraj za 10 minuta grada.', '24/7', 'sutra', kickers. Removing `kiosk.ticker.*` will break app/src/kiosk/ticker.ts (you delete it) and kiosk.ts's import of highlights (agent E removes it; leave kiosk.ts alone). Done when `npx vitest run test/app/kiosk-frame.test.ts test/app/copy-guards.test.ts test/app/contrast.test.ts test/app/i18n.test.ts` passes; expect typecheck to fail only in kiosk.ts/invitation.ts until E lands.

### Brief 5

AGENT E — integrator: invitation + controller + kiosk unit tests (steps 6, 7, 11). Start when A–D are merged. Files you own: app/src/kiosk/invitation.ts (full rewrite per plan step 6), app/src/kiosk/markup.ts (codeBlockMarkup :50-60, cardMarkup moves to invitation.ts unchanged in name), app/src/kiosk.ts (edits a–p in plan step 7 with current line numbers), app/src/kiosk/mapview.ts (`vehiclesVisible` on KioskMapInput :759-805 and modes in requestKioskMap :806-952), kiosk-city.css :27 QR floor 264 and the handheld order :42-52, kiosk.css `.k-qr .qr` padding 12 px, test/app/kiosk.test.ts. Read first: brief §4 (ladder), §11 wall, principles 1–9; app/src/kiosk.ts whole (1314 lines) — especially :136-153 shell, :236-255 state and click handler, :307-329 paintTicker, :339-371 theme/context, :506-546 paintMap, :567-700 lastRun/arrivals/paintLocal, :711-780 code painter and copy handler, :950-984 beacon, :1176-1201 loadTeaser, :1203-1288 wiring and timers; app/src/kiosk/invitation.ts; the new modules nearby.ts, timeline.ts, sentence.ts and their exports; app/src/map/city-map.ts:2029-2040 (setFeedState holds; you add modes-empty for down) and :798/:2012 setHighlight. Rules: the header middle shows the sentence only when no pairing notice (`role=status`) and no session pill; departures come from the existing board cache (`arrivalsAtStop` :646) — never a second fetch; `ensureStops()` when a stop exists at adoption; `cityStore.ensure(['heritage','streets','settlements'])` on beacon live; the teaser-failure alert is not shown in the invitation (beacon alerts stay); long press LONG_PRESS_MS = 1500 on `.k-head-brand` opens Postavke, the visually-hidden named `kiosk-settings` button remains; no `pair-copy`, no `kiosk-theme`, no `pause-highlights`; the map ring follows the current sentence's refs (`setHighlight`), never the camera. Then rewrite test/app/kiosk.test.ts exactly as listed in plan step 11 (line-referenced). Done when `npm run typecheck` is clean and `npx vitest run` has 0 failures.

### Brief 6

AGENT F — e2e, the rotation instrument, documents (steps 12, 13). Draft in parallel with E; finish when E is green. Files you own: e2e/kiosk-layout.spec.ts (:67-77), e2e/readable-city.spec.ts (:116-150), e2e/a11y.spec.ts (:235-242), e2e/redesign.spec.ts (:81-90), e2e/experience-fixtures.ts (installKioskFeedFixture: stub /api/city/departures with a 6-row ZET board for FIXTURE_STOP and /api/kiosk/sentences ⇒ {sentences:[]}), scripts/kiosk-rotation.mjs (new; port kioskSample/startKioskLog/summariseRotation from review.local/companion/walkthroughs/1720-mon/lib.mjs:245-300 to the new DOM: `kiosk-sentence-kicker/-text`, `.k-nearby-row[data-key][data-kind]`, `.k-nearby-time[data-live]`, `kiosk-map-note`, `[data-testid=kiosk-qr] svg`, `data-feed`; add the in-page MutationObserver count and WeakMap node ids; args --url/--minutes/--at/--out; refuse a non-local URL unless --production and review.local/companion/screen.json exist — never create a production screen; exit 1 when a threshold fails), PRODUCT.md, DESIGN.md, docs/kiosk.md (:3-24, :41-57, :111-133, :135-142, :144-146, :226-231), app/src/kiosk/INTEGRATION.md (:88-96, :139-155). Read first: brief §11, §15 items 7–11, §16 (pending — your script is its instrument), ledger [B-17]…[B-38] (what the old rotation log measured), e2e/helpers.ts (provisionKiosk, localContext, E2E_STOP_ID), the current four specs. Thresholds to encode are the acceptance rows of this plan (departures 1–3, sentences ≤ 80 / no '…' / no overflow / no verbatim repeat within 10 min / none expired, rows nodeReuse 1.0, hiddenRows 0, caveat rows 0, ≤ 1 solar row, no last-tram row after it left, mutationsPerMinute ≤ contentChanges + 5, QR svg ≥ 240, outage: note once, no live time, header !~ /nedostupn/, alert hidden). Local run: `npm run dev` then `node scripts/kiosk-rotation.mjs --minutes 10 --at 2026-09-22T21:30+02:00 --out review.local/companion/rotation/2130.jsonl` for the five slots. Done when the seven kiosk e2e specs pass and the five rotation summaries meet every threshold.

## Verifier verdict (adversarial pass, 22 Sep)

Refuted as written: **yes**. Problems found:

- OUTAGE STEP CONTRADICTS [O-45]/§4.8: step 7(j) hides vehicles with `setModes(new Set())`, but app/src/map/overlays.ts:306-309 `stopFilter(new Set())` returns NEVER and :608 `routeStopsFilter(modes, stopRoutes)` gates the stop layers, so an empty mode set removes the stop rings together with the vehicles. The brief says the map keeps network, stops, BAJS, closures, places.
- DELETED MODULE STILL IMPORTED BY AN UNNAMED TEST: test/city/readable.test.ts:6 imports createHighlightSequence, kioskHighlights, highlightBounds, inHighlightBounds, KioskHighlight from app/src/kiosk/highlights and runs 7 tests (describe 'passive highlight sequence', :42-83). Step 10 deletes highlights.ts; the plan never names this file, so `npx vitest run` fails.
- REMOVED STRING KEY STILL READ IN PRODUCTION CODE: app/src/kiosk/teaser.ts:47 builds the 'invitation' teaser card from `s.invitation.support`; step 9 removes `invite.support`/`KioskStrings.invitation.support` → typecheck fails. Consumers: test/app/kiosk-local.test.ts:283-284 (card ids include 'invitation'), test/integration/feed-to-layers.test.ts:184-207.
- test/app/i18n.test.ts:57 asserts `hr.kiosk.ticker.transit === 'Promet'`; step 9 removes kiosk.ticker.*; the plan's testsToUpdate row for i18n.test.ts mentions only parity.
- test/app/kiosk-css.test.ts assertions not covered: :126 `rule('.k-theme')` must contain 'min-width: 44px' (the glyph button goes; the CSS rule at kiosk.css:199 must stay or the test change); :103 asserts invitation.ts contains 'dataset.overflow' (the rewrite drops it); :75-76 assert kiosk.css `.kiosk[data-size='wide'|'compact']` carry `--k-qr: calc(240px * var(--k-sign-zoom))` (kiosk.css:118,:149) while step 6 changes only kiosk-city.css:27 — either both sheets change to 264 or the test keeps asserting a misleading 240.
- e2e/screen-creation.spec.ts:18 `page.getByTestId('kiosk-settings').click()` on the now visually-hidden (1×1 clipped, under the header) button will hit Playwright's pointer-intercept/actionability retry and time out; not in the plan.
- STEP 11 LINE LIST IS MATERIALLY INCOMPLETE for test/app/kiosk.test.ts. Unlisted invitation-phase assertions on removed DOM: :857-875 (lightweight, weather temp :870), :907 kiosk-ticker, describe 'the invitation composition' :1343-1395 (:1348 aside children ['weather','k-highlight',…], :1349-1351/:1376 weather, :1365 `.k-invite-side` children include 'k-invite-benefit', :1387 highlight 'Ilica'), :1544-1580 ('the panels are reconciled…' weather node identity :1548-1568, :1570 ticker; 'reports a layout defect…' `.k-highlight-content`/dataset.overflow :1576), :1710-1717 lightweight weather, :1846 presence sweep lists 'kiosk-highlight','kiosk-panel-weather', :1862 weather temp, describe 'T5.3: the theme button' :1893-1930 (three tests on `kiosk-theme`), the configured-stop/Promet-card describe :2049-2193 (:2053, :2096, :2101, :2115-2118 `.k-highlight-where`, :2134, :2183), :1303 `.k-weather-current .k-temp` inside the alert test.
- TYPE SHAPE ERRORS IN STEP 2: `map = item.geo as MapHighlight` — FeedItem.geo is a bare geometry (worker/feed/schema.ts:61) while MapHighlight is `{id, geometry}` (app/src/map/city-map.ts:653-656); highlights.ts:72,76 wraps it correctly. Wire types SentenceFact/WrittenSentence/SentenceRequest are declared in app/src/kiosk/sentence.ts (step 4) but the Worker route (step 5) validates against them; only acceptSentence is moved to shared/.
- CONTRAST: step 10 sets `[data-kicker='bicikli']{color:var(--tone-bike)}` and acceptance 9 requires contrast.test.ts to pass with the kicker colours. Computed from tokens.css: --palette-light-bike #178f7f on surface-1 #fbfcfe = 3.88:1, dark #178f7f on #192430 = 3.94:1 — below AA 4.5:1 (bike is a graphic token tested at 3:1 in contrast.test.ts:69-80). Also the test reads `--palette-<theme>-<name>`, not `--tone-*` names as the plan writes them. The other proposed pairs pass (success 6.25/8.97, events 6.95/8.14, warning 6.23/9.07, accent 6.96/7.51, text-muted 7.09/8.97).
- COPY GUARD AS WRITTEN FAILS ON EXISTING COPY: the proposed rule 'no kiosk.* value contains … or nedostupn' with four exceptions would fail on kiosk.status.reconnecting, kiosk.invite.qrWaiting, kiosk.weather.loading, kiosk.lines.loading, kiosk.setup.creating, kiosk.setup.loadingStops, kiosk.settings.saving (all '…'); two of the four named exceptions (weather.unavailable, lines.unavailable) do not even contain 'nedostupn'.
- ACCEPTANCE CRITERION 1 GREP ≠ 0 AS WRITTEN: `grep -rn "kiosk/ticker|…" app/src test e2e scripts` also hits comments the plan does not touch — app/src/kiosk/front.ts:225, app/src/ui/kiosk.css:34 and :216 (all 'kiosk/ticker.ts'); step 10's own verify grep (adds corner-qr|join-code) hits scripts/audit-production.mjs:250 ('join-code').
- STEP 3 CSS: class `.k-nearby-title` is used both for the section <h2> ('U blizini', uppercase label size) and for every row's title span (scaled sup size, nowrap/ellipsis) with conflicting rules. Handheld order rule targets `.k-nearby` but under `.k-overview{display:contents}` the flex child is the `.k-nearby-host` wrapper.
- ROTATION INSTRUMENT (step 12) NOT EXECUTABLE AS SPECIFIED: (a) a plain scripts/kiosk-rotation.mjs importing e2e/experience-fixtures.ts and e2e/helpers.ts (TypeScript that imports worker modules) has no transpile path — scripts/replay-twin.mjs bundles with esbuild for this reason; (b) installKioskFeedFixture shifts fixture times by Date.now()−FIXTURE_NOW, not by `--at`, so with page.clock at 04:30/21:30 the closures' until, events, boards and validUntil are hours off and 'departures.min ≥ 1' cannot hold; (c) `sentences.expiredShown` reads a `data-valid-until` attribute that step 7's paintSentence never writes.
- ACCEPTANCE 9 'kiosk-layout dark screenshots at 21:30 fixture': e2e/kiosk-layout.spec.ts iterates colorScheme light/dark with a localStorage theme and no clock, and screenshots the PAIRED layers (test-results/kiosk-<w>-<theme>-<layer>.png), not the wall; there is no 21:30 fixture.
- STEP 7(f) overflow probe writes candidate text into `.k-ticker-text` while `.k-ticker` may carry `hidden` (first paint after a pairing notice/presentation) → scrollWidth 0 → an overflowing sentence is accepted on that paint.
- STEP 2(h) story fallback 'nearest StreetStory whose settlement polygon contains the place' is undefined: StreetStory has no geometry (shared/city/types.ts:42-49); and matchStreet needs an exact normalised name, which a stop name ('Trg bana J. Jelačića') rarely gives. The needed chunks are heavy: settlements = 27 chunks ≈ 1.9 MB raw, streets 8 × ~91 KB, heritage 14 × ~91 KB (app/public/data/city/manifest.json); the plan's risk row names only streets.
- WORDING/PRECISION: 'handheld keeps 264' — handheld --k-qr is 240 today (kiosk.css:740); same-origin check should name `isSameOrigin` (worker/routes/pairing.ts:45, Origin header only, no Sec-Fetch-Site) and `readCappedBody` (:54); `dataText` lives in app/src/panels/panel.ts; step 2(b) should state explicitly that a closure without `until` is not a timed row (all 32 fixture closures carry expectedEndTime, so it is fine in practice).

Corrections:

VERIFIED OK (no change): every cited path exists; kiosk.ts pointers (:67, :131, :136-153, :236-255, :307-329, :339-346, :370, :514-534, :580-602, :660-672, :688-700, :711-780, :854, :977-980, :1190-1200, :1222-1223, :1259-1279) are exact; reconcile.ts:143 is keyed on data-key (KEY='data-key', :11); closuresByDistance (local.ts:338) includes stale via isLive (:41, status!=='down'); nearestPharmacy :382, weatherNow :60, sunToday :123, lastDeparturesAhead :642, locatedEvents :72, matchStreet :38, distanceM :7, kBadge :25, kindOfRoute :32, codeBlockMarkup :50-60, stripMarkup :49-66, unquote brief.ts:171, BRIEF_MODEL_AKT :46, isTestEnvironment (worker/config.ts:21), ROUTES worker/index.ts:39, RL_OPEN city.ts:16, KioskMapInput :759-801, requestKioskMap :806-952 (setModes at :941), setHighlight city-map.ts:798/:2012, setFeedState :2029-2040, ICON_NAMES, FLAGS.FEED_LASTRUN=true, wrangler binds AI/FEED/RL_OPEN, .dev.vars and playwright webServer set APP_ENV=test (no local AI billing), review-experience.mjs:461 writes kiosk-window.png, review.local/companion/screen.json exists, lib.mjs:246-300 has kioskSample/startKioskLog/summariseRotation, hr strings 'za {n} min'/'sada'/'do {time}'/'sutra'/'Postavke zaslona' are as the plan assumes, docs/kiosk.md, INTEGRATION.md, PRODUCT.md (:46-52, :71-74) and DESIGN.md (:78-93, :113, :120, :126) line refs hold, `-t "invitation"`/`-t down`/`-g outage`/`-g kiosk` filters match existing titles.

CORRECTIONS, by step:
(7j) Outage: do NOT pass `modes: new Set()`. In requestKioskMap (mapview.ts:814) set `let points = input.vehiclesVisible === false ? [] : vehiclePoints(...)` and leave setModes/prozor untouched, so stops (overlays.ts:608 routeStopsFilter), network, closures, BAJS and places stay. Add to the outage e2e/unit assertions: `kiosk-map` `data-feed=down`, zero vehicle pills, and the stop rings still drawn (assert the stops source is non-empty or `data-stops`>0 if the handle exposes it; else assert `.k-map-legend` still lists the tram item and no `setModes(new Set())` call in the fake handle).
(10) Add to the deletion list: test/city/readable.test.ts describe 'passive highlight sequence' (:42-83) and its import line :6 (keep the bikeCount/search tests; move the DPR-independence bounds case to test/app/kiosk-nearby.test.ts against the copied `highlightBounds`/`inBounds`). Also scrub comment references so acceptance grep = 0: app/src/kiosk/front.ts:225, app/src/ui/kiosk.css:34 and :216 ('kiosk/ticker.ts'), and edit scripts/audit-production.mjs:250 (drop 'join-code') or exclude scripts/audit-production.mjs from the verify grep — never run that script (it creates production screens, brief Q15).
(9) teaser.ts:47 → `body: s.invitation.lead` (keep the 'invitation' card id so kiosk-local.test.ts:284 and feed-to-layers pass). i18n.test.ts:57 → `expect(hr.kiosk.sentence.kicker.promet).toBe('Promet')`. Scope the new copy-guards rule to `kiosk.nearby.*`, `kiosk.sentence.*`, `kiosk.legend.*`, `kiosk.handheld.*` (or extend exceptions with status.reconnecting, invite.qrWaiting, weather.loading, lines.loading, setup.creating, setup.loadingStops, settings.saving).
(11) Replace the line list with a grep-driven sweep in test/app/kiosk.test.ts for `k-highlight|kiosk-highlight|kiosk-panel-weather|kiosk-ticker|pair-copy|kiosk-theme|pause-highlights|k-weather-current|k-invite-benefit|TICKER_PERIOD_MS|Čekamo` (56 hits at b300af3) and add the unlisted ranges: :857-875, :907, :1343-1395 (rewrite aside/card children to ['k-nearby-host','k-panel--card'] and ['k-lead','k-hint','k-invite-code']), :1544-1580 (node identity now on `li.k-nearby-row[data-key]`; drop dataset.overflow), :1710-1717, :1846 sweep (replace with 'kiosk-nearby','kiosk-sentence'), :1862, :1893-1930 (theme via `settings-theme` in the panel; delete the header-glyph cases), :2049-2193 (arrivals → `li[data-kind=departure]` count ≤3 and blue/grey `data-live`), :1303.
(kiosk-css.test.ts) :103 replace `dataset.overflow` with `expect(timeline).not.toMatch(/\.hidden\s*=\s*true/)` and `expect(invitation).toContain('mountTimeline')`; :126 keep `.k-theme` rule in kiosk.css (or retarget to `.k-strip-verdict`/`.k-return`); :75-76 change kiosk.css:118 and :149 to `--k-qr: calc(264px * var(--k-sign-zoom))` and kiosk-city.css:27 to `max(264px, …)`, test asserts 264 and `.k-qr .qr` `padding: 12px`; handheld (kiosk.css:740) stays 240 — fix the plan text 'handheld keeps 264' → 'keeps 240'.
(12/e2e) e2e/screen-creation.spec.ts:18 → `await page.getByTestId('kiosk-settings').dispatchEvent('click')` (or focus + Enter), add to F's file list.
(2) `map: { id: item.id, geometry: item.geo }` (Point/LineString only, as highlights.ts:71-72 filters); declare `NearbyItem.map?: MapHighlight`. Move SentenceFact, SentenceKicker, WrittenSentence, SentenceRequest/Response to shared/kiosk/sentence-accept.ts (or worker/protocol.ts) beside acceptSentence; app/src/kiosk/sentence.ts re-exports them. Story fallback: 'the first StreetStory (by id) whose `settlementId` equals the settlement whose polygons contain the place (inPolygons, shared/city/geo.ts:24); none ⇒ no story row that turn'. Add settlements' ~1.9 MB to the risk row and gate `ensure(['streets','settlements'])` on the first paint after beacon live rather than at boot, or load them lazily when the always-row index first hits 0. State: a closure without `until` is not a timed row.
(10 CSS) Kicker colours: `bicikli` → `var(--k-green)` (success 6.25:1 light / 8.97:1 dark) not `--tone-bike` (3.88/3.94 fails AA); contrast.test.ts pairs must be written as palette roles: ('success','surface-1'), ('events','surface-1'), ('warning','surface-1'), ('accent','surface-1'), ('text-muted','surface-1') in both faces.
(3) Rename the section heading class to `k-nearby-heading` (rows keep `k-nearby-title`), handheld rule `.kiosk[data-size=handheld] .k-nearby-host{order:2}`; designHeightPx should describe the `<ol>` box (section height minus the header), and measureHeight() must measure the `<ol>`.
(7f) Measure overflow on a permanently laid-out, visually-hidden probe `<span class="k-ticker-text k-ticker-probe" aria-hidden="true">` inside `.k-head-mid` with the same font tokens, never on the possibly-`hidden` live text; paintSentence must also write `data-valid-until` on `kiosk-sentence-text` (the rotation summary reads it).
(12 instrument) Write the ten-minute instrument as a Playwright script/spec (`e2e/kiosk-rotation.spec.ts` or `scripts/kiosk-rotation.mjs` bundled with esbuild like scripts/replay-twin.mjs) so it can import e2e/experience-fixtures.ts; give it a fixture shift keyed to `--at` (delta = at − FIXTURE_NOW) and a departures fixture for FIXTURE_STOP generated relative to `--at` (6 rows in the next 40 min, 3 with live minutes), and stub `/api/kiosk/sentences` ⇒ {sentences:[]}; thresholds as listed; document that `--production` reuses review.local/companion/screen.json and never creates a screen.
(acceptance 9) Replace 'kiosk-layout dark screenshots at 21:30' with: `npx playwright test e2e/readable-city.spec.ts -g outage` writes test-results/readable-city/kiosk-outage.png, plus one new wall screenshot in readable-city under `?tema=tamna` at 1920×1080 with page.clock at 21:30 — that is the visual-gate artefact for the night palette.
(5) Name the reused helpers: `isSameOrigin(request, url)` and `readCappedBody(request, 8192)` from worker/routes/pairing.ts; `json`/`clientIp` from worker/http.ts; drop the 'Sec-Fetch-Site' clause (isSameOrigin checks Origin only).
(agentBriefs) Agent D additionally owns test/city/readable.test.ts (delete the highlight describe) and app/src/kiosk/teaser.ts:47; Agent E's brief must say 'sweep kiosk.test.ts by grep, the plan's line list is a starting point'; Agent F additionally owns e2e/screen-creation.spec.ts:18 and scripts/audit-production.mjs:250.
