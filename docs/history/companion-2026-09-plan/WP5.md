# WP5 — Copy catalogue, vocabulary, dead paths, docs and tests

Part of the companion brief `docs/companion-2026-09-22.md` (§15). Line numbers refer to the working tree at build `b300af3`; re-anchor with `grep` before editing. Read-only rules of the brief §1 apply: no `wrangler deploy`, no production screen outside the documented local flow, `docs/prijava` and `app/prijava` untouched.

## 0. Reconciliation with the other packages (binding; read first)

The planner text below was written before the seven packages were compared and before the adversarial verifier ran. Where this section, the verifier verdict at the end, or the brief §15 disagree with the planner text, **this section wins, then the verdict, then the planner text**.

**Owner rulings of 22 September, evening ([O-62]–[O-74], binding over everything below).** (1) Q14 is decided: the schema stays on every surface (wall toggle by WP3, phone toggle kept by WP4); WP5 deletes nothing schema-related. (2) Vocabulary: the word "zid" never appears in a UI string or a Croatian document [O-66]; "zaslon" is the word ("Na zaslonu: …", "Javni zaslon, odobreno 22. rujna 2026."). (3) Standard, natural Croatian; the owner reads every new or changed Croatian string before D4 (add the read-through as the last row of step 10's checklist). (4) README.md is being rewritten by the owner; WP5/WP6 only add command lines to whatever it says then.

These decisions override the planner text below wherever they differ. Apply **every correction in the verifier verdict** (the last section of this file), with the following settlements. Canonical ids: the plan's "WP2 (wall composition …)" is **WP1**; the map package is **WP2**; the trust package is **WP0**.

**Lane and gate.** WP5 is lane C and runs **after deploy D3** (phone), except the five isolated files of phase A that no other package rewrites: `app/src/city/conditions.ts:20,26`, `app/src/city/air.ts:5`, `app/src/layers/zrak-i-nebo.ts:262`, `app/src/layers/uprava-i-pravo.ts:270`, `app/src/motion/schematic-host.ts:28` (+ `test/motion/schematic-host.test.ts`). Everything else in steps 1–9 waits for D3; step 10 (the §11 rewrite of PRODUCT.md, DESIGN.md, docs/kiosk.md, INTEGRATION.md) is the **last** commit before D4. The plan's claim "steps 1–7 and 10 do not wait" is withdrawn.

**Deletions follow their last consumer (verifier sequencing).** A2 and A3 run before A1 and each deletes only the leaves its own files read; A1 deletes what the shared scanner (`test/app/i18n-scan.ts`, seam S8, built by WP0) reports unreferenced **after** A2 + A3 merge. Keep `shell.skip` (two `data-i18n` skip links read it). `kiosk.safety.confirmed`, `kiosk.lines.modelNote`, `kiosk.paired.dataFrom/fetchedAt/ongoingUntil` move to B1/A5 with their code edits. `tiles.*`, `kvart.wholeCity/walkMinutes`, `notify.waste` die with `experience/producers/*` in step 9.

**What other packages already own (strike from WP5).** WP1 deletes `kiosk/highlights.ts`, `kiosk/ticker.ts`, `test/app/ticker.test.ts`, corner-qr/join-code, `kiosk.ticker.*`, `kiosk.invite.codeWaiting`, `kiosk.safety.pharmacy`, `kiosk.safety.confirmed` and rewrites `frame.ts`, `invitation.ts`, `teaser.ts:47`; WP0 fixes `dashboard.ts:846` and `grad-teaser.ts:44`; WP4 renames `layers.u-pokretu` → "Karta" (with `kiosk-local.test.ts:95`, `kiosk.test.ts:1416`), deletes the Sada stop prompt, moves `columnsFor`/`zagrebInstant` to `app/src/kiosk/columns.ts`, adds `arrivals.timetable` ("Vozni red") and step 11 (end of session). WP5's A2 therefore shrinks to ia-map §7 #21 (teaser exports, `kiosk.ts:54`), #23 (`.k-lines--overlay`), #18/#22 orphan strings; A5 (step 7) drops its `tiles.ts`/`tiles.test.ts` edits and step 6 its `producers/last-run.ts:57` repoint (both modules die in step 9); WP5 never edits `invitation.ts`.

**Keys (seam S9).** No top-level `nearby.*`, no `sentence.kicker.*`, no `time.untilDate`: the wall's strings live under `kiosk.nearby.*`, `kiosk.sentence.*`, `kiosk.legend.*`, `kiosk.handheld.*` (WP1/WP2), setup/settings under `kiosk.setup.*`/`kiosk.settings.*` (WP3), phone under `sada.*`/`directory.*` (WP4); "do {date}" is the existing `events.untilDate`; the one timetable word is `arrivals.timetable`; `arrivals.scheduled` value → "vozni red". Bike plural leaves are `city.bikeCount_one/_few/_other` + `city.bikeCountUnknown`; `CityWord` is the string-valued keys of `hr.city` (flatten `city.facts.*` → `city.fact-*`, `city.airIndex.*` → `city.airIndex-1..6`). Delete `kiosk.invitation` or give it a consumer. Value edits to owner-approved verbatim strings (`session.expired*`, "deset minuta" → "10 minuta") only after the owner confirms (brief §17).

**Vocabulary sweep (step 6) after WP4.** "Karta" is the destination word (WP4 sets `layers.u-pokretu`); WP5 sweeps only the remaining synonyms (`transport.mapRegion`, `transport.sheetLabel`, `izvori-render.ts:70`, `city/strings.ts` `movement`/`network`); "Promet" stays as the kicker and subject word (`kiosk.say.transit`, `kiosk.sentence.kicker.promet`). Stop = "stajalište" (tram/bus), "stanica" only for BAJS, "postaja" only for DHMZ/air. Tests that break: the verifier's list (`transport-workspace.test.ts:344,423,432,868`, `transport-detail.test.ts:103,123,131,136,152,155`, `kiosk.test.ts:2030,2035,2162`, `timeband.test.ts:200,788`, `producers.test.ts:113,419,433`, `lastrun.test.ts:169`, `dashboard.test.ts:1339,1341`, `panel.test.ts:93-96`, `kiosk-frame.test.ts:69`, `kiosk-local.test.ts:974-975`, `transport-search.test.ts:103`, `producers-mobility.test.ts:178-184`, `scan.test.ts:103-124,388`, `type-tokens.test.ts:107-108`, `workspace-css.test.ts:25`, `layers.test.ts:133,326-345,372-378`, `print.css:4`). `copy-guards` "one name per concept" uses one constant `TRANSPORT_TAB_WORD = 'Karta'` and asserts `city/strings.ts` no longer exports `movement: 'Kretanje'` / `network: 'Prijevoz i raspored'`.

**Step 9 — composition retirements after D3 (file-level, copied from `plan/WP4/notes.md` "Dead after WP4").** `experience/producers/*` (11 files; `SAFETY_ICON`/`safetyVerdict` already moved by WP1 to `experience/safety-state.ts`), `experience/tiles.ts`, `experience/timeband.ts` (minus the pair WP4 moved), `experience/timeband-sync.ts`, `transport/view.ts overviewMarkup|peekMarkup|delaysMarkup|resultsMarkup|castButton`, `chrome.ts clockMarkup|fabMarkup|searchLaunchMarkup|bellMarkup`, `dashboard.ts mapFull|setMapView|regions.fab|castTimer|CAST_SENT_MS|openSearch`, `layers/u-pokretu.ts renderLightweight*` (decide R-L2 for the workspace path: the lagano phone keeps the sheet with a null map slot; document in docs/kiosk.md "Lagani prikaz", not DESIGN.md), CSS `.tb-*`, `.ki-fab*`, `.ki-domains*`, `.ki-clock`, `.ki-search`, `.ki-bell`, `city.css:2-7`, i18n `cityOverview.*`, `timeband.*`, `tiles.*`, `cast.*`, `export.*`, `shell.frozen*`, `transit.mostDeviating`, `transport.{trams,buses,showClosures,fitCity,mapModeSchema,mapModeMap,schemaTramsOnly,modesLabel,toolsLabel,peekLoading,runningRoutes,noRunning,screenStop,notices,openNotice,closuresNow_*}`, `panels.{delays,moreRoutes_*,mapExpand,mapCollapse}`, `shell.{clockLabel,clockOnly}`, the `city/strings.ts` browse keys, and the tests `timeband`/`timeband-sync`/`producers`/`producers-mobility`/`tiles`/`lastrun:165`. The orphan scanner is the arbiter for keys; this list is the arbiter for modules.

**Acceptance fixes.** #2 names the leaf count (`leafKeys(hr).length === <n>`, enumerated); #4 drops `configuredBoard` (WP1's), excludes negative assertions in test files, adds `type-tokens`/`workspace-css`/`layers.test`/`print.css`; #6 second grep → `grep -rnE "s\.paired\.(dataFrom|fetchedAt|ongoingUntil)|strings\.lines\.modelNote|strings\.safety\.confirmed|panels\.sunComputed|s\.arrivals\.note" app/src/kiosk app/src/kiosk.ts | wc -l` = 0; #9 doc guards whitespace-tolerant (`/gumbom za\s+zaustavljanje/`, `/Dodir na stajalište otvara\s+istraživanje grada/`, `/svakih 8\s+sekundi/`, `/Radovi u\s+gradu/`), the "one map-linked highlight" phrase is PRODUCT.md:50 (not DESIGN.md), "Ništa se ne izmišlja" is **added**. Pointers: `kiosk.safety.confirmed` = hr.json:982; `paired.ts` `ongoingUntil` :753; `intlLocale` from `../experience/text`; `ResultsData`/`resultsMarkup` = `view.ts:205-232`; `kiosk.ts:704` has no invitation coverage line.

**Step 10 (docs, last).** The one full rewrite to the brief §11 with the dated 22 September 2026 approval paragraph; each earlier package has only edited the sentences its acceptance invalidated. Keep the seven `docs.test.ts` pins verbatim; no em dashes.

## Goal

One copy catalogue (app/src/i18n/hr.json + en.json) read through the existing typed adapters (i18n.t, kiosk/strings.ts, transport/strings.ts, and city/strings.ts turned into the same thin adapter), one Croatian word per concept (Karta / stajalište / uvijek / "vozni red"), no disclaimer, caveat or fetch time in any wall markup, the 77 orphan keys and the dead paths of ia-map §7 deleted with their CSS and tests, the new brief-decided strings added under stable keys for WP2/WP3/WP4 to consume, and PRODUCT.md / DESIGN.md / docs/kiosk.md / app/src/kiosk/INTEGRATION.md rewritten to the §11 contracts with executable guards so none of it can drift back. Measured facts with line numbers: review.local/companion/plan/WP5/notes.md.

## Depends on

- WP2 (wall composition: invitation.ts/highlights.ts/kiosk.ts sequencer, frame.ts footer, nearby-list testids, legend mount) — steps 8 partly and 9 wait for it; steps 1-7 and 10 do not
- WP3 (setup field + click-toggle settings consume kiosk.setup.field/preview/previewCity/create and kiosk.settings.place/frame/frameValue/rhythm from step 3; WP5 deletes the old setup/settings keys in step 9 once WP3 stops reading them)
- WP4 (phone Sada/Karta/Još consume nearby.*, sentence.kicker.*, arrivals.departing, directory.*, location.*, city.* from steps 1-3; WP5 deletes cityOverview.*, timeband.next/seg.*, the ct browse keys, .day-stop-prompt and the e2e browse assertions in step 9 once WP4 lands)
- WP0 (trust) — no file overlap; the '+N' pill guard belongs to WP2

## Data, protocol and artefact changes

none (no worker, wire or artefact change; hr.json/en.json gain the `city`, `sentence`, `nearby`, `location`, `kiosk.legend`, `kiosk.handheld` groups and the setup/settings/directory/arrivals/time leaves listed in step 3, and lose the 77 orphans plus the composition-dependent leaves of step 9)

## Contracts this package supersedes

- PRODUCT.md:44-53 (20 Sep: one map-linked highlight held for 20 seconds, pause control, mobility/city-life alternation)
- PRODUCT.md:62-69 ('Sada leads with local departures' stays; 'Timetable-only rows show clock times; tracked estimates are labelled on every row' → colour + accessible word, never 'procjena'; 'Search, filters, saved choices…' unchanged)
- PRODUCT.md:71-87 (19 Sep: one button Pokreni zaslon and a gear; whole-city default window; header line of city news condensed server-side — the sentence stays but at 20 s and ≤ 80 characters with the six kickers)
- PRODUCT.md:95-102 (18 Sep: 'Phone: Sada, Karta, Događanja, Još' → Sada · Karta · Još; 'Culture pins show known activity during seven calendar days' → tonight's venues on the map, the week in Još)
- PRODUCT.md:104-105 ('The public display is a whole-city window. Idle touchscreen discovery returns after 90 seconds')
- PRODUCT.md:112 ('Sada, Promet, Događanja and Još on the phone')
- DESIGN.md:78-99 (20 September passive-view contract: highlight, legend with '?', 'Highlights hold for 20 seconds, alternate mobility and city life, and use real cultural/heritage records when events are quiet')
- DESIGN.md:101-155 (historical public-screen composition: 'The other column carries weather, a transit-exceptions card, an events card…', header line replaced on a fixed period, 'Starting a screen is one button and no configuration; … settings panel', 'Stops stay tap-able … hit tolerance … a tapped stop leads with its arrivals', 'Quiet-day discovery uses a real cultural or heritage record with its own attribution. Touch exploration has search, categories, list/details and an explicit return action; 90-second inactivity restores the overview')
- DESIGN.md:171-174 ('Phone destinations are Sada, Karta, Događanja and Još … Desktop exposes all six domains directly')
- DESIGN.md:180-192 ('Secondary map actions live in one disclosure'; 'Filters use one labelled disclosure rather than an unmarked horizontal strip')
- DESIGN.md:194-197 ('Još separates Saved, preferences, additional destinations and help')
- DESIGN.md:199-205 ('Living city, culture, transport, useful places and heritage are progressive map groups … geographic clusters use a distinct plus-count mark')
- DESIGN.md:207-210 ('The single Zaslon control…' → Zaslon and Podijeli grad, both labelled)
- DESIGN.md:219-222 ('Preserve … frozen attributed exports' → the content clears to the scan invitation and /hitno)
- DESIGN.md:228-231 ('A ZET arrival is an estimate and is labelled one' → blue countdown / grey clock with an accessible word)
- docs/kiosk.md:3-24 (Pasivni pregled 20. 9.: 'jedan istaknuti sadržaj', 's gumbom za zaustavljanje', 'navodi što, gdje, kada, izvor i starost')
- docs/kiosk.md:36-62 (steps 2-3: 'Pritisnuti Pokreni zaslon. To je cijelo postavljanje', 'zupčanik u zaglavlju otvara Postavke', four sections Područje/Stajalište/Tema/Zaslon)
- docs/kiosk.md:79-163 ('prozor cijeloga grada, od Črnomerca do Maksimira', 'Dodir na stajalište otvara istraživanje grada … 90 sekundi bez dodira vraća prozor', 'Desni stupac nosi tri ploče', 'traku gradskih vijesti … izmjena svakih 8 sekundi', 'Radovi u gradu')
- docs/kiosk.md:188-197 (Osnovno opened by the verdict word; 90 s return)
- docs/kiosk.md:199-231 ('Popis to kaže u jednoj rečenici ispod redaka: Procjena iz ZET-ovih podataka o vozilima; ostalo po voznom redu.'; the 8 s ticker paragraphs)
- docs/kiosk.md:331-334 (troubleshooting rows naming the gear and Postavke)
- app/src/kiosk/INTEGRATION.md:85-96 (testids kiosk-ticker, kiosk-lastrun, three front panels), 131-137 (strings-hr.ts / strings-en.ts), 139-149 (Header ticker built by kiosk/ticker.ts, 8 s swap; the gear as the only setup surface)
- docs/companion-2026-09-22.md §13 rows 8, 9, 12, 13, 14, 15, 16, 21, 22, 25, 30 (out) and 1, 2, 5, 11, 24, 29 (replaced) are executed by this package; row 11 is decided here: Karta

## Steps

### 1. Turn app/src/city/strings.ts into a thin adapter over hr.json `city.*` (mirror transport/strings.ts) and move its 84 pairs into the catalogue  `[M]`

Files: `app/src/city/strings.ts`, `app/src/i18n/hr.json`, `app/src/i18n/en.json`, `test/city/readable.test.ts`

Add a top-level `"city": {…}` group to hr.json (insert before the closing brace after `motion`, currently line 1198) and en.json (after `motion`, line ~1161). Keep only the keys that survive §11/§13: map (value stays 'Karta'), culture, heritage, search ('Mjesto, ulica, linija ili stajalište'), today/tomorrow/week ('Danas','Sutra','Idućih 7 dana' — capitalised chip forms), the 17 category words (water, toilet, sport, dogs, recycling, market, wifi, cycle-parking, garage, charging, rail, bikes, air, streets, cycle-paths), program, ongoing, noProgram, noResults, unavailable, loading, source, original, saved, save, back, more, onMap, noLocation, multiVenue, events, venues, available, returns, rent, return, unknown, observed, stale, schedule (value → 'Vozni red'), departures, noDepartures, whyStreet, sourceLanguage, river, consultation, partial, copy, copied, freshUnknown, selected, rentMode, returnMode, copyFailed, notFound, fetched, noConsultations. DROP now (no renderer or renderer dies in this WP): inactive, closed, start, next, preview, leave, conditions, noMap, locationDenied, matching, cityLife, hour, publicProgram, quiet ('Upoznaj ovo mjesto' §13 #12), reference ('Podatak iz registra…' §13 #13 — the sentence moves once to app/izvori/index.html, step 9), siteNote (§13 #12), scheduleNote (§5 #6). DROP after WP4 lands (Karta browse/taxonomy gone): all, city ('Grad oko tebe'), movement, useful, network, layers ('Što tražiš?'), list, here, locate, legend, streetBrowse, cluster, allVenues, activeVenues. New file body: `import hr from '../i18n/hr.json'; import { createDefaultI18n, type SupportedLocale } from '../i18n/create-default-i18n'; export type CityWord = keyof typeof hr.city; export const ct = (i18n: Pick<I18n,'getLocale'>, key: CityWord, vars?) => catalogue(i18n.getLocale()).t(`city.${key}`, vars);` with the same BY_LOCALE cache as transport/strings.ts:22-32. `bikeCount(i18n, value)` keeps its signature and becomes `ct(i18n,'bikes', {count})` over new plural leaves `city.bikes_one` '{count} bicikl', `city.bikes_few` '{count} bicikla', `city.bikes_other` '{count} bicikala' (en: '{count} bike' / '{count} bikes'); a non-finite value renders `city.bikesUnknown` '— bicikala' never '?' (§13 #14). Acceptance: `grep -cP '[čćžšđ]' app/src/city/strings.ts` = 0; test/city/readable.test.ts:9 still imports bikeCount and passes.

### 2. Fold the 31 inline HR/EN ternaries and the hard-coded string tables into catalogue keys  `[L]`

Files: `app/src/experience/directory.ts`, `app/src/city/next-departures.ts`, `app/src/city/markup.ts`, `app/src/city/location.ts`, `app/src/city/conditions.ts`, `app/src/city/air.ts`, `app/src/kiosk/invitation.ts`, `app/src/kiosk/highlights.ts`, `app/src/layers/zrak-i-nebo.ts`, `app/src/layers/uprava-i-pravo.ts`, `app/src/motion/schematic-host.ts`, `app/src/i18n/hr.json`, `app/src/i18n/en.json`, `test/motion/schematic-host.test.ts`

Site → key (hr value): directory.ts:93 → directory.savedRecord 'Spremljeni zapis {id}'; :94 → directory.kindRoute 'Linija', directory.kindStop 'Stajalište' (place kind keeps ct venues); :95 → directory.remove 'Ukloni', directory.removeLabel 'Ukloni: {name}'; :97 → directory.saved 'Spremljeno', directory.savedEmpty 'Spremi liniju, stajalište ili mjesto na karti. Ovdje ih možeš ponovno otvoriti na ovom uređaju.'; :125 'Odredišta' → delete the heading (Još per [O-60] lists Događanja ovaj tjedan · Vrijeme · Grad · Sigurnost as plain rows; WP4 composes, WP5 supplies directory.eventsWeek 'Događanja ovaj tjedan'); :126 → directory.preferences 'Postavke'. city/location.ts:13 → location.device 'Tvoja lokacija' (drops the gendered 'dopustio/la'); :14 → location.area 'Središte karte'; :15 → the 'Gradska referentna točka:' and 'Stajalište zaslona:' prefixes die (§13 #1: the place is the title) — `locationLabel` returns `location.name` alone for kind screen|city. next-departures.ts:14 prompt → delete (§13 #2; WP4 renders departures for the automatic nearest stop); :19 titles → delete; :22 'Procjena' → `arrivals.live` as a visually-hidden word beside the blue time (see step 7). markup.ts:13-20 SAFE_FACTS → `city.facts.{payment,maintenance,type,sport,sockets,connector,capacity,designated-accessible-spaces,charging-points,racks,paper,plastic,glass,metal,batteries,biowaste,tyres,electronics,period,classification,registry}` (20 leaves, hr = first tuple member, en = second); :49 → city.airNote 'Preliminarni indeks postaje, ne ocjena za cijeli grad.' (phone place detail only; wall path publicDisplay=true drops it); :54 locale tag → `i18n.getLocale()==='en'?'en-GB':'hr-HR'` helper `intlLocale` already in layers/uprava-i-pravo.ts (reuse); :57 unchanged (ct). conditions.ts:20 → city.airMethod 'Opažanja postaja su preliminarna, ne ocjena za cijeli grad. Indeksi čestica koriste pomične prosjeke.'; :26 → city.consultationsNote 'Nacionalni izvori, ne savjetovanja Grada Zagreba.'. air.ts:5 → city.airIndex.1..6 ('Dobra','Prihvatljiva','Umjerena','Loša','Vrlo loša','Izrazito loša'). kiosk/invitation.ts:45 → kiosk.handheld.info 'Za javni zaslon otvori /kiosk/ na tom uređaju i odaberi Pokreni. Ovaj kod otvara osobnu sesiju; skeniranje ne mijenja javni prikaz.'; :58 → kiosk.legend.tram 'Tramvajska linija', kiosk.legend.bikes 'BAJS: broj bicikala', kiosk.legend.venues 'Mjesta s programom večeras' (three items, no '?', §13 #14; WP2 mounts them); :82,:84,:89,:90 die with the highlight (WP2 deletes the highlight region; if WP2 has not landed when this step runs, route them through kiosk.highlight.* temporarily — do NOT leave literals). highlights.ts: whole file is retired by WP2; if still present, replace :63-64,73-75,82,94,104,107 literals with `arrivals.live`/`arrivals.scheduled`/`time.untilDate`/`city.*` keys and delete the credit line (:56-57 stamp) — no 'Dohvaćeno', 'Vozni red od', 'Zapis od' strings remain. zrak-i-nebo.ts:262 → weather.reference 'Mjerenja, sunce, zrak i bilten Save'. uprava-i-pravo.ts:270 → civic.consultations 'Nacionalna savjetovanja' (conditions.ts:26 `ct('consultation')` reads the same key). schematic-host.ts:28 → `export const HONESTY_NOTE_HR = hr.motion.note;` importing '../i18n/hr.json' (test/motion/schematic-host.test.ts:9,70-71,127 keep passing). Keep deliberately as code constants (proper nouns / licence text, not copy): kultura.ts:41-51, uprava-i-pravo.ts:37-48, grad-teaser.ts:18-24, kiosk/local.ts:433-439, kiosk/paired.ts:108-123, kiosk/front.ts:255-256, kiosk/districts.ts, worker/hitno/*. Add to test/app/copy-guards.test.ts a guard: for every .ts under app/src except app/src/i18n/, `/(?:\ben|english|isEn)\s*\?\s*['"`]/` and `/getLocale\(\)\.startsWith\('en'\)\s*\?\s*['"`]/` match 0 lines.

### 3. Delete the 77 orphan keys, retire kiosk.ticker.* and add the new brief-decided keys; update the KioskStrings tree and the catalogue tests  `[L]`

Files: `app/src/i18n/hr.json`, `app/src/i18n/en.json`, `app/src/kiosk/strings.ts`, `test/app/i18n.test.ts`, `test/app/copy-guards.test.ts`, `test/app/i18n-orphans.test.ts`, `app/src/transport/workspace.ts`, `app/d/index.html`

DELETE (hr.json line as of b300af3; same leaf in en.json): common.print 6, shared.safetyOpen 38 (and its TWINS row copy-guards.test.ts:88,97 — common.links.hitno stays the canonical 'Sigurnost, bez skeniranja'), attribution.source 62, shell.connecting 84, shell.footerNote 89, shell.skip 92 (skip link stays hard-coded in app/d/index.html:14, app/index.html:25), shell.sunset 98, tiles.bikesFree 170, tiles.parkingFree 171, tiles.waste 172-174 (+ notify.waste 511, kvart.wholeCity 485, kvart.walkMinutes 489 — FLAGS.FEED_BIKES/PARKING/WASTE stay false; the producers keep no strings), overview.allClear 177 (drop copy-guards.test.ts:116), civic.assemblyNext 332, transit.mostDeviating 375, transport.search 393, transport.searchLabel 394 (and delete workspace.ts:595 so the placeholder/label are both ct 'search'), transport.noResults 396, transport.routes 400, transport.stops 401, transport.searchResults 464, transport.lightSearch/lightSearchLabel/lightNoResults 480-482, cast.toScreen 496, cast.fab 497, cast.fabLabel 498, cast.sent 501 (keep cast.target/targetStop/noScreen/peer/frozen/connecting while castReasonText in experience/presentation.ts reads them — verify with grep at execution), session.joinedNotice 535, session.joinedPeer 536, session.openSheet 578, export.print 588, panels.vehicles 597, panels.map 608, panels.schematic 611, panels.sun 617, panels.events 619, panels.cityWork 623, panels.humidity 627, landing.pages.scan 768, scan.unlock 788, scan.confirmTitle/confirmPeer/confirmStop/confirmStopOnly/confirmHint 792-796, scan.cancel 797, kiosk.header.temporaryUntil 841, kiosk.header.venue 842, kiosk.invite.codeWaiting 862, kiosk.weather.station 879, kiosk.say.transitRegular 915, kiosk.say.quake 921, kiosk.say.tonight 930, kiosk.say.tonightMore_* 932-934, kiosk.front.tomorrowCity 944, kiosk.ticker.* 964-970, kiosk.safety.basics 980, kiosk.safety.confirmed 983 ('DHMZ · EMSC · {time}' — fetch/confirmation time off the wall; frame.ts:55 always prints kiosk.safety.sources), kiosk.session.join 1002, kiosk.session.joinHint 1003, kiosk.paired.delays 1029, kiosk.paired.fetchedAt 1051 and kiosk.paired.dataFrom 1050 (paired statusLine prints only the stale word, step 8), kiosk.paired.coverageLines_* 1056-1058, kiosk.settings.saved 1119, kiosk.lines.modelNote 896. ADD (hr / en): `sentence.kicker.{promet:'Promet',kultura:'Kultura',vrijeme:'Vrijeme',bicikli:'Bicikli',nocas:'Noćas',radovi:'Radovi'}` (en Transit/Culture/Weather/Bikes/Tonight/Works) — replaces kiosk.ticker.*; `nearby.title` 'U blizini' / 'Nearby'; `nearby.pill` '{km} km · ~{min} min' (rendered '2 km · ~15 min' [O-30]; WP2 fills km/min from the frame); `nearby.pillLabel` 'Krug od {km} km, oko {min} minuta' (aria, [A]); `nearby.always` 'uvijek' / 'always'; `nearby.lastTonight` 'Zadnji polasci večeras' / 'Last departures tonight'; `nearby.firstMorning` 'Prvi jutarnji tramvaj' / 'First morning tram'; `nearby.sunset` 'Zalazak sunca', `nearby.sunrise` 'Izlazak sunca' (only the next one is rendered [O-48]); `nearby.pharmacy` '24/7'; `nearby.pharmacyLabel` 'Dežurna ljekarna 24/7: {address}' (aria for the green cross, [O-39]); `nearby.opens` 'otvara {time}' / 'opens {time}' ([A], tomorrow's openings [O-40]); `nearby.zetDown` 'ZET trenutačno ne šalje položaje vozila; vremena su po voznom redu.' ([A], the one quiet map note [O-45]); `arrivals.departing` 'kreće' / 'departing' (the ≤30 s row word, replaces arrivals.now 'sada' on the U blizini rows — WP2/WP4 decide per surface; keep arrivals.now for the stop detail); `arrivals.scheduled` value change 'po redu vožnje' → 'vozni red' / 'timetable'; `time.untilDate` 'do {date}' / 'until {date}' (shared 'do 25. 9.' [O-53]; replaces kiosk.paired.ongoingUntil — repoint paired.ts to it and delete the kiosk copy); `kiosk.legend.{tram,bikes,venues}` (step 2); `kiosk.handheld.info` (step 2); `kiosk.setup.field` 'Adresa ili stajalište' / 'Address or stop'; `kiosk.setup.preview_one` 'Na zaslonu: {place} i {count} stajalište uokolo', `_few`/`_other` '… {count} stajališta uokolo' / en 'The wall shows {place} and {count} stops around it'; `kiosk.setup.previewCity` 'Na zaslonu: cijeli grad' ([O-52] empty field); `kiosk.setup.create` value change 'Pokreni zaslon' → 'Pokreni' / 'Start'; `kiosk.settings.place` 'Mjesto' / 'Place'; `kiosk.settings.frame` 'Kadar' / 'Frame'; `kiosk.settings.frameValue_one` '{count} stajalište odavde', `_few`/`_other` '{count} stajališta odavde' / en '{count} stops from here' (4/6/8 [O-37],[O-43]); `kiosk.settings.rhythm` 'Ritam' / 'Rhythm' (toggle label; the 20 s value is fixed [O-14] — no value strings); `kiosk.settings.longPress` 'Drži za postavke' / 'Hold for settings' ([A], the only hint the wall may carry, aria-only); `directory.saved/savedEmpty/savedRecord/kindRoute/kindStop/remove/removeLabel/eventsWeek/preferences`, `location.device/area`, `city.facts.*`, `city.airIndex.*`, `city.airNote`, `city.airMethod`, `city.consultationsNote`, `city.bikes_*`, `city.bikesUnknown`, `weather.reference`, `civic.consultations` (step 2); `session.expired` value → 'Deset minuta je prošlo. Zaslon u blizini otključava novih 10 minuta.' and `session.expiredHint` → 'Sigurnost ostaje otvorena na /hitno.' ([O-59]: no frozen view, no exports — value edits pinned by i18n.test.ts:82; owner to confirm wording). KioskStrings (app/src/kiosk/strings.ts): remove `header.temporaryUntil/venue` (:22,351), `invitation.codeWaiting` (:39,356), `weather.station` (:60,358), `lines.modelNote` (:96,364), `front.tomorrowCity` (:119,371), `ticker` (:136-144,379), `say.transitRegular/quake/tonight/tonightMore` (:150-159,381-384), `safety.basics/confirmed` (:181,185,387), `session.join/joinHint` (:193-194,394), `paired.delays/coverageLines/dataFrom/fetchedAt/ongoingUntil` (:205,220,226-227,233,398-405), `settings.saved` (:295,420); add `legend: group('legend',['tram','bikes','venues'])`, `handheld: group('handheld',['info'])`, `setup.field/previewCity` + `preview: forms('setup','preview')`, `settings.place/frame/rhythm/longPress` + `frameValue: forms('settings','frameValue')`, `sentence: { kicker: record([...six], k=>`sentence.kicker.${k}`) }`, `nearby: { …t('nearby.*') }`. Tests: i18n.test.ts:57 (`hr.kiosk.ticker.transit`) → `expect(hr.sentence.kicker.promet).toBe('Promet')`; extend DEAD_KEYS (:21-43) with every deleted leaf above plus 'kiosk.ticker.weather','cityOverview.title','timeband.next'; :82 verbatim `session.expired` → the new sentence. copy-guards.test.ts:83-98: drop safetyOpen from SHARED_HR and TWINS, :116 drop overview.allClear. NEW test/app/i18n-orphans.test.ts: walk app/src (*.ts) + app/*.html + app/*/index.html; collect references from `t('k')`, `i18n.t("k")`, `tr(x,'k')`, `trPlural(x,'k')`, `ct(x,'k')`, `group('g',[...])`, `forms('g','b')`, `record([...],k=>`g.${k}`)` (resolve to prefixes), `data-i18n*="k"`, and template prefixes `t(`prefix.${`; every hr.json leaf (plural suffix stripped) must be referenced exactly or fall under a referenced prefix or the allowlist {events.category., events.sources., kiosk.events., panels.severity., panels.closureType., panels.direction., motion.compass., layers., landing.alt., scan.errors., scan.venue., common.theme., kiosk.header.themeWord., kiosk.safety.verdict., weather.points., freshness., timeband.seg., presentation.}; assert the unreferenced list `toEqual([])`.

### 4. Delete the kiosk dead paths that have no consumer today: header ticker, corner-qr/join-code, teaser exports, overlay CSS; fix the raw-key QR label  `[M]`

Files: `app/src/kiosk/ticker.ts`, `app/src/kiosk.ts`, `app/src/ui/kiosk.css`, `app/src/kiosk/teaser.ts`, `app/src/layers/grad-teaser.ts`, `app/src/kiosk/local.ts`, `app/src/dashboard.ts`, `test/app/ticker.test.ts`, `test/app/kiosk.test.ts`, `test/app/kiosk-css.test.ts`, `test/app/kiosk-local.test.ts`, `test/integration/feed-to-layers.test.ts`, `test/app/dashboard.test.ts`, `scripts/audit-production.mjs`

(a) `git rm app/src/kiosk/ticker.ts test/app/ticker.test.ts`; kiosk.ts:66-67 delete TICKER_SWAP_MS (export) and rename `paintTicker` → `paintAmbient` at :254,309,694,1265 (the highlight sequencer WP2 replaces; the name stops lying); kiosk.css delete :221-225 (.k-ticker*, @keyframes k-ticker-in), the `--k-ticker-size` tokens :120,151,742, and the `.k-ticker[data-swap='1']` members of :714,718; fix the comment :34. kiosk.test.ts: delete import :20 and the seven `kiosk-ticker` null-assertions :753,809,819,827,832,907,1137 (and reword the `it` titles at :709,789,814,1343 that say 'ticker'); kiosk-css.test.ts:63-69 → one assertion `expect(css).not.toMatch(/k-ticker/)`. (b) corner-qr/join-code: kiosk.ts delete :718-719,735-736,750-752 and the `joinCode` argument of `swapCode`; kiosk.css delete :435,685 and the `.k-join-code` members of :714,718; scripts/audit-production.mjs:250 → `for (const tid of ['pair-code'])`. (c) kiosk.css:275 `.k-lines--overlay` delete. (d) teaser: `git rm app/src/kiosk/teaser.ts`; kiosk.ts:54 delete the re-export; grad-teaser.ts: keep only `cityTeaserAttribution` (move it into kiosk/local.ts next to CITY_SOURCE and delete the file, updating local.ts:14) — this also removes the inert missing-key call `kiosk.teaserPublished` (:44); kiosk-local.test.ts: delete import :26 and lines :283-292; feed-to-layers.test.ts: delete import :20 and the four `it` blocks at :183-220 (keep the layer tests). (e) Live defect §0.1: dashboard.ts:846 `i18n.t('kiosk.qrLabel', …)` → `i18n.t('kiosk.invite.qrLabel', …)`; add to dashboard.test.ts (share dialog describe) `expect(qr.getAttribute('aria-label')).toMatch(/^QR kod za otključavanje/)`.

### 5. Delete the phone/desktop dead paths: FAB slot, legacy cast, timeband render + swipe sync, scan confirm card, resultsMarkup, search launcher, bell, lightweight Karta face  `[L]`

Files: `app/src/experience/chrome.ts`, `app/src/dashboard.ts`, `app/src/ui/dashboard.css`, `app/src/experience/timeband.ts`, `app/src/experience/timeband-sync.ts`, `app/src/ui/layers.css`, `app/src/transport/view.ts`, `app/src/scan.ts`, `app/src/ui/scan.css`, `app/src/layers/u-pokretu.ts`, `test/app/dashboard.test.ts`, `test/app/shell-css.test.ts`, `test/app/timeband.test.ts`, `test/app/timeband-sync.test.ts`, `test/app/scan.test.ts`, `test/app/layers.test.ts`, `e2e/cast.spec.ts`

FAB: chrome.ts:193-200 delete `fabMarkup`; dashboard.ts:24 drop the import, :260 drop the `ki-fab-slot` region and `regions.fab`, :362-364 drop the paint and `dataset.fab`; dashboard.css delete :60, :132-140, the `.ki-fab` member of :202 and :216; dashboard.test.ts:1046 order → ['ki-head ki-status','ki-presentation','ki-banners','ki-main','ki-tabbar'], delete :1053 and :1055; shell-css.test.ts delete :129-131, :158-168, the '.ki-fab' member at :197, :215, :309, :332; e2e/cast.spec.ts:19 delete. Cast legacy: dashboard.ts delete :57 CAST_SENT_MS, :212-214 castSentAt/castTimer, `castSent`/`sentAt` at :299,:401, :875, `case 'cast'` :1008, :1140; transport/view.ts delete :347-351 castButton and, if then unused, the `cast?: CastState` members at :42,387,437,492,616 and the `castReasonText` import (presentation.ts keeps `CastState`/`castReasonText` for the panel). Timeband: timeband.ts delete :48 NEXT_CAP, :211-213 `Lane.next`, :390-393, :419-465 renderTimebandSeg, :467-516 laneBody/renderTimeband (keep buildTimeband, columnsFor, bucketOf, zagrebInstant, tickTimebandClock, FILTER_KEY — used by grad-sada.ts:15, paired.ts:27, dashboard.ts:28,1124); `git rm app/src/experience/timeband-sync.ts test/app/timeband-sync.test.ts`; dashboard.ts:29 import and :1071 attach (+ the `detachTimebandSync()` call in destroy) delete; layers.css delete :201-225 (.tb-heads/.tb-lanes/.tb-axis/.tb-head/.tb-h/.tb-clock/.tb-weather…; grep `\.tb-` in app/src/ui first and delete every orphan, keeping `[data-testid=tb]`-independent `.day-*` rules); timeband.test.ts: import :11 drop renderTimeband/renderTimebandSeg, delete describes :488-513 ('Zatim' foot), :515-537, :539-671; rewrite the tickTimebandClock setup at :676 with a hand-written `<time class="day-clock">` root. Scan confirm: scan.ts delete :82-108 (sentenceCase, confirmLabel, confirmStopLine), :202-208 hideConfirm, :276-299 renderConfirm and the `confirmBox` element/`aria-labelledby` in :142-171; scan.css delete :128-144; scan.test.ts delete :692-693 and any `confirmLabel`/`confirmStopLine` unit tests (grep). resultsMarkup: view.ts delete :200-232 (`ResultsData`, `resultsMarkup`). Search launcher/bell: chrome.ts delete :142-145 searchLaunchMarkup and :160-164 bellMarkup (notify.bellLabel stays for directory.ts:117); dashboard.ts:613-617 `openSearch` → query `[data-testid=transport-search]` only. Lightweight face: u-pokretu.ts delete the branch :87-90 and :135-238 (renderLightweight, LIGHT_FILTER, fold helpers); layers.test.ts delete the lightweight-face describe (~:380-413, the `light.querySelector` block); document in DESIGN.md 'Lagani prikaz' that the phone's lightweight mode keeps the full sheet without a map slot (the shipped behaviour).

### 6. Vocabulary sweep: one word per concept across hr.json/en.json values and the code that overrides them  `[M]`

Files: `app/src/i18n/hr.json`, `app/src/i18n/en.json`, `app/src/experience/chrome.ts`, `app/src/transport/view.ts`, `app/src/izvori-render.ts`, `test/app/copy-guards.test.ts`, `test/app/dashboard.test.ts`, `test/app/transport-workspace.test.ts`, `test/app/entries-kiosk.test.ts`, `e2e/experience.spec.ts`

Transport destination = Karta: hr.json layers.u-pokretu (68) 'Promet' → 'Karta' (en 'Transit' → 'Map'); delete chrome.ts:86-89 `layerLabel` override (`ct('map')` dies; tabs, document title dashboard.ts:379, presentation label presentation.ts:40 and the kiosk session pill kiosk.ts:395 all read layers.u-pokretu); transport.mapRegion (433) 'Karta prometa' → 'Karta'; transport.sheetLabel (466) 'Detalji prometa' → 'Detalji'; kiosk.paired.overviewTransport (1045) 'Promet oko stanice' → 'Karta oko stajališta'; izvori-render.ts:70 'Prijevoz' → 'Promet' (subject word, allowed). 'Promet' survives only as a subject word: sentence.kicker.promet, kiosk.say.transit (paired kicker), landing.liveTransit 'Promet · ZET', landing.alt.transport; landing.evidence.domains (707) → 'Sada · Karta · Vrijeme · Sigurnost · Grad · Događanja' ([A], keeps the six subjects, names the tab). Stop = stajalište (tram/bus; 'stanica' only for BAJS, 'postaja' only for DHMZ/air): edit values at hr.json 374 'Sa stajališta {stop}', 395 '…ime stajališta…', 419 'Stajalište', 420 'Stajališta na liniji', 421 'Linije na stajalištu', 427 'Stajalište zaslona', 429-430 '…stajališta…', 435-437 'još {count} stajalište/stajališta/stajališta', 441 'stoji na stajalištu', 461 'sva stajališta ({count})', 469 'Sada u pokretu na linijama ovog stajališta', 470 '…linija s ovog stajališta…', 487 'Spremi stajalište {name}' and fix view.ts:341 to pass the stop NAME as {name} (id leak [B-7]); 500 '…stajalište {stop}', 569 '…stajalište {stop}.', 571 'Sa zaslona na stajalištu {stop}.', 883 'Linije s ovog stajališta', 1006 'Stajalište {stop}', 1090-1094 and 1104,1107 stajalište forms (or deleted by WP3's one-field setup); en.json unchanged ('stop'). Estimate words: arrivals.scheduled → 'vozni red'; delete tiles.scheduled (169, 'po rasporedu · ZET GTFS') and repoint producers/last-run.ts:57 to arrivals.scheduled; view.ts:548 and next-departures.ts:22 'Procjena' → `arrivals.live` rendered visually hidden beside the blue countdown (`<span class="visually-hidden">uživo</span>`), the grey clock row carries `arrivals.scheduled` the same way; the only remaining 'Procjena' in the catalogue is arrivals.note (phone stop detail). Now: weather.now (181) 'Trenutno' → 'Sada'. Ten minutes as digits ([A], touches i18n.test.ts:79-83 'approved copy verbatim' — ask the owner before applying): session.expiredCta 'Skeniraj za novih 10 minuta', scan/landing 'Deset minuta' → '10 minuta'; kiosk.invitation stays verbatim. Add to copy-guards.test.ts a 'vocabulary' describe: `expect(hr.layers['u-pokretu']).toBe('Karta')`; `expect(JSON.stringify(hr)).not.toMatch(/Kretanje|Prijevoz i raspored|Živi grad|Sada u gradu\.|Grad oko tebe|Upoznaj ovo mjesto|Podatak iz registra|Obuhvat zaštite|Zatim"|ZET GTFS|Vozni red od|Dohvaćeno /)`; `expect(hr.arrivals.scheduled).toBe('vozni red')`; every hr leaf matching `/\b[Ss]tanic/` must be under an allowlisted key set (BAJS wording in city.*) — target: 0 leaves outside it; `expect(leafKeys(hr).filter(k=>leaf(hr,k)!.includes('Procjena'))).toEqual(['arrivals.note'])`. Tests to adjust: dashboard.test.ts:726,737 and transport-workspace.test.ts:142,201,225, entries-kiosk.test.ts:87,90, map.test.ts:95, overlays.test.ts:89,109, shell-css.test.ts:115,117, e2e/experience.spec.ts:139 wherever they assert the tab/label 'Karta' via ct('map') — they now read `hr.layers['u-pokretu']`.

### 7. Multi-day items say 'do {date}', never 'cijeli dan' (§13 #22)  `[S]`

Files: `app/src/layers/kultura.ts`, `app/src/experience/tiles.ts`, `app/src/kiosk/front.ts`, `app/src/kiosk/paired.ts`, `test/app/layers.test.ts`, `test/app/tiles.test.ts`

kultura.ts:198-203 `timeCell`: when `item.data?.precision==='range'` or `multiDay` (computed at :222-223) print `i18n.t('time.untilDate',{date: zagrebDayMonth(item.until)})` in the lead cell instead of time.allDay, and drop the duplicate 'do …' from the sub-line (:224); single-day day-precision keeps 'cijeli dan'. tiles.ts:96,111,117: `timeTextOf`/`tileAria` — a tile with `allDay && until` on a later day prints time.untilDate; producers/events.ts passes `until`. front.ts:143 (paired events) and paired.ts:725-728 same rule via `s.paired` reading time.untilDate (delete kiosk.paired.ongoingUntil, step 3). Tests: layers.test.ts:238-244 (kvartovske day-precision notice stays 'cijeli dan' — unchanged) and :668 (a day-precision entry — unchanged); add a range case in tiles.test.ts next to :85-87 and :157-160 asserting `do 25. 9.`; the detail view text.ts:70-80 already prints a range — unchanged.

### 8. Disclaimers, caveats and fetch times off every wall surface (invitation and paired)  `[M]`

Files: `app/src/kiosk/paired.ts`, `app/src/kiosk/front.ts`, `app/src/kiosk/markup.ts`, `app/src/kiosk/frame.ts`, `app/src/kiosk/essentials.ts`, `app/src/city/markup.ts`, `app/src/motion/schema-map.ts`, `app/src/motion/schematic-host.ts`, `app/src/transport/workspace.ts`, `app/src/kiosk.ts`, `test/app/kiosk.test.ts`, `test/app/kiosk-front.test.ts`, `test/app/kiosk-local.test.ts`, `test/motion/schema-map.test.ts`, `test/motion/schematic-host.test.ts`

paired.ts:94-103 `statusLine` → return `s.paired.stale` for a stale snapshot, '' otherwise (no 'dohvaćeno {time}', no 'podaci od {time}'); :415 delete the `s.arrivals.note` foot; :454 place detail is rendered with `publicDisplay=true` → city/markup.ts:67 prints the source line only, never `ct('reference')` (the sentence moves to /izvori, step 9), and :58 `siteNote` is deleted outright; :510 delete `panels.sunComputed` on the wall (and the key if then unreferenced). front.ts:387 delete the `arrivals.note` foot; :389 the kiosk-lastrun foot goes with tiles.scheduled (WP2 owns the last-departures row); :420 credit `ZET ${time}` → 'ZET'; :245 weather meta 'DHMZ · HH:MM' → 'DHMZ' (observation time is honest but is a time stamp on the wall; the panel itself is retired by WP2). kiosk/markup.ts:105 delete the `modelNote` line under the lines board (key deleted in step 3). frame.ts:55 always `strings.safety.sources` ('DHMZ · EMSC'); the confirmedAt plumbing in frameStrip (:33-37) can stay for the phone. essentials.ts:33 staleMark: keep the stale word, drop any time. kiosk.ts:704 `coverage` 'prikazano N od M' on the invitation: delete (whole rows only [O-22]; paired fitRows keeps its own). motion.note on the wall: schema-map.ts:78,175 and schematic-host.ts:27,85 print `HONESTY_NOTE_HR` only when not `kiosk`/public-display (add the flag to their options; the phone keeps it inside the vehicle detail view.ts:~365 and not as the sheet foot — workspace.ts:635 foot removed [A]). Guard: new test in kiosk.test.ts 'the wall carries no disclaimer, caveat or fetch time' — mount the invitation with the standard fixture and paired Sada/Promet/Vrijeme, take `text(q(root,'.k-overview'))`, `text(q(root,'[data-testid=safety-strip]'))` and each paired aside, assert none matches `/Podatak iz registra|Obuhvat zaštite|Procjena|Vozni red od|Zapis od|[Dd]ohvaćeno|podaci od \d|potvrđeno \d|Izračunato na uređaju|ne procjena dolaska|ostalo po voznom redu|Položaj (je|vozila)|\b\d\d:\d\d · (DHMZ|ZET)\b|nepotvrđeno\)|\?/`. Update kiosk-front.test.ts:107 (footMarkup no longer carries the note → `toBeUndefined()`), :128 stale credit → 'ZET · zastarjelo', kiosk-local.test.ts and kiosk.test.ts assertions that expect 'Dohvaćeno'/'Vozni red od'/'Procjena: 3 min' (:2060) — these compositions are rewritten by WP2; where WP2 has landed, apply the guard to WP2's markup instead.

### 9. Composition-dependent retirements after WP2/WP3/WP4 land: remove the strings and CSS their rewrites orphan, retarget the e2e specs, move the register sentence to /izvori  `[M]`

Files: `app/src/i18n/hr.json`, `app/src/i18n/en.json`, `app/src/city/strings.ts`, `app/src/kiosk/strings.ts`, `app/src/ui/overview.css`, `app/src/ui/kiosk-city.css`, `app/izvori/index.html`, `test/app/i18n.test.ts`, `test/app/layers.test.ts`, `e2e/kiosk-layout.spec.ts`, `e2e/city.spec.ts`, `e2e/a11y.spec.ts`, `e2e/experience.spec.ts`, `e2e/readable-city.spec.ts`, `e2e/round-f.spec.ts`, `scripts/capture-landing.mjs`, `scripts/review-experience.mjs`

After WP4 (phone Sada/Karta/Još): delete cityOverview.* (hr 1171-1176, en 1134-1139) and layers.test.ts:119's special case (the Sada title is the place, `data-testid=place-title`), timeband.next (144) and, if WP4 drops the segment buttons, timeband.seg.* (130-138) and timeband.segLabel; the ct browse keys listed in step 1 (all, city, movement, useful, network, layers, list, here, locate, legend, streetBrowse, cluster, allVenues, activeVenues); overview.css:71 `.day-stop-prompt`; `.city-filter-disclosure`/`.city-group`/`.city-times` rules in workspace CSS; kvart.saveRoute/saveStop/unsave if WP4 renames the star labels. After WP3 (setup): delete kiosk.setup.intro/validity/search/results_*/noResults/routesAt/loadingStops/errorStops and kiosk.settings.area/areaWhole/areaHint/stop/stopNone/stopHint/open/hint as WP3's screens stop reading them (check with the orphan test, step 3). After WP2 (wall): delete kiosk.basics.* + kiosk.safety.openBasics if Osnovno is replaced by read-only touch [O-58]; kiosk.front.eventsNone/linesRegular/closures_*/notices_*/moreLate_* and kiosk.say.transitNoData/nearbyNone/nearby_* if the paired Promet card no longer renders exceptions; kiosk.weather.* leaves the invitation stopped reading. Register sentence: add one paragraph to app/izvori/index.html after the intro (line 18) 'Podaci gradskog kataloga (kultura, baština, voda, WC, tržnice …) su iz registara, ne provjera uživo; obuhvat zaštite baštine označava zaštićeno područje, ne ulaz.' — the one place §13 #13 allows it. e2e: kiosk-layout.spec.ts:66-77 → assert under the stale fixture that every `[data-testid=nearby-row][data-kind=departure]` has `data-time="timetable"` and no `.k-panel-credit` exists (testids to match WP2's brief; if WP2 names them differently use WP2's); city.spec.ts:26 delete the 'Obuhvat zaštite' assertion, :27-31 follow WP4's Karta (search-only path: `search.fill('BAJS')`); a11y.spec.ts:241 → `toContainText('BAJS: broj bicikala')` and `not.toContainText('?')`, :268-270 follow WP4; experience.spec.ts:65 → WP4's section labels; readable-city.spec.ts:141-145 pause-highlights → delete (no operator control [O-43]); round-f.spec.ts:96 and scripts/review-experience.mjs:116-121, scripts/capture-landing.mjs:181-184 → the Karta cold open already draws vehicles; drop the 'Kretanje' group click. Re-run the orphan test (step 3) — it is the arbiter of what else died.

### 10. Rewrite the four documents to the §11 contracts and date the approval  `[M]`

Files: `PRODUCT.md`, `DESIGN.md`, `docs/kiosk.md`, `app/src/kiosk/INTEGRATION.md`, `app/src/kiosk/front.ts`, `app/src/kiosk/layout.ts`, `test/docs/docs.test.ts`

PRODUCT.md: insert before line 44 a new paragraph 'On 22 September 2026 the owner approved the companion round (docs/companion-2026-09-22.md §10–§13). This supersedes below: the one map-linked highlight held for 20 seconds and its pause control; the whole-city default window; the one-button start and the gear; tap-to-explore and the 90-second reset; the frozen attributed snapshot with exports at the end of the ten minutes; Sada, Karta, Događanja and Još; "tracked estimates are labelled on every row". Now: the wall frames 6–7 stops around a place named in the header; one written sentence with a coloured kicker changes every 20 seconds (≤ 80 characters, no ellipsis); the right column is "U blizini · 2 km · ~15 min", one time-ordered list of at most three departures (blue countdown = tracked vehicle, grey clock time = timetable, an accessible word for each, never the word "procjena"), timed rows and one "uvijek" row; no fetch time, disclaimer, count or operator control on the wall; setup is one field "Adresa ili stajalište" and Pokreni; settings are click-toggles behind a long press; touch, where present, is read-only and the wall returns by itself; the phone opens on the place, one sentence and three departures, tabs Sada · Karta · Još, "Podijeli grad" as a labelled header button; after ten minutes the content clears to the scan invitation and /hitno. Vocabulary: Karta (destination), Promet (subject), stajalište, uvijek, vozni red.' Keep lines 44-114 as history but prefix their first sentences with 'Superseded 22 September 2026:' where they conflict (46-53, 62-69, 71-76, 80-87, 96-97, 104-105, 112). DESIGN.md: replace 78-99 ('20 September passive-view contract') with the §11 wall contract (header, map frame, U blizini rules, footer, night, outage, read-only touch, no chrome); mark 101-155 as history and strike 'The other column carries weather, a transit-exceptions card…', 'The header carries one line of city news…replaced on a fixed period' (now 20 s, ≤ 80 chars), 'Stops stay tap-able…hit tolerance…' (touch is read-only, 60 s), 'Touch exploration has search, categories…90-second inactivity'; 171-174 → 'Phone destinations are Sada, Karta and Još; desktop is the phone, wider: Sada feed and Karta side by side, no six-domain bar'; 180-192 → one search field, no disclosures, no filter strip; 194-197 → Još per [O-60]; 199-205 → 'no cluster marks, no unlabelled dots; every BAJS station shows its count; categories appear only as search results and pins once searched'; 207-210 → 'Zaslon and Podijeli grad are two labelled header buttons beside the timer'; 219-222 → 'Preserve ten-minute direct sessions, five-minute one-hop peer sessions, resume behaviour and no-JavaScript /hitno; at the end of ten minutes the content clears to the scan invitation and the /hitno link (no frozen view, no exports)'; 228-231 → colour + accessible word rule. docs/kiosk.md: replace 3-24 with 'Javni zaslon, odobreno 22. rujna 2026.' (the §11 wall in Croatian: mjesto u zaglavlju, jedna rečenica svakih 20 s, U blizini · 2 km · ~15 min, najviše tri polaska, plava = praćeno vozilo, siva = vozni red, red "uvijek", bez vremena dohvata i napomena, bez gumba za posjetitelja, dodir samo za čitanje i vraća se sam nakon 60 s); steps 2-3 (36-62) → one field 'Adresa ili stajalište' + Pokreni; postavke dugim pritiskom kao preklopni gumbi Mjesto · Kadar (4/6/8 stajališta odavde) · Tema · Ritam; 79-163 rewrite (frame 6–7 stajališta oko mjesta; delete 'Radovi u gradu', 'tri ploče', 'svakih 8 sekundi', 'Dodir na stajalište otvara istraživanje', '90 sekundi bez dodira'); 188-197 Osnovno → dodir samo za čitanje (or delete if WP2 removes Osnovno); 199-231: delete the rule that prints 'Procjena iz ZET-ovih podataka o vozilima; ostalo po voznom redu.' under rows, add 'plava/siva' and 'nema vremena dohvata na zidu', keep 'Ništa se ne izmišlja'; 331-334 troubleshooting rows → dugi pritisak. Keep verbatim the phrases test/docs/docs.test.ts:48-60 asserts: 'POST /api/screens', 'Isti Wi-Fi je dopušten.', 'APP_ENV=test', 'E2E_ADMIN_BYPASS', '24 sata', 'git push', 'fizička matrica'; never reintroduce 'Ovaj zaslon i tvoj telefon dijele istu mrežu.'. INTEGRATION.md: 85-96 testids → drop kiosk-ticker, kiosk-lastrun and 'three panels'; name WP2's `nearby-list`/`nearby-row`; 131-137 → 'Kiosk copy lives in i18n/hr.json/en.json under kiosk.* and is read through kiosk/strings.ts (typed, compile-checked); the old kiosk.* keys are gone'; 139-149 'Header' → the model-written sentence per §11 (20 s, ≤ 80 characters, kicker from sentence.kicker.*), settings behind a long press. Code comments: front.ts:1-15 ('five panels' → paired compositions only), layout.ts:11-15 (handheld claim), kiosk.css:34. Extend test/docs/docs.test.ts: docs/kiosk.md must not contain 'svakih 8 sekundi', 'gumbom za zaustavljanje', 'Dodir na stajalište otvara istraživanje grada', 'Radovi u gradu', 'Procjena iz ZET-ovih podataka o vozilima; ostalo po voznom redu.', 'zupčanik'; must contain 'U blizini' and 'Adresa ili stajalište'; PRODUCT.md must contain '22 September 2026'; DESIGN.md must contain 'U blizini' and not contain 'one map-linked highlight held for 20 seconds', 'geographic clusters use a distinct plus-count mark', 'frozen attributed exports', 'Touch exploration has search'.

## Parallel lanes

Phase A runs now, in parallel, on disjoint files: A1 = steps 1+2+3 as ONE agent (they all edit hr.json/en.json and kiosk/strings.ts; splitting them creates JSON merge conflicts) · A2 = step 4 (kiosk dead paths: kiosk.ts, kiosk.css, teaser/grad-teaser, kiosk tests, audit script) · A3 = step 5 (phone dead paths: chrome.ts, dashboard.ts, dashboard.css, timeband.ts, view.ts, scan.ts, u-pokretu.ts and their tests) · A4 = step 10 (documents and comments) · A5 = step 7 (all-day → do {date}; small, touches kultura.ts/tiles.ts/front.ts/paired.ts only). A2 and A3 both touch dashboard.ts (A2: line 846 only; A3: FAB/cast/timeband lines) — sequence A2 before A3 or give the one-line fix to A3. Step 6 (vocabulary values) runs after A1 merges (same JSON files) and in parallel with A2-A5. Phase B = steps 8 and 9, sequential after WP2/WP3/WP4 merge (they rewrite invitation.ts, kiosk.ts, workspace.ts, grad-sada.ts, directory.ts, settings.ts); the orphan test from A1 is the arbiter of what B deletes. Never run steps 4/8 and WP2 concurrently on kiosk.ts/invitation.ts.

## Tests to update

- test/app/i18n.test.ts:21-43 (DEAD_KEYS += the 77 orphans, kiosk.ticker.*, cityOverview.*, timeband.next), :56-60 (hr.kiosk.ticker.transit → hr.sentence.kicker.promet), :78-84 (session.expired new verbatim)
- test/app/copy-guards.test.ts:83-118 (drop shared.safetyOpen from SHARED_HR/TWINS, drop overview.allClear pin; add the ternary guard, the vocabulary describe, the 'Procjena only in arrivals.note' rule)
- test/app/i18n-orphans.test.ts (new: every hr.json leaf referenced; unreferenced list toEqual([]))
- test/app/ticker.test.ts (delete)
- test/app/timeband-sync.test.ts (delete)
- test/app/timeband.test.ts:11,480-513,515-537,539-671,676 (drop renderTimeband/renderTimebandSeg tests; hand-written root for tickTimebandClock)
- test/app/kiosk.test.ts:20,753,809,819,827,832,907,1137 (ticker), :166-186 and :1343-1362 and :709-760 (invitation composition — WP2 rewrites; WP5 adds the 'no disclaimer/fetch time on the wall' guard), :2049-2067 (configuredBoard), :1090 (Osnovno rows if removed)
- test/app/kiosk-css.test.ts:63-69 (→ expect no .k-ticker)
- test/app/kiosk-front.test.ts:107,128 (no arrivals.note foot; credit 'ZET · zastarjelo')
- test/app/kiosk-local.test.ts:26,283-292 (teaserCards/safetyStripText removed)
- test/integration/feed-to-layers.test.ts:20,184-220 (teaser tests removed), :96 (.day-stop-prompt gone after WP4)
- test/app/dashboard.test.ts:1046,1053,1055 (no ki-fab-slot / dataset.fab), :726,737 (Karta label from layers.u-pokretu), new share-QR aria-label assertion; keep :1323-1360 loadLastRun (the phone still fetches lastRun)
- test/app/shell-css.test.ts:129-131,158-168,197,215,309,332 (.ki-fab rules gone)
- test/app/scan.test.ts:692-693 (+ confirmLabel/confirmStopLine tests) (scan confirm removed)
- test/app/layers.test.ts:119 (Sada title = place after WP4), ~380-413 (lightweight face describe removed), :238-244,668 (unchanged: day-precision stays 'cijeli dan')
- test/app/tiles.test.ts:85-87,157-160 (+ new range case 'do 25. 9.')
- test/app/transport-workspace.test.ts:142,201,225 and test/app/entries-kiosk.test.ts:87,90, test/app/map.test.ts:95, test/app/overlays.test.ts:89,109, test/app/shell-css.test.ts:115,117 (tab/label 'Karta' now from hr.layers['u-pokretu'])
- test/motion/schematic-host.test.ts:9,70-71,127 (HONESTY_NOTE_HR now equals hr.motion.note; passes unchanged) and test/motion/schema-map.test.ts (note hidden when kiosk)
- test/city/readable.test.ts:9 (bikeCount via plurals; '—' never '?')
- test/docs/docs.test.ts:48-60 (+ new assertions on docs/kiosk.md, PRODUCT.md, DESIGN.md)
- e2e/kiosk-layout.spec.ts:66-77 (kiosk-panel-promet → nearby rows data-time=timetable under the stale fixture)
- e2e/cast.spec.ts:19 (cast-fab assertion removed)
- e2e/city.spec.ts:26-31 (siteNote gone; browse chrome per WP4)
- e2e/a11y.spec.ts:241 (legend 'BAJS: broj bicikala', no '?'), :268-270 (disclosure gone per WP4)
- e2e/experience.spec.ts:65 (section labels per WP4), :139 (Karta label)
- e2e/readable-city.spec.ts:141-145 (pause-highlights removed)
- e2e/round-f.spec.ts:96, scripts/review-experience.mjs:116-121, scripts/capture-landing.mjs:181-184 (no 'Kretanje' group click), scripts/audit-production.mjs:250 (['pair-code'])

## Acceptance (executable)

| # | Criterion | How (command / threshold) |
|---|---|---|
| A1 | One copy catalogue: the city adapter carries no Croatian literals and no bilingual ternary survives outside app/src/i18n | `grep -cP '[čćžšđ]' app/src/city/strings.ts` prints 0; `grep -rnE "(\ben\|english\|isEn)\s*\?\s*['\"\`]\|getLocale\(\)\.startsWith\('en'\)\s*\?\s*['\"\`]" app/src --include=*.ts \| grep -v '^app/src/i18n/' \| wc -l` prints 0; `npx vitest run test/app/copy-guards.test.ts` passes |
| A2 | No orphan key in either catalogue and hr/en key parity | `npx vitest run test/app/i18n-orphans.test.ts test/app/i18n.test.ts` passes (unreferenced leaves toEqual([]); DEAD_KEYS absent); `node -e` flat-leaf count of hr.json ≤ 1045 − 77 + new keys (report the number in the PR) |
| A3 | KioskStrings tree compiles against the trimmed catalogue and the worker/app typecheck is clean | `npm run typecheck` exits 0 |
| A4 | Dead files and selectors are gone | `for f in app/src/kiosk/ticker.ts app/src/experience/timeband-sync.ts app/src/kiosk/teaser.ts test/app/ticker.test.ts test/app/timeband-sync.test.ts; do test -e $f && echo LEFT $f; done` prints nothing; `grep -rnE 'corner-qr\|join-code\|k-ticker\|k-join-code\|k-lines--overlay\|ki-fab\|\.tb-(heads\|lanes\|axis\|head\|h\|clock\|weather)\b\|scan-confirm\|renderTimeband\|attachTimebandSync\|fabMarkup\|resultsMarkup\|searchLaunchMarkup\|bellMarkup\|renderConfirm\|renderLightweight\|castButton\|configuredBoard\|teaserCards\|safetyStripText\|kiosk\.qrLabel' app/src scripts e2e test --include=*.ts --include=*.css --include=*.mjs \| wc -l` prints 0 |
| A5 | Vocabulary: one word per concept in the Croatian catalogue | `node -e "const hr=require('./app/src/i18n/hr.json');const s=JSON.stringify(hr);for(const w of ['Kretanje','Prijevoz i raspored','Živi grad','Sada u gradu.','Grad oko tebe','Upoznaj ovo mjesto','Podatak iz registra','Obuhvat zaštite','ZET GTFS','Vozni red od','Dohvaćeno ','po redu vožnje'])if(s.includes(w))throw w;if(hr.layers['u-pokretu']!=='Karta')throw 'layers';if(hr.arrivals.scheduled!=='vozni red')throw 'scheduled';console.log('ok')"` prints ok; `grep -nE '\b[Ss]tanic' app/src/i18n/hr.json` lists only keys named in the WP5 allowlist (BAJS strings under city.*), target 0 otherwise; `grep -c 'Procjena' app/src/i18n/hr.json` prints 1 (arrivals.note) |
| A6 | The wall carries no disclaimer, caveat or fetch time | `npx vitest run test/app/kiosk.test.ts -t 'no disclaimer'` passes: invitation aside, safety strip and every paired aside text matches none of /Podatak iz registra\|Obuhvat zaštite\|Procjena\|Vozni red od\|Zapis od\|[Dd]ohvaćeno\|podaci od \d\|potvrđeno \d\|Izračunato na uređaju\|ne procjena dolaska\|ostalo po voznom redu\|Položaj (je\|vozila)\|\d\d:\d\d · (DHMZ\|ZET)\|\?/; `grep -rn "arrivals.note\\|modelNote\\|sunComputed\\|fetchedAt\\|dataFrom" app/src/kiosk \| wc -l` prints 0 |
| A7 | The share-dialog QR is announced in Croatian, not as a key (§0.1) | `npx vitest run test/app/dashboard.test.ts -t 'QR'` passes: aria-label matches /^QR kod za otključavanje/; `grep -c "kiosk.qrLabel" app/src/dashboard.ts` prints 0 |
| A8 | Multi-day items print an end date, single all-day items keep 'cijeli dan' | `npx vitest run test/app/tiles.test.ts test/app/layers.test.ts -t 'cijeli dan\|do 25. 9.'` passes (range case asserts 'do 25. 9.') |
| A9 | Documents match the contracts and cannot drift back | `npx vitest run test/docs/docs.test.ts` passes; `grep -cE 'svakih 8 sekundi\|gumbom za zaustavljanje\|Dodir na stajalište otvara istraživanje grada\|Radovi u gradu\|Procjena iz ZET-ovih podataka o vozilima; ostalo po voznom redu\.\|zupčanik' docs/kiosk.md` prints 0; `grep -c '22 September 2026' PRODUCT.md` ≥ 1; `grep -cE 'one map-linked highlight held for 20 seconds\|geographic clusters use a distinct plus-count mark\|frozen attributed exports\|Touch exploration has search' DESIGN.md` prints 0; `grep -cE 'kiosk-ticker\|kiosk-lastrun\|strings-hr\.ts' app/src/kiosk/INTEGRATION.md` prints 0 |
| A10 | Whole suite green; nothing under docs/prijava or app/prijava changed; no production screen created | `npm test` and `npm run typecheck` exit 0; `npm run e2e -- e2e/kiosk-layout.spec.ts e2e/cast.spec.ts e2e/city.spec.ts e2e/a11y.spec.ts` exit 0 against the local worker (APP_ENV=test); `git diff --stat -- docs/prijava app/prijava` is empty; scripts/audit-production.mjs and scripts/capture-landing.mjs are not run |

## Risks

- Merge conflicts on app/src/kiosk.ts, kiosk/invitation.ts, transport/workspace.ts, layers/grad-sada.ts, experience/directory.ts with WP2/WP3/WP4 — mitigated by the A/B split; WP5 phase A touches those files only at the lines listed (kiosk.ts:54,66-67,254,309,568-602 untouched,655-672,694,718-752,846-ish in dashboard.ts,1265).
- kiosk/strings.ts is compile-checked against hr.json: deleting a leaf without trimming the tree breaks `npm run typecheck` — do steps 3's JSON deletions and tree edits in one commit.
- The orphan scanner can false-flag dynamically composed keys (events.sources.${…}, kiosk/strings.ts group() lists, data-i18n in HTML); the allowlist in step 3 covers the 20 known prefixes — extend it only with evidence, never by loosening the threshold.
- Value edits to owner-approved verbatim strings (session.expired, session.expiredCta, 'deset minuta' → '10 minuta') are pinned by test/app/i18n.test.ts:78-84; apply only after the owner confirms the wording ([A] items in step 6 and 3).
- 'Procjena' removal changes the accessibility contract of copy-guards 'an arrival time is never bare (WP5)' (test/app/copy-guards.test.ts:188-240): the row must keep a non-colour mark — the t-live dot plus a visually-hidden 'uživo' / 'vozni red' word; keep that test green, do not delete it.
- lastRun plumbing: kiosk.ts ensureLastRun (568-602) looks dead today but WP2's 'Zadnji polasci večeras' row needs LastRunSnapshot — delete only configuredBoard/prometRows/prometBoard, not ensureLastRun, unless WP2's final code does not read InvitationModel.lastRun; the phone's dashboard.ts loadLastRun stays (test/app/dashboard.test.ts:1323-1360).
- Kiosk touch exploration code in mapview.ts (ia-map §7 #9) may be reused by WP2's read-only touch [O-58]; WP5 must not delete it — listed as handed to WP2.
- renderLightweight was unreachable, so deleting it changes no shipped behaviour, but the lagano phone keeps a sheet with a null map slot; document it (step 5) rather than 'fix' it here.
- e2e retargets (kiosk-layout, city, a11y, experience) depend on WP2/WP4 testids (nearby-list, nearby-row, place-title); agree names in the WP2/WP4 briefs before phase B or the specs go red for the wrong reason.
- docs/kiosk.md is asserted by test/docs/docs.test.ts:48-60 — the rewrite must keep the seven pinned phrases verbatim.
- Deleting kiosk.paired.dataFrom/fetchedAt removes the paired block time stamps; a stale block still says 'zastarjelo' — confirm with the owner that paired presentations (also on the wall) follow the no-fetch-time rule [O-27].

## Agent briefs (self-contained; one per lane)

### Brief 1

A1 — Catalogue fold (steps 1, 2, 3). Repo /home/matija/projects/kajima/working-deployed, tree b300af3. Read review.local/companion/plan/WP5/notes.md first. (1) Rewrite app/src/city/strings.ts as a 20-line adapter shaped exactly like app/src/transport/strings.ts (BY_LOCALE cache, `ct(i18n,key,vars?)` → `catalogue(locale).t('city.'+key,vars)`, `CityWord = keyof typeof hr.city`); move the 84 HR/EN pairs into a new top-level `city` group in app/src/i18n/hr.json (insert before the final `}` after `motion`, line 1198) and en.json (after `motion`), dropping inactive, closed, start, next, preview, leave, conditions, noMap, locationDenied, matching, cityLife, hour, publicProgram, quiet, reference, siteNote, scheduleNote; `schedule` value 'Vozni red'; add city.bikes_one/_few/_other ('{count} bicikl/bicikla/bicikala'; en '{count} bike/bikes') and city.bikesUnknown ('— bicikala'), reimplement bikeCount over them (test/city/readable.test.ts:9 must pass). (2) Replace every inline `en ? 'English' : 'Hrvatski'` ternary at directory.ts:93,94,95,97,125,126 · city/next-departures.ts:14,19,22 · city/markup.ts:49,54,57 · city/location.ts:13,14,15 · city/conditions.ts:20,26 · city/air.ts:5 · kiosk/invitation.ts:45,58,82,84,89,90 · kiosk/highlights.ts:63,64,73,74,75,82,94,104,107 · layers/zrak-i-nebo.ts:262 · layers/uprava-i-pravo.ts:270 with the keys named in step 2 of the plan (directory.savedRecord/kindRoute/kindStop/remove/removeLabel/saved/savedEmpty/eventsWeek/preferences; location.device 'Tvoja lokacija', location.area 'Središte karte', locationLabel returns the bare name for kind screen|city; city.facts.* (20), city.airIndex.1-6, city.airNote, city.airMethod, city.consultationsNote; kiosk.handheld.info; kiosk.legend.tram/bikes/venues ('Tramvajska linija','BAJS: broj bicikala','Mjesta s programom večeras'); weather.reference; civic.consultations); delete the Sada stop prompt (next-departures.ts:14) and the 'Sa stajališta zaslona'/'Tvoje spremljeno stajalište' titles; SAFE_FACTS (markup.ts:13-20) → catalogue; schematic-host.ts:28 `HONESTY_NOTE_HR = hr.motion.note` via `import hr from '../i18n/hr.json'`. Leave proper-noun/licence constants (kultura.ts:41-51, uprava-i-pravo.ts:37-48, grad-teaser.ts:18-24, kiosk/local.ts:433-439, kiosk/paired.ts:108-123, front.ts:255-256, districts.ts, worker/hitno) alone. (3) Delete the 77 orphan leaves (hr.json line list in notes.md, same leaves in en.json) plus kiosk.ticker.*, kiosk.lines.modelNote, kiosk.safety.confirmed, kiosk.paired.dataFrom/fetchedAt/ongoingUntil, kiosk.invite.codeWaiting; delete workspace.ts:595; add the new groups/leaves: sentence.kicker.{promet,kultura,vrijeme,bicikli,nocas,radovi}; nearby.{title 'U blizini', pill '{km} km · ~{min} min', pillLabel, always 'uvijek', lastTonight 'Zadnji polasci večeras', firstMorning 'Prvi jutarnji tramvaj', sunset 'Zalazak sunca', sunrise 'Izlazak sunca', pharmacy '24/7', pharmacyLabel 'Dežurna ljekarna 24/7: {address}', opens 'otvara {time}', zetDown}; arrivals.departing 'kreće'; arrivals.scheduled → 'vozni red'; time.untilDate 'do {date}'; kiosk.setup.field 'Adresa ili stajalište', kiosk.setup.preview_one/_few/_other 'Na zaslonu: {place} i {count} stajalište/stajališta/stajališta uokolo', kiosk.setup.previewCity 'Na zaslonu: cijeli grad', kiosk.setup.create → 'Pokreni'; kiosk.settings.place 'Mjesto', frame 'Kadar', frameValue_one/_few/_other '{count} stajalište/stajališta/stajališta odavde', rhythm 'Ritam', longPress 'Drži za postavke'; session.expired → 'Deset minuta je prošlo. Zaslon u blizini otključava novih 10 minuta.', session.expiredHint → 'Sigurnost ostaje otvorena na /hitno.' (flag both as owner-confirm in the PR). Update app/src/kiosk/strings.ts (remove the leaves listed in step 3; add legend, handheld, setup.field/preview/previewCity, settings.place/frame/frameValue/rhythm/longPress, sentence.kicker, nearby). Tests: i18n.test.ts:57 → hr.sentence.kicker.promet; extend DEAD_KEYS; :82 new verbatim; copy-guards.test.ts drop safetyOpen (88,97) and overview.allClear (116), add the ternary guard over app/src (exclude app/src/i18n/); write test/app/i18n-orphans.test.ts as specified in step 3 with the 20-prefix allowlist and `expect(unreferenced).toEqual([])`. Done when `npm run typecheck`, `npx vitest run test/app/i18n.test.ts test/app/copy-guards.test.ts test/app/i18n-orphans.test.ts test/city/readable.test.ts` pass and `grep -cP '[čćžšđ]' app/src/city/strings.ts` prints 0. Do not touch worker/, docs/prijava, app/prijava; never run scripts/audit-production.mjs or scripts/capture-landing.mjs.

### Brief 2

A2 — Kiosk dead paths (step 4). Repo /home/matija/projects/kajima/working-deployed; read review.local/companion/plan/WP5/notes.md 'Dead paths' rows 1, 10, 21, 23, 27. Delete app/src/kiosk/ticker.ts and test/app/ticker.test.ts; in app/src/kiosk.ts delete lines 66-67 (TICKER_SWAP_MS), rename paintTicker → paintAmbient at 254, 309, 694, 1265, delete the corner-qr/join-code queries and writes at 718-719, 735-736, 750-752 and the joinCode argument of swapCode, delete the re-export at line 54; in app/src/ui/kiosk.css delete 221-225 (.k-ticker*, @keyframes k-ticker-in), the --k-ticker-size tokens at 120, 151, 742, the .k-join-code rules at 435 and 685, the .k-ticker/.k-join-code members of the selector lists at 714 and 718, and line 275 (.k-lines--overlay); fix the comment at line 34. Delete app/src/kiosk/teaser.ts; move `cityTeaserAttribution` from app/src/layers/grad-teaser.ts into app/src/kiosk/local.ts (update the import at local.ts:14) and delete grad-teaser.ts. Fix the live defect at app/src/dashboard.ts:846: `i18n.t('kiosk.qrLabel', …)` → `i18n.t('kiosk.invite.qrLabel', …)` and add an assertion in test/app/dashboard.test.ts's share-dialog test that the QR aria-label matches /^QR kod za otključavanje/. Tests: test/app/kiosk.test.ts delete import line 20 and the kiosk-ticker null-assertions at 753, 809, 819, 827, 832, 907, 1137 (reword the it titles at 709, 789, 814, 1343 that say 'ticker'); test/app/kiosk-css.test.ts:63-69 → `expect(css).not.toMatch(/k-ticker/)`; test/app/kiosk-local.test.ts delete import 26 and lines 283-292; test/integration/feed-to-layers.test.ts delete import 20 and the four `it` blocks in 183-220; scripts/audit-production.mjs:250 → `['pair-code']`. Do not touch kiosk.ts:568-602 (ensureLastRun), 655-672 (configuredBoard) or app/src/kiosk/mapview.ts — WP2 owns them. Done when `npm run typecheck && npx vitest run test/app/kiosk.test.ts test/app/kiosk-css.test.ts test/app/kiosk-local.test.ts test/integration test/app/dashboard.test.ts` pass and `grep -rnE 'corner-qr|join-code|k-ticker|k-join-code|k-lines--overlay|TICKER_SWAP_MS|teaserCards|safetyStripText|kiosk\.qrLabel' app/src scripts test e2e | wc -l` prints 0.

### Brief 3

A3 — Phone/desktop dead paths (step 5). Repo /home/matija/projects/kajima/working-deployed; read review.local/companion/plan/WP5/notes.md 'Dead paths' rows 2, 3, 4, 6, 7, 8, 15, 16. FAB: delete app/src/experience/chrome.ts:193-200 (fabMarkup); app/src/dashboard.ts drop the import at 24, the ki-fab-slot region at 260 and the paint at 362-364 (and dataset.fab); app/src/ui/dashboard.css delete 60, 132-140, the .ki-fab member of 202, and 216. Cast legacy: dashboard.ts delete 57 (CAST_SENT_MS), 212-214, the castSent/sentAt fields at 299 and 401, 875, `case 'cast'` at 1008, 1140; app/src/transport/view.ts delete 347-351 (castButton) and, if unused afterwards, the `cast?: CastState` members at 42, 387, 437, 492, 616 and the castReasonText import (experience/presentation.ts keeps its own). Timeband: app/src/experience/timeband.ts delete NEXT_CAP (48), Lane.next (211-213), 390-393, renderTimebandSeg (419-465), laneBody/renderTimeband (467-516) — keep buildTimeband, columnsFor, bucketOf, zagrebInstant, tickTimebandClock, FILTER_KEY; delete app/src/experience/timeband-sync.ts and test/app/timeband-sync.test.ts; dashboard.ts drop import 29 and the attach at 1071 plus the detach call in destroy; app/src/ui/layers.css delete 201-225 and any other orphan `.tb-` rule (grep app/src/ui for `\.tb-` and check each selector is emitted by no renderer). Scan confirm: app/src/scan.ts delete 82-108 (sentenceCase, confirmLabel, confirmStopLine), 202-208 (hideConfirm), 276-299 (renderConfirm) and the confirmBox element in the 142-171 markup; app/src/ui/scan.css delete 128-144. resultsMarkup: view.ts delete 200-232 (ResultsData, resultsMarkup). Search launcher and bell: chrome.ts delete 142-145 and 160-164; dashboard.ts:613-617 openSearch → `main.querySelector('[data-testid=transport-search]')` only. Lightweight face: app/src/layers/u-pokretu.ts delete the branch at 87-90 and 135-238 (renderLightweight, LIGHT_FILTER, fold helpers). Tests: test/app/dashboard.test.ts:1046 order → ['ki-head ki-status','ki-presentation','ki-banners','ki-main','ki-tabbar'], delete 1053 and 1055; test/app/shell-css.test.ts delete 129-131, 158-168, the '.ki-fab' entry at 197, 215, 309, 332; test/app/timeband.test.ts import at 11 drop renderTimeband/renderTimebandSeg, delete describes 488-513, 515-537, 539-671, rewrite the setup at 676 with a hand-written `<time class="day-clock">` root; test/app/scan.test.ts delete 692-693 and any confirmLabel/confirmStopLine tests; test/app/layers.test.ts delete the lightweight-face describe (~380-413); e2e/cast.spec.ts delete line 19. Coordinate with A2 on dashboard.ts (A2 edits line 846 only). Done when `npm run typecheck && npx vitest run test/app` pass and `grep -rnE 'ki-fab|\.tb-(heads|lanes|axis|head|h|clock|weather)\b|scan-confirm|renderTimeband|attachTimebandSync|fabMarkup|resultsMarkup|searchLaunchMarkup|bellMarkup|renderConfirm|renderLightweight|castButton|CAST_SENT_MS' app/src test e2e | wc -l` prints 0.

### Brief 4

A4 — Documents (step 10). Repo /home/matija/projects/kajima/working-deployed; read docs/companion-2026-09-22.md §4, §10, §11, §13 and review.local/companion/ledger.md [O-26]-[O-61], then PRODUCT.md, DESIGN.md, docs/kiosk.md, app/src/kiosk/INTEGRATION.md, test/docs/docs.test.ts. PRODUCT.md: insert the dated 'On 22 September 2026 …' approval paragraph before line 44 with the exact supersession list in step 10; prefix the conflicting sentences at 46-53, 62-69, 71-76, 80-87, 96-97, 104-105, 112 with 'Superseded 22 September 2026:'. DESIGN.md: replace 78-99 with the §11 wall contract; mark 101-155 as history and strike the sentences listed in step 10; rewrite 171-174 (Sada · Karta · Još; desktop = phone, wider), 180-192 (one search field, no disclosures), 194-197 (Još per [O-60]), 199-205 (no cluster marks, no unlabelled dots, BAJS counts, categories only as results/pins), 207-210 (Zaslon and Podijeli grad), 219-222 (content clears to the scan invitation; no frozen exports), 228-231 (blue countdown / grey clock + accessible word, never 'procjena'). docs/kiosk.md (Croatian): replace 3-24 with 'Javni zaslon, odobreno 22. rujna 2026.'; steps 2-3 at 36-62 → one field 'Adresa ili stajalište' and Pokreni, settings behind a long press as click-toggles Mjesto · Kadar (4/6/8 stajališta odavde) · Tema · Ritam; rewrite 79-163 (frame 6–7 stajališta oko mjesta; 'U blizini · 2 km · ~15 min'; najviše tri polaska; plava/siva; red 'uvijek'; bez vremena dohvata, napomena, brojanja i gumba za posjetitelja; dodir samo za čitanje, vraća se sam nakon 60 s) deleting 'Radovi u gradu', 'tri ploče', 'svakih 8 sekundi', 'Dodir na stajalište otvara istraživanje grada', '90 sekundi bez dodira'; 188-197 → read-only touch; 199-231 remove the 'Procjena iz ZET-ovih podataka o vozilima; ostalo po voznom redu.' sentence-under-rows rule, add the colour rule and 'nema vremena dohvata na zidu', keep 'Ništa se ne izmišlja'; 331-334 → dugi pritisak. Keep verbatim: 'POST /api/screens', 'Isti Wi-Fi je dopušten.', 'APP_ENV=test', 'E2E_ADMIN_BYPASS', '24 sata', 'git push', 'fizička matrica'; never write 'Ovaj zaslon i tvoj telefon dijele istu mrežu.'. INTEGRATION.md: 85-96 drop kiosk-ticker/kiosk-lastrun/three panels and name nearby-list/nearby-row; 131-137 → kiosk/strings.ts over hr.json; 139-149 → the 20 s model-written sentence with sentence.kicker.*. Code comments: app/src/kiosk/front.ts:1-15, app/src/kiosk/layout.ts:11-15, app/src/ui/kiosk.css:34. Extend test/docs/docs.test.ts with the assertions in step 10 (kiosk.md must not contain the six retired phrases and must contain 'U blizini' and 'Adresa ili stajalište'; PRODUCT.md contains '22 September 2026'; DESIGN.md contains 'U blizini' and none of the four struck phrases). Use no em dash and no ' -- ' in any of these files (copy-guards). Done when `npx vitest run test/docs` passes and the grep thresholds in the acceptance list hold. Do not edit docs/companion-2026-09-22.md, docs/prijava, app/prijava.

### Brief 5

A5 — All-day ranges (step 7). Repo /home/matija/projects/kajima/working-deployed. In app/src/layers/kultura.ts:198-203 `timeCell`, when the item is multi-day (precision 'range' or `until` on a later Zagreb day — the check already at 222-223) print `i18n.t('time.untilDate',{date: zagrebDayMonth(item.until)})` in the lead cell instead of `time.allDay`, and drop the duplicated 'do …' sub-line at 224; single-day day-precision entries keep 'cijeli dan'. Apply the same rule in app/src/experience/tiles.ts `timeTextOf`/`tileAria` (96, 111, 117) for a tile with allDay and an `until` on a later day (producers/events.ts must pass `until` — check), in app/src/kiosk/front.ts:143 and app/src/kiosk/paired.ts:725-728 (read `time.untilDate`; A1 deletes kiosk.paired.ongoingUntil — coordinate: if A1 has not merged, read `kiosk.paired.ongoingUntil` and leave a TODO for A1). Tests: keep test/app/layers.test.ts:238-244 and :668 green (day-precision stays 'cijeli dan'); add a range case beside test/app/tiles.test.ts:85-87 and :157-160 asserting the lead reads 'do 25. 9.'. Done when `npx vitest run test/app/tiles.test.ts test/app/layers.test.ts test/app/kiosk-front.test.ts` pass.

### Brief 6

A6 — Vocabulary values (step 6; runs after A1 merges). Repo /home/matija/projects/kajima/working-deployed; read review.local/companion/plan/WP5/notes.md 'Vocabulary sites'. In app/src/i18n/hr.json set layers.u-pokretu (line 68) to 'Karta' (en.json 'Map'), transport.mapRegion (433) 'Karta', transport.sheetLabel (466) 'Detalji', kiosk.paired.overviewTransport (1045) 'Karta oko stajališta', landing.evidence.domains (707) 'Sada · Karta · Vrijeme · Sigurnost · Grad · Događanja', weather.now (181) 'Sada'; replace 'stanica/stanicu/stanice/stanici' with the stajalište forms at 374, 395, 419-421, 427, 429-430, 435-437, 441, 461, 469-470, 487, 500, 569, 571, 883, 1006, 1090-1094, 1104, 1107 as spelled in step 6 (leave BAJS wording under city.* alone); delete tiles.scheduled (169) and repoint app/src/experience/producers/last-run.ts:57 to arrivals.scheduled. Code: delete the layerLabel override in app/src/experience/chrome.ts:86-89 so the tab reads layers.u-pokretu; app/src/transport/view.ts:341 pass the stop's NAME (not id) into kvart.saveStop; view.ts:548 and app/src/city/next-departures.ts:22 replace the visible 'Procjena' with `<span class="visually-hidden">${i18n.t('arrivals.live')}</span>` beside the live time (the grey row keeps arrivals.scheduled the same way) — keep test/app/copy-guards.test.ts:188-240 green; app/src/izvori-render.ts:70 'Prijevoz' → 'Promet'. Add to copy-guards.test.ts a 'vocabulary' describe: hr.layers['u-pokretu']==='Karta'; JSON.stringify(hr) contains none of Kretanje, 'Prijevoz i raspored', 'Živi grad', 'Sada u gradu.', 'Grad oko tebe', 'Upoznaj ovo mjesto', 'Podatak iz registra', 'Obuhvat zaštite', 'ZET GTFS', 'Vozni red od', 'Dohvaćeno ', 'po redu vožnje'; hr.arrivals.scheduled==='vozni red'; the leaves containing 'Procjena' equal ['arrivals.note']; every leaf matching /\b[Ss]tanic/ is under an explicit allowlist array (BAJS strings in city.*). Fix the tests that asserted the old label via ct('map'): test/app/dashboard.test.ts:726,737; test/app/transport-workspace.test.ts:142,201,225; test/app/entries-kiosk.test.ts:87,90; test/app/map.test.ts:95; test/app/overlays.test.ts:89,109; test/app/shell-css.test.ts:115,117; e2e/experience.spec.ts:139. Do NOT change 'deset minuta' strings or kiosk.invitation without the owner's confirmation (flag in the PR). Done when `npm run typecheck && npx vitest run test/app/copy-guards.test.ts test/app/i18n.test.ts test/app/dashboard.test.ts test/app/transport-workspace.test.ts` pass.

### Brief 7

B1 — Wall hygiene and composition-dependent retirements (steps 8 and 9; run only after WP2, WP3 and WP4 have merged). Repo /home/matija/projects/kajima/working-deployed; read review.local/companion/plan/WP5/notes.md 'Disclaimers / fetch times on the wall' and 'Tests pinning old compositions'. (8) app/src/kiosk/paired.ts:94-103 statusLine → stale word only; delete the arrivals.note foot at 415 and panels.sunComputed at 510; the place detail at 454 renders publicDisplay=true and app/src/city/markup.ts:67 prints only the source line (delete the `ct('reference')` foot everywhere and the heritage siteNote at 58); app/src/kiosk/front.ts:387 delete the note foot, :420 credit 'ZET', :245 meta 'DHMZ'; app/src/kiosk/markup.ts:105 delete the modelNote line; app/src/kiosk/frame.ts:55 always strings.safety.sources; app/src/kiosk/essentials.ts:33 no time; kiosk.ts coverage line at ~704 gone; motion.note (app/src/motion/schema-map.ts:78,175, app/src/motion/schematic-host.ts:27,85) hidden when the renderer is a public display (add the option), and app/src/transport/workspace.ts:635 sheet foot removed on the phone (the note stays in the vehicle detail). Add the kiosk.test.ts guard 'the wall carries no disclaimer, caveat or fetch time' with the regex in step 8 over the invitation aside, the safety strip and each paired aside; update kiosk-front.test.ts:107,128 and any kiosk.test.ts/kiosk-local.test.ts assertion expecting 'Dohvaćeno', 'Vozni red od', 'Procjena: 3 min'. (9) Run `npx vitest run test/app/i18n-orphans.test.ts`; delete every leaf it reports that WP2/WP3/WP4 orphaned (expected: cityOverview.*, timeband.next and timeband.seg.*/segLabel if the segments are gone, the ct browse keys all/city/movement/useful/network/layers/list/here/locate/legend/streetBrowse/cluster/allVenues/activeVenues, kiosk.setup.intro/validity/search/results_*/noResults/routesAt/loadingStops/errorStops, kiosk.settings.area/areaWhole/areaHint/stop/stopNone/stopHint/open/hint, kiosk.basics.* + kiosk.safety.openBasics if Osnovno is gone, kiosk.front.*/kiosk.say.* leaves no paired card reads, kiosk.weather.* leaves the invitation stopped reading) and trim app/src/kiosk/strings.ts and app/src/city/strings.ts types to match; extend DEAD_KEYS in test/app/i18n.test.ts with them; delete app/src/ui/overview.css:71 (.day-stop-prompt) and the orphan .city-filter-disclosure/.city-group/.city-times rules; test/app/layers.test.ts:119 → the Sada title is the place; app/izvori/index.html: add after line 18 the one register sentence 'Podaci gradskog kataloga (kultura, baština, voda, WC, tržnice …) su iz registara, ne provjera uživo; obuhvat zaštite baštine označava zaštićeno područje, ne ulaz.'. e2e: kiosk-layout.spec.ts:66-77 → under the stale fixture every [data-testid=nearby-row][data-kind=departure] has data-time="timetable" (use WP2's actual testids); city.spec.ts:26 delete, :27-31 follow WP4's search-only Karta; a11y.spec.ts:241 → 'BAJS: broj bicikala' and no '?', :268-270 follow WP4; experience.spec.ts:65 → WP4's labels; readable-city.spec.ts:141-145 delete; round-f.spec.ts:96, scripts/review-experience.mjs:116-121, scripts/capture-landing.mjs:181-184 drop the 'Kretanje' group click. Done when `npm test`, `npm run typecheck` and `npm run e2e -- e2e/kiosk-layout.spec.ts e2e/cast.spec.ts e2e/city.spec.ts e2e/a11y.spec.ts` pass against the local worker (APP_ENV=test) and the orphan test reports []. Never create a production screen (do not run scripts/audit-production.mjs or scripts/capture-landing.mjs).

## Verifier verdict (adversarial pass, 22 Sep)

Refuted as written: **yes**. Problems found:

- PHASE-A SEQUENCING IS BROKEN (typecheck cannot pass on A1 alone). Step 3 (A1) deletes JSON leaves + KioskStrings tree members that are still read by code A2/A5/B1 edit later: kiosk.invite.codeWaiting is read at app/src/kiosk.ts:736 (A2 file); kiosk.ticker.* at app/src/kiosk/ticker.ts:72-152 (A2 deletes the file); kiosk.safety.confirmed at app/src/kiosk/frame.ts:55 (B1); kiosk.lines.modelNote at app/src/kiosk/markup.ts:105 (B1); kiosk.paired.dataFrom/fetchedAt at app/src/kiosk/paired.ts:101 (B1); kiosk.paired.ongoingUntil at app/src/kiosk/paired.ts:753 (A5, and the plan cites 725-728). The tree is compile-checked (strings.ts:331 `group<G,K extends Leaf<G>>`), so A1's 'done when npm run typecheck passes' is unreachable while those files are untouched; 'Phase A runs in parallel on disjoint files' is false.
- THE '77 ORPHANS' CLAIM IS WRONG IN BOTH DIRECTIONS. A scanner built to step 3's own spec (review scratch: orphans.mjs) finds only ~29 of the 77 unreferenced today; ~48 are read by dead-path code deleted in steps 4/5/7/8 (e.g. cast.fab at transport/view.ts:350, transport.search at experience/chrome.ts:144, transport.noResults/routes/stops/searchResults via resultsMarkup, transport.lightSearch* via renderLightweight, scan.confirm*/cancel via renderConfirm, tiles.bikesFree/parkingFree/waste.label + kvart.wholeCity/walkMinutes + notify.waste via experience/producers/*) or by the KioskStrings tree. Conversely it finds 12 truly unreferenced leaves the plan omits: events.thisWeek, events.ongoingCount_*, events.fromDate, events.selectHint, events.addToCalendar, events.sourcesTitle, events.dayCount, transport.keyboardHint, transport.moreStops_*, transport.selectionOnScreen, transport.stopVehiclesNow, transport.closuresAndNotices.
- shell.skip IS NOT AN ORPHAN. app/d/index.html:14 and app/index.html:25 carry data-i18n="shell.skip"; translatePage (i18n.ts:104-109) sets textContent = t(key), which falls back to the raw key when the leaf is missing (resolveTemplate: `lookupTemplate(key) ?? key`). Deleting the key turns both skip links into the literal text 'shell.skip' and loses the English translation. The notes' 'delete the key, keep the HTML' is a live defect.
- ORPHAN-TEST ALLOWLIST IS INCOMPLETE. Live template prefixes the step-3 allowlist omits: `notify.${key}` (app/src/experience/notify-sheet.ts:36 → notify.delays/works/dhmz/waste) and `timeband.${…}` (app/src/experience/presentation.ts:51 → timeband.today/tonight/tomorrow/week/sada; the plan allows only timeband.seg.). Conditional keys `tr(i18n, cond ? 'unfollow' : 'follow')` (view.ts:400), `tr(i18n, up ? 'details' : 'collapse')` (workspace.ts:581), `tr(i18n, schema ? 'mapModeMap' : 'mapModeSchema')` (workspace.ts:621) are invisible to a `tr(x,'k')` matcher. As specified, the new test would flag live keys or force the agent to loosen the threshold.
- time.untilDate DOES NOT EXIST AND WOULD DUPLICATE events.untilDate. hr.json:293 'do {date}' is `events.untilDate` (read by app/src/layers/kultura.ts:217), not `time.untilDate`. Step 3 'adds' time.untilDate 'do {date}' and step 7 reads it — creating exactly the twin the WP exists to remove.
- ACCEPTANCE #6 SECOND COMMAND CAN NEVER PRINT 0. `grep -rn "arrivals.note\|modelNote\|sunComputed\|fetchedAt\|dataFrom" app/src/kiosk` matches the data field `fetchedAt` at app/src/kiosk.ts:590 (ensureLastRun, which the plan says to keep), kiosk/front.ts:392, kiosk/local.ts:324, kiosk/paired.ts:100 (14 hits today). It is not an executable threshold as written.
- ACCEPTANCE #4 CONTRADICTS A2'S BRIEF AND COUNTS NEGATIVE ASSERTIONS. The grep demands 0 hits for `configuredBoard`, but A2 is told 'Do not touch kiosk.ts:655-672 (configuredBoard) — WP2 owns them' (kiosk.ts:660,670 remain). test/app/kiosk-css.test.ts:107 `expect(shell).not.toContain('k-lines--overlay')` is itself a hit. Unlisted files also hit today: test/app/type-tokens.test.ts:107-108 (.tb-h, .tb-clock), test/app/workspace-css.test.ts:25 (.tb-lanes), test/app/layers.test.ts:133 (.tb-lanes), app/src/ui/print.css:4 (.ki-fab-slot).
- ACCEPTANCE #2 THRESHOLD IS NOT DEFINED. 'flat-leaf count ≤ 1045 − 77 + new keys' names no number for 'new keys'; not executable.
- STEP 6 NAMES THE WRONG TESTS. dashboard.test.ts:726,737 (`dataset.view` 'map'), shell-css.test.ts:115,117 (data-stage='map'), transport-workspace.test.ts:142,201,225, entries-kiosk.test.ts:87,90, map.test.ts:95, overlays.test.ts:89,109, e2e/experience.spec.ts:139 all assert the literal 'map' (renderer/mode/data-view), none reads the tab label. The test that actually pins 'u-pokretu': 'Promet' is test/app/kiosk-local.test.ts:95 — not listed. dashboard.test.ts:155 already expects 'Karta' and keeps passing.
- STEP 6 OMITS THE TESTS ITS VALUE EDITS BREAK. 'po redu vožnje' → 'vozni red' breaks transport-workspace.test.ts:868, transport-detail.test.ts:103,123,131,136,152,155, kiosk.test.ts:2030,2035,2162 (literal). Deleting tiles.scheduled ('po rasporedu · ZET GTFS') breaks timeband.test.ts:788, producers.test.ts:419,433, lastrun.test.ts:169 and dashboard.test.ts:1339,1341 — which the plan explicitly says to 'keep unchanged'. 'stanica' → 'stajalište' breaks transport-workspace.test.ts:344 ('Stanica ovog zaslona'), :423 ('stoji na stanici'), :432 ('sve stanice'), timeband.test.ts:200 and producers.test.ts:113 ('Sa stanice …').
- STEP 3 OMITS TESTS THAT PIN DELETED KEYS. test/app/panel.test.ts:93,96 reads common.print and asserts 'Ispiši'; test/app/kiosk-frame.test.ts:69 asserts 'DHMZ · EMSC · {time}' (kiosk.safety.confirmed); test/app/kiosk-local.test.ts:974-975 tests hr.paired.coverageLines; test/app/transport-search.test.ts:103 calls tr(hr,'noResults'); test/app/producers-mobility.test.ts:178-184 asserts 'Cijeli grad' (kvart.wholeCity); test/app/scan.test.ts:388 asserts 'Telefon druge osobe · 5 minuta' (scan.confirmPeer) outside the confirmLabel describe the plan says to grep.
- STEP 2 DELETES THE SADA STOP PROMPT IN PHASE A BUT ITS TEST IS SCHEDULED FOR PHASE B. next-departures.ts:14 (`.day-stop-prompt`) is removed by A1; test/integration/feed-to-layers.test.ts:96 `expect(gradSada.querySelector('.day-stop-prompt')).not.toBeNull()` is listed under 'gone after WP4'. Also nothing says what nextDepartures returns when no stop exists before WP4 lands.
- LIGHTWEIGHT DELETION BREAKS MORE THAN THE 381 DESCRIBE. test/app/layers.test.ts's ctx() helper (line 62) sets no `city`, so `renderLayer('u-pokretu', ctx({lightweight:true}))` at :326-345 ('mounts the lightweight host…', asserts transport-workspace toBeNull) and :372-378 ('renders no map panel at all in lightweight mode (R-L2)', asserts factory not called) both enter renderLightweight (u-pokretu.ts:87). Production unreachability holds (core/city-store.ts:4 snapshot(): CityState; dashboard.ts:403 always passes it) but these two tests need rewriting and R-L2 needs a decision.
- DOC GUARDS ARE VACUOUS BECAUSE PHRASES WRAP LINES. In docs/kiosk.md 'gumbom za zaustavljanje' is split across lines 11-12 and 'Dodir na stajalište otvara istraživanje grada' across 107-108; `grep -c` and `toContain` already report 0 today, so the acceptance and the new docs.test.ts assertions prove nothing. 'Ništa se ne izmišlja' does not occur (docs/kiosk.md:211 says 'ne izmišlja' inside another sentence) — the plan says 'keep' it. 'one map-linked highlight held for 20 seconds' is in PRODUCT.md:50, not DESIGN.md, so that DESIGN.md guard is also vacuous. DESIGN.md has no 'Lagani prikaz' section (it is docs/kiosk.md:233).
- WRONG LINE POINTERS / NAMES. hr.json:983 is kiosk.safety.openBasics; kiosk.safety.confirmed is line 982. paired.ts ongoingUntil is at :753 (plan: 725-728). kiosk.ts:704 is the paired `fitRows(paired.element, s.paired.coverage)`, not an invitation coverage line — step 8's deletion target does not exist. `intlLocale` is exported from app/src/experience/text.ts:10 (uprava-i-pravo.ts:13 only imports it). transport/view.ts:200 is `ResultIds`; `ResultsData` starts at :205, resultsMarkup at :217. kiosk-front.test.ts:128 is the `empty('stale')` sentence; the credit assertion is :109 (toContain('ZET'), unaffected). '31 inline ternaries' is a miscount: the grep yields 35, matching the plan's own site list.
- CROSS-WP KEY NAMES CONFLICT WITH THE BRIEF. Brief §17 Q9 fixes the new strings under `kiosk.nearby.*`, `kiosk.sentence.*`, `kiosk.setup.*` ('one place to edit'); the plan puts them at top-level `nearby.*` and `sentence.kicker.*`, and KioskStrings.group() only resolves `kiosk.*`. Wordings also diverge from the brief: nearby.zetDown 'vremena su po voznom redu' vs Q9 'polasci su po voznom redu'; nearby.firstMorning 'Prvi jutarnji tramvaj' vs 'Prvi tramvaj'; nearby.lastTonight 'Zadnji polasci večeras' vs 'Zadnji tramvaji'; kiosk.legend.venues 'Mjesta s programom večeras' vs Q6 'Kultura večeras' (WP2). WP2/WP4 will read different paths/words unless settled before A1.
- PLURAL LEAVES COLLIDE WITH AN EXISTING LEAF. city.bikes already exists ('BAJS', the category word, used by placeCategory via ct(i18n,p.category)). Adding city.bikes_one/_few/_other beside it works only because resolvePlural checks `_category` first; CityWord = keyof typeof hr.city then also includes 'bikes_one', 'facts' and 'airIndex' (objects, not strings), so `ct(i18n,'facts')` type-checks but returns the raw key.
- HIDDEN 'uživo' WORD MUST BE FROZEN-AWARE. copy-guards.test.ts:231 asserts a frozen sheet never contains arrivals.live; step 6's `<span class="visually-hidden">${i18n.t('arrivals.live')}</span>` beside the blue time at view.ts:548 must be emitted only when `d.frozenAt === undefined`, which the plan does not say.
- ORPHANED-IN-SPIRIT 'kiosk.invitation' IS PINNED BUT UNUSED. hr.kiosk.invitation ('Skeniraj za 10 minuta grada. Manje ekrana, više Zagreba.') has no consumer in app/src (only i18n.test.ts:79 pins it); the plan says 'kiosk.invitation stays verbatim' — it would be reported by the new orphan test.
- WP4 HANDS WP5 DELETIONS THE PLAN NEVER ENUMERATES. review.local/companion/plan/WP4/notes.md 'Dead after WP4 (WP5 removes)': experience/producers/* (11 files; move SAFETY_ICON/safetyVerdict first — kiosk/frame.ts:13 imports it), experience/tiles.ts, experience/timeband.ts (all but columnsFor/zagrebInstant; kiosk/paired.ts:27 imports columnsFor), transport/view.ts overviewMarkup/peekMarkup/delaysMarkup, chrome.ts clockMarkup, dashboard.ts mapFull/setMapView, city.css:2-7. Step 9 only says 'the orphan test is the arbiter' — which covers keys, not modules. Step 7 (A5) meanwhile edits tiles.ts/tiles.test.ts and step 6 repoints producers/last-run.ts:57, both of which WP4 declares dead.
- test/city/readable.test.ts:6 imports createHighlightSequence/kioskHighlights/highlightBounds from app/src/kiosk/highlights, which WP2 retires; acceptance 'readable.test.ts:9 still imports bikeCount and passes' silently depends on WP2 trimming that file.

Corrections:

SEQUENCING. Re-split phase A so every key deletion follows its last consumer: A2 (kiosk dead paths) and A3 (phone dead paths) run first and each deletes the leaves only its files read (A2: kiosk.ticker.*, kiosk.invite.codeWaiting, kiosk.header.temporaryUntil/venue, kiosk.session.join/joinHint, kiosk.paired.delays/coverageLines_*, kiosk.settings.saved, kiosk.weather.station, kiosk.say.transitRegular/quake/tonight/tonightMore_*, kiosk.front.tomorrowCity, kiosk.safety.basics + their KioskStrings members; A3: transport.search/searchLabel/noResults/routes/stops/searchResults/lightSearch*/keyboardHint/moreStops_*/selectionOnScreen/stopVehiclesNow/closuresAndNotices, scan.unlock/confirm*/cancel, cast.toScreen/fab/fabLabel/sent, panels.*, session.openSheet, export.print). A1 then deletes only what the scanner reports unreferenced after A2+A3 merge (common.print with panel.test.ts:93-96 rewritten, shared.safetyOpen, attribution.source, shell.connecting/footerNote/sunset, overview.allClear, civic.assemblyNext, transit.mostDeviating, session.joinedNotice/joinedPeer, landing.pages.scan, events.thisWeek/ongoingCount_*/fromDate/selectHint/addToCalendar/sourcesTitle/dayCount, nav.safetyHint, timeband.segLabel if WP4 drops the segments) and adds the new groups. Keep shell.skip (the two data-i18n skip links read it). Move kiosk.safety.confirmed, kiosk.lines.modelNote, kiosk.paired.dataFrom/fetchedAt/ongoingUntil deletions into B1/A5 together with frame.ts:55, kiosk/markup.ts:105, paired.ts:94-103 and :753 edits (A5 repoints paired.ts:753 to events.untilDate). tiles.bikesFree/parkingFree/waste.label, kvart.wholeCity/walkMinutes, notify.waste stay until WP5 deletes experience/producers/* per WP4's hand-over (add a step 9 list: producers/* after moving SAFETY_ICON/safetyVerdict to experience/safety-state.ts, tiles.ts, timeband.ts minus columnsFor/zagrebInstant moved next to kiosk/paired.ts, view.ts overviewMarkup/peekMarkup/delaysMarkup, chrome.ts clockMarkup, dashboard.ts mapFull/setMapView, city.css:2-7). Drop step 7's tiles.ts/tiles.test.ts edits and step 6's last-run.ts:57 repoint if WP4 lands first. Move the next-departures.ts:14 prompt deletion to step 9 (after WP4), or have A1 return '' and update feed-to-layers.test.ts:96 in the same commit.

KEYS. Reuse events.untilDate (hr.json:293) everywhere step 7 says time.untilDate; do not add time.untilDate. Name the bike plural city.bikeCount_one/_few/_other + city.bikeCountUnknown and define CityWord as the string-valued keys of hr.city (or flatten city.facts.* to city.fact-*, city.airIndex.* to city.airIndex-1..6). Settle the new-key paths with WP1-WP4 before A1: either move to kiosk.nearby.*/kiosk.sentence.*/kiosk.setup.* as brief Q9 states (then KioskStrings group() resolves them) or amend Q9; adopt one wording per row title and one legend third item (Q6 'Kultura večeras' vs 'Mjesta s programom večeras') and one outage note ('polasci su po voznom redu' per Q9). Extend the orphan-scanner allowlist with notify. and timeband. (both live templates) and make the scanner collect every quoted identifier inside tr(/trPlural(/ct( argument lists so conditional keys resolve; report the initial unreferenced list in the PR rather than asserting [] blindly. Extend DEAD_KEYS with the 12 omitted orphans. Delete kiosk.invitation or give it a consumer.

TESTS TO UPDATE (replace the 'map'-literal list). Step 6: kiosk-local.test.ts:95 ('u-pokretu': 'Karta'); transport-workspace.test.ts:344,423,432,868; transport-detail.test.ts:103,123,131,136,152,155; kiosk.test.ts:2030,2035,2162; timeband.test.ts:200,788; producers.test.ts:113,419,433; lastrun.test.ts:169; dashboard.test.ts:1339,1341 (context now 'vozni red'); scan.test.ts:325 and entries-dashboard.test.ts:133 if 'deset minuta' becomes digits (owner-confirm). Step 3/4/5: panel.test.ts:93-96, kiosk-frame.test.ts:69, kiosk-local.test.ts:974-975, transport-search.test.ts:103, producers-mobility.test.ts:178-184, scan.test.ts:103-124 and :388, type-tokens.test.ts:107-108, workspace-css.test.ts:25, layers.test.ts:133 and :326-345 and :372-378 (decide R-L2 for the workspace path), print.css:4 (.ki-fab-slot). Step 6 also: emit the hidden arrivals.live word only when d.frozenAt is undefined (copy-guards:231). readable.test.ts: strip the highlights imports when WP2 deletes kiosk/highlights.ts.

ACCEPTANCE FIXES. #2: state the new-leaf count (enumerate the added leaves; today 953 plural-merged / 1045 flat) and assert `leafKeys(hr).length === <n>` in i18n-orphans.test.ts. #4: drop configuredBoard (WP2's), exclude test files' negative assertions or reword kiosk-css.test.ts:107 to a regex over the stylesheet, and add type-tokens/workspace-css/layers.test/print.css to the file list. #6: replace the second grep with `grep -rnE "s\.paired\.(dataFrom|fetchedAt|ongoingUntil)|strings\.lines\.modelNote|strings\.safety\.confirmed|panels\.sunComputed|strings\.arrivals\.note|s\.arrivals\.note" app/src/kiosk app/src/kiosk.ts | wc -l` = 0. #9: make every doc guard whitespace-tolerant (`/gumbom za\s+zaustavljanje/`, `/Dodir na stajalište otvara\s+istraživanje grada/`, `/svakih 8\s+sekundi/`, `/Radovi u\s+gradu/`), drop the DESIGN.md 'one map-linked highlight' phrase (it lives in PRODUCT.md:50) and 'keep Ništa se ne izmišlja' → 'add', and write the lightweight note in docs/kiosk.md § 'Lagani prikaz' (line 233), not DESIGN.md. Also fix the pointers: safety.confirmed = hr.json:982; paired.ts ongoingUntil = :753; intlLocale import from '../experience/text'; ResultsData/resultsMarkup = view.ts:205-232; kiosk.ts:704 has no invitation coverage line (nothing to delete there); kiosk-front.test.ts credit assertion is :109 and needs no change.
