// Moved to shared/motion/geo.ts (R-TE15): the twin and the app share one
// metre plane, so the code lives where both can import it. Re-exported here
// until D1 retires the last app-side importer.
export * from '../../../shared/motion/geo';
